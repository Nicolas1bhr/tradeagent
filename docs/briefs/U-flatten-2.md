# U-flatten-2 — the reduction-only exception: a confirmed breach closes what is open, by code, under the app's own press kind, resolved by machine
Read `docs/HOW-WE-BUILD.md`, `CLAUDE.md` (rule 3; two-press; rule 4), `docs/COUNCIL.md:14-15,33`, `docs/CONTRACTS.md` (the `U-flatten-1` section), then
`src/TradeAgent.Gateway/TradingGateway.cs:3817-4037` (`OperatorCloseAllAsync`: `RiskReducingScope.Begin` first, `RefuseWhileAPressIsOpen`, `BeginComposite`,
per symbol `SettleAnUnresolvedReducerOrRefuse` `:3704-3778`, the drift re-read `:3917-3935`, `OpenPressRow` `:3486-3540` written FLAGGED with its rationale
`:3419-3449`, `Connector.ClosePositionAsync` `:3964`, outcomes `:3986-4019`), `:3225-3238` (`IsPressRecord`/`PressKindOf`: exactly two kinds), `:3296-3314`
("a press ends when a person has read them"), `:1234-1247` (a flagged row pauses all order flow), `:1526-1536` (`CanIncreaseExposure`), the cancel press
(`CancelPress`), `:2936-2951` (`CloseAsync` and the gates it inherits — NOT the route), `tests/TradeAgent.FaultTests/EmergencyPressTests.cs`,
`UnknownCloseTests.cs:75-85` (`PressBudget` 20 s for a verdict that is the book), `PressAtomicityTests.cs`. Branch `u-flatten-2`, worktree
`~/Projects/ai-trading-software-for-mihael-worktrees/U-flatten-2`, rebased onto `main` AFTER `U-flatten-1` lands. **Money path:** every guard ships with a
test RED before it and ONE mutant, quoted, over `RecordingConnector`. No schema. No box, no ATAS, no money; the simulator only.

**Why.** `U-flatten-1` closes the day and sends nothing. The only flatten is the owner's press, and a press is a person's: its rows stay flagged until
that person reads them, which pauses every order until a human looks — a human in the loop after every breach. The press mechanics are right; what is
new is an app-owned kind, a code-enforced reduction-only exception to two-press (named in `CONTRACTS.md`), and resolution by MACHINE: a leg is
resolved when its close is terminal AND the position reads back flat; anything else stays flagged and pauses, exactly as today.

1. **Openers first.** On a confirmed daily breach every working order on the account that could increase exposure is cancelled through the cancel
   press mechanics under the app's kind (`op-budget-cancel-`) before any close; a per-symbol breach cancels that symbol's. RED: a resting opener fills
   after the flatten and the book is not flat. Mutant (the cancel skipped): the same.
2. **The reduction-only exception, enforced in code.** `op-budget-close-` legs reuse `OperatorCloseAllAsync`'s sequence through ONE shared method —
   `RiskReducingScope`, `BeginComposite`, `SettleAnUnresolvedReducerOrRefuse`, the drift re-read, the flagged write-ahead row, `ClosePositionAsync` —
   plus a check before the wire that the leg opposes the position and is at most its size; a leg that would reverse or open throws before sending. A
   daily breach closes every position, a per-symbol breach that symbol, daily outranking; the activity line names the budget and the record, never
   "you pressed"; `LOSS_BUDGET_REACHED` stays the caller's answer. RED: two open positions, a confirmed breach → the book flat. Mutant (routed through
   `CloseAsync`): `TRADING_PAUSED_UNRECONCILED` after the first leg. Second mutant (size check removed, position drifted smaller): a long 2 reads short.
3. **Resolved by machine, or paused.** A leg whose close is terminal and whose position reads back flat is resolved by the app — flag cleared, the
   outcome written ONCE as its own record keyed to the breach (`loss_flatten:{account}:{utcDay}` / `…:{symbol}:{utcDay}` in `kv`: request ids, fills, residual, the read-back) — the breach record is NEVER updated; a rejected, cancelled, timed-out or unsettled leg stays flagged, pauses
   order flow as today, and the report says which. The owner's button is never refused because of an app press (a third kind in `IsPressRecord`/
   `PressKindOf`); an app leg unresolved on a symbol refuses the owner's leg there in words — the existing rule. RED: after an all-filled app flatten
   the next day's first order is refused `TRADING_PAUSED_UNRECONCILED`. Mutant (resolved on "terminal" alone, no read-back): a cancelled close resolves
   with the position still open.
4. **Crash recovery.** A breach record with no flatten record at startup re-runs the flatten, after reconciliation and never over an unreconciled row (the
   startup sweep's rule). RED: killed between the record and the composite, restarted → the position stays open and nothing says so. Mutant (recovery
   keyed on the composite's existence instead of the outcome): a composite begun and killed is never finished.
5. **Told.** Section 4, the Situation, `status` (`loss_flatten`: `flat` / `unresolved`; the reopen's own states belong to `U-reopen-1`), the Safety rows; every sentence
   promising "nothing is closed for you" (`GatewaySchema.cs:90-97`, `WorkspaceBuilder.cs:331-341`, `USER-GUIDE.md:747-754`, `LossBudgetOrThrow`'s doc,
   `CONTRACTS.md`) rewritten. RED: the report after a flatten reads as before. Mutant (`flat` from the composite's ok, not the read-back): an
   unresolved leg reads flat.
Not this unit: the reopen of a closed day — cooldown, strikes, receipts (`U-reopen-1`); the data-loss exit (`U-flatten-3`); strategy stops (`U-protect`); the directors' assessments; the box.
Gate and report as `U-flatten-1`, plus: a press fixture whose verdict is the book takes `PressBudget`, never the simulator's 2 s (the windows-red family).

## Report

Gate sha `ebd6b2a`. The tip is one commit past it — this report, docs-only — so `src` and `tests` at the tip are identical to the gate sha (a self-referencing sha cannot be written into the commit that carries it). Branch `u-flatten-2`, rebased onto `main` at `3725179` (U-allocator-1 included; the rebase dropped my base's nine allocator commits as already applied and replayed five). 7 commits, 17 files, +2251/−144. NO schema rung: the record is `kv`. Every gate output is in the session scratchpad, never the repo.
**Gate, Release, on this tip.** `dotnet build TradeAgent.sln -c Release --no-incremental` → 17 projects, `Build succeeded.` `0 Warning(s)` `0 Error(s)`. `dotnet test <proj> -c Release --no-build`, each to its own file: Unit `Passed!  - Failed:     0, Passed:  1102, Skipped:     0, Total:  1102, Duration: 17 s`; Fault `Passed!  - Failed:     0, Passed:   320, Skipped:     0, Total:   320, Duration: 1 m 23 s`; Integration `Passed!  - Failed:     0, Passed:   668, Skipped:     1, Total:   669, Duration: 11 m 3 s`. Touched/added classes 3× each: `LossFlattenTests` 9/9, `ReductionOnlyTests` 7/7, `LossWatchTests` 5/5, `LossFlattenSurfacesTests` 5/5, `LossDayClosedSurfacesTests` 6/6 — 15 runs, `exit=0` on every one. No `Timing` red anywhere, so nothing was re-run and no assertion was loosened. Names vs `main`: `[Fact]`/`[Theory]` 1709 → 1726, sorted set 1741 → 1758, 19 added, **0 removed, 2 RENAMED** — `Section_four_..._that_nothing_was_closed_for_them` → `..._what_that_does_to_positions` and `The_situation_line_..._that_nothing_was_closed` → `..._what_happens_to_the_positions`, same bodies, renamed because the NAME stated the promise this unit withdraws.
1. **Openers first** (`op-budget-cancel-`, press mechanics, then the working book read again). RED, no flatten: `book after the flatten: [FB-1 Buy 2 FILLED | FB-3 Buy 1 WORKING]` → `after the opener fills: ES 3`, `Assert.Equal() Failure: Expected: 0 Actual: 3`. Mutant, the cancel skipped: `[FB-1 Buy 2 FILLED | FB-3 Buy 1 WORKING | FB-4 Sell 2 FILLED]`, `position: ES 0`, `after the opener fills: ES 1`. The settle half has its own test: a platform that refuses the cancel leaves `closes` unchanged, ES 2, `HasUnconfirmedWork` true.
2. **Reduction-only, enforced in code**, through ONE shared `CloseCapturedAsync` both callers use. RED, flatten and sweep disabled: `Assert.NotNull() Failure: Value is null`. Mutant, routed through `CloseAsync`: `still open: ES: TRADING_PAUSED_UNRECONCILED; still open: NQ: TRADING_PAUSED_UNRECONCILED`, `book: [ES 2 | NQ 1]`. Second mutant, size check removed: 3 red — `Expected: typeof(GatewayDeniedException)` twice, and at the wire `closes on the wire: 0 -> 1`, `position: ES 0`. `LOSS_BUDGET_REACHED` is still the caller's answer, asserted.
3. **Resolved by machine, or paused.** RED, judged but never cleared: `unconfirmed work: True`, `Assert.False() Failure`. Mutant, terminal alone with no read-back: the REJECTED leg over `position: ES 1` resolves, `Expected: True Actual: False` on `HasUnconfirmedWork`. The outcome is its own write-once `kv` row and the breach row's bytes are compared before and after.
4. **Crash recovery**, keyed on the ABSENCE of the outcome record. RED, no sweep: `Assert.NotNull() Failure: Value is null` with ES 2 still open. Mutant, keyed on the composite: same red, a composite begun and killed is never finished. It never runs while anything is unreconciled, with its own test.
5. **Told.** RED, four of five: `Not found: "_lossFlattenNote,"`, `Not found: "CLOSED YOUR OPEN POSITIONS"`, `Not found: ""loss_flatten":"flat""`. Mutant, `flat` from the composite's ok: `TradeAgent CLOSED YOUR OPEN POSITIONS … and the account reads flat` over an open ES 1, `Expected: False Actual: True`.
**Choices, all now in `CONTRACTS.md` under this unit.** (a) The key carries the CONNECTOR — `loss_flatten:{connector}:{account}:{utcDay}` — not the brief's `{account}:{utcDay}`, because an account id is unique only within a platform and a PAPER flatten must not answer for a LIVE closure (edge (c)); the account is read off the breach record and the flatten refuses unless the gateway is operating that account. (b) The cancel also takes every working order on an instrument about to be closed, reducing ones included: a protective sell is a reducer only while the long exists. (c) The app clears its OWN flags behind a read-back and restores `ExecutionCapability` on `ReconcileAsync`'s two lines — otherwise a correct flatten is a manual outage. (d) Only app legs take the extra position read before the wire; the owner's press is byte for byte what it was.
**Deviations.** The shared close method and `ReductionOnlyOrThrow` landed in item 1's commit, because item 1's verdict ("the book is not flat") needs a close to exist; item 2's commit is its tests and its REDs. Two `LossWatchTests` wire assertions changed from "nothing is sent" to one close, the right symbol and a flat read-back — the product's claim reversed, and the new assertions are stronger, not looser. `LossDayClosedSurfacesTests.ClosedDay`'s SETUP moved (the loser is now closed by the app at the price that closed the day, so the figure is made to recover by widening the budget instead); no assertion in it was loosened.
**Two limits, named not implied.** `FakeConnector.ClosePositionAsync` re-reads and re-sizes itself, so a wire-level REVERSAL cannot be produced against the simulator however wrong the composed size is; what the flow test states is that no close reached the connector and the row says why, and `ReductionOnlyTests` holds the arithmetic directly. Inherited: the shared close path catches a definite `ConnectorRejectedException` in the same arm as a timeout and records UNKNOWN, so a broker's flat refusal of a close reads as "we do not know" — the safe direction, and not this unit's to change.
**NOT done.** No reopen of a closed day — no cooldown, no strikes, no receipts (`U-reopen-1`); `status.loss_flatten` is `flat`/`unresolved` and nothing else. No data-loss exit (`U-flatten-3`), no stops or targets (`U-protect`), no schema rung, no table, no assessments. NOT VERIFIED anywhere but this Mac: no Windows box, no ATAS, no real money — every order in every test went to the simulator through `RecordingConnector`, and the wire counts are asserted.
