# U-review-med — the three MED findings of the third review as one batch: a forward clock step, the per-order limits at the wire, the sighting keyed by scope
Read `docs/HOW-WE-BUILD.md` (MED and LOW together become one batch unit), `CLAUDE.md`, `docs/REVIEW-2026-09-16.md` findings 6, 7 and 8 with their probes (`C4`, `P2`,
`P4`), `docs/CONTRACTS.md` (`U-reopen-1`: "a step forward is ordinary and lengthens nothing"; "Every gate is evaluated at the moment of dispatch, after the awaited
reads"), then `src/TradeAgent.Gateway/TradingGateway.cs:2508,2605-2660` (the clock high-water mark refusing only `at < HighWater`), `LossReopen.cs:135-148` (`ClockKey`
and its claim), `:3793` (`RiskCheckOrThrow` above `_dispatchGate.WaitAsync :3815`), `:4514-4518` (`ReauthorizeAtDispatchOrThrow`), `:3301-3316,2368-2378` (`Confirmed`
keyed by `LossBreach.DayKey(account, at)`), the probe tests on branch `review-probes-c` @ `58aa4fa` — your RED tests: bring them over, quote them red on your base,
make them green. Branch `u-review-med`, worktree `~/Projects/ai-trading-software-for-mihael-worktrees/U-review-med`, from `main`, rebased AFTER `U-scope-identity`
lands (item 3 touches the same code). **Money path:** a test RED before every guard and ONE mutant, quoted, over `RecordingConnector`. No schema. No box, no money.

1. **A closure is held against a monotone reading as well as the wall clock.** The clock mark carries a monotone reading (`Environment.TickCount64` or a `Stopwatch` seeded
   at the breach and persisted with the mark); eligibility requires the closure to have RUN on both, and a forward wall-clock step larger than a few watch intervals
   with no matching monotone elapse is suspect exactly as a backward one is (`loss_clock_suspect`, refused, said in `status`); across a restart the monotone term restarts
   conservatively from the restart. `CONTRACTS.md` and `LossReopen.ClockKey` say what the code now keeps. RED: `C4` (closed 12:00:40Z, one `MoveTo(+2 days)`, one tick,
   receipt written, a buy SENT). Mutant (the monotone term dropped): the same.
2. **The per-order limits are asked again at the wire.** The allowlist, `MaxOrderQuantity`, `MaxNotionalPerOrder` and the quote-age rule are re-run inside the gate
   beside the four position gates, on the reference price the record already carries, for placements AND approvals; the contract heading becomes true. RED: `P2` (ES off
   the allowlist and the cap cut to 1 while the order sits in the gate's read; `ES Buy 5` reaches the broker). Mutant (re-run above the gate only): the same.
3. **The sighting is keyed by scope, the day by the confirming pull.** `Confirmed` files the first sighting under `(connector, account, symbol)` — the scope, not the
   day's key — and the record's `Day` is taken from the CONFIRMING pull's instant at `Compose`, so a pair straddling midnight confirms; the day that lost the money is
   the day the record names. RED: `P4` (both pulls `reached=True`, `closed=[]`, no breach row at 00:00:10Z). Mutant (the day taken from the first pull): a breach
   confirmed at 00:00:10Z closes YESTERDAY, which reopens at midnight tonight — assert the day.
Not this unit: a fifth gate; the UNVERIFIED list of the review; anything on the bridge.
Gate and report as `U-flatten-1`; every probe you brought over named with its new name and its red quoted.

## Report
Tip `6116df7`, rebased onto `main` @ `b896e36` (the gate below ran at `9206207`, whose tree differs from `6116df7` by one line of `BUILD-STATUS.md` prose that main added under it and nothing else) — after BOTH `U-scope-identity` and `U-approve-gates` landed, so item 2 went INTO `PositionGatesOrThrow`, the shared gate method, and serves `PlaceAsync` and `ApproveAsync` from one sequence; the modify approval runs no position gate and gets the same re-run on its own branch.
Gate, Release, on the tip, every output in the scratchpad: `dotnet build TradeAgent.sln -c Release --no-incremental` → `0 Warning(s)` `0 Error(s)`; UnitTests `Passed!  - Failed:     0, Passed:  1146, Skipped:     0, Total:  1146`; FaultTests `Passed!  - Failed:     0, Passed:   374, Skipped:     0, Total:   374`; IntegrationTests `Passed!  - Failed:     0, Passed:   670, Skipped:     1, Total:   671`.
Test names against `main`: `[Fact]`/`[Theory]` 1813 → 1821; nothing removed, renamed or moved; eight added, all in the three new classes. `SetForward` also reads as added — it is a test-clock helper the `public void <Name>(` pattern catches, not a test. The eleven classes touched or added ran 3× green (34 + 7); no `Timing` test went red at all.
1. A closure is held against a monotone reading as well as the wall clock. RED `A_clock_set_forward_past_the_instant_with_no_time_elapsed_ends_no_closure` (probe `C4`), on the base: `after ONE forward jump  : reopened=[loss_reopen:fake:SIM-001:2026-03-10]` / `clock suspect row       : none`. Mutant, the monotone term dropped: same test, `Collection: ["loss_reopen:fake:SIM-001:2026-03-10"]`. Second guard, the restart penalty: RED `A_restart_does_not_credit_the_gap_it_could_not_see`; mutant, the gap credited: same line.
2. The per-order limits are asked again at the wire. RED `An_instrument_taken_off_the_allowlist_inside_the_gate_does_not_reach_the_wire` (probe `P2`), on the base: `the order in flight       : SENT` / `orders at the broker      : [ES Buy 5]`. Mutant, the call inside the shared gate removed: `Expected: "RISK_LIMIT_EXCEEDED" Actual: "SENT"`, `orders at the broker      : [ES Buy 5]`.
3. The sighting is keyed by scope, the day by the confirming pull. RED `A_breach_first_seen_in_the_last_tick_of_a_utc_day_is_still_confirmed` (probe `P4`, a `[Theory]` carrying the 22:59 control), on the base: `pull 2: reached=True closed=[]` / `breach rows : []`. Mutant, the day off the first pull: `Expected: "2026-03-11" Actual: "2026-03-10"`, `record day=2026-03-10`.
Choices, all written into `docs/CONTRACTS.md` under `U-review-med`: the monotone reading is `GatewayOptions.Clock.GetTimestamp()` (`Stopwatch` in production), so a fake clock has to be coherent — eight existing test clocks gained `GetTimestamp`/`TimestampFrequency` tracking their wall half, with no assertion loosened and no test removed, because a fake that moves only the wall IS a moved machine clock and that is what the new class measures; the slack is four watch intervals (60 s shipped); a restart is a new gateway run, is never suspect, and its gap is ADDED to the instant off the `LossBreachRecord.ClockUnverified` baseline (null on an older row is measured from zero); a bounded extension may only push the instant later.
More choices: item 2 does NOT re-ask the rate limit (advisory; the wire reservation bounds the minute), the PAPER-vs-account check (`ReauthorizeAtDispatchOrThrow` owns it) or `quantity <= 0`; a price the intent NAMED skips the quote-age rule; a value cap switched on inside the window fails closed with `RISK_CHECK_UNAVAILABLE` off the cached instrument list rather than an awaited read inside the gate; `LossReopen.Step` was added because `Hours` renders a 90-second jump as "0 hours".
Deviations: `A_size_cap_narrowed_while_a_proposal_waited_refuses_the_approval` was GREEN on the base — `ApproveAsync` already ran the risk check inside the gate — so it is a guard checked in both directions, not the RED; the whole `U-review-med` contract section covers all three items and landed with item 3's commit; the eight test-clock files are the only existing tests touched.
NOT done: a fifth position gate; the review's UNVERIFIED list; anything on the bridge; the box; and any defence of a closure against a clock moved while the process is DOWN beyond declining to credit the gap — there is no trustworthy elapsed-time source across a process boundary, and the contract says so rather than implying one.
