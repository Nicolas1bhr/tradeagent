# U14a — CoidWitness durability: rebase over U2a, then make `Unreadable` a value everywhere

Fresh builder on Opus. Read `docs/HOW-WE-BUILD.md`, `CLAUDE.md`, then only "## Round 10" of
`docs/hardening/records/U14.md` (the five directives as built, the mapping table, the crash-point list). Toolchain:
`export PATH="$HOME/.dotnet:$PATH" DOTNET_ROOT="$HOME/.dotnet"`; no `timeout` binary; full suite 3–6 min, to a file.

Worktree `~/Projects/ai-trading-software-for-mihael-worktrees/u14-build`, branch `u14-coid-witness-rewrite` @ `01fcd60`
(517 green, 0 warnings). **Item 0: `git rebase main`.** U2a landed today and both units rewrote
`src/TradeAgent.Connectors.Atas/AtasConnector.cs` (read loop and status logic); expect a conflict at `6884cdf`. Keep
every guard of both sides. U2a's: risk-reducing scope and one absolute deadline per operation; the transport tri-state
and attempt marker; liveness = an answer within the ordinary deadline (10 s grace); the whole-frame ceiling; the
chunk-progress rule. U14's: refusal decided once at the top of `Dispatch`; the `_incompatible`/`_unauthenticated`
markers with counters; the row always derives a status newer than any marker; the heartbeat predicate on every
read-loop turn; drop on `HeartbeatTimeout`; frame-loop writes refused after teardown. Never drop a guard to compile; if
two genuinely contradict, stop and name both. Build + full suite green before item 1; a red test here is a real
interaction between the units, and fixing it is part of item 0.

The rule behind items 1–3: `Unreadable` is a VALUE that reaches every consumer, the snapshot is the only thing anyone
renders from, and nothing re-reads a path after the snapshot.

1. `CoidWitness.cs` `LastDecidingLine()` (~:2346) turns an `Unreadable` snapshot into null, so `Settled()` appends
   RESOLVED unseen. Tri-state (line / none / unreadable); `Settled()` writes nothing over unreadable and the standing
   stays degraded. Mutant: collapse the tri-state.
2. The snapshot exposes paths, not captured lines; the probe (`tools/probe/Program.cs` ~:1075) and the support package
   (`Doctor.cs` ~:284) reopen files under a swallowing catch. The snapshot carries file → lines captured at read time;
   probe, report and zip render only from the snapshot handed to them; an `Unreadable` snapshot goes INTO the zip as a
   note, never silently absent.
3. Candidate (temp/rewrite) contents are read after the before/after listing comparison (~:1300 vs ~:1719). Read them
   inside the window; adopt from the snapshot; a candidate changed after it → retry → `Unreadable("changing")`.

Every fix: test RED before, GREEN after, one mutant watched red and quoted (commit before mutating; `cp` restore;
`touch`). Test-name diff against `01fcd60` after any structural edit: nothing removed. Commit per item, one-sentence
messages, no `Co-Authored-By` trailers, no push, no other worktree, no box. Gate: `dotnet build TradeAgent.sln
--no-incremental` → 0 warnings; full suite → 0 failed; paste the counts.

## Report

Tip: the commit carrying this report (last code commit `ee6ccc2`); worktree clean, not pushed, box not used, no other worktree touched.
Gate, this Mac: `dotnet build TradeAgent.sln -c Release --no-incremental` → **0 Warning(s), 0 Error(s)** (Debug and `tools/probe` the same); `dotnet test TradeAgent.sln -c Release` → EXIT=0, **75 + 112 + 505 = 692 passed, 0 failed** (517 at `01fcd60`; 685 after the rebase alone).
Test names vs `01fcd60` ∪ `main`: nothing removed, 7 added. One name absent from both, `A_corrupt_file_reads_as_unreadable_rather_than_as_empty`, was renamed by U14's own `1a07bef` (2026-09-03) to `…_is_not_written_over` — pre-existing on the branch, not the rebase.
**Item 0 — `git rebase main`, 85 commits, two conflicts, neither a contradiction.**
(a) Textual at `AtasConnector.cs:260`, both sides adding at one point: U2a's `WorstCaseOrderPath`/`WorstCaseOperationPath`/`EmergencyBudget` against U14's `_observed`/`_incompatibleAt`/`_unauthenticatedAt` + `NoteIncompatible`/`NoteUnauthenticated`. Both kept verbatim — the U2a lines serve one absolute deadline per operation (the shutdown drain has to outlast the order path); the U14 lines serve the marker counters and are the only writers of either marker, 2 assignment sites, both inside the two helpers.
(b) A silent auto-merge that did not compile: `error CS0111 … already defines a member called 'Observe'` — U2a's helper for the chunked write and U14's for the abandoned frame read, identical bodies. One kept and documented for both; it serves U2a's chunk-progress rule and U14's read-loop heartbeat predicate, and all four call sites bind to it.
Re-read in the merged file, all present, none contradicting: `RiskReducingScope`, the `TransportLedger` tri-state with `Attempt()`, `_lastAnswerAt` written only at the one reply site, `WriteChunkBytes`/`_lastWriteProgressAt`, the `FrameTimeout` whole-frame ceiling; and `if (_refused) return true;` first in `Dispatch`, `PeerHasGoneQuiet()` on every read-loop turn after the dispatch, the drop on it, `StatusDetail` picking the newest of three stamps. Build + full suite green before item 1.
**Item 1** RED `A_commit_over_an_unreadable_sidecar_does_not_say_the_gap_was_closed` — `Assert.DoesNotContain() Failure: Sub-string found … "RESOLVED coid-witness"` → GREEN with `DecidingLine` (line / `None` / `Unread`) behind one `DecidingIn`, and `Settled` writing nothing and leaving `_degraded` standing on `CouldNotRead` → mutant collapsing `Unread` to `None` **RED 1/33**.
**Item 2** RED `The_support_package_says_so_when_the_witness_failure_log_could_not_be_read` — the zip held only `[engineering.log, activity.txt, environment.json]` — and `An_unreadable_set_is_handed_over_as_a_refusal_rather_than_as_no_files` → GREEN with `CoidWitness.Sidecars` carrying path→lines, `Doctor` rendering from it and writing `bridge-sidecar-UNREADABLE.txt`, and the probe's `ReadTail` reopen gone → mutant dropping the refusal **RED 2/5**.
**Item 3** RED `A_candidate_that_changes_while_it_is_read_is_refused_rather_than_adopted` — `Expected: null / Actual: "BRK-TA-REAL"`, an acknowledgement adopted from a temp that moved under the reader → GREEN with the candidates listed AND read between the two listings and both listings compared → mutant dropping `Same(beforeTemps, afterTemps)` **RED 1/183**.
NOT done: item 2 makes `TradeAgent.Diagnostics` reference `TradeAgent.AtasBridge`, so `TradeAgent.AtasBridge.dll` is now in the app's build output. `InstallBridge` copies from `<base>/bridge` only, so ATAS still gets the packaged build — read at `AtasInstallation.cs:191`, **not** run; that the solution still compiles with `-p:AtasBridgeBuild=true` is **NOT VERIFIED**, no box was used.
`tools/probe`'s new rendering is **NOT VERIFIED** by a test: no test project references that program. It was built at 0 warnings and read, never run.
Everything above is macOS/APFS. No `Co-Authored-By` trailers; `docs/hardening/` untouched; nothing pushed.
