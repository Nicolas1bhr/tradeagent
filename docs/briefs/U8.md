# U8 — the deployment and monitoring documents, and a user guide that tells the truth

Fresh builder on Opus, docs only. Read `docs/HOW-WE-BUILD.md`, `CLAUDE.md` ("the user never sees a terminal" applies to
the user guide: no command line in any sentence addressed to the owner), then `BUILD-STATUS.md` from its
"2026-09-02 / 2026-09-03" section to the end, `docs/hardening/records/U3-update-proof.md` and `U4-windows-eyes.md` (what
was actually seen on Windows), the "Docs to change at integration" list in `docs/hardening/records/U2d.md`, and the
current `docs/USER-GUIDE.md`. Worktree `~/Projects/ai-trading-software-for-mihael-worktrees/u8-docs`, new branch
`u8-docs` from `main` (`git worktree add -b u8-docs <path> main`). You may create `docs/DEPLOYMENT.md` and
`docs/MONITORING-PHASE.md` and edit `docs/USER-GUIDE.md`; nothing else — no code, no tests, not `BUILD-STATUS.md`. No box:
cite `U4-windows-eyes.md` instead of re-walking the app. Facts now true on `main`: U2d's updater refusals and hard stop
are shipped; the bridge protocol is 3 (an old bridge is refused with the reinstall sentence); code signing is deferred.

1. **`docs/USER-GUIDE.md` corrections.** "Trading through ATAS does not work" has been false since 2026-08-31: say what
   works, in the owner's words (orders through the bridge on the connected account, LIVE_CONFIRM parking, the two-press
   controls, the emergency answer at two seconds and what "NOT confirmed — check your positions and orders in ATAS"
   asks of them). The setup journey: count the screens from the app's setup code (the guide says 12; `U4` saw 16), list
   them by on-screen title, say which resume. "Add-on" → the app's word, "bridge", everywhere. The update paragraph gains
   the failure branch (missing or mismatched checksum → nothing is installed, the reason is on the strip and in Settings)
   and the hard stop (TradeAgent will not replace itself while an order's outcome is unconfirmed, and says so). The
   first-install SmartScreen sentence: what the owner sees, which button, that it is expected for an unsigned release.
   "What you will be asked to do": the only burden is clicking Yes on a Windows prompt.
2. **`docs/DEPLOYMENT.md`.** Audience: Nicolas at a machine he can sit at. Preconditions (Windows 11; ATAS installed and
   signed in; an account visible in ATAS, simulated is fine); the release download and SmartScreen; the setup-journey
   pointer; the bridge deployment (the DLL, the `proto=3` reading); confirming health from inside the app; Tailscale +
   SSH for a machine he monitors; a first-hour checklist; rollback (reinstall the previous release; the database and the
   home survive). Every step is one a record shows was done once, or is marked "not yet walked".
3. **`docs/MONITORING-PHASE.md`.** The first weeks: `trade status` over SSH, the support zip, the on-machine logs (name
   them; which are append-only and which rotate — from the code), the dashboard's health rows. A daily check of five
   items, each with the reading that means fine and the one that means stop; a weekly check; the stop rule (an UNKNOWN
   order older than N minutes without a resolved card; a bridge refused; an update refused; kill switch found on); how
   to stop (kill switch, then close positions in ATAS by hand). Outbound alerts: one paragraph, "not built; why; what it
   would need". Every reading not verified against the code is marked NOT verified.

Every behavioural sentence maps to a line: `sentence → file:line or record §`, kept in the report's source map, not in
the documents. A sentence you cannot source is removed or marked "not yet walked". Owner-facing text: short sentences,
the app's own words for screens and buttons, no paths. Commit per document, no trailers, no push, no other worktree.

## Report — append as you go, commit it, ≤20 lines: tip sha; the three files with line counts; sourced sentences vs
"not yet walked" markers; anything the guide claimed that you could not confirm either way; what you did NOT do.
