# U8 — the deployment and monitoring documents, and a user guide that tells the truth

Fresh builder on Opus, docs only. Read `docs/HOW-WE-BUILD.md`, `CLAUDE.md` ("the user never sees a terminal" applies to
the user guide: no command line in any sentence addressed to the owner), then `BUILD-STATUS.md` from its
"2026-09-02 / 2026-09-03" section to the end, `docs/hardening/records/U3-update-proof.md` and `U4-windows-eyes.md` (what
was actually seen on Windows), the "Docs to change at integration" list in `docs/hardening/records/U2d.md`, and the
current `docs/USER-GUIDE.md`. Worktree `~/Projects/ai-trading-software-for-mihael-worktrees/u8-docs`, new branch
`u8-docs` from `main` (`git worktree add -b u8-docs <path> main`). You may create `docs/DEPLOYMENT.md` and
`docs/MONITORING-PHASE.md` and edit `docs/USER-GUIDE.md`; nothing else — no code, no tests, not `BUILD-STATUS.md`. No box:
cite `U4-windows-eyes.md` instead of re-walking the app. Facts now true on `main`: U2d's updater refusals and hard stop
are shipped; the bridge protocol is 3 (an old bridge is refused with the reinstall sentence); code signing is deferred.

1. **`docs/USER-GUIDE.md` corrections.** "Trading through ATAS does not work" has been false since 2026-08-31: say what
   works, in the owner's words (orders through the bridge on the connected account, LIVE_CONFIRM parking, the two-press
   controls, the emergency answer at two seconds and what "NOT confirmed — check your positions and orders in ATAS"
   asks of them). The setup journey: count the screens from the app's setup code (the guide says 12; `U4` saw 16), list
   them by on-screen title, say which resume. "Add-on" → the app's word, "bridge", everywhere. The update paragraph gains
   the failure branch (missing or mismatched checksum → nothing is installed, the reason is on the strip and in Settings)
   and the hard stop (TradeAgent will not replace itself while an order's outcome is unconfirmed, and says so). The
   first-install SmartScreen sentence: what the owner sees, which button, that it is expected for an unsigned release.
   "What you will be asked to do": the only burden is clicking Yes on a Windows prompt.
2. **`docs/DEPLOYMENT.md`.** Audience: Nicolas at a machine he can sit at. Preconditions (Windows 11; ATAS installed and
   signed in; an account visible in ATAS, simulated is fine); the release download and SmartScreen; the setup-journey
   pointer; the bridge deployment (the DLL, the `proto=3` reading); confirming health from inside the app; Tailscale +
   SSH for a machine he monitors; a first-hour checklist; rollback (reinstall the previous release; the database and the
   home survive). Every step is one a record shows was done once, or is marked "not yet walked".
3. **`docs/MONITORING-PHASE.md`.** The first weeks: `trade status` over SSH, the support zip, the on-machine logs (name
   them; which are append-only and which rotate — from the code), the dashboard's health rows. A daily check of five
   items, each with the reading that means fine and the one that means stop; a weekly check; the stop rule (an UNKNOWN
   order older than N minutes without a resolved card; a bridge refused; an update refused; kill switch found on); how
   to stop (kill switch, then close positions in ATAS by hand). Outbound alerts: one paragraph, "not built; why; what it
   would need". Every reading not verified against the code is marked NOT verified.

Every behavioural sentence maps to a line: `sentence → file:line or record §`, kept in the report's source map, not in
the documents. A sentence you cannot source is removed or marked "not yet walked". Owner-facing text: short sentences,
the app's own words for screens and buttons, no paths. Commit per document, no trailers, no push, no other worktree.

## Report — append as you go, commit it, ≤20 lines: tip sha; the three files with line counts; sourced sentences vs
"not yet walked" markers; anything the guide claimed that you could not confirm either way; what you did NOT do.

## Report

Branch `u8-docs`, 6 commits. All three documents complete at `077871b`; this report is the tip on top of it.
No push, no rebase, no other worktree, no code, no tests, no `BUILD-STATUS.md`.
Files: `docs/USER-GUIDE.md` 494 lines (was 288), `docs/DEPLOYMENT.md` 319 new, `docs/MONITORING-PHASE.md` 247 new.
**86 behavioural sentences sourced** to file:line or a record section — full map below. **11 marked "not yet walked"
or NOT verified**: SmartScreen on a browser download; a clean-machine install; the eight auto-passing setup screens;
`proto=3` read on hardware; rollback, downgrade and an interrupted install; a support zip opened on a deployed machine;
any log/sidecar inspected after days of running; the five-minute stop threshold (a judgment, not a constant); the
`PresentedNoProof` sentence; real money; the update-strip refusal rendering (compiles, no UI run).
**Three things the old guide claimed that are false, now corrected:** "Trading through ATAS does not work yet" (walked
2026-08-31, BUILD-STATUS:1317); "12 screens" (16, `Onboarding.cs:4-12`); "There is no timer that decides for you" —
there is, a 15-minute approval TTL (`GatewayTypes.cs:27`, `DashboardView.cs:213-217`). Also corrected: six pages → seven
(`MainWindow.cs:393-401`); "add-on" → "bridge" except where quoting the app's own words, which still say "add-on" in two
places (`OnboardingView.cs:941`, `BridgeProtocol.cs:167`) — an inconsistency in the app, recorded not papered over.
**Could not confirm either way:** whether anything ever writes into `%LOCALAPPDATA%\TradeAgent\logs` — the directory is
created (`Paths.cs:21`) and swept by the support collector (`Doctor.cs:279`), and no writer exists in `src/`.
**Found while sourcing, not fixed (docs-only unit):** there is no in-app way to reinstall the bridge after setup
completes — setup renders only while onboarding is incomplete (`MainWindow.cs:183`), `Onboarding.Clear`'s only caller is
the wizard's own Back (`OnboardingView.cs:349`), and the Checks page prints repair text with no buttons behind it
(`DashboardView.cs:910-919`). So `Doctor.cs`'s "Press Install bridge." names a control that does not exist, and the
protocol-3 refusal's own repair sentence has no in-product path. Written into both documents as a defect.
**Did NOT do:** no Windows box, no app run, no screenshots, nothing re-walked; no code, test or `BUILD-STATUS.md` edit;
U4's other UX defects left for U6; no push, no rebase (main unmoved at `44fb21f`).

## Source map

`sentence → source`. Records: U3 = `docs/hardening/records/U3-update-proof.md`, U4 = `.../U4-windows-eyes.md`,
U2d = `.../U2d.md`, BS = `BUILD-STATUS.md`. Paths relative to the repo root.

### docs/USER-GUIDE.md

| Sentence | Source |
|---|---|
| installs for you alone, no administrator password, no all-users question | `packaging/TradeAgent.iss:35-48` (`PrivilegesRequired=lowest`) |
| unsigned, so Windows may show "Windows protected your PC"; More info → Run anyway | **not yet walked** — U3 § Pre-state (no `Zone.Identifier`, no SmartScreen), § NOT VERIFIED |
| setup will say so and offer to close TradeAgent if it is open | `packaging/TradeAgent.iss:64-70` (`CloseApplications`, Restart Manager) |
| Windows 11 required | `packaging/TradeAgent.iss:62` (`MinVersion=10.0.22000`) |
| no .NET, no Node, no developer tools | `packaging/build.ps1:110,117` (self-contained publish) |
| the only burden is clicking Yes on a Windows prompt, and it is ATAS's installer that raises it | `Prerequisites.cs:46-90` (`_runElevated` → `Elevation.RunElevatedAsync`, `Verb="runas"`) |
| "the answer was No… Nothing was installed and nothing was changed. You can try again" | `Provisioning/Elevation.cs:76-79` (verbatim) |
| checks at startup then every six hours | `App/AppHost.cs:244-247` (`pass % (12*60*6)`, 5 s tick at `:254`) |
| strip: "TradeAgent 0.2.0 is available… You are running …", What's new / Install update / Later | `App/MainWindow.cs:363-372, 600` |
| second press names what it interrupts, e.g. "…, 1 order still working" | `App/MainWindow.cs:609-615` |
| Install update is switched off while an order is unconfirmed; strip says "It can be installed once…" | `App/MainWindow.cs:591, 598-599, 607` |
| Settings shows this version, the newest published, Install update, Turn off automatic checks, Check for updates | `App/SettingsView.cs:143-157, 499-515` |
| missing/unfetchable/non-matching checksum → nothing downloaded, nothing installed, reason on both surfaces | `Provisioning/UpdateService.cs:543-571`; U2d § "Docs to change at integration" |
| "TradeAgent 0.2.0 cannot be verified… Nothing was installed." | `UpdateService.cs:545,549-550` (verbatim shape) |
| re-hashed immediately before the installer runs; a mismatch stops it | `UpdateService.cs:473-482` |
| hard stop: will not replace itself while an order's outcome is unconfirmed; "Settle or reconcile it on the Dashboard, then install." | `UpdateService.cs:594-597` (verbatim) |
| if it cannot tell whether anything is unconfirmed it refuses and asks you to close and reopen | `UpdateService.cs:579-591` |
| asked twice — before the download and again before Launch | `UpdateService.cs:447, 489` |
| new orders stop from the first byte of the download until Setup runs | `UpdateService.cs:458-468` (`InstallInProgress`); `Gateway/TradingGateway.cs` `UPDATE_INSTALL_IN_PROGRESS`; U2d round 2 |
| every press is logged; the automatic check's repeat refusal is deduplicated | `UpdateService.cs:604-633` |
| an update replaces the program only; records, settings and ATAS untouched | U3 § "The update, watched" (`open_requests 1` unchanged, history intact); `packaging/TradeAgent.iss:119-122` |
| the AI cannot check, download or install | `TradeCli/Program.cs:208-240` (no such verb); `CLAUDE.md` operator-authority rule |
| sixteen screens; the counter reads STEP n OF 16 | `Core/Onboarding.cs:4-12`; `App/OnboardingView.cs:96-101`; `App/Ui.cs:84-88` (`ToUpperInvariant`) |
| the sixteen titles, verbatim | `Core/Onboarding.cs:19-38` |
| a screen that can check itself does so every two seconds and moves on | `App/OnboardingView.cs:74-77, 382-397, 403-450` |
| eight shown, eight flashed past on the one walk | U4 § "Setup journey" |
| it resumes; closed at 9/16, reopened at 9/16 | `App/OnboardingView.cs:23`; U4 § "Setup journey" (RESUME WORKS) |
| Back returns to the last decision; the four targets; Choose your account is not one | `App/OnboardingView.cs:181-183, 324-329, 344-355`; U4 defect 16 |
| screen 9's five numbered steps and the refresh-button note | `App/OnboardingView.cs:948-967` (verbatim) |
| close ATAS first; the file cannot be placed while ATAS uses the folder | `App/OnboardingView.cs:939`; `Connectors.Atas/AtasInstallation.cs:181-197` |
| the button on screen 8 says "Install the add-on" | `App/OnboardingView.cs:941` (the app's own word, quoted) |
| bridge/app must be the same generation; the refusal sentence | `Core/Versioning.cs:35,48`; `Connectors.Atas/BridgeProtocol.cs:165-167` (verbatim) |
| no in-app way to reinstall the bridge after setup | `App/MainWindow.cs:183`; `App/OnboardingView.cs:349`; `App/DashboardView.cs:910-919`; `App/SettingsView.cs` (no such control) |
| seven pages, and their names | `App/MainWindow.cs:393-401` |
| Settings changes platform and account; changing the platform clears the account, moves no money | `App/SettingsView.cs:96-99, 177-180` (verbatim) |
| Inbox: originals are copied, not moved | `App/InboxView.cs:149, 190` (`File.Copy`) |
| Inbox records name, size, fingerprint, arrival time | `App/InboxView.cs:214, 219, 245` (`Sha256`, `SizeBytes`, `FirstSeenAt`) |
| the folder is rescanned every 30 s | `App/AppHost.cs:239-242` (`pass % 6` of a 5 s tick) |
| measurement and claim are different lists | `CLAUDE.md` (material vs material_note); `App/InboxView.cs:214` (`store.Present()`) |
| the four mode names | `App/Ui.cs:503-510` |
| the four mode descriptions | `App/DashboardView.cs:697-704` |
| real money is a separate switch and is re-armed on leaving a live mode | `Gateway/TradingGateway.cs:248-262`; `App/DashboardView.cs:706-713` |
| starts in practice mode | `Core/Trading.cs:46` |
| amber bar "The AI is asking permission — 1 order waiting" / Review the request | `App/MainWindow.cs:312, 522-524` (verbatim) |
| Approve is two presses ("Confirm: place this order"); Decline is one | `App/DashboardView.cs:218-221`; `App/Ui.cs:218-263` |
| a request goes stale after fifteen minutes; "approve before HH:mm; from then, approving declines it instead" | `Gateway/GatewayTypes.cs:27`; `Gateway/TradingGateway.cs:872-888`; `App/DashboardView.cs:213-217` (verbatim) |
| every gate is re-checked at the moment of pressing | `App/DashboardView.cs:234-241`; BS § U2b (age, mode, chain, identity, account, limits) |
| STOP AI TRADING is one press both ways, in the chrome, on every page; reads RESUME when on | `App/MainWindow.cs:289-291`; `App/DashboardView.cs:636-642, 715-716` |
| "Stopping the AI removes its permission to trade. It does not touch your orders or positions." | `App/DashboardView.cs:646` (verbatim) |
| the other two are two-press, with the armed sentence | `App/DashboardView.cs:653-658` (verbatim labels) |
| cancel-all is per-order cancels of the captured set | BS § U2c1b ("the account-wide sweep is gone from the gateway") |
| both write one record per target before the wire, then trading pauses until a person clears them | `App/DashboardView.cs:580-592`; BS § U2c1b ("A press is its records") |
| each line carries what the platform answered and what the position is now | `App/DashboardView.cs:270-302` |
| the note box "What you saw in ATAS — required"; buttons off until it is filled; editing disarms | `App/DashboardView.cs:366-371, 407-418` |
| a second press while lines are open is refused, "close-all sent at HH:MM; resolve it first" | `App/DashboardView.cs:584-587`; BS § U2c1b |
| close-all re-reads the position and refuses on drift | BS § U2c1b |
| none of it lives in memory; a restart keeps the lines and the pause | `App/DashboardView.cs:589-591`; BS § U2c1b |
| an emergency waits two seconds | `Connectors.Atas/AtasConnector.cs:118, 278`; `Gateway/GatewayPipeServer.cs:731-733` |
| "'cancel-all' is NOT confirmed — check your positions and orders in ATAS." | `Connectors.Atas/AtasConnector.cs:183-192` (verbatim) |
| the read form: "could not be read, so the operation was not started. Nothing was placed or cancelled." | `Connectors.Atas/AtasConnector.cs:193-194` (verbatim) |
| the op word is the instruction (`cancel-all`, `close`, `cancel`, `place`) | `Connectors.Atas/BridgeProtocol.cs:19` |
| the five limits, and value is off by default | `App/DashboardView.cs:668-680`; `Core/Trading.cs:26,34-37` (`MaxNotionalPerOrder` unset = 0) |
| limits are enforced before anything reaches the broker; the AI cannot raise them | `App/DashboardView.cs:669` (verbatim); `TradeCli/Program.cs:208-240` (no verb) |
| a restart marks any in-flight order unknown and pauses trading | `Gateway/TradingGateway.cs:147-186` |
| the restart activity line, verbatim | `Gateway/TradingGateway.cs:183-184` |
| ATAS trading works: the 31 August walk, all five steps | BS:1317-1360 § "Verified on real Windows 11 hardware, 2026-08-31" |
| an order whose identifier could not be recorded is not sent | `AtasBridge/AtasStrategyAdapter.cs:1400-1430` |
| fully automatic is withheld on ATAS until both capabilities are confirmed | `Diagnostics/Doctor.cs:193-213` (verbatim) |
| the uninstaller leaves your records where they are | `packaging/TradeAgent.iss:119-122` |
| Checks: Check everything, Create support package, Show the file; logs only, no passwords | `App/DashboardView.cs:864-874, 896` (verbatim) |

### docs/DEPLOYMENT.md

Preconditions → `packaging/TradeAgent.iss:58,62`, `Diagnostics/Doctor.cs:58`, U4 § "Setup journey" (account
`CRYPTO5EB41`). Release cut, the full-sha rule and the five-way hash → U3 § "Building and publishing v0.1.1". The
`ATAS adapter PRESENT` line and its read-back → `packaging/build.ps1:210, 310-319`. Silent switches, Setup's own window,
per-user paths, HKCU entry → U3 § "The update, watched", `packaging/TradeAgent.iss:35-48`. Setup journey → as the guide's
rows above. Bridge staging and copy → `packaging/build.ps1:46`, `packaging/TradeAgent.iss:93`,
`Connectors.Atas/AtasInstallation.cs:42,115,181-197`. Protocol 3 and exact equality → `Core/Versioning.cs:35,48`;
refusal permanence → `Connectors.Atas/AtasConnector.cs:280-290`. `probe atas` one-line summary and PROTOCOL VERDICT →
`tools/probe/Program.cs:602-610, 950-959`. The `bridge.auth` `server_image` rewrite → U4 § "The override card" (note).
Twelve health rows and the four gating ones → `Core/Health.cs:25-29, 69-80`. Post-setup and post-update readings → U4,
U3 (verbatim). Doctor checks → `Diagnostics/Doctor.cs:43-167, 193-213`. Tailscale/SSH, `win-state.sh` exit 3, the RDP
capture limitation, `win-ps.sh`, autologon → `tools/README.md`; `Start-Process` detach → U3 § "Deviations". First-hour
items 1-6 → `TradeCli/PipeClient.cs:17-18`, U3, U4, `Core/Trading.cs:46`, `Gateway/TradingGateway.cs:256-262`,
`App/DashboardView.cs:638-642`. Support-package contents → `Diagnostics/Doctor.cs:244-323`. Rollback → **not yet walked**
(U3 § NOT VERIFIED); home survives → `Core/Paths.cs:9-30`, `packaging/TradeAgent.iss:119-122`, U3; schema 3 →
`Core/Versioning.cs:42`. §9's bridge-reinstall gap → the four citations in the report above.

### docs/MONITORING-PHASE.md

`trade` location and PATH → `AgentRuntime/ToolDeployer.cs:22`, `WorkspaceBuilder.cs:36`; token error →
`TradeCli/PipeClient.cs:17-18`. Status fields → `Gateway/GatewayTypes.cs:112-116`, snake-case `Core/Protocol.cs:90`.
Verb list → `TradeCli/Program.cs:208-240`. Support zip path, entries, secret exclusion, the UNREADABLE marker →
`Diagnostics/Doctor.cs:244-323`; button → `App/DashboardView.cs:864-868, 941-946`. Log locations →
`Core/Paths.cs:9-30`; rotation of `activity`/`engineering_log`/`health_event` and their sizes →
`Core/Db/Stores.cs:373-381`, driven from `App/AppHost.cs:235` and `GatewayHost/Program.cs:135`; nothing deletes the
trading tables (searched); witness cap → `AtasBridge/CoidWitness.cs:116`; sidecar 64 KiB and one rotated generation →
`CoidWitness.cs:136, 235-239, 2864`; the two `bridge` directories → `Core/Paths.cs:23` vs `packaging/build.ps1:46`,
resolved at `CoidWitness.cs:758`. Health rows and state words → `Core/Health.cs:25-29, 69-80`,
`App/DashboardView.cs:931-938`; cosmetic defects → U4 defects 2, 3, 4, 11. Daily item 4's two sentences →
`Core/Versioning.cs:13-34`, `Connectors.Atas/AtasConnector.cs:280-290`; the credential sentence as rendered → U4 § "The
bridge-refusal sentence". Weekly items → `Diagnostics/Doctor.cs:193-213`, `App/DashboardView.cs:638-642`. Stop rule 1's
numbers → `Gateway/GatewayTypes.cs:39` (15 s), `Core/Db/Stores.cs:95` (30 s), `App/AppHost.cs:234`; ATAS never
self-resolving → U4 § "The override card" (8 samples over 70 s). Stop rule 3 → `UpdateService.cs:543-571, 577-599`.
Stop rule 4 → `Gateway/TradingGateway.cs:232-239` and its three callers. Stop rule 5 → `TradingGateway.cs:147-186`.
How to stop → `App/MainWindow.cs:289-291`, `App/DashboardView.cs:363-371`, `Connectors.Atas/AtasConnector.cs:118`,
`CLAUDE.md` (operator authority is in-process only). Outbound alerts: nothing to cite — **not built**, and the paragraph
says so.
