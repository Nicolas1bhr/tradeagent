# U-press-budget-legs — the five multi-leg press fixtures outside DispatchRecoveryTests still on the flat 20 s budget take the per-leg arithmetic
Read `docs/HOW-WE-BUILD.md` (step 6, the `Timing` paragraph; "The fresh-fixer rule"), `CLAUDE.md`, the `## 2026-09-15 — U-close-all-win landed` section of
`BUILD-STATUS.md` (the commit count per press, `5 + 5×legs`, the runner's worst commit 2234 ms, and the five presses it named and did NOT move), then
`tests/TradeAgent.FaultTests/UnknownCloseTests.cs` (`Unresolved.PressBudget`, `PressBudgetFor(legs)` as landed, and the press at `:259` with 2 positions),
`EmergencyPressTests.cs:224,252` (2 orders each), `PressInFlightTests.cs:151` (2 positions), `LossFlattenTests.cs:206` (ES+NQ — `U-flatten-3` touches this file; rebase
after it lands). Branch `u-press-budget-legs`, worktree `~/Projects/ai-trading-software-for-mihael-worktrees/U-press-budget-legs`, rebased onto `main` first.
Test-only; no assertion loosened; no test deleted; no box, no ATAS, no money.

**Why.** `U-close-all-win` measured that a two-leg press makes 14 durable commits before its second leg reaches the wire and that the hosted Windows runner's worst
commit was 2234 ms, so a flat 20 s is short for any press with two legs; it moved the eleven presses in its own file and named these five as a debt, not as safe.

1. **Move the five, one token each** (`Unresolved.PressBudgetFor(2)`), stating the legs at each site; then sweep `tests/` for any other press fixture with two or more
   legs on the flat budget (`grep -rn 'PressBudget\b' tests/`) and name each moved or not at risk with its leg count. Prove one of them bites: the throwaway
   commit-stall hook `U-close-all-win` used (every commit held 2234 ms, reverted after) → the flat budget RED on the two-position press, the sized budget GREEN.
Gate: `dotnet build TradeAgent.sln -c Release --no-incremental` → 0 warnings; the Fault project 3× and the three test projects in Release to a file → 0 failed; names vs
`main` → nothing removed. Commit once, one sentence, no trailers. Append `## Report` (≤12 lines): tip sha, gate counts, the sites moved with their leg counts, the
stall reproduction quoted, what you did NOT do.

## Report

Gate run at `33bffa7` (this branch rebased onto `main` `6c7dd09`); rebased again AFTER the gate onto the docs-only `main` `2dcf8f3` (BUILD-STATUS.md, one line) as `5f91659`, `git patch-id --stable` identical before and after — `8e6bfd7…`. TEST-ONLY: `git diff main -- src/` empty, `git diff -U0 | grep '^[+-].*Assert\.'` empty, no test added, renamed or deleted.
Gate, Release: build `--no-incremental` → 0 warnings, 0 errors; Fault `--no-build` 3× → 350/350, 350/350, 350/350; Unit 1145/1145; Integration 668 passed + 1 skipped of 669 → 0 failed; names vs `main` → 0 removed, 0 added (1794 = 1794; `[Fact]`/`[Theory]` 1798 = 1798).
**Legs MEASURED, not read off the fixture:** a throwaway probe printed each press's own capture count (`OperatorCancelAllAsync`'s `captured`, `CloseCapturedAsync`'s `captured`, `CancelWorkingOrdersAsync`'s `targets`) beside the running test's name over the whole Fault project, then was reverted; `git status` clean of it.
**Moved, one token each:** `UnknownCloseTests.cs:261` 2 legs (ES+NQ) → `For(2)`; `EmergencyPressTests.cs:261` 2 legs (both orders rest, both cancels are the verdict) → `For(2)`; `PressInFlightTests.cs:153` 2 legs (ES waits, NQ closes) → `For(2)`; `LossFlattenTests.cs:52`, the shared `Ready` behind nine flattens, 2 legs (the daily breach closes ES and NQ; the resting-opener fixture cancels one order then closes one position inside the SAME deadline) → `For(2)`; and `EmergencyPressTests.cs:229` **ONE leg, not the brief's two** — pk-1 rests and pk-2 fills, so the cancel-all captures one working order and the close-all one position, each under its own deadline → `For(1)` (23 s).
**Swept (`grep -rn 'PressBudget' tests/`): every other flat-20 s site is ONE leg, measured, and left flat** — `UnknownCloseTests.cs:216,332`; `EmergencyPressTests.cs:49,106,146,192,289,465,493`; `PressInFlightTests.cs:80,123,207`; `PressAtomicityTests.cs:69`; `ValuationLossTests.cs:60`; `LossDirectorHoldTests.cs:46`; `LossReleaseTests.cs:46`; `LossReopenTests.cs:44`; `LossReopenApprovalTests.cs:52`; `LossRuleSnapshotTests.cs:46`; `LossStrikeTests.cs:48`. None is at risk of the two-leg arithmetic; 20 s is still under the 23 s one leg gets, which this item does not cover. `StrandedDispatchTests.cs:45` and `AgentCloseAtDispatchTests.cs:114` are doc references, no budget passed.
**Named, not moved, outside `PressBudget`:** `IntegrationTests/SweepRequestIdTests.cs:290` `SweepBudget = 20 s` feeds ten AGENT sweeps (pipe `cancel-all`/`close-all`, not a press), measured up to SIX legs (`Every_mutating_op_dispatches_once_for_one_request_id` 6; two five-order fixtures 5). Different constant, different project, `PressBudgetFor` is FaultTests-only: a debt, stated rather than touched.
**It bites, quoted.** Throwaway hook holding EVERY `Database.Write` commit 2234 ms (reverted; `git status` clean of it), `A_confirmed_daily_breach_closes_every_open_position_and_the_book_reads_flat`: on `PressBudgetFor(2)` GREEN — `Passed! - Failed: 0, Passed: 1 … Duration: 1 m 58 s`. Mutant (the flat `PressBudget` put back, same stall): RED — `Assert.Equal() Failure: Values differ / Expected: 2 / Actual: 1` at `LossFlattenTests.cs:223`, `leg : op-budget-close-…-1 NQ captured 1 -> UNKNOWN filled 0`, `book : [NQ 1]`, the leg refused with `the operation deadline had already passed and nothing was sent to the simulator`.
**The fixture I tried FIRST did not bite:** `UnknownCloseTests.cs:261` under the same 2234 ms stall passed on BOTH budgets (1 m 25 s each) — its ES leg is refused by the settle's own failed read before it can spend the budget, so 20 s still reached NQ. That site's move rests on the measured two legs and the arithmetic, not on a red.
**NOT done:** no product code and no product mutant; no assertion loosened or touched; no test deleted; no `Timing` trait; the one-leg flat sites and the Integration `SweepBudget` family not moved; no box, no CI run, no ATAS, no money.
