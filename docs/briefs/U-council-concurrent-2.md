# U-council-concurrent-2 — the consequential boundary: two sealed assessments, one bounded challenge, one disposition applied by code
Read `docs/HOW-WE-BUILD.md`, `CLAUDE.md`, `docs/COUNCIL.md:59-65` (verbatim: both directors submit an assessment BEFORE either sees the other's;
one bounded challenge; one disposition; a deadline with a predetermined default; code applies the policy so neither director can veto an
eligible deployment forever; two assessments are two turns; deduplicate boundary events by entity and revision), `:30-31` (rule 7), `:222`
(the retirement-candidate event reuses the same shape), then `Db/PublicationStore.cs` (`recipients`, `classification`, `Commit`, `IdOf`,
`delivery` committed vs delivered), `CouncilRelay.cs:365-385` (`Deliver`), `MissionEventStore.cs:14-43` and `MissionEventIds.ForRole`,
`DailyReports.cs:486-533` (the owner-message deadline shape, `overdue` at `:503`), `DailyReport.cs:200-206`, `CampaignStore.cs:291-297,320-337`
(`TrialRefusal` reads, `RegisterTrial` writes — two transactions) and `:401-431` (`ChargeVerdict`, one transaction), `Promotions.Standing` and the
verdict flow from `U-referee-2`. Branch `u-council-concurrent-2`, worktree `~/Projects/ai-trading-software-for-mihael-worktrees/U-council-
concurrent-2`, rebased onto `main` AFTER `U-referee-2` and `U-council-concurrent-1` land. Money-path grade: every guard ships with a test RED
before it and ONE mutant, quoted. No box, no ATAS, no money, no order. **Schema 16.**

**Why.** `COUNCIL.md:59-65` is the one paragraph of the doctrine with no product code at all: `grep -rn "assessment\|challenge" src` is empty,
`mission_event` has no boundary kind, and the report's "Decisions" are owner messages and warn lines. `U-referee-2` defers the directors'
sealed assessments here by name, and `:222` needs the same shape for retirement. `U-referee-1` left one race: the trial refusal is read in one
transaction and the trial registered in another, so two roles can exceed a campaign's trial budget by one.

1. **A boundary event, deduplicated by entity and revision:** `boundary_event` (kind — `promotion`, `retirement`; entity id; revision;
   opened_at; deadline; the predetermined default; disposition; disposed_at, by) keyed by (kind, entity, revision) — a repeat writes nothing
   and buys no turn (the `MissionEventIds.ForRole` shape); opening one wakes both directors with one paid turn each. RED: two raises of one
   proposal buy two turns each. Mutant (the key taken from the attempt): a restart manufactures senior spend.
2. **Two sealed assessments:** each director's `assessment` publication is committed but its `delivery` to the peer is WITHHELD until both
   exist, then both released together by `Deliver`; a second assessment from the same director for the same boundary is refused. RED: the
   first assessment reaches the second director's `in/` before they have written theirs. Mutant (released when one exists): the same.
3. **One bounded challenge, then one disposition:** exactly one `challenge` publication per boundary, capped like a report, from either
   director after both assessments are delivered; a second is refused and unpaid. RED: a second challenge accepted and paid. Mutant (the cap
   dropped): an over-length challenge published.
4. **The deadline's default is applied by CODE:** at the deadline, or when the challenge window closes, the app writes the disposition itself
   from the policy's default (`Promotions.Standing` for a promotion boundary), never from a director; the Situation shows each director their
   open boundaries and deadlines; section 8's decisions list every disposition with its evidence, owner and deadline. RED: the deadline
   passes and the boundary stays open for ever. Mutant (the comparison inverted): a boundary disposed the instant it opens.
5. **The trial budget charged in one transaction:** `TrialRefusal` and `RegisterTrial` become one `Database.Write` (or the charge precedes the
   run, as `ChargeVerdict` does). RED (the `Barrier(2)` idiom on the last trial): `Expected: 200 / Actual: 201`. Mutant (the check outside the
   write): the same.

Not this unit: the allocator and evolution (`COUNCIL.md:272`); retirement beyond reusing the tables; promotion itself and `TryAuthorizeExecution`;
the boundary on a screen beyond the Situation and the report; a third role.
Gate: `dotnet build TradeAgent.sln -c Release --no-incremental` → 0 warnings; the three test projects in Release to a file → 0 failed; touched
classes 3×; a `Timing` red re-run alone 3×, never loosened. Commit per item, one sentence, no trailers. Append `## Report` (≤20 lines): tip sha,
gate counts, one line per item with its RED and mutant, what you did NOT do.

## Report

**Gated tip `0937b43`** (this report commit is docs-only on top), **rebased onto `main` `b77aaa8`** — `U-council-concurrent-1` landed while I built, `git merge-base --is-ancestor 4753c46 main` printed YES, and I rebased TWICE with **no conflict to resolve** either time: the first replayed my five commits over the landing, the second took `main`'s CI-verdict commit, which touches only `BUILD-STATUS.md` and `docs/RESUME-HERE.md` — `git diff` between the two tips over `src` and `tests` is EMPTY, and the build and the touched classes were re-run at this sha to say so. `git rev-list --count HEAD..main` prints 0. `U-venue-catalog` had NOT landed, so the ladder here is 15 → 16 and its 17 goes above mine untouched. `git status --porcelain` and `git diff --stat` both print nothing after every mutant was restored.
**Gate**, all Release, outputs written to `/tmp` so nothing was left inside the worktree: `dotnet build TradeAgent.sln -c Release --no-incremental` → **`Build succeeded. / 0 Warning(s) / 0 Error(s)`**, 17 projects (DLL mtimes 20:11:14-16 against a 20:11:22 wall clock, because 2.8 s looked too fast). Touched classes 3× (`BoundaryLedgerTests`, `CouncilRelayTests`, `CouncilLoopTests`, `DailyReportTests`, `RefereeVerdictTests`, `CampaignLedgerTests`): **`Failed: 0, Passed: 81, Total: 81`** on each of the three runs. `--no-build -c Release`: Unit **`Failed: 0, Passed: 1008`**, Fault **`Failed: 0, Passed: 277`**, Integration **`Failed: 0, Passed: 657, Skipped: 1`**. Nothing went red, so no `Timing` re-run — and `ps` showed **two other legs' suites (`U-press-inflight-win`, `U-venue-catalog`) running on this Mac at the same time**, which makes that green stronger rather than weaker; the ppid-1 scan over 40% found only Apple's `WindowServer` at 40.6%. Names vs `main`: **1522 → 1542, the removed set EMPTY**, the 20 added are this unit's own; `file` reads all 159 test sources as text.
**1 — `boundary_event`, keyed `kind:entity:revision`, opened by the referee in the verdict's own transaction and waking both directors once.** RED (the rung absent, which is `main`): **`SQLite Error 1: 'no such table: boundary_event'`**, 4 failed / 0 passed. Mutant (the id minted per raise — the key taken from the attempt): **`Assert.Equal() Failure: Expected: 2 / Actual: 4`** paid wakes after a restart; with the UNIQUE index left in, that mutant cannot even land the row — **`SQLite Error 19: 'UNIQUE constraint failed: boundary_event.kind, boundary_event.entity, boundary_event.revision'`**.
**2 — the sealed pair: committed at once, `delivery` WITHHELD until both exist, released together by `Deliver`.** RED (`Commit` in place of `CommitSealed`): **`Assert.Empty() Failure: Collection was not empty / Collection: ["abb09c28b6ee4e…"]`** — the first assessment in the chair's `in/` before it had written its own. Mutant (released when ONE exists, `COUNT(DISTINCT role) >= 1`): the same message on the same line. A second assessment from one director is refused in words and by `PRIMARY KEY(boundary_id, role, kind)`.
**3 — one bounded challenge per boundary, from either director, after both assessments are delivered.** RED (no count at all): **`Assert.Single() Failure: The collection contained 2 items`** — two `challenge` wakes, so the second was accepted AND paid. Mutant (the cap dropped): **`Assert.Empty() Failure: Collection was not empty`** with the over-length challenge's wake in it.
**4 — the disposition written by CODE at the deadline, or when the challenge window closes.** RED (`ApplyDue` applying nothing): **`Assert.Equal() Failure: Strings differ / Expected: "deploy" / Actual: null`** in the loop and in the report, and `Assert.Single() Failure: The collection was empty` at the store. Mutant (`now <= DeadlineAt`): **`Collection: [BoundaryRow { … DisposedAt = 09/13/2026 12:01:00 … DisposedBy = policy }]`** — disposed a minute after opening, before either assessment. The loop sleeps until the earliest deadline and launches nothing to settle one; section **9** of the report (the brief says 8; 8 is Research evidence) lists every boundary with its evidence, its owner and its deadline, OVERDUE when open past it.
**5 — the trial charged in ONE `Database.Write`.** RED (the gate removed from the write, leaving only the caller's look — `main`'s shape), two real threads through a `Barrier(2)` on the last of 200: **`Assert.Equal() Failure: Expected: 200 / Actual: 201`**. Mutant (the already-registered early return dropped): a re-asked trial refused with `campaign 1 has registered all 1 of its research trials …`. **NOT VERIFIED:** a second mutant — the count read *outside* `db.Write` — **passed**, because the test's barrier synchronises the pre-run look and not the store's own read, so the two threads did not collide there; the RED above is the edit the brief names as that mutant and it is quoted.
**Where COUNCIL is silent, the choices — all in `CONTRACTS.md`.** A promotion boundary is (version, campaign): re-asking is free, a renewed campaign is a new question. **Opening buys the two turns and releasing buys none** — ":63, two assessments are two turns" is the count, and waking both again to read each other would double every boundary; a CHALLENGE does buy the peer one, because a challenge nobody is woken for is answered after the deadline. **Which boundary a submission answers is the app's** (the oldest open one this role has not answered), for the reason `KindFor` decides what a role's output is. "The challenge window closes" = both assessments delivered AND the one challenge written. The window is a **24-hour constant and not a setting**, because a deadline the owner could move is the veto :62 forbids. `withheld` is a delivery STATE, not a missing row, so a restart can tell a held delivery from a failed one. `disposed_by` has exactly one possible value today and is printed anyway. A trial refused after its run **rolls the run back and its figures are never served**.
**Schema 16**, `if (have < 16)` under 15 with the `Versioning.cs` paragraph; nothing below it edited. The pin in `BoundaryLedgerTests` is a FLOOR (`>= 16`) plus "the stamped number is this build's" plus both tables' column lists in order — asserted because the KEYS are the design and because `boundary_event` deliberately carries no `attempt`, `session` or `opened_by`.
**NOT done.** Nothing reads a disposition: a `deploy` deploys nothing, `TryAuthorizeExecution` and the order path are untouched, and the allocator, evolution and retirement are absent beyond `BoundaryKind.Retirement` being a value nothing opens. No screen beyond the Situation's per-role lines and the report — no boundary anywhere in the app's own window, no button, no way for the owner to open, extend or answer one. No pipe op and no `trade` verb reaches any of it, and a test asks the shipped `Ops` vocabulary by name and asserts no method takes a disposition. Both directors' `AGENTS.md` now says how a boundary is answered. `BUILD-STATUS.md` untouched, which is the manager's pass. No box, no ATAS, no money, no order placed, no authority granted, no network, no console.
