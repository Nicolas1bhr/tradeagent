# U-data-2 — a second candle source, a bar that says what it is, and validated data arrival as a wake
Read `docs/HOW-WE-BUILD.md`, `CLAUDE.md`, `docs/COUNCIL.md:145` (quality flags on bars), `:164-172` (Binance first; Revolut X public candles, five
minutes, a ninety-day target, actual depth recorded, a candle WITHOUT volume midpoint-derived and flagged, never trade evidence; Binance results
hypothesis evidence, not execution evidence), `:89-91` ("validated data arrival" among the persisted wakes), then `Core/Data/BinanceArchive.cs`
(`Source`, `Interval = "1m"`, `MonthsWanted = 12`, the checksum sidecar, never the month in progress), `KlineNormaliser.cs` (`Header` six columns, gaps
never filled), `DatasetReader.cs:7-13` (`KlineBar`), `BarFeed.cs`, `Provisioning/BinanceDataService.cs`, `BinanceArchiveClient.cs`, `Downloader.cs`
(the injectable request timeout), `Db/DatasetStore.cs`, `Db/MissionEventStore.cs:11-44` (kinds: no `data`), `App/SettingsView.cs:161-182`, the
`## 2026-09-08 — U-data-binance landed` and `U-archive-win landed` sections of `BUILD-STATUS.md` (the loopback harness, the host scan, the
one real fetch as evidence), `docs/RESEARCH-REQUIRED.md:170` (Revolut X: only the SIGNED trading base is recorded; no public-candles path, no
sandbox). Branch `u-data-2`, worktree `~/Projects/ai-trading-software-for-mihael-worktrees/U-data-2`, rebased onto `main` AFTER `U-venue-catalog`
lands. Every item ships with a test RED before it and ONE mutant, quoted. NOT the money path. No box, no ATAS, no money, no network. **Schema 18.**

**Why.** One source, one interval (a `const`), one coverage target and a bar with five numbers and no flag: the pipeline's Binance-specific
assumptions cannot be told from its general ones, and COUNCIL names a second source whose candles may carry no volume. Nothing wakes a role
when validated data arrives. THE FACT the builder must carry: this repository records NO Revolut X public-candles endpoint; the source's URL
shape is DATA (`sources.json`, the `runtimes.json` pattern), marked unverified, built and proved against the loopback harness only, and the one
real fetch is the owner's to authorise once the endpoint is supplied — say so in `CONTRACTS.md` and in the report.

1. **The collector is an interface:** `CandleSource` (id, interval, coverage target, URL shape, whether the vendor publishes a checksum, whether
   candles carry volume); Binance is one implementation, its dataset rows byte-identical to today's. RED: a source declaring five minutes and
   ninety days produces a `1m`/12-month row. Mutant (the interval read from `BinanceArchive.Interval`): the same.
2. **A per-bar quality flag** (`traded` / `midpoint_derived`) in the normalised file — header versioned so an old file still reads — and counted
   on the dataset row. RED: a volume-less candle normalises to `volume=0` and nothing says so. Mutant (the flag written, not counted): the row
   claims depth it does not have.
3. **A volume-less candle is never trade evidence:** `data-bars`, `BarFeed`, a backtest over such a dataset and section 8 say so in words. RED:
   a run over midpoint candles reports like a run over traded bars. Mutant (said only on `data-list`): the backtest reply silent.
4. **The second source end to end against the loopback harness:** the source's marks per request, the host scan extended to its host, provenance
   identical (url, published hash where the vendor publishes one, computed hash, bytes, download time), the dataset naming its venue
   (`U-venue-catalog`). RED: a second source's dataset records no raw file. Mutant (the published-hash check skipped where none is published):
   a source with no checksum accepted as verified.
5. **`data` is a wake:** an ACCEPTED dataset raises one deduplicated `MissionEventKind.Data` event for Research; a re-verification raises
   nothing; the Situation names the new dataset. RED: collecting twelve months wakes nobody. Mutant (keyed by the collection attempt): a
   rebuild buys a paid turn every press.

Not this unit: the Revolut X trading connector or any credential; `U-bars` (ATAS futures, the box); Databento (a paid key); live bars.
Gate: `dotnet build TradeAgent.sln -c Release --no-incremental` → 0 warnings; the three test projects in Release to a file → 0 failed; touched
classes 3×; a `Timing` red re-run alone 3×, never loosened. Commit per item, one sentence, no trailers. Append `## Report` (≤20 lines): tip sha,
gate counts, one line per item with its RED and mutant, the endpoint fact restated, what you did NOT do.

## Report

Gated sha `4f78646`, rebased onto `origin/main` `9f8f878` (three `U-flatten` briefs; no conflict) and gated there; this report is the docs-only commit on top of it, so the branch tip is one commit further on. Build `dotnet build TradeAgent.sln -c Release --no-incremental` → **0 Warning(s), 0 Error(s)**. Suites in Release `--no-build`, one at a time, each to a file: Unit **1038**, Fault **277**, Integration **668** = **1983 passed, 0 failed, 1 skipped**. Touched classes 3× → **107/107** Unit and **27/27** Integration on each of the three runs. Test names vs `origin/main` → **14 added, 0 removed** (sets 1568 → 1582). No `Timing` red anywhere, so no re-run was owed.

1. **The collector is an interface.** `ICandleSource` — id, display name, venue, interval, coverage target in UTC days, URL shape, publishes-checksum, candles-carry-volume — with Binance one implementation and every declaration recorded on the row at **schema 18**. RED, which for this item IS the brief's mutant (the interval and the target read back off `BinanceArchive.Interval` and twelve months): `Expected: "5m" / Actual: "1m"`, `Failed: 1, Passed: 3`. **Binance byte-identity proved, not asserted:** the normalised file's SHA-256 was measured on `main`@`39d3e4f` before one line of product code changed — `6a5958f4f261e333136a652be9f95972726a34af42aefe66da8d19b75184b6bb` — and is the constant the test still meets, header and bar count with it.
2. **A per-bar quality flag.** Header versioned: six columns as before for a source whose candles always carry volume, seven with `quality` for one that may not, so a file written earlier still reads and every bar in it reads `traded`. RED (the flag not produced): the FILE's midpoint rows, `Expected: 4 / Actual: 0` at `CandleSourceTests.cs:194`. Mutant (written but not counted, `MidpointDerived = 0`): the ROW's count, `Expected: 4 / Actual: 0` at `:199` — the assertions are ordered so the two failures are distinguishable.
3. **Never trade evidence, in words, from one definition** (`BarQuality.Note`): `data-bars` (plus a `quality` on every bar and both counts), the backtest reply, section 8 of the owner's report, `BarFeed.MidpointNote`; and `data-list`, the pipe schema and the Situation's Data line. RED (the note suppressed everywhere): `Failed: 5, Passed: 0`, the backtest reply `Not found: "MIDPOINT-DERIVED"`. Mutant (said only on `data-list` — removed from the backtest reply): `Failed: 1, Passed: 4`, same `Not found`.
4. **The second source end to end against the loopback harness.** One request per period on the harness's own marks (`GET /candles`, no `.CHECKSUM` ever asked for), provenance identical (url, computed hash, bytes, download time), `published_sha256` EMPTY where the vendor publishes none, and the dataset naming `revolut-x` — a venue now in the catalogue with NO instruments and `verified=false`, so a run over it is refused an increment rather than given a guess. RED (the sidecar demanded unconditionally, which is exactly what the code did before): `Assert.NotNull() Failure: Value is null` — no dataset at all, so no raw file — `Failed: 4, Passed: 2`. Mutant (the app's computed hash recorded as the published one): `Expected: "" / Actual: "7b657e8ac55e2c25595c08ba5dfdf8f06ff51127e"…`, `Failed: 2, Passed: 4`. Host scan extended: a test naming the vendor's host is an offender, and a separate test holds the shipped row to having no endpoint at all.
5. **`data` is a wake,** raised for Research by the app's own collector, keyed by the dataset's own SHA-256. RED (nothing raised): `Expected: True / Actual: False`, `Failed: 2, Passed: 6`. Mutant (keyed by the collection attempt): `Expected: "data:6a5958f4f261e333136a652be9f95972726a"… / Actual: "data:2026-09-14T17:23:22.5430520+00:00#re"…` — a rebuild would buy a paid turn every press. A rebuild of the same bytes raises nothing and the Situation names the dataset.

**THE ENDPOINT FACT, restated.** This repository records NO Revolut X public-candles endpoint: `docs/RESEARCH-REQUIRED.md`:170 holds only the SIGNED trading base and there is no sandbox. The shipped `revolut-x-public-candles` row therefore carries an **empty base URL** and an **unverified** URL shape (DATA in `sources.json`, the `runtimes.json` pattern); asking it for a period is a refusal in words rather than a request; every proof above is against a loopback `HttpListener`; **no run of this repository has ever sent Revolut X a request**; and the first real fetch is the owner's to authorise by putting the endpoint in `sources.json`. Said in `docs/CONTRACTS.md` under a new "Candle sources" section.

**Choices and deviations, named.** (a) Items 1 and 2 are ONE commit: they share `KlineNormaliser.Normalise`, the same three `dataset` columns and the schema-18 rung, and splitting them would have produced a commit that does not compile. (b) `BinanceDataService` → `MarketDataService`, because it now collects any source. (c) `MonthResult`/`RawMonth`/`MonthOutcome` and the `month` column KEPT and documented — a "month" is one PERIOD — rather than renamed, which would be a migration for cosmetics. (d) The coverage target is in UTC DAYS for both sources (365 for twelve months) because a month count cannot state ninety days, and the ACTUAL depth is computed from `first_bar`/`last_bar` rather than stored, so two columns cannot disagree about one fact. (e) Gaps are counted at the SOURCE's interval; at one minute a clean 5-minute dataset read as 80% holes. (f) The second source's response format (kline column order, an empty volume column meaning midpoint-derived) is this build's REQUIREMENT of a source and as unverified as its URL — not a discovery about the vendor. (g) `VenueCatalogTests`' schema-rollback fixture extended to drop 18's columns too: it simulates a pre-17 database and a reopen runs every rung above 16.

**NOT done, NOT verified.** No network, no box, no ATAS, no money, no order, no credential — and nothing on the agent pipe collects, rebuilds or wakes. Not run on Windows. The Settings press was not exercised on a screen; the collector behind it was, through its own API. The second source's real response shape and real URL: **NOT VERIFIED**, and not verifiable from here. The brief's "Not this unit" list was left alone: no Revolut X connector, no `U-bars`, no Databento, no live bars.
