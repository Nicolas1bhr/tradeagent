# U-material-origin — a file the app wrote into a role's home is measured as the app's, never as the agent's
**Boundary protected:** the evidence zone's measurement (`CLAUDE.md`: `material` is written only by the scanner; `docs/PRINCIPLES.md` § Evidence: distinguish
measurements, external claims and agent interpretations). **Defect (SOURCE, found by `U-language-in-home`):** `MaterialScanner.cs:66,81,135-136` records every
file under a role's tracked directories (`trading`, `research`, `strategies`, `data`, `scripts`, `in`, `out`) as `MaterialOrigin.Agent` — so the reference the app
writes to `research/STRATEGY-LANGUAGE.md`, the three programs under `strategies/examples/` and the deliveries the app puts in `in/` are measured as "the agent
produced it". A false origin in the one table the agent cannot edit is a measurement error, not a formatting one. **Observable result:** after a start, those
files carry an origin that says the app wrote them; a role's edit of one is measured as the agent's; nothing the agent writes can claim the app's origin. Not the
money path. No schema rung unless the origin column needs a new value (it is an enum stored as text — check `Materials.cs` and `MaterialStore.cs` first).
Read first: `src/TradeAgent.Core/Materials.cs` (`MaterialOrigin`), `MaterialScanner.cs:1-140` (the pass, the inbox attestation, the role paths), `Db/MaterialStore.cs`;
`src/TradeAgent.AgentRuntime/WorkspaceBuilder.cs` and `ResearchLibrary` (what the app writes and when); `CouncilRelay.cs` (what it delivers into `in/`);
`tests/…/MaterialScannerTests.cs`. Rebase onto `main` first; resolve conflicts yourself. Off-limits: `TradingGateway.cs`, `Database.cs`, `Versioning.cs`,
`GatewayPipeServer.cs` (the deployment leg's).
Items, one commit each with a one-sentence message:
1. `MaterialOrigin.App`: the app records what it writes into a role home — path relative to the home and sha256 — in one app-owned manifest (`state/`, written by
   `WorkspaceBuilder`/`ResearchLibrary`/the relay at write time, never by a role; say where and why in `CONTRACTS.md`). The scanner, meeting a file whose path AND
   hash match the manifest, records `App`; a file at a manifested path with another hash is `Agent` (the role changed it); a path the manifest does not name is
   what it is today. The inbox attestation is untouched.
2. `material-list` and the owner's report print the origin word for `App` files as "written by TradeAgent" and never count them as the agent's work; `AGENTS.md`'s
   sentence about materials names the three origins in one line.
Red-first tests: (a) `The_language_reference_and_the_examples_are_measured_as_the_apps_after_a_start` (RED today: `Agent`); (b) `A_roles_edit_of_an_app_file_is_
measured_as_the_agents`; (c) `A_file_the_agent_writes_at_an_unmanifested_path_cannot_be_measured_as_the_apps`; (d) `A_delivery_the_relay_writes_into_in_is_the_apps`.
Mutant to watch red and quote: the hash check dropped (path match alone → `App`) → (b) red. Nothing removed or renamed; exact column-list assertions extended.
Gate and report as `docs/HOW-WE-BUILD.md` (`--no-incremental` Release 0 warnings; three suites 0 failed; classes 3×; names 0 removed; `## Report` ≤ 20 lines here).
No push, no merge; touch nothing in `docs/briefs/` but this file.

## Report
Tip `f02b917`, rebased onto `main` at `3746774` (the language-in-home landing): 2 commits replayed, the 5 already-landed ones skipped by patch-id, no conflict. Gate there: `dotnet build TradeAgent.sln -c Release --no-incremental` → **0 Warning(s), 0 Error(s)**, 19 projects; Unit **1186/0/0**, Fault **393/0/0**, Integration **688 passed / 0 failed / 1 skipped** (689, 11m07s). Touched classes 3×: `MaterialAppOriginTests` 5/5 and `MaterialOverPipeTests` 6/6, identical each run. Test names `main`→tip **1919 → 1925, 0 removed**, 6 added.
1. DONE — `MaterialOrigin.App` and `AppFileManifest` (`state/app-files.tsv`: outside the workspace, beside `data/` and `reports/`, no verb and no pipe op writes there). `WorkspaceBuilder.Build`, `ResearchLibrary.Write` and `CouncilRelay.Deliver` record `<path relative to the home>` + sha256 AFTER the bytes land; the scanner records `App` only when the path AND the hash match, a manifested path holding other bytes is `Agent`, and an unmanifested path is what it was. The quarantine move is deliberately not recorded — those are the role's own bytes. **No schema rung**: `material.origin` is TEXT with no CHECK, so `App` is a new value in it. `MaterialStore.Observe` gained a `Func<MaterialOrigin>` overload so the bytes are read only for a row about to be written: a re-sighting of an unchanged file still costs a directory listing and nothing else. The role group now carries `[Agent, App]`, so `MarkMissing` still stamps a deleted app file gone. Inbox attestation untouched. `CONTRACTS.md` says where the manifest lives and why.
2. DONE — `material-list` takes `origin: app`, prints `app`, and its note reads "origin 'app' reads: written by TradeAgent … never counted as your work"; `trade schema`'s `material-list` entry says the same and its `origin` argument lists `app`; `AGENTS.md` names the three words in one line (`inbox` / `app` / `agent`) and says the role cannot set them; the owner's Inbox page prints "written by TradeAgent" instead of falling through to "the AI made this". `Present(MaterialOrigin.Agent)` no longer returns them.
RED on the base (enum and manifest present, scanner unwired), quoted: (a) `Assert.Equal() Failure: Values differ / Expected: App / Actual: Agent` at `MaterialAppOriginTests.cs:73` (now `:75`); (b) the same at `:103` (now `:105`); (d) the same at `:195` (now `:197`). (c) was GREEN on the base — nothing could be `App` yet — so it is recorded as **a guard checked, not a RED**.
MUTANT (the brief's), the hash check dropped so `_appFiles.Names(rel)` alone decides → (b) red: `Expected: Agent / Actual: App` at `MaterialAppOriginTests.cs:110` (now `:112`). Put back, green.
SECOND MUTANT, the path half dropped (`Names` always true, `Wrote` matching the sha anywhere) → (c) red at `:147` (now `:149`) and (d) red at `:198` (now `:200`), both `Expected: Agent / Actual: App`. Put back. That is what makes (c) a guard with a demonstrated bite rather than an assertion nobody has seen fail.
ITEM 2 MUTANT, the `App` arm removed from the page and the `"app"` case from the filter → `Expected: "written by TradeAgent" / Actual: "the AI made this"` at `MaterialAppOriginTests.cs:220`, and over the pipe `{"code":"INVALID_REQUEST","message":"origin 'app' is not one of: ..."}` at `MaterialOverPipeTests.cs:210`. Put back.
DEVIATIONS: (i) on `material-list` the `origin` FIELD stays the machine word `app` — a value a role filters on — and "written by TradeAgent" is printed in the reply's `note` and in `trade schema`; the owner's page prints the phrase itself. (ii) Two tests beyond the brief's four, because item 2 had none: `An_app_file_reads_as_written_by_TradeAgent_and_never_as_the_agents_work` (unit) and `A_file_TradeAgent_wrote_is_listed_as_the_apps_and_is_not_returned_as_the_agents` (integration, over the pipe); both are mutant-checked above. (iii) `CONTRACTS.md`'s `origin` vocabulary line gained `inbox-unattested` as well as `app` — the handler has accepted it since it was added and the line did not say so.
NOT DONE: the CLI help line `trade material list [--origin inbox|agent]` in `src/TradeAgent.TradeCli/Program.cs` — OFF-LIMITS this leg — still omits `app`; `--origin app` reaches the gateway regardless and `trade schema` names it. "The owner's report" is read as the owner's Inbox page: `DailyReports.cs` has no material section and none was added. Rows measured before this unit keep the word they were measured with — the app rewrites the reference and the examples on every start, so those correct themselves at the next one, while a delivery already sitting in `in/` stays `Agent`. No Windows box, no provider, no venue, no order. No push, no merge.
