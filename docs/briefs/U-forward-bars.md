# U-forward-bars — real, advancing one-minute bars the app collects itself: closed bars only, receipt provenance, apart from the frozen evaluation datasets
**Arrow closed:** forward paper on ADVANCING market data (`manager-prompt.md` § 5 "Actual market observation"). **Observable result:** while the app runs, every
CLOSED 1m bar of the configured pair arrives from Binance's market-data-only host within about a minute of its close, with the instant it was received, gaps
counted and never filled, served read-only to the roles and to the runner to come; nothing here places an order. **Schema 24** (after `U-paper-verdict`'s 23, which lands before you; if it has not landed when you gate, your ladder reads 22 → 24 and that is expected — write no 23 block).
Not the money path. RUN from this Mac 2026-09-19: `GET https://data-api.binance.vision/api/v3/klines?symbol=BTCUSDT&interval=1m&limit=2` → HTTP 200, 0.46 s,
no key (official: developers.binance.com/en/docs/products/spot/faqs/market_data_only). Never call the real host from a test.
Read first: `docs/PRINCIPLES.md` § Evidence; `docs/CONTRACTS.md` "Candle sources" (2130-2174); `KlineNormaliser.cs:68-134`; `BinanceArchive.cs:27-36`;
`CandleSourceCatalog.cs:80-134`; `CandleSourceClient.cs:30-60`; `MarketDataService.cs:42-112,189-208`; `DatasetReader.cs:49-86`; `Protocol.cs:41-47`
(`data-list`/`data-bars`); `GatewayPipeServer.cs` their handlers; `AppHost.cs:928-993`; `Database.cs:1118-1183`; `tests/…/BinanceArchiveTests.cs` (the loopback
`HttpListener` harness with an injectable seconds-long client timeout — the shape `U-archive-win` demanded).
Items, one commit each with a one-sentence message:
1. Schema 25: `forward_bar` (source, symbol, open_time, open, high, low, close, volume, close_time, received_at, fetch_id; PRIMARY KEY (source, symbol, open_time)),
   `forward_fetch` (id, source, symbol, url, requested_at, received_at, http_status, bars, first_open, last_open, body_sha256, note) and `forward_gap` (source,
   symbol, from_open, to_open, bars_missing, seen_at). A bar is written only when its `close_time` precedes `received_at` (closed) and only through
   `ForwardBarStore.Append(fetch, bars)` in one transaction; a re-fetched bar that differs from the stored one is NOT overwritten — the first reading stands and
   the disagreement is counted in `forward_fetch.note`. Times normalised through `KlineNormaliser.TryUnitOf`; a body that does not parse is a recorded failure.
2. `ForwardBarCollector` (Provisioning): every 60 s for `Settings.MarketDataPair`, while the Data page's "collect live bars" toggle is on (one press, default ON;
   the `SettingsView.cs:157-165` family), GET `/api/v3/klines?symbol=S&interval=1m&startTime=<last stored open + 60 s>&limit=1000` on the host recorded as a
   catalogue entry `binance-spot-forward-klines` beside the archive's (`CandleSourceCatalog.cs:119-134`, `Verified = true`, the doc URL on the row — data, not
   code); 10 s request timeout; exponential backoff to 5 min on failure; every attempt a `forward_fetch` row; a gap between the stored last bar and the next
   received bar a `forward_gap` row. Started by `AppHost` with the app, stopped with it, independent of the mission loop; raises an in-process
   `BarClosed(symbol, openTime)` event the runner will subscribe to. Never blocks a UI thread; a failing host is a status line, not a crash.
3. Read-only surface: `data-list` names the forward series (symbol, first/last bar, bars, gaps, `last_received_at`, and the sentence "FORWARD — collected by
   TradeAgent minute by minute; no vendor checksum; not evaluation evidence"); `data-bars --source forward` serves a bounded window (≤ `DatasetReader.MaxBars`) to
   any role — no holdout applies (every forward bar post-dates every freeze; say so in the contract); `ForwardBarStore.Since(symbol, openExclusive, limit)` and
   `Freshness(symbol, now)` (the last closed bar's age) for the runner; `status` carries `forward_data {symbol, last_bar, age_seconds, gaps_today, last_error}`;
   the owner's report prints one line in section 7. Staleness is a fact answered here and consumed by the program's `data_freshness` bound at dispatch.
4. `CONTRACTS.md` new section "Forward bars" (what is claimed: closed bars as the vendor served them at receipt; what is not: a vendor checksum, executability,
   evaluation evidence); `USER-GUIDE.md` the Data page paragraph; `docs/RESEARCH-REQUIRED.md` the host and endpoint with their source and date.
Red-first tests (Unit/Integration, loopback listener serving canned kline JSON, never the real host): (a) `Only_closed_bars_are_stored_and_the_open_one_is_not`;
(b) `Every_fetch_is_recorded_succeeded_or_failed_and_a_bar_names_its_fetch`; (c) `A_gap_is_recorded_and_never_filled`; (d) `A_differing_refetch_does_not_
overwrite_the_first_reading`; (e) `Data_bars_serves_forward_bars_to_a_role_bounded_and_without_a_holdout_refusal`; (f) `The_collector_backs_off_after_failures_
and_recovers`; (g) `A_body_that_does_not_parse_is_a_recorded_failure_not_a_bar`. Mutants to watch red and quote: (i) the closed-bar check dropped → (a) red;
(ii) `INSERT OR REPLACE` in place of first-reading-stands → (d) red. The collector's timer injectable so tests never wait a minute.
Gate and report as `docs/HOW-WE-BUILD.md`: `--no-incremental` Release build 0 warnings; the three suites 0 failed; touched classes 3×; names vs `main` 0
removed; `## Report` ≤ 20 lines appended here. No push, no merge; touch nothing in `docs/briefs/` but this file.

## Report
All four items DONE. Rebased onto `main` at `e89a4cb`, so the ladder is now the real **22 → 23 → 24**.
**The rebase over rung 23.** Two files conflicted, both resolved by hand and by keeping BOTH sides: `Database.cs` — the incoming rung-23 block sits above my rung-24 block, each closed, `have < 23` then `have < 24`; `Versioning.cs` — both doc paragraphs in ladder order, constant **24**. `VenueCatalogTests`'s rollback fixture auto-merged with both arms (23's two `paper_policy` drops and my three `forward_*` table drops) and was checked line by line. Nothing else conflicted. `git diff 3331fc0 HEAD -- src tests` is **rung 23 and nothing else**: the eight `U-paper-verdict` files, plus the rung-23 block in `Database.cs`, its paragraph in `Versioning.cs` and its two fixture lines. Every file this unit owns is byte-identical across the rebase.
**Gate on the rebased tip, all quoted from runs on this Mac.** `dotnet build TradeAgent.sln -c Release --no-incremental` → `0 Error(s)` / `0 Warning(s)`. Unit `Failed: 0, Passed: 1168, Skipped: 0`; Fault `Failed: 0, Passed: 374, Skipped: 0`; Integration `Failed: 0, Passed: 685, Skipped: 1` in 11 m 7 s. Touched classes 3×: the five unit classes `Passed: 34` three times, `ForwardBarsOverPipeTests` `Passed: 8` three times. The ladder matters most now, so every class that asserts `schema_version` — the rollback fixture and the eleven ledger classes — also ran 3×: `Passed: 109` three times. Test-name diff vs `main`: main 1871, tip 1889, **0 removed**, 18 added.
**RED first.** (e) is a behavioural red: on the base the whole integration class ran and gave `Failed: 7, Passed: 0`, the headline one refusing with `{"code":"HOLDOUT_WITHHELD","message":"dataset 1 (BTCUSDT 1m v1) holds out every bar from 2026-08-01 01:00:00Z onwards…"}` — `--source` was ignored and the archive answered. The rung's rollback fixture is a behavioural red too: `SqliteException : SQLite Error 1: 'no such table: forward_bar'` at `VenueCatalogTests.cs:246`. (a)(b)(c)(d)(f)(g) are red on the base only as a COMPILE failure — `error CS0246: The type or namespace name 'ForwardBars' could not be found`, same for `ForwardFetchAttempt` and `ForwardBarCollector` — because their subject is a new type. Stated plainly rather than dressed up; the behavioural evidence for (a) and (d) is the two mutants.
**Mutants, both red, quoted at the rebased tip.** (i) closed-bar check dropped → `Only_closed_bars_are_stored_and_the_open_one_is_not … Assert.Equal() Failure: Expected: 2 Actual: 3` at `ForwardBarStoreTests.cs:59`. (ii) `INSERT OR REPLACE` → `A_differing_refetch_does_not_overwrite_the_first_reading … Expected: 0 Actual: 2` at `ForwardBarStoreTests.cs:167`. Both put back; tree clean, 0 warnings after.
**Two defects the mutants and tests caught, both fixed.** (1) Mutant (ii) first passed: `Append` read the row and `continue`d, so `INSERT OR REPLACE` was never reached — a check-then-skip cannot state that nothing was overwritten. The insert now runs always and its conflict clause is what refuses. (2) `Since` clamped at exactly `DatasetReader.MaxBars`, so a 10,050-bar window came back as 10,000 with nothing saying so; it now clamps at `MaxRows = MaxBars + 1` and the pipe op refuses on the extra row, as `DatasetReader.Read` does.
**Deviations, with reasons.** (1) Item 1's line says "Schema 25"; the brief's own header, the manager and `U-paper-envelope` all say 24, so 24 it is — a typo in the item line. (2) Section 7 of the owner's report is "Other operating costs" and its record held only a gap list; the brief says section 7, so the line is there, worded as a COUNT and never a price, and the existing gap saying nothing here is priced is kept and asserted. (3) The app wiring (AppHost start/stop, the one-press default-ON toggle, `TradeAgentSettings.CollectLiveBars`) is in item 2's commit, which is where the brief puts it.
**One existing assertion changed, named.** `DataOpsTests.The_schema_names_both_data_operations_and_neither_of_them_mutates` pinned the exact usage string `trade data bars --pair P [--from D] [--to D]`; it now pins the same string plus `[--source archive|forward]` and additionally asserts `source` is present and NOT required. No test removed, renamed or weakened. `SuiteReachesNoVendorTests` was STRENGTHENED twice: the forward host (`data-api.binance.vision` does not contain `data.binance.vision`, so the old scan would have missed it) and a file-level rule that a test building a `ForwardBarCollector` must name a `baseUrl` — proved to fire by removing it from one call site: `ForwardBarCollectorTests.cs builds a ForwardBarCollector and never names a baseUrl`.
**No vendor was called.** Every test points at `FakeArchive`, the blessed loopback `HttpListener`; the collector's timer, clock, base URL and 10 s leash are all injected and the suite's longest forward test is the 2-second timeout one. The 2026-09-19 `data-api.binance.vision` measurement in `RESEARCH-REQUIRED.md` and on the catalogue row is the manager's, quoted from this brief — **I did not re-run it**.
**NOT DONE / NOT VERIFIED.** The Settings card was never rendered: `tools/mac-run.sh` and `tools/mac-shot.sh` were not run, so "the Market data card looks right" is NOT VERIFIED — it compiles with 0 warnings and uses only existing `Ui`/`Theme` helpers, and that is all that is claimed. Nothing was run on Windows. No order was placed anywhere. `BUILD-STATUS.md` is untouched — the manager's to land. Staleness adds NO new dispatch gate: `data_freshness` is still enforced by `RefuseAStaleDecisionOrThrow` against the decision's own bar, and the contract says so.
