# U-box-precut — the v0.1.2 box session, everything except the cut, on the pushed tree at `67ae250` (code `d92a61b`)

Fresh builder on Opus, with the box GRANTED to you alone for this unit. Read `docs/HOW-WE-BUILD.md`, `CLAUDE.md`,
`docs/RESUME-HERE.md` (the "Confirm the machine" block, the five facts, and traps 24/35/40/42/43), `tools/README.md`
(all of it: win-state, win-run, win-push, winagent, probe atas, atas-gate), `docs/DEPLOYMENT.md` §4–5, and the
U-bridge-reinstall and U-press-atomic sections of `BUILD-STATUS.md`. `export PATH="$HOME/.dotnet:$PATH"
DOTNET_ROOT="$HOME/.dotnet"`; no `timeout`; no full suite on this Mac (another leg is running one). Worktree
`~/Projects/ai-trading-software-for-mihael-worktrees/box-precut` on branch `u-box-precut` from `main`. Credentials are
in `~/.tradeagent/win.env`, sourced by the scripts: never print them, never quote a host name or IP into a file (write
`$TA_WIN_NAME`). ATAS's accounts are SIMULATED and no broker is attached: orders on them are yours to place; you never
change mode, live activation or the kill switch, and you never close ATAS unless capture works (trap 43).

1. **Push and prove the tree.** Stop TradeAgent on the box (trap 42), `tools/win-push.sh`, rebuild the app in Release
   there, and prove the pushed tree by hash (a hash of the same file set here and there) BEFORE any figure counts.
   Start the UI agent (`tools/win-agent.sh`); it is NOT RUNNING at dispatch.
2. **Walk the protocol-3 refusal and the repair.** Run the new build against the box's OLD (v2) bridge: quote the
   refusal sentence the status row shows. Then the repair the way the owner would: `Reinstall the bridge` on Checks,
   two-press, through the UI agent; ATAS closed first only if capture works; restart ATAS; the strategy restores STOPPED
   (trap 24) → activate it (trap 40, watch trap 35). Read `proto=3` from the app; quote the health row before and after.
   Fallback if the button cannot be pressed: deploy the DLL by file and record the button as NOT VERIFIED.
3. **`tools/atas-gate`** on the pushed tree, the README's command: exit code and both directions' lines quoted.
4. **The first review's two box items.** (a) The press id shape `TA-op-close-<nonce>-<i>`: open a small SIM position,
   press Close All (two-press, from the app or the operator console), and read the client order id back off ATAS's own
   order: accepted as-is, truncated, or refused. Leave the book flat. (b) Whether a real bridge spends 30–50 s in send
   gate + frame: measure one ordinary order path's real timings from the bridge's and the app's own log lines and say
   how far from `WorstCaseOrderPath` = 50 s the real numbers sit; do not change the constants.
5. **The backlog's box items** (`docs/hardening/briefs/U6-U9-backlog.md`, "For the v0.1.2 box session"): which teardown
   callback fires (`OnStopping` vs `OnDispose`) when the strategy is stopped and when ATAS closes; a 64-char and a
   65-char client-order-id probe against ATAS; mutant B4 (the no-buffer pipe stall) run once; and U9's five adapter
   compile warnings from the Release build of the bridge, listed with file:line and a proposed disposition each.

NOT the cut: no installer build, no `gh release`, no update of the installed app, no product change. Everything is a
measurement; commit any probe or script per item on your branch, no trailers. Leave the box as you found it, with the
new build running instead of 0.1.1: TradeAgent up, ATAS up with the bridge active at protocol 3, the book flat.

## Report — append here, commit it, ≤20 lines: tree hash proven; each item verified by running (quoted) or NOT VERIFIED
with what you tried; the sentences seen in item 2; the id ATAS showed in 4a; the numbers in 4b; what you did NOT do.

## Report

Tree proven twice by hash (sha256 of the sorted `sha256  path` manifest over the same file set here and there): **280 files, `2f734263cea4fa8583f26748a5200379dcf70af576b8341678d4cb863d485cd2`** at the first push, and **282 files, `57f04c4a8afaa76f73e62103103c0db096acf6110747033b151f000f9bf94ee2`** after the two instruments below were committed. App rebuilt on the box each time (`dotnet build src\TradeAgent.App -c Release --no-incremental` → 0 warnings, 0 errors); UI agent started; bridge published into `<app>\bridge` the way `packaging/build.ps1` stages it, so the Checks button had a source.
1. **BEFORE** — new build against the box's v2 bridge, `trade status`: `ATAS bridge FAILED "bridge 8.0.14 speaks protocol 2, this build speaks 3 — press Reinstall the bridge on the Checks page"`; `Trading connection` carries the same sentence, `Execution capability PAUSED "no trading connection"`.
2. **REPAIR** — `Reinstall the bridge` on Checks, two-press, through the UI agent: armed label `Confirm: replace the bridge — trading through ATAS stops until it is started again`, result `The bridge is in place. Open ATAS, open a chart, and start the TradeAgent Bridge strategy on it.` **The copy succeeded with ATAS RUNNING** — all 14 deployed files' sha256 equal the staged ones — so `ATAS_BRIDGE_IN_USE` never fired and `DEPLOYMENT.md` §4's "ATAS must be closed for the copy" does not hold at ATAS 8.0.14.397. ATAS then closed (Save and close, 15 s), relaunched, `Update later` on its own 8.0.14.398 prompt, strategy restored **STOPPED** (trap 24) → activated (row ▶→■).
3. **AFTER**: `ATAS bridge READY "connected · bridge 8.0.14, protocol 3"`, Trading connection / Account `CRYPTO5EB41` / Market data all READY. `probe atas` exit 0, `PROTOCOL VERDICT: MATCH — Versions.BridgeCompatible(3) = True`, `atas=8.0.14.397 bridge=8.0.14 proto=3 | SupportsClientOrderId=false SupportsOrderHistory=false IsSimulated=true | ReconciliationProvable=false | autonomy=refused | auth=ok`.
4. **atas-gate** (README's command, pushed tree): **exit 0**, `GATE PASSED` — `[PASS] ITradingManager.ClosePosition was never called — calls = 0` and `[PASS] ITradingManager.ClosePosition WAS called once the witness could be written — calls = 1`.
5. **4a** 1-lot SIM long opened in ATAS's own trading panel, `Close all positions` pressed twice on Safety. The press minted `TA-op-close-31d4779568274e53-0` (29 chars) and the bridge's write-ahead record carries it verbatim — but the order **ATAS** built came back (`trade executions`, which reads `Order.Comment`) as `client_order_id: "Close position"`, ATAS's own text: `ClosePosition` writes ours only into an EMPTY `Comment`, so the press id is **never offered to ATAS at all** — not accepted, not truncated, not refused. A close carries nothing of ours to reconcile on.
6. **4b** (UTC, one real close over the real bridge): press row `created_at 18:21:59.5235` → `dispatched_at 18:21:59.5263` (2.7 ms) → bridge write-ahead `18:21:59.5317` (**8.2 ms** app→bridge: send gate + frame) → filled `18:21:59.864` (341 ms) → app gave up at `18:22:01.518` (2.0 s = `EmergencyDeadline`) and recorded UNKNOWN, `'close' is NOT confirmed … The bridge is busy`. Ordinary RPCs: `trade positions/orders/executions` 106–115 ms against `status` (no bridge call) 108–127 ms, so an ordinary round trip is a few ms; the bridge's hello reports `place=sync;call=2541us;settled=126744us`. **Send gate + frame = 8.2 ms against the 40 s those two stages are budgeted (`WorstCaseOrderPath` 10+30+10 = 50 s) — ~4,900× below, so the 30–50 s premise is false on this box.** The finding that matters: the bridge's `WaitFor(AckTimeout = 3 s)` outlasts the connector's 2 s emergency budget, so a real Close All settles **UNKNOWN by construction** even though the close filled in 341 ms.
7. **5a teardown** — measured with a sibling probe strategy (`tools/atas-lifecycle`, ES chart) because the bridge writes no log line naming the callback and adding one is a product change: stopping the strategy fires **`OnStopping` only** (`20:38:34.256 OnStopping state=Started`); closing ATAS with it running fires **both, 1 ms apart** (`20:39:41.022 OnStopping state=Started`, `20:39:41.023 OnDispose state=Stopped`). Also: `Add` creates a throwaway instance that gets `OnDispose` with no start — the bridge's idempotent teardown covers it.
8. **5b coid length** — `probe atas --place-test-order --yes --coid <64>` and `<65>`: **both accepted verbatim**, read back off ATAS's own collection byte-identical (`TA-COID64-…ABCD` id 12062155, `TA-COID65-…ABCDE` id 12062157), both cancelled. ATAS's `Comment` carries ≥65 characters; the 64-char ceiling in `CONTRACTS.md` is ours, not ATAS's. No broker attached, so this is ATAS's object only.
9. **5c mutant B4** — `const int Buffer = 8192` → `0`, clean build, `ConnectorSendDeadlineTests.An_emergency_on_an_idle_stalled_bridge_answers_in_two_seconds_and_drops_it_at_the_grace` → **Failed 1**, `the connection was dropped on the caller's clock, at 2.02s`; reverted, clean build → **Passed 1** (10 s). Run once on the box, source restored. (An *incremental* `dotnet test` did not rebuild after the revert and stayed red — the `--no-incremental` rule earned again.)
10. **5d five adapter warnings** (Release bridge build, `--no-incremental`): `MSB3277` WindowsBase 4.0.0.0 vs 10.0.0.0 at `Microsoft.Common.CurrentVersion.targets(2453,5)`, pulled in by `ATAS.Indicators.dll`/`ATAS.Indicators.Technical.dll` — **suppress with `NoWarn` in the bridge csproj and a comment naming the vendor**, since the ATAS assemblies are `Private=false` and the platform supplies the resolved one; and `CS0618` ×4, `AtasStrategyAdapter.cs(1548,13)` `OpenOrder`, `(1632,9)` `ModifyOrder`, `(1693,9)` `CancelOrder`, `(1858,24)` `ClosePosition` — **`#pragma warning disable CS0618` at each of the four call sites with the trap-25 reason**, not a project-wide `NoWarn`, because the day one of them is flipped to the Async overload the pragma is the thing that has to be deleted. Then `TreatWarningsAsErrors` can go on.
11. **State left**: TradeAgent = the repo Release build (assembly still says 0.1.1) up; ATAS up, bridge started, `protocol 3`; **book flat** (`BTCUSDT 0`, `orders --all` `[]`); Strategies folder back to its original 14 files (the probe DLL deleted); ATAS workspace **not** re-saved, so it is byte-identical to what it was; mode `LIVE_CONFIRM`, `live_activated false`, kill switch untouched. `Execution capability PAUSED — 2 request(s) unconfirmed`: one was there at dispatch, the second is 4a's press record, left unresolved because settling it is the owner's assertion, not mine.
12. **Did NOT do**: no installer build, no `gh release`, no update of the installed `%LOCALAPPDATA%\Programs\TradeAgent` app, no product-code change committed (the B4 mutant was reverted on the box), no mode / live-activation / kill-switch / settings change, no full test suite anywhere, no push and no merge. `--coid` on `probe atas` and `tools/atas-lifecycle` are the only two files added, both harness. The ATAS 8.0.14.398 update was declined.
