# U-reopen-1 — a closed scope reopens by code only when it has earned it: a computed instant, a fresh flat book, a write-once receipt, a clock that cannot lie it open
Read `docs/HOW-WE-BUILD.md`, `CLAUDE.md` (rule 3; two-press), `docs/COUNCIL.md:14-15,33,59-63`, the `U-flatten-1` and `U-flatten-2` sections of `docs/CONTRACTS.md`, then
`src/TradeAgent.Gateway/LossBreach.cs` (keys `:47,53`, `LossBreachRecord :139-197`), `TradingGateway.cs:213` (`Now` = `_opt.Clock`), `:727`, `:844-862` (`ClosureToday`),
`:1536-1612` (`LossBudgetOrThrow`: `CanIncreaseExposure` → `DayClosed :1554` → `SymbolClosed :1557` → zero-budget return → ledger), `:1635-1658`, `:1716-1906` (`LossWatchAsync`,
`Confirmed :1808`, `Close :1826`), `:1975-2001`, `:2979,3096` (the approval path re-runs the gate), `:5169-5250` (the health pass; the watch is LAST `:5243`), `GatewayTypes.cs:33,
89,104,335-342`, `Db/Database.cs:154,1132-1168` (`kv`; `SetKv` is an UPSERT — write-once is enforced in code today), `GatewaySchema.cs:98-104`, `WorkspaceBuilder.cs:341-350`,
`MissionLoop.cs:492,611`, `Core/Trading.cs:845-897`, `DailyReports.cs:351-356`, `DailyReport.cs:429-457`, `DashboardView.cs:1511-1516`, the `U-flatten-2` flatten record as landed,
`tests/TradeAgent.FaultTests/LossDayClosureTests.cs` (the rollover test `:212-250` pins today's rule and MOVES), `LossWatchTests.cs`, `LossDayClosedSurfacesTests.cs`,
`tests/Shared/RecordingConnector.cs`. Branch `u-reopen-1`, worktree `~/Projects/ai-trading-software-for-mihael-worktrees/U-reopen-1`, rebased onto `main` AFTER `U-flatten-2`
lands. **Money path:** every guard ships with a test RED before it and ONE mutant, quoted, over `RecordingConnector`. No schema. No box, no ATAS, no money. Settled with the
owner 2026-09-15: option B — reopen by code, never "until the owner has read it" — on his condition of "exquisite logic and edge case handling"; one Astra consult shaped the rules.
**Why.** Today a closure ends because the key changes at UTC midnight: a 23:58Z breach reopens at 00:00Z; nothing checks that the flatten resolved, that the book is flat, that
the clock moved forward honestly, or that a parked approval predates the breach; and no record says a reopen happened.
1. **The closure is a state, not a key.** `DayClosed`/`SymbolClosed` and `ClosureToday` look across days: a scope (account, or account+symbol) is closed from a breach
   record's `ConfirmedAt` until a receipt `loss_reopen:{account}:{utcDay}` / `…:{symbol}:{utcDay}` exists for it, found by `KvStartingWith("loss_breach:{account}:")`.
   While a scope is closed the watcher writes NO further breach record for it (one episode until reopened: the flatten's own fills after midnight are not a second
   breach). RED: a breach at 23:58Z, the clock at 00:01Z → `PlaceAsync` dispatches. Mutant (`ClosureToday` back to the day key): the day reads open at midnight.
2. **Eligibility is computed from the immutable record; the reopen is a receipt.** `EligibleAt` = max(the first UTC midnight after `ConfirmedAt`, `ConfirmedAt` +
   `LossMinClosure`), a pure function of the record; `LossMinClosure` = **24 h**, a `GatewayOptions` value fixed in this unit (an owner setting in `U-reopen-2`). The
   receipt is written ONLY by the watcher tick, under `_dispatchGate`, write-once AT THE SQL LAYER (a new `Database.AddKvOnce`: `INSERT … ON CONFLICT(key) DO NOTHING`,
   returning whether it inserted), with its evidence: `EligibleAt`, the instant, the fresh flat read, the epoch. The admission gate NEVER writes it — until the receipt
   exists it refuses. RED: eligible, no tick yet → an order dispatches. Mutant (the gate writes the receipt on first admission): two placements in flight both pass.
3. **Flatness is fresh evidence, not a terminal state.** The receipt requires, on the tick: a `GetPositionsAsync` from the current connection epoch reading flat for
   the scope; the `U-flatten-2` record for the breach reading `flat` when one exists (a breach confirmed with no open position needs none); no open or flagged request
   on the scope (`HasUnconfirmedWork`, every `op-budget-*` leg resolved, every opener cancellation settled); a valuation that is available (`RISK_CHECK_UNAVAILABLE`
   stays independently blocking). A daily closure and a symbol closure compose by AND. RED: eligible by time, one leg still flagged → the receipt is written. Mutant
   (flatness taken from the flatten record alone): a position re-opened after the flatten reads flat. Second RED: a resting opener still working → the receipt is written.
4. **A clock that steps back cannot reopen.** Each tick persists `clock_high_water` (`kv`, monotone: written only when `Now` exceeds it); eligibility also requires
   `Now` ≥ that mark, and a `Now` below it records `loss_clock_suspect` once, refuses the reopen, and says so in `status`. A forward jump only lengthens nothing and
   is allowed. RED: the clock moved back 26 h after the breach → the receipt is written. Mutant (the mark upserted on every tick): a stepped-back clock reopens.
5. **A parked approval from before the breach dies with it.** `ApproveAsync` refuses, after reopen too, any `AWAITING_APPROVAL` request of the scope whose `CreatedAt`
   precedes the latest breach's `ConfirmedAt` — its own code and sentence, never `LOSS_BUDGET_REACHED`. RED: approved after the receipt → sent. Mutant (compared to
   `EligibleAt` instead of `ConfirmedAt`): a request parked during the closure is approved.
6. **Told, and the old sentences rewritten:** `status` gains `loss_reopens_at` (computed; absent when held by 3 or 4, with the reason) and `loss_reopened_at` (the receipt);
   the Situation line, the Safety row, section 4 ("reopened at … by code: 24 h after the breach, the book flat" / "closed since … — earliest … — waiting on: …"),
   `GatewaySchema.cs:98-104`, `WorkspaceBuilder.cs:341-350`, `USER-GUIDE.md`, `CONTRACTS.md` (the rule; 24 h as the owner's to overrule; the day is UTC, not a venue's; records
   are keyed by account so a change of platform carries no closure; the reopened day's figure is its own UTC date's ledger, flatten fills included). RED: the report silent
   on a reopen. Mutant (`loss_reopened_at` derived from the day key): reads reopened while held.
Not this unit: strikes, the review card, owner-set durations, a bounded director hold (`U-reopen-2`); the data-loss exit (`U-flatten-3`); stops (`U-protect`). Gate and
report as `U-flatten-1`; the moved rollover test named in the report with what it now pins.

## Report
Gated tip `3bec613` (this report commits on top), rebased onto `main` `e7d7ebe` — `U-flatten-2` had landed. **Gate, Release, `--no-incremental`:** 17 projects, **0 Warning(s), 0 Error(s)**; Unit `Failed: 0, Passed: 1111`, Fault `Failed: 0, Passed: 329`, Integration `Failed: 0, Passed: 668, Skipped: 1` (11 m 5 s) — 2108 passed, 0 failed. Touched/added classes 3× each, every run green: Fault LossDayClosureTests 4, LossBoundaryTests 2, LossFlattenTests 9, LossReopenTests 6, LossReopenApprovalTests 3; Unit LossDayClosedSurfacesTests 6, LossReopenRuleTests 4, LossReopenSurfacesTests 4. No `Timing` red, nothing re-run, no assertion loosened, no test deleted.
**Names vs `main`:** `[Fact]`/`[Theory]` 1727 → 1744; sorted set 1759 → 1776; **0 removed, 20 added, 3 MOVED** (renamed, bodies re-pinned to the new rule): the brief's rollover test `The_closure_lasts_the_whole_utc_day_and_the_next_one_starts_clean` → `The_closure_outlives_the_utc_day_it_began_in_and_ends_only_on_a_receipt` (now pins: a 22:30Z breach still refuses at 00:30Z and two days later, and trades only once a receipt exists); `A_breach_on_the_next_utc_day_opens_its_own_boundary` → `A_breach_on_the_next_utc_day_while_the_scope_is_still_closed_opens_no_second_boundary` (one episode, one row, one boundary); `An_all_filled_flatten_resolves_itself_and_the_next_day_opens_clean` → `An_all_filled_flatten_resolves_itself_and_its_own_rows_do_not_refuse_the_next_day`.
1. **Closure is a state.** `OpenClosures` scans `loss_breach:{account}:` across days; closed from `ConfirmedAt` until a receipt for THAT record; no second breach row or boundary while closed; every flatten key off the breach's day (`LossFlatten.KeyFor`). RED (the moved test): `Assert.NotNull() Failure: Value is null` at 00:30Z. Mutant (`DayClosed` back to `ReadBreach(DayKey(account, Now))`): the same, `next UTC day : 2026-03-11 00:30Z`.
2. **Eligibility computed, reopen is a receipt.** `LossReopen.EligibleAt` = max(next UTC midnight, `ConfirmedAt` + `GatewayOptions.LossMinClosure` = 24 h); receipt `loss_reopen:{connector}:{account}:{utcDay}` written only by the tick under `_dispatchGate` via new `Database.AddKvOnce` (`INSERT … ON CONFLICT DO NOTHING`). RED: `Assert.Single() Failure: The collection was empty`, `reopened :` with `eligible, no tick : LOSS_BUDGET_REACHED`. Mutant (the gate writes it on first admission): `Assert.Throws() Failure: No exception was thrown`. Second mutant (midnight term dropped): `confirmed 2026-03-10 09:00Z, 2h closure -> eligible 2026-03-10 11:00Z`.
3. **Flatness is fresh evidence.** Receipt needs: position read on the current epoch flat for the scope, the flatten record `Flat` with no opener unsettled, no unresolved `op-budget-close-`/`op-budget-cancel-`, `HasUnconfirmedWork` false; unvaluable book blocks by staying out of `Measured`. Three REDs, all `Assert.Empty() Failure: Collection was not empty` / `reopened : [loss_reopen:fake:SIM-001:2026-03-10]` — a flagged leg over a flat book (`flatten : flat=False legs=1`, `book : []`), an opener that would not cancel, a position re-opened by hand. Mutant (flatness from the flatten record alone): the third red, `book : [ES 1]`.
4. **A clock that stepped back cannot reopen.** `loss_clock_high_water:{connector}:{account}`, monotone, raised only while something is closed and seeded at `Close`; `loss_clock_suspect:…` written once. RED: `Assert.Empty() Failure: Collection was not empty`, `high water / now : 2026-03-12 16:00Z / 2026-03-11 14:00Z`. Mutant (mark upserted every tick): the same red.
5. **A parked proposal dies with the breach.** `ApproveAsync` declines — new `APPROVAL_PREDATES_LOSS_BREACH`, never `LOSS_BUDGET_REACHED` — any AWAITING_APPROVAL request written before the latest breach of its scope, above the mode and the budgets, after the reopen too. RED: `Assert.Throws() Failure: No exception was thrown` (the stale proposal reached the wire); while still closed the answer was `LOSS_BUDGET_REACHED` and the row stayed parked. Mutant (compared to `EligibleAt`): a proposal written DURING the closure refused — `this order was proposed at 2026-03-10 13:00 UTC, before your loss budget was reached at 2026-03-10 12:00 UTC`.
6. **Told, old sentences withdrawn.** `status.loss_reopens_at` / `loss_reopen_held` / `loss_reopened_at`, the Situation line, the Safety row, section 4 (`reopens:` / `reopened:`), `GatewaySchema`, `AGENTS.md`, `USER-GUIDE.md`, `CONTRACTS.md`; the breach and flatten sentences, `LOSS_BUDGET_REACHED`'s next step and the schema's "midnight UTC" all rewritten. RED (behaviour stripped, shapes kept): 3 of 4 red, `Assert.Equal() Failure: Values differ / Expected: 2026-03-11T12:00:40.0000000+00:00 / Actual: null`. Mutant (`loss_reopened_at` off the receipt's presence): `Assert.Null() Failure: Value … has a value`, `after the second close: closed=2026-03-11T12:02:20Z reopened=2026-03-11T12:01:40Z`.
**Choices, all in `CONTRACTS.md` under `U-reopen-1`:** 24 h and the connector in every key I added (the breach key untouched); `ClosureToday`/`SymbolsClosedToday`/`FlattenToday` keep their names though they answer across days; the clock mark kept only while something is closed, not every tick; surfaces judge "held" from rows this app wrote only, never a platform read, so `loss_reopens_at` is the EARLIEST and not a promise; `loss_reopened_at` absent whenever anything is closed; `LatestBreach` skips an unparseable row (`OpenClosures` still throws on an unreadable STANDING one); the watch reads the platform on a tick when both budgets are zero but a closure stands, so zeroing a budget after a breach cannot close an account for ever. Deviations: item 2's mutant is watched on ONE admission rather than two in flight — `_dispatchGate` serialises placements, so two cannot both pass; item 6's RED is the repo's shapes-kept strip, because the surfaces were built after items 1–5.
**Edge cases:** 23:58Z → eligible 23:58Z next day (`LossReopenRuleTests`), second breach while closed is the same episode, day AND symbol compose (each its own receipt, account-wide press conditions on both), clock back refuses / forward only lengthens, restart before the tick admits nothing, the gate never writes, a parked approval dies with its own code, a widened or zeroed budget still refuses off the record (held by `A_loser_closed_under_the_budget_…` under the new cross-day lookup), valuation unavailable blocks independently, the reopened day's figure is its own UTC date's ledger. **NOT done:** no schema rung; `LossMinClosure` is not a setting, no strikes, no review card, no director hold (`U-reopen-2`); no data-loss exit (`U-flatten-3`); no stops (`U-protect`); no screen shot, no box, no ATAS, no money — every wire assertion is `RecordingConnector`. **Inherited limit:** a breach row is keyed by ACCOUNT, not platform (`U-flatten-1`'s key), so on a switch to a platform sharing an account id its closures and item 5 still apply.
