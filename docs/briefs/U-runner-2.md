# U-runner-2 — the evaluator: closed bars of a named dataset in, bounded intents out, a fault a defined outcome
Read `docs/HOW-WE-BUILD.md`, `CLAUDE.md`, `docs/COUNCIL.md:126-166`, then `U-runner-1`'s product on `main` (`docs/STRATEGY-LANGUAGE.md`,
`Core/Strategy/`, `StrategyLimits`, `Program.WarmUpBars`), `Db/DatasetStore.cs:132-193` (`Checked` re-hashes every raw file on EVERY
read), `Data/DatasetReader.cs:20-61` (path-based, a 10 000-bar cap that REFUSES), `KlineNormaliser.cs:44-45,152-164` (a gap is never
filled), `GatewayPipeServer.cs:1975-1993` (`data-bars` resolves `Newest(pair)` only). Branch `u-runner-2`, worktree `~/Projects/ai-
trading-software-for-mihael-worktrees/U-runner-2`, rebased onto `main` AFTER `U-runner-1` lands. Every item ships with a test RED
before it and ONE mutant watched going red, quoted. No box, no ATAS, no money, no gateway, no orders. **No schema number.**

**Why.** No dataset can be read by id, and the one reader refuses a twelve-month window; no indicator and no evaluator exist. The
language's semantics — closed bars only, a gap reported and never filled, exits before entries, a signal at bar n executing only
after it, per-event limits, a fault meaning no new exposure — are prose. Per-bar quality flags and instrument increments do NOT
exist on a dataset (`DatasetStore.cs:31-54`, counts only): the evaluator takes what the dataset has and says so in `CONTRACTS.md`.

1. **Bars by dataset id.** `DatasetStore.ById(long)`; a `BarFeed` streaming "all closed bars of dataset X in [t0, t1], ascending"
   in chunks with no cap; the `Checked` verdict taken ONCE at the start of a run and a `REJECTED` dataset refused with the reason
   (`DatasetStore.cs:154`). RED: no read by id; a twelve-month window is refused `OverCap`. Mutant (`Checked` skipped): a rejected
   dataset feeds a run.
2. **Indicators** SMA, EMA, ATR, RSI, rolling high/low and the session opening-range accumulator, exactly as `docs/STRATEGY-
   LANGUAGE.md` specifies (initialisation, numeric semantics), computed from closed bars only, undefined during warm-up, each
   with a golden series pinned over a fixture of bars. RED: no indicator exists. Mutant (EMA seeded from the first close instead
   of the documented initialisation): the golden series changes.
3. **Gaps and warm-up.** A missing minute is not a bar: it advances no lookback and the run counts and reports it; evaluation
   before `WarmUpBars` is refused. RED: over a gap fixture the SMA differs from the documented value → equal. Mutant (the
   previous close carried across the gap): red.
4. **The rule engine per closed bar.** State = position (long/flat), entry price, bars since entry, the pending intent; exits
   evaluated before entries; no same-event reversal; no duplicate entry while one is pending; a signal from bar n yields an
   intent stamped with n and executable only after it — the evaluator EMITS intents and places nothing; time filters honoured
   with the versioned timezone and calendar (weekdays, entry windows, the opening-range interval, the scheduled session exit;
   DST and a missing session defined and tested). RED: two entries on one event. Mutant (entries before exits): a reversal
   in one event.
5. **Limits and faults.** A per-event operation budget and a state-size limit from `StrategyLimits`; an interpreter fault, a
   division by zero, an over-budget event or a timeout is a DEFINED outcome (`EvaluationOutcome.Faulted` with the reason, the
   run halts, no intent emitted), never an exception escaping the evaluator. RED: a program over the per-event budget throws.
   Mutant (operations counted per rule instead of per event): a program of many small rules passes. The three day-one
   programs evaluate end to end over a synthetic bar fixture with the expected intents pinned per program.
Not this unit: fills, fees, slippage, stops and targets between evaluations, capital, the trace, persistence, the report, the
pipe (`U-runner-3`); live or paper execution; the relay; promotion; the referee.
Gate: `dotnet build TradeAgent.sln -c Release --no-incremental` → 0 warnings; the three test projects in Release to a file → 0
failed; touched classes 3×; a `Timing` red re-run alone 3×, never loosened. Commit per item, one sentence, no trailers.
Append `## Report` (≤20 lines): tip sha, gate counts, one line per item with its RED and mutant, what you did NOT do.

## Report

**Gated tip `39c94e0`** (this report commit is docs-only on top), rebased onto `main` `5fcccdd` — `U-runner-1` had landed as `dadb09b`, `git merge-base --is-ancestor 1e92fe3 main` held, the rebase of all five commits was clean, and `git rev-list --count HEAD..main` printed 0 after it. Five commits, one per item.
**Gate**, all Release, output to files inside the worktree, deleted before this commit: `dotnet build TradeAgent.sln -c Release --no-incremental` → **`Build succeeded. / 0 Warning(s) / 0 Error(s)`**, 17 projects. The twelve touched classes (the seven new ones plus `DayOneStrategyTests`, `DatasetLedgerTests`, `DataOpsTests`, `KlineNormaliserTests`, `DataOverPipeTests`) 3× each → **`Failed: 0, Passed: 106`** every run in Unit and **`Failed: 0, Passed: 6`** every run in Integration. `--no-build -c Release`: Unit **`Failed: 0, Passed: 783`** (706 on `main`), Fault **`Failed: 0, Passed: 277`**, Integration **`Failed: 0, Passed: 627, Skipped: 1`**. Nothing went red, so no `Timing` re-run; `ps -eo pid,ppid,pcpu` before the suites found no orphaned test process (only `WindowServer` 40% and `mediaanalysisd` 74%, both macOS daemons). Names vs `main`: **nothing removed** — strict `[Fact]`/`[Theory]` set **1266 → 1327** (61 added), the wider public-method set **1325 → 1388** (63 added) — and `file` reads all 127 test files as text, none binary.
**1 — bars by dataset id** (`DatasetStore.ById`, `Data/BarFeed.cs`; `DatasetReader.Read`'s cap and body unchanged in behaviour, its row parse extracted to a public `TryBar` both readers share). RED `Assert.NotNull() Failure: Value is null` and `the bar feed is not implemented` (6 failed / 0 passed). Mutant (`Checked` not called in `Open`): `Assert.False() Failure`, 2 failed / 4 passed. A real twelve-month window — 525,600 minute bars — is `OverCap` to the reader and streams whole through the feed; the verdict is taken once, proven by deleting the raw file mid-run and still streaming.
**2 — the seven indicators** (`StrategyIndicators.cs`, 280 pinned values over a 40-bar fixture). RED `Assert.Equal() Failure: Values differ / Expected: 101.50 / Actual: null` (18 failed / 0 passed). Mutant (EMA seeded from the first close): `Expected: ["null","null","null","101.125",…] / Actual: ["100.00","100.300",…]`, 3 failed / 15 passed. Every value was recomputed OUTSIDE the build in Python's 60-digit decimal: all 280 agreed, worst relative deviation **2.274E-27** (`rsi4` bar 23, the longest Wilder chain) — .NET `decimal` holds ~29 digits, so that is the last digit either type has. Three indicators are also recomputed naively from the raw window inside the test, and four have exact hand-checkable anchors.
**3 — gaps and warm-up** (`StrategyEvaluator.Step` intake). RED `Assert.Null() Failure … Actual: "the evaluator is not implemented"` (8 failed / 0 passed). Mutant (the previous close carried across the gap): **`Expected: 99.60 / Actual: 99.30`**, 1 failed / 7 passed — the documented mean of the five bars that exist against the mean of four and a filler. A bar out of order, repeated, or off the interval grid is a defined fault; gap runs are listed to `KlineNormaliser.MaxGapRunsListed` and counted without bound.
**4 — the rule engine** (`Decide`/`Exit`/`Entry`, `StrategyInterpreter.cs`, `StrategyCalendar.cs`). RED `Assert.Single() Failure: The collection was empty` (22 failed / 6 passed). Mutant: **`Expected: Exit / Actual: Enter`**, 3 failed / 25 passed. **The brief's mutant is written "entries before exits"; with the position read from the account reading, swapping the two blocks cannot reverse anything — an entry is gated on being flat either way. The guard that produces a one-event reversal when removed is "an exit that fires ENDS the event", and that is the mutant that was run.** The calendar rule table was checked outside the build against Python's IANA database: **525,888 half-hour readings across six zones and 2023-2027, zero mismatches.**
**5 — limits and faults** (`StrategyLimits.MaxOperationsPerEvent` 16,384 and `MaxStateBytes` 262,144; `EvaluationLimits`). RED `Assert.True() Failure / Expected: True / Actual: False` on the budget and the state limit, and **`System.OverflowException : Value was either too large or too small for a Decimal`** escaping the evaluator (3 failed / 8 passed). Mutant (the budget counted per rule): `Expected: True / Actual: False` and **`Expected: 57 / Actual: 3`**, 2 failed / 9 passed. The budget is set ABOVE what any program the parser accepts can cost (8,401 operations, 138,464 bytes — pinned as an inequality in the test), so a run may be given a tighter budget and never a wider one; the three day-one programs evaluate end to end with intents pinned — crossover bar 60/142, RSI bar 30/59, breakout bar 61/154 — and those bars were recomputed outside the build too.
**Where the brief overrode COUNCIL, and where the document was extended.** COUNCIL has the language READING account state; the brief and `U-runner-1`'s recorded departure make it evaluator INPUT, not expression vocabulary, and it is — a rule cannot say `equity`. COUNCIL has bars carrying "instrument increments and quality flags"; a dataset carries neither (`DatasetStore` rows are counts, hashes and coverage), so an intent's quantity has no increment applied and `docs/CONTRACTS.md` now says so, with rounding and the gateway's limits left downstream. COUNCIL wants a timeout as a defined outcome and the brief forbids a clock in the evaluator: the CALLER holds the deadline (a `CancellationToken`) and the stopped run is the defined fault. `docs/STRATEGY-LANGUAGE.md` gained the arithmetic it was silent on (EMA's `a` computed once with `1-a` from it, Wilder's `avg = (avg*(p-1)+x)/p`, decimal throughout, a rolling extreme's cost) and a whole "Evaluation" section; its opening-range row was CORRECTED from "bars closing inside the interval" to "bars whose open time is inside" — the literal reading excluded the 09:59 bar and included the 09:29 one — and all time filters are read from the bar's open time, the only timestamp a dataset carries. No `StrategyVersions` number moved, so the three golden `StrategyId`s are untouched.
**Sizing was not one of the five items and is in anyway**, because COUNCIL's "bounded market intents" is not bounded without a quantity: the declared rule is applied to the supplied account state, the entry-bar stop and target prices are stated on the intent (risk sizing needs the distance), and nothing is enforced between evaluations.
**NOT done:** no fills, fees, slippage, stop or target enforcement between evaluations, capital accounting, trace, persistence, report or pipe op (`U-runner-3`); no relay, promotion or referee; no schema number — `ById` is a query on the existing table; no box, no ATAS, no money, no gateway, no orders, no verb; nothing reads `workspace/strategies/`; `BUILD-STATUS.md` untouched, which is the manager's pass. The known windows red (`U-crlf-strategy-win`, CRLF `.strategy` fixtures) is not fixed here and is not added to: with all three fixtures rewritten to CRLF locally, `DayOneEvaluationTests` ran **`Failed: 0, Passed: 6`**, because it parses the fixtures rather than comparing their bytes to the document.
