# U-freshness — a strategy declares its timeframe, its data freshness and its maximum decision age, and the runner asks again at the wire
Read `docs/HOW-WE-BUILD.md`, `CLAUDE.md` (the four rules; a definite refusal versus an ambiguous failure), `docs/COUNCIL.md:96-97` (verbatim: "A
promoted strategy declares its timeframe, its required data freshness and its maximum decision age, and the runner checks them again when the
intent reaches execution"), `:14-15` (freshness among the code-enforced gates), `:33` ("never a late trade"), `:34-36` (a changed assumption
invalidates the evidence), `:74-76` (fixture versus market data in the Situation), then `Core/Strategy/StrategyProgram.cs:61`, the parser and
`StrategyCanonical.cs` (what is hashed), `docs/STRATEGY-LANGUAGE.md:16`, `Strategy/StrategyEvaluator.cs` (an intent stamped with its bar),
`Db/Database.cs:621-632` (`strategy_version`: no timeframe), the promotion row (`U-referee-2`), `Gateway/GatewayTypes.cs:17` (`MaxQuoteAge`),
`TradingGateway.cs:1244-1262` (the quote-age check and `ContractSizeOrThrow` inside the dispatch path), `:1571`, `DispatchPlaceAsync` (~`:2219`),
`Gateway/DailyReport.cs:348-352` (section 3), `MissionLoop.cs:585-600` (the Situation's data line). Branch `u-freshness`, worktree `~/Projects/
ai-trading-software-for-mihael-worktrees/U-freshness`, rebased onto `main` AFTER `U-referee-2` and `U-venue-catalog` land. **Money path:** every
guard ships with a test RED before it and ONE mutant, quoted. No box, no ATAS, no money, no order sent. **Schema 19.**

**Why.** A program declares an instrument and a timezone and nothing else; `strategy_version` has no timeframe; the only age the gateway knows
is a QUOTE's (`MaxQuoteAge` 30 s). Nothing knows how old the bar an intent came from is, so rule 1's freshness gate and `:33`'s "never a late
trade" have no implementation. A live or paper bar feed does not exist and COUNCIL names no unit that produces one: this gate is proved over
recorded bars and a controlled clock, and says so.

1. **Three declarations on the program,** parsed, canonicalised and hashed — `timeframe`, `data_freshness`, `max_decision_age` (durations) — so a
   changed bound is a different `StrategyId`; the three day-one programs gain them and their golden ids move once, recorded. RED: a program
   declaring them fails to parse. Mutant (left out of the canonical form): two different bounds share one id.
2. **They travel onto the version and the promotion row** (a changed assumption invalidates the evidence). RED: `no such column:
   max_decision_age`. Mutant (read from the request at promotion time instead of the frozen program): a promotion restates its own bound.
3. **An intent carries the bar it was computed from** (open time, close time) out of the evaluator and through `PlaceIntent`; nothing today
   names a time a gate could read. RED: an intent reaches the gateway with no bar time. Mutant (the bar's open time used as the decision time):
   a one-minute intent a minute younger than it is.
4. **The gate at DISPATCH, beside the quote check and `ContractSizeOrThrow`:** an intent older than its `max_decision_age`, or computed from a
   bar older than its `data_freshness`, is REFUSED with the policy's safe outcome and nothing is sent; a definite refusal, recorded, never an
   UNKNOWN. RED: an intent an hour old dispatches. Mutant (the check moved above the awaited reads): an intent that aged during the reads still
   sends — `Expected: refused / Actual: DISPATCHING`.
5. **The owner and the roles are told:** section 3 gains the freshest bar's age per dataset and whether the promoted strategy's bound is
   satisfiable now; the Situation's data line says the same and marks fixture versus market data. RED: the report silent about data age with a
   promoted strategy present. Mutant (age from `accepted_at` instead of the last bar): a year-old dataset collected today reads fresh.

Not this unit: a live or paper bar feed; `U-flatten`; the allocator; any venue connector; the referee's verdict (`U-referee-2`).
Gate: `dotnet build TradeAgent.sln -c Release --no-incremental` → 0 warnings; the three test projects in Release to a file → 0 failed; touched
classes 3×; a `Timing` red re-run alone 3×, never loosened. Commit per item, one sentence, no trailers. Append `## Report` (≤20 lines): tip sha,
gate counts, one line per item with its RED and mutant, the moved golden ids, what you did NOT do.

## Report

Gated sha `2902d9c`; the tip is the report commit on top of it, `src` and `tests` identical. I DID rebase: `git merge-base --is-ancestor c8306d4 origin/main` printed 0, so the branch was rebased onto `origin/main` `f7c4f31` and gated there. `origin/main` then moved again DURING the gate, to `a6bacde`, which edits `BUILD-STATUS.md` and nothing else; my branch is 1 behind and nothing reads that file, so the manager's checklist step 2 rebase is trivial and does not touch a gated input.
Gate on `2902d9c`: `dotnet build TradeAgent.sln -c Release --no-incremental` → `Build succeeded. 0 Warning(s) 0 Error(s)`. Touched classes 3×: Unit 101/101 and Fault 6/6 on each of the three passes, 0 failed. Suites in Release `--no-build`, one at a time: Unit 1072 passed / 0 failed; Fault 283 passed / 0 failed; Integration 668 passed / 0 failed / 1 skipped (10 m 50 s). No `Timing` red anywhere, so nothing was owed a re-run alone. Test names (grep, `[[:space:]]`): base `c8306d4` 1680, `origin/main` 1680, tip 1709 — 0 removed vs either, 29 added; both sets non-empty.
1. Three declarations (`timeframe`/`data_freshness`/`max_decision_age`, all three or none, durations `INT(s|m|h|d)` 1s–7d, canonicalised as whole seconds, hashed). RED: ``line 5: `timeframe` is not a declaration this language has.`` Mutant (the three lines dropped from `StrategyCanonical.Of`): `Assert.NotEqual() Failure: Strings are equal / Expected: Not "59b54dee4104dee9349675ea7cad789b90909aca795a80039b"··· / Actual: the same` — two different bounds, one id.
2. They travel onto `strategy_version` and `strategy_promotion` at schema 19 (INTEGER seconds, nullable, not backfilled). RED: `SQLite Error 1: 'table strategy_version has no column named timeframe'`. Mutant (`Referee.Verdict` reading `version.Timeframe` instead of the re-parsed frozen program): `Expected: FreshnessBounds { Timeframe = 00:01:00, DataFreshness = 00:02:00, MaxDecisionAge = 00:00:30 } / Actual: null`.
3. The intent carries its bar and its bounds onto `PlaceIntent.Decision` (`IntentDecision`, all four or nothing). RED (compile — the guard IS the type): `error CS0117: 'StrategyIntent' does not contain a definition for 'Freshness'`, `error CS0103: The name 'IntentDecision' does not exist in the current context`. Mutant (bar OPEN carried as the decision instant): `Expected: 2026-09-14T12:01:00.0000000+00:00 / Actual: 2026-09-14T12:00:00.0000000+00:00`.
4. The gate at DISPATCH (`RefuseAStaleDecisionOrThrow`, after `ReauthorizeAtDispatchOrThrow`, nothing awaited after it). RED: `Assert.StartsWith() Failure ... String: "ok — FILLED"`, `record: FILLED`, `orders at the broker: 1`. Mutant (moved into `RiskCheckOrThrow`, above the awaited reads; clock advanced 5 m inside the position read): `aged inside the reads: True / outcome: ok — FILLED / orders at the broker: 1`. On a refusal: `DECISION_EXPIRED`, record stays `CREATED`, `NeedsReconciliation` false, `conn.Placed` empty — a definite refusal, never an UNKNOWN.
5. Section 3 gains a freshest-bar line per dataset and whether the promoted bound is satisfiable; the Situation's data line says the same and marks fixture versus market data. RED: `Assert.Contains() Failure: Sub-string not found / Not found: "newest bars:"`. Mutant (`now - AcceptedAt` instead of `now - LastBar`): `Not found: "freshest bar 400d old"` — 400 days of history collected at the snapshot read fresh.
Golden ids moved once: crossover `8873b586…`→`3b336473…`, breakout `88f6586a…`→`70ec1a6e…`, mean reversion `eeb61430…`→`16d6192f…`. Each recomputed OUTSIDE the build from canonical+parameters+manifest and matched what the build produced.
Choices where the brief was silent: the three declarations are OPTIONAL and all-or-none — required would refuse ~120 existing program texts, a language break this unit had no budget for; a program declaring none emits an intent with no `Decision` block and the gate then has nothing to refuse on, and nothing forces a PROMOTED version to declare them (`U-referee-2` is out of scope). The duration spelling and its 1s–7d bounds are mine. At dispatch the two bounds are two limits on ONE measurement — on a signal computed at a bar's close the freshest observation and the decision are the same instant — the tighter binds and the refusal names which; they diverge in item 5, where freshness is measured against a dataset's last bar. Beyond the brief I also refuse a decision whose bar has not closed. The report's bound is judged against the freshest MARKET-data bar in the installation (fixtures excluded), not the holdout and not the program's instrument: a promotion row names no instrument and re-parsing a source on that page is work `Promotions.Standing` already avoids. `Promotions.Current` was extracted out of `AppHost` so the two readers of "what is promoted" ask once.
Deviations from the brief: item 2's RED reads `no such column: timeframe`, not `max_decision_age` — SQLite names the FIRST missing column of the INSERT. Item 3's mutant makes the intent read one minute OLDER, not younger as the brief says; the magnitude is a minute and the red is exact, the sign in the brief is not. Item 4's mutant actual is `FILLED`, not `DISPATCHING` — the fake broker fills immediately, which is strictly worse. Item 3's RED is a COMPILE failure because the guard is a type that did not exist; item 5's runtime RED was taken by stashing this unit's item-5 work and asserting on the rendered text of the pre-change tree.
Three commits are not items: the four pre-existing tests that pinned what this unit moved (`StrategyIdentityTests`' canonical text, `PromotionLedgerTests`' column list and read/write surface, `VenueCatalogTests`' roll-back to 16 — whose own comment already required every rung above 16 to be undone) were UPDATED, none deleted; one safety fix to the report's id shortening; and the future-bar refusal with its own owner sentence plus a test that the dataset list at its cap still fits `DailyReportText.MaxLines`.
NOT done: no live or paper bar feed — the gate is proved over recorded bars and a controlled clock (`GatewayOptions.Clock`); nothing reached a venue, ATAS, the Windows box or real money, and no order left the process. No runner turns a `StrategyIntent` into a `PlaceIntent` (none exists), so `IntentDecision.From` is tested and read by the gate but has no production caller yet. `U-flatten`, the allocator, every venue connector and the referee's verdict untouched. The Fault and Integration suites ran while another leg's Integration suite was running on this Mac; no `Timing` red appeared.
