# U-api-worker — the app-owned harness: one provider, one role on it, every tool a grant, every boundary counted
Read `docs/HOW-WE-BUILD.md`, `CLAUDE.md` (the no-terminal rule; a pasted key in the app's own window is allowed), `docs/COUNCIL.md`
("Workers run on an app-owned harness", rules 2–5, the round-4 grant fields, the `U-api-worker` line), `docs/RESUME-HERE.md`
step 6, then `IAgentRuntime.cs`, `AgentSession.cs:22-69` (`IAgentConversation`), `AgentSupervisor.cs:44-67`, `AppHost.cs:60-124`,
`TurnMeter.cs` (`Begin`, `Close`, `TurnUsage.Read:80-121`), `ListPrices.cs:125-144`, `GatewaySchema.cs`. Branch `u-api-worker`,
worktree `~/Projects/ai-trading-software-for-mihael-worktrees/U-api-worker`, rebased onto `main` first. Money path: every guard
ships with a test that was RED before it and ONE mutant watched going red, quoted. No box, no ATAS, no money, no real provider call.

**Why.** Workers cannot be capped or tool-restricted on a vendor CLI ("its caps are advisory"). The only runtime is
`CliAgentRuntime` (`AgentSupervisor.cs:49`); `IAgentRuntime`/`IAgentConversation` are the seams; the meter, the prices, the role
folders and the pipe's read ops exist. A key must not sit on disk beside an uncontained CLI, so this slice holds it in memory.

1. **`ApiAgentRuntime` (id `openai-api`)** — manifest data: base URL, the OpenAI models `ListPrices` prices (a third `foreach`
   tagging `openai-api`); `ApiConversation : IAgentConversation` runs the turn: the role's mission and the Situation as input,
   the provider's tool-calling loop executed BY THE APP (each call → item 2) until the model ends the turn or a budget trips;
   `TurnEnded` carries `Usage` summed over every response, the model named on each (exact price, never the estimate). RED:
   `RuntimeCatalog.Require("openai-api")` throws (today); mutant (usage from the last response only) → a two-request turn priced as one.
2. **Tools are grants, default deny.** A fixed set the app implements: `read_file` and `list_files` inside the role's own
   folder and `in/` (bounded bytes per call and per turn); `write_file` inside `out/` and the role's `trading/` only (staged,
   the relay and `U-turn-commit` publish); `trade` → the pipe's READ ops for every role, the mutating ops for Operations
   only, under the app-assigned identity (role + attempt); `data` (`data-list`/`data-bars`), `report`. No shell, no HTTP, no
   packages, no path outside the folder; every call written to a `tool_call` row (attempt, tool, argument summary, bytes,
   served or refused) — the observed-deliveries record. RED: Research's `trade buy` served (today's pipe would); mutant (the
   role check dropped) → served. Path RED: `read_file("../operations/PLAN.md")` served.
3. **Every boundary counted.** `TurnMeter.Begin` reserves before the first request; per request the app counts the input it
   sends and caps output with the provider's max-output parameter; cumulative input, output and retrieval bytes are checked
   against `TurnAllowance` before EACH request; a turn over its allowance ends `CONTEXT_BUDGET_EXCEEDED` (staged files kept,
   the row saying so); usage that never arrives is unresolved, never zero; a never-answering endpoint fails in seconds. RED: a
   fourth request runs past the allowance; mutant (the check after the request) → the same. Lost-usage RED → the reservation.
4. **One role on it.** The owner chooses per role runtime and model on the Safety page (Research defaults to `openai-api`
   once a key is held, Operations stays on codex); `ConversationFor(role)` picks the runtime; the key is pasted on the Safety
   page (`PasswordChar`), held in memory only, never written, cleared on exit, the sentence saying why, and the daily report's
   AI-spending section says "harness key: held / not held". RED: a role on `openai-api` with no key starts anything.
5. **Tests over a loopback `HttpListener`** playing the provider (canned tool calls, usage on each), never the network (the
   vendor-reach scan extended to the provider's host); `CONTRACTS.md` and the guide describe the harness.
Not this unit: memory search, the chair on the harness, a grant TABLE with fencing, a second provider, keys on disk (`U-contain-2`).

Gate: `dotnet build TradeAgent.sln -c Release --no-incremental` → 0 warnings; the three test projects in Release to a file →
0 failed; touched classes 3×; a `Timing` red re-run alone 3×, never loosened. Commit per item, one sentence, no trailers.
`## Report` (≤20 lines): tip sha, gate counts, per item its RED and mutant, what you did NOT do.

## Report

**Seven commits on `main` `13605dd`** (tip = this commit). **The gate below ran at `6a91ae0`**, and a fourth `git rebase main` then took `13605dd` — `git diff --stat` between the two tips is exactly `BUILD-STATUS.md` and `docs/RESUME-HERE.md`, two manager-owned records this unit never touched, so the gate's verdict stands and the suites were not re-run for it; the `--no-incremental` build WAS re-run at this tip and is still `0 Warning(s) 0 Error(s)`. `git rebase main` ran four times as `main` moved: it dropped `U-containment`'s six by patch-id, then took two `U-referee` briefs, then took `U-runner-3`. **The third rebase had three real conflicts, all resolved inside it so the messages survive:** `Database.cs` and `Versioning.cs` now carry BOTH migrations in ladder order — 11 → 12 (`strategy_*`) → 13 (`tool_call`), one constant at 13 — and `WorkspaceRevisionTests` took `main`'s side outright, because `U-runner-3` had already made the identical repair (the schema pin became a floor) for the identical reason. `GatewayPipeServer.cs` auto-merged: the new `backtest` op reaches `BacktestFor(ctx, …)` through the `Handle(req, role, attemptId, ct)` signature this unit changed.
**Gate**, Release, at that tip: `dotnet build --no-incremental` → `0 Warning(s) 0 Error(s)`. Touched classes 3×: 201 passed, 0 failed, on each of three runs (unit); 103 passed 1 skipped on each of three (integration subset). Suites `--no-build`: Unit `Failed: 0, Passed: 920, Total: 920`; Fault `Failed: 0, Passed: 277, Total: 277`; Integration `Failed: 0, Passed: 645, Skipped: 1, Total: 646`. No `Timing` red anywhere, so nothing was re-run. Test names vs `main`: **1351 → 1398, the removed set EMPTY**, 47 added; `file` says every test source is still text. The only ppid-1 process over 40% was macOS's own `WindowServer`.
1. **`ApiAgentRuntime` (`openai-api`)** — manifest data (endpoint, `max_completion_tokens`, `gpt-5.6-luna`), `ApiConversation` running the provider's tool loop, usage summed over every response. RED (src stashed, 4 failed 0 passed): `Assert.NotEmpty() Failure: Collection was empty` and `Assert.Null() … Actual: "costs.json has no price for gpt-5.6-luna"`. Mutant (`total.Plus(reported)` → `reported`): `Expected: 6000 / Actual: 3000` — a three-request turn priced as one.
2. **Tools are grants, default deny** — six tools, `tool_call` rows at schema 13, `trade` through `GatewayPipeServer.CallAsync` so the role check is the pipe's own line. RED (src stashed): `Expected: 13 / Actual: 11`. Mutant A (that role check dropped): `Assert.False() Failure / Expected: False / Actual: True` — Research's `trade buy` served. Mutant B (both layers of `ToolPaths` dropped): the same assertion red — `read_file("../agent/trading/PLAN.md")` served.
3. **Every boundary counted before each request** — reserve, then input/output tokens and retrieval bytes, then `CONTEXT_BUDGET_EXCEEDED` with staged files kept and `"ended"` in `ai_attempt.context`. RED (the three allowance arms removed): `Expected: 3 / Actual: 24`, `Expected: 2 / Actual: 24`, `Expected: 8 / Actual: 24`. Mutant (`>=` → `>` on the input arm): `Expected: 3 / Actual: 4` — a fourth request past the allowance.
4. **One role on it** — `RoleRuntime` per role, Research defaults to the harness once a key is held, the chair stays on codex, `HarnessKey` in memory only (masked box, cleared in `DisposeAsync`), `harness key: held / not held` on the daily report. RED (the `keyHeld` arm removed): `Expected: "openai-api" / Actual: "codex"`. Mutant (the key check dropped): `Assert.Empty() Failure: Collection was not empty / Collection: ["{\"model\":\"gpt-5.6-luna\",…]` — a role with no key sent a request.
5. **`FakeProvider` on loopback, never the network** — a HEAD answered with headers only, every response closed in a `finally`; the vendor-reach scan extended to the provider's host. RED: the extended scan immediately caught a live offender — `HarnessRoleTests.cs builds an ApiAgentRuntime and never sets BaseUrl, so it would send a request to the AI provider`. Mutant (the repoint removed from `ApiWorkerTests`): `ApiWorkerTests.cs uses ApiAgentRuntime and never sets BaseUrl…`. A first version matching `new ApiAgentRuntime` found NOTHING — that file uses a target-typed `new(...)` — so the scan matches the TYPE and reads code, not comments.
**Departures, stated.** (a) For items 2–5 the guard and its surface are born together, so "RED before it" is that guard removed from the new code and quoted above; items 1 and 2 additionally have a RED run against the stashed source. (b) Item 3's brief mutant "the check after the request" is behaviourally identical inside a turn — the position only decides whether the FIRST request is checked, and nothing carries over — so the watched mutant is the inclusive comparison loosened, which does change the verdict. (c) A sixth commit updates three existing expectations this unit changed (the price catalogue gains a third runtime, the vendor-file collection gains the scan, the honesty check holds a harness to an endpoint rather than an executable); the fourth, the schema pin, `U-runner-3` had already fixed. (d) `TurnMeter` gained `roleRuntime` beside `roleModel`, because a price is looked up by (runtime, model). (e) `Containment.RefusalToLaunch` is NOT applied to the harness: it refuses to START an uncontained child and the harness has none. (f) `backtest` is deliberately NOT on the harness's `trade` op list — that list is closed, and exposing a new pipe op to a worker is a decision rather than an inheritance; briefing it is a one-line follow-up.
**NOT done.** No real provider call — no run of this repository has ever made one, so the request and response shapes are the vendor's published chat-completions contract and the manifest stays `Verified = false`. No box, no ATAS, no money, no network. The chair is not on the harness, and there is no memory-search tool, no grant TABLE with fencing, no second provider and no key on disk (`U-contain-2`). **No test drives `MissionLoop` over a harness conversation**: `ConversationFor(role)` is wired and the loop reads it, but the end-to-end loop turn on the harness is unproven.
