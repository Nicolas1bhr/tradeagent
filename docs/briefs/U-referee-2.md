# U-referee-2 — the verdict: a promotion record bound by hash, invalidated by a changed assumption, forward evidence after the freeze, delivered
Read `docs/HOW-WE-BUILD.md`, `CLAUDE.md`, `docs/COUNCIL.md` (rule 9 `:34-36` what a promotion binds; `:32-33` what it authorises; `:74` the
Situation's promoted strategy; `:89-91` the verdict wake; `:135-136` forward evidence before capital; `:179-180` the owner's report; `:196-197`
referee-budgeted disclosures), then `U-referee-1` and `U-runner-3` on `main` (`Db/StrategyStore.cs`, `CampaignStore`, `Referee.RequestVerdict`,
`Strategy/Backtest.cs` — the run id's inputs), `Db/PublicationStore.cs` (`Commit` :167 vs `Record` :222, `IdOf` :129), `Db/MissionEventStore.cs:11-43`
(kinds), `AgentRuntime/MissionLoop.cs:320-441` and `App/AppHost.cs:965-1016` (the Situation), `Gateway/DailyReports.cs:402-436`, `DailyReport.cs:408-411`.
Branch `u-referee-2`, worktree `~/Projects/ai-trading-software-for-mihael-worktrees/U-referee-2`, rebased onto `main` AFTER `U-referee-1` lands.
Money-path grade: every guard ships with a test RED before it and ONE mutant, quoted. No box, no money, no order. **Schema 15.**

**Why.** `strategy_version` has no state: nothing says a version is promoted, refused or invalidated, and the binding material rule 9 demands
(code, parameters, dependencies, data, evaluator, execution-and-cost model, scoring policy) is scattered across one `strategy_run` row. Nothing
compares a version's `created_at` with a run's window, so a version frozen today promotes on last year's bars. No verdict reaches a role or the
owner: `MissionEventKind` has no verdict, section 8 has no promotion line. The referee is code; nothing here is a role's opinion.

1. **`strategy_promotion`**: one immutable content-addressed row (id = sha256 over the bound tuple) binding version id, campaign id, scoring-policy
   sha256, interpreter build, holdout dataset id and its sha256 at run time, execution model, evaluator version and the holdout run id, with
   `verdict` (`promoted`/`refused`), `reason`, `at`; app-only writer, `ON CONFLICT DO NOTHING`, no update, no delete. RED: `VersionById` returns
   a row with nothing to promote. Mutant (the id minted from the clock): one version promoted twice with two records.
2. **The holdout run is the referee's own**: `Referee.Verdict` (after `RequestVerdict` charged the budget) runs the version over the campaign's
   holdout window through the app's backtest, in-process, and scores it by the campaign's fixed policy; its trace and metrics are recorded with
   the run and never published whole. RED: no holdout run can be made — every run is refused past the cutoff (item 2 of `U-referee-1`), and the
   referee has no door. Mutant (the referee's run recorded as a research trial): the holdout charged as research.
3. **A changed assumption invalidates the evidence** (`COUNCIL.md:35`): a promotion whose dataset is later `REJECTED`, whose interpreter build
   or scoring-policy sha no longer matches the current, reads `invalidated` with the reason; `Promotions.Standing(versionId)` is the one reader.
   RED: `Reject` on the dataset leaves the promotion standing. Mutant (checked against the dataset's CURRENT sha instead of the sha on the run):
   a re-collected dataset revalidates a promotion.
4. **Forward evidence after the freeze**: a promotion requires the holdout run's window to begin AFTER the version's `created_at`; a run over
   history alone is refused in words ("evidence that precedes the freeze"). RED: a version created today promotes on last year's bars. Mutant
   (the comparison against the run's `created_at`): a run recorded today over pre-freeze bars counts as forward.
5. **The verdict is delivered and told**: a `verdict` mission event (one, keyed by promotion id) wakes the Research role; a `PublicationKind.Verdict`
   publication through `Commit` carries the SANITISED feedback the policy allows (the verdict, the reason class, never the holdout metrics or
   trace); the Situation names the promoted version (`COUNCIL.md:74`); section 8 lists every promotion, refusal and invalidation under "measured
   by TradeAgent" and its gap line goes only when a run or a verdict exists. RED: no verdict kind, no promotion line. Mutant (the holdout
   metrics published to the team): the holdout leaks through the relay.

Not this unit: `TryAuthorizeExecution` and the order path (a later unit READS `Promotions.Standing` there); paper or live running of a promoted
version (`U-flatten` and after); the directors' sealed assessments and the bounded challenge (`U-council-concurrent`); the allocator.
Gate: `dotnet build TradeAgent.sln -c Release --no-incremental` → 0 warnings; the three test projects in Release to a file → 0 failed; touched
classes 3×; a `Timing` red re-run alone 3×, never loosened. Commit per item, one sentence, no trailers. Append `## Report` (≤20 lines): tip sha,
gate counts, one line per item with its RED and mutant, what you did NOT do.

## Report

**Gated tip `3f075be`** (this report commit is docs-only on top), rebased onto `main` `9b2fb76` — `U-referee-1` (my base `19ef096`, already an ancestor of `main` when I started) and `U-harness-loop` are both BELOW me. I rebased twice and neither had a conflict to resolve: the first took `U-harness-loop` and replayed all six commits clean, because `MissionLoop.cs` was touched by both units in different regions (its `Cut` above, my `Promoted` below) and `docs/CONTRACTS.md` likewise; the second, after the gate below, took `main`'s CI-verdict commit, which touches only `BUILD-STATUS.md` and `docs/RESUME-HERE.md` — `git diff` between the two tips over everything else is EMPTY, and the build and the touched classes were re-run at this sha to say so. `git rev-list --count HEAD..main` printed 0. Seven commits: the five items, the wire proof of the disclosure boundary, and one noun in section 8.
**Gate**, all Release, outputs to files inside the worktree, deleted before this commit: `dotnet build TradeAgent.sln -c Release --no-incremental` → **`Build succeeded. / 0 Warning(s) / 0 Error(s)`**, 17 projects (DLL timestamps checked against the clock, because two seconds looked too fast). Touched classes 3× each → **`Failed: 0, Passed: 18`** every run (`PromotionLedgerTests`, `RefereeVerdictTests`) and **`Failed: 0, Passed: 1`** every run (`VerdictOverPipeTests`). `--no-build -c Release`: Unit **`Failed: 0, Passed: 980`**, Fault **`Failed: 0, Passed: 277`**, Integration **`Failed: 0, Passed: 657, Skipped: 1`**. Nothing went red, so no `Timing` re-run; the orphan sweep found one process over 40% — Apple's own `mediaanalysisd` at 117%, not mine — and nothing of mine beside it. Names vs `main`: **nothing removed**, 1544 → 1563 (19 added), and `file` reads all 140 test files as text.
**1 — `strategy_promotion`: one immutable row addressed by the nine facts it binds.** RED (the rung present, the table not): **`SQLite Error 1: 'no such table: strategy_promotion'`**, 3 failed / 1 passed. Mutant (the id minted from the clock — `At` hashed into the tuple): **`Assert.Equal() Failure: Strings differ / Expected: "e9e4f75fcd4d…" / Actual: "935238d65836…"`**, two ids for one judgement and therefore two records of it. The store has ONE write, `Record`, and a reflection test holds its public surface to `Record/ById/For/All/Standing` — no update, no delete, no method a later caller could reach for.
**2 — the holdout run is the referee's own and is not a research trial.** RED (the referee with no door, i.e. `BarAudience.Pipe(null)`): **`the holdout of campaign 1 could not be run: dataset 1 (BTCUSDT 1m v1) holds out every bar from 2026-08-01 01:00:00Z onwards … which reaches them`**, 2 failed / 1 passed. Mutant (the run registered as a trial): **`Assert.Equal() Failure: Values differ / Expected: 1 / Actual: 2`** — the evaluation the verdict budget paid for, charged again to whoever submitted the version.
**3 — a changed assumption invalidates the evidence, computed at read time.** RED (the recorded hashes never compared with the current facts): **`Assert.Equal() Failure: Strings differ / Expected: "invalidated" / Actual: "promoted"`** on all three, the rejected dataset among them. Mutant (the dataset's CURRENT sha compared with itself): the same message on the re-collected case alone, 1 failed / 7 passed. The row is asserted untouched afterwards: invalidation is never written back.
**4 — forward evidence after the freeze.** RED (the clause removed): **`a version frozen after the holdout begins promoted on it`**, 1 failed / 5 passed — and the run in that test is the profitable one, so it is the date that refuses it. Mutant (compared against when the RUN was made rather than the window it covered): the same message, because every run is made now and that comparison refuses nothing.
**5 — delivered and told.** RED (no delivery, no promotion line): **`Assert.Single() Failure: The collection was empty`** for Research's wake, and **`Assert.Contains() Failure: Sub-string not found / Not found: "PROMOTED version 5d221b931c28"`** in the report. Mutant (the holdout's net appended to the note): **`Assert.Equal() Failure: Strings differ / Expected: "1" / Actual: "148"`** — with the two hashes stripped the note's only digits must be the campaign id, and 48 was the net. The note is asserted digit by digit rather than against a list of figures, because a leaked 0 or 6 would pass a list.
**Where COUNCIL is silent, the choices — all in `CONTRACTS.md` as choices.** A new `strategy_promotion` table rather than an outcome column on `strategy_verdict`: that row is the CHARGE, written when the question is put, and this one is the ANSWER — one row written twice for two reasons would be two facts in one place. The scoring policy is CODE (`ScoringPolicyV1`) bound to the campaign's text by its sha, and a campaign that fixed another policy is **not judged at all**. Its clauses are forward evidence, COMPLETED, at least one closed trade, and net above its declared costs — and nothing about drawdown or a minimum sample, because those would be numbers nobody measured. The execution model is the JUDGE'S (a parameter of `Verdict`, default `Frictionless`, hashed into the promotion), so the submission does not choose the costs it is judged under. `reason` is a closed vocabulary on the row, which is what makes it impossible for the team's note to carry a figure. `Standing` reads the dataset's ledger STATE rather than re-hashing the file, because it is meant for the money path. The verdict's wake is keyed by the promotion through a new optional `eventId` on `PublicationStore.Commit`, null everywhere else. Research is the only recipient: the chair reads section 8.
**Beyond the brief, and the one thing I found while wiring it.** `trade report` serves the WHOLE rendered report to the agent (`text`, verbatim), so listing the holdout run in section 8 the way research runs are listed would have handed back through the report exactly what `data-bars` and `backtest` refuse. The holdout run is therefore named, dated and counted and **not valued**, and `VerdictOverPipeTests` proves it over the wire on a connection that is refused the same bars a moment later.
**Schema 15**, added as `if (have < 15)` under 14 with the `Versioning.cs` paragraph; nothing below it edited. The pin in `PromotionLedgerTests` is a FLOOR (`>= 15`) plus "the stamped number is this build's" plus the thirteen column names in order, so an additive rung above it needs no edit here — and the column list is asserted because the ABSENCE of an `invalidated` column is the design.
**NOT done.** There is no way for the owner to ASK for a verdict: `TradingGateway.Referee.Verdict` is in-process, tested, and has no button — the state `CampaignStore.Renew` was left in, and the same reason (no screen work in this unit). No pipe op and no `trade` verb reaches any of it, and a test asks the whole op vocabulary by name. `TryAuthorizeExecution` and the order path are untouched: nothing reads `Promotions.Standing` yet, which is the later unit the brief names. No paper or live running of a promoted version, no allocator, no directors' sealed assessments. The trial-budget-by-one race `U-referee-1` named is untouched. The agent's own guide (`WorkspaceBuilder`) still says nothing about verdicts; the Situation's line does, including that it cannot ask for one. `BUILD-STATUS.md` untouched, which is the manager's pass. No box, no ATAS, no money, no order placed, no authority granted, no network.
