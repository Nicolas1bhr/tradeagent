# U-council-concurrent-1 — two roles' turns may overlap: every single-slot assumption the serial council allowed is made per role
Read `docs/HOW-WE-BUILD.md`, `CLAUDE.md`, `docs/COUNCIL.md` (`:259-260` serial "so leases can wait"; `:79` leases in the execution record; `:30-31`
rule 7, quiescence of EVERY managed agent; `:26-29`; `:99-101`), then `MissionLoop.cs` (`:949` "ONE ROLE PER TURN, AND ONE TURN AT A TIME", `LoopAsync`,
`TurnAsync`, the single fields `_sessionTurns`/`_consecutiveErrors`/`_reportedCap`, the per-role `_cutLastTurn`), `TurnMeter.cs` (`_open` per role since
`U-budget-reserve`; `_staged` ONE slot `:461,571,585-594,637`; the ctor's `LoseOpen` `:485-494`), `AiAttemptStore.cs:255-280,340,375-386`, `CouncilRelay.cs:
141-158,183,206-285,317-327,365-385`, `AgentPresence.cs:65-68`, `MaterialScanner.cs:29`, `MissionEventStore.cs:349,449`, `Database.cs:17,68-84` (one lock,
re-entrant), `AppHost.cs:214-217` (stale), `CONTRACTS.md:640-646,859-862` (the app's two leases are in memory, deliberately). Branch `u-council-concurrent-1`,
worktree `~/Projects/ai-trading-software-for-mihael-worktrees/U-council-concurrent-1`, rebased onto `main` first. Money-path grade: every guard ships
with a test RED before it and ONE mutant, quoted. No box, no ATAS, no money, no order. **No schema number** (the choice below).

**Why.** Nothing prevents a second `TurnAsync`; `_staged` is one slot, so role B's close can land inside role A's transaction (named by
`U-budget-reserve`); a second `TurnMeter` over the same file marks EVERY live launch LOST; `Run(role: null)` walks both roles' `out/` and quarantines
the other's in-flight file; quiescence is process-wide, so any overlap records every dropped file `InboxUnattested`; one `_sessionTurns` rotates
whichever conversation runs. COUNCIL is silent on where a lease lives — THE CHOICE: in memory (as `CONTRACTS.md:643-646` chooses for the app's two
leases), the LAUNCHED `ai_attempt` row its durable witness, reconciled by `LoseOpen` on the next start; said so in `CONTRACTS.md`. Two roles, one turn each.

1. **A per-role turn lease.** Taken before `BeginTurn`, released in the `finally` beside `Commit`; a second `TurnAsync` for the same role is
   REFUSED in words, never queued; the loop may launch the other role while one is turning (the chair's busy check and the owner's chat
   serialisation unchanged). RED (the `Barrier(2)` idiom of `BudgetReservationTests:65-107`): two threads on one loop both admit and both
   consume the same wake — `Expected: 1 / Actual: 2` LAUNCHED rows for the role. Mutant (the lease taken after `BeginTurn`): the same.
2. **`_staged` per role, keyed by attempt.** `CommitStaged(role)` commits only that role's held close; `Begin`'s opportunistic drain drains only
   its own role's. RED: A stages, B ends, A commits → A `Expected: ENDED / Actual: LAUNCHED`, B's close inside A's transaction. Mutant
   (`CommitStaged` ignoring the role): the same.
3. **`LoseOpen` loses only what no live turn in this process holds:** a registry of live attempt ids (in memory); the ctor pass and any second
   meter over the same file skip them; every other LAUNCHED row still becomes LOST at its reservation. RED: a second `TurnMeter` built mid-turn →
   `Expected: LAUNCHED / Actual: LOST`, the day short by a reservation. Mutant (filtered by role instead of by liveness): the other role's live
   turn lost.
4. **The relay pass is the turning role's own, and never holds the lock across disk:** `Run(role, attempt)` touches only that role's `out/`;
   the reconcile-everything pass runs only on start when nothing is live; the files are READ before the `Database.Write` opens so one role's
   commit never blocks the other's turn on I/O. RED: A's pass quarantines B's in-flight `report-<b>.md` (`Assert.Empty(Quarantined(research))`).
   Mutant (`Attributable` accepting any LAUNCHED row): a stale process's file published as live work.
5. **Quiescence still means every managed agent, and the loop's counters are per role:** a scan pass runs only when no role is turning;
   `_sessionTurns`, `_consecutiveErrors` and `_reportedCap` per role; the card names every turning role. RED: with two roles overlapping a
   dropped file is `Expected: Inbox / Actual: InboxUnattested`, and A's five failures rotate B's session. Mutant (quiescence asked of the
   turning role only): the attestation red. `CouncilLoopTests`' `Peak == 1` becomes the measured `Peak == 2`, and `AppHost.cs:214-217` is rewritten.
Not this unit: the consequential boundary, assessments, challenges, dispositions and the trial-budget race (`U-council-concurrent-2`); a third
role; the chair on the harness; the allocator; two active roles on a screen beyond the card's line.
Gate: `dotnet build TradeAgent.sln -c Release --no-incremental` → 0 warnings; the three test projects in Release to a file → 0 failed; touched
classes 3×; a `Timing` red re-run alone 3×, never loosened. Commit per item, one sentence, no trailers. Append `## Report` (≤20 lines): tip sha,
gate counts, one line per item with its RED and mutant, the lease choice restated, what you did NOT do.


## Report

**Six commits on `main` `4cd8d1b`, and this report** (tip = this commit, which touches only this file). `git rebase main` ran THREE times and had **no conflicts** any time: the first dropped the five already-landed commits by patch-id — the four `U-harness-loop` ones and this brief, which the manager had cherry-picked; the second, after `U-referee-2` landed during the first gate run, replayed mine over it (its `MissionLoop` change is all inside `MissionSituation` and none of it meets the loop's own path); the third was over two docs-only commits that landed after the gate — `git diff --stat <gated tip> HEAD -- src tests` prints NOTHING, so the run below is a run of this code. The gate is the SECOND run, at what was then the tip, `49c7135`. **No schema number**, as the brief chose.
**Gate**, Release, outputs to files since deleted: `dotnet build TradeAgent.sln -c Release --no-incremental` → `0 Warning(s) 0 Error(s)`, 17 projects produced. Touched classes 3× (`CouncilLoopTests`, `BudgetReservationTests`, `AiAttemptLedgerTests`, `TurnCommitTests`, `CouncilRelayTests`, `HarnessLoopTests`, `TurnAllowanceTests`, `MissionLoopTests`, `TurnMeterTests`): `Failed: 0, Passed: 102, Total: 102` on each of the three runs. Suites `--no-build`: Unit `Failed: 0, Passed: 988, Total: 988`; Fault `Failed: 0, Passed: 277, Total: 277`; Integration `Failed: 0, Passed: 657, Skipped: 1, Total: 658`. No `Timing` red anywhere, so nothing was re-run. Test names vs `main`: **1565 → 1573, the removed set EMPTY**, the eight added are this unit's own; `file` says every test source is still UTF-8 text. The `ps` scan for ppid-1 processes over 40% CPU printed nothing before the final runs (an earlier scan found macOS's own `mediaanalysisd`, which is not a test process and was not killed).
1. **A per-role turn lease** (`MissionLoop._turning`), taken immediately before `BeginTurn`, dropped beside `Commit` and again by a `using` on every other way out; a second `TurnAsync` for that role is refused in words through a new `Refused` event the app logs, never queued; and a role that IS turning is stepped over when the next turn's role is chosen, which is what lets the other role work. `_working` is gone — the card's "working" IS the lease. RED (two real threads through a `Barrier(2)` in `SituationAsync`): `Assert.Equal() Failure … Expected: 1 / Actual: 2` launches for one role. Mutant (the lease taken after `BeginTurn`): the same, `Expected: 1 / Actual: 2`.
2. **`_staged` per role, keyed by the attempt it closes**; `CommitStaged(role)` and `Begin`'s opportunistic drain touch only their own role's. RED (chair ends, Research ends, chair commits): `Expected: ENDED / Actual: LAUNCHED` on the chair's row. Mutant (`CommitStaged` ignoring the role and writing every held close): `Expected: LAUNCHED / Actual: ENDED` on Research's row — its close inside the chair's transaction.
3. **`LiveAttempts`, the register of launches this process is flying**, consulted by `AiAttemptStore.LoseOpen(at, except)`: a second meter over the same file loses every other open row at its reservation and skips the live one. Skipping releases no money — a LAUNCHED row's reservation counts against the ceiling exactly as a LOST row's cost does. RED (a second meter built mid-turn): `Expected: LAUNCHED / Actual: LOST`. Mutant (the skip narrowed by role — only the chair's live rows): the same, on Research's live turn. A restart holds none and loses them all; `TurnCommitTests.World.Open` and `AiAttemptLedgerTests.Meter` now say so by handing each "process" its own register.
4. **The relay pass is the turning role's own** — `Run(role, attempt)` touches only that role's `out/`, `Reconcile()` is the start-up pass and the only one over every role, the fence's third verdict LEAVES a file whose launch this process is flying instead of quarantining it, and the staged files are READ before the turn's `Database.Write` opens. REDs: `Assert.Empty() Failure: Collection was not empty / Collection: ["report-turn-b.md"]`, and `the other role's launch record was blocked behind this role's file read` (the stack showing it inside `Database.Write`). Mutant (`Fence` publishing any LAUNCHED row): a stale process's file published as live work — `Collection: [Publication { … Attempt = turn-open … }]`.
5. **Quiescence is of every managed agent** (`docs/COUNCIL.md` rule 7): a material pass runs only when NO role is turning, and no role may launch while one runs — both directions, because a turn launched inside the window a pass is measuring costs the same claim. `_sessionTurns`, the run of consecutive errors and the cap notice are per role. REDs: `Expected: Inbox / Actual: InboxUnattested` for a file dropped between turns while the other role tried to launch, and `Expected: 0 / Actual: 1` for the Research Director's session rotated by the chair's turns. Mutants: the lease dropping its pass check (quiescence asked only when the pass starts) → the same `Inbox / InboxUnattested`; one error slot → `Expected: 00:02:00 / Actual: 00:00:30`. `CouncilLoopTests` now MEASURES `Peak == 2` in a new test that also reads the card mid-flight (`Operations Director and Research Director: working`); the old `Peak == 1` is kept where it is true — two turns taken one after the other — with its comment saying so. `AppHost`'s stale "one open attempt at a time is safe because the council is serial", the loop's own two sentences saying the same, and `CONTRACTS.md` are rewritten; `CONTRACTS.md` states the lease choice.
**The lease choice, restated.** All three leases are **in memory** — the same choice `CONTRACTS.md` already records for the gateway's dispatch lease and the owner's composite lease: a claim that outlived the process holding it could never be released. The durable witness that a turn was in flight is its **LAUNCHED `ai_attempt` row**, which the next start turns LOST at its reservation and reconciles from disk. No schema, no table, no new column.
**NOT done.** No box, no ATAS, no money, no order, no network, no console, no authority granted. Nothing here changes what a role MAY do: only Operations places orders (`GatewayPipeServer` refuses a mutating op from any other role) and the gateway's dispatch gate is still `SemaphoreSlim(1,1)`. `LoopAsync` still drives one turn at a time — the guards make an overlap safe and two callers prove it, but a second driver is not this unit. `WorkspaceRevisions.Snapshot` still reads its two size-capped memory files inside the turn's transaction; only the relay's own unbounded `out/` read moved outside it. Not touched: the consequential boundary, assessments, dispositions, the trial-budget race (`U-council-concurrent-2`), a third role, the chair on the harness, the allocator, and any screen beyond the card's one line.
