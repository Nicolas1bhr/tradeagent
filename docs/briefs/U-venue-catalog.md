# U-venue-catalog — the instrument as a recorded fact with a source, not a number an agent typed into a request
Read `docs/HOW-WE-BUILD.md`, `CLAUDE.md`, `docs/COUNCIL.md:145,152` (bars "with instrument increments"; size "rounded down to the increment, then
the gateway's limits"), `:95` (calendars per connector and per instrument), `:239` (venue capabilities before unattended real money),
`docs/DECISIONS.md:73-78` (vendor facts are data, not code, with a `Verified` flag), then `Core/Strategy/Backtest.cs:21-25,67,89-133` (the run's
increment "because the dataset has none"; `ExecutionModel.Canonical`), `Data/BarFeed.cs:65-69`, `Gateway/Backtests.cs:85`, `Db/DatasetStore.cs`
and `Database.cs:444-481,747-748`, `ConnectorSdk/Contracts.cs:12-13` (`InstrumentInfo`: `TickSize`, `ContractSize`, nothing else),
`Connectors.Fake/FakeBroker.cs:45-60` (four hard-coded futures; `TickSize` null for an unknown symbol), `GatewayPipeServer.cs` (`data-list`,
read ops), `App/SettingsView.cs:161-208` (Market data and the holdout card). Branch `u-venue-catalog`, worktree `~/Projects/ai-trading-
software-for-mihael-worktrees/U-venue-catalog`, rebased onto `main` first. Every item ships with a test RED before it and ONE mutant,
quoted. NOT the money path: nothing here reaches `PlaceAsync` (`U-freshness` does). No box, no ATAS, no money. **Schema 17.**

**Why.** COUNCIL requires an increment on the bars and a size rounded down to it, and no increment exists anywhere: every backtest's
increment is a number the agent typed (`Backtests.cs:85`), defaulting to `Frictionless` 1 on a pair whose real step is 0.00001. The word
"venue" exists only in comments: no table, no type, no calendar kind. Until an instrument is a recorded fact with a source and a verified
flag, a backtest's size and a later live intent have nothing in common to be bound to (`U-freshness`, `U-referee-2`'s promotion).

1. **`venue` and `venue_instrument`, app-owned data:** venue id, display name, calendar kind (`continuous`/`sessioned`); symbol, `tick_size`,
   `quantity_increment`, `source`, `recorded_at`, `verified` — shipped as a built-in catalogue (the Binance spot pair the app collects, the
   simulator's four futures) overridable by `venues.json` under the app's data folder, `Verified=false` unless the row says who verified it.
   RED: `no such table: venue_instrument`. Mutant (an unverified row served as verified): a catalogue with `Verified=false` serves silently.
2. **No pipe op writes it; `venue-list` reads it** (a read op, not in `Ops.Mutating`, the `data-list` shape) and `trade venue list` shows it. RED:
   `the schema does not name 'venue-list'`. Mutant (`venue-list` in `Ops.Mutating`): refused for the one role that needs it.
3. **A dataset names its venue and instrument:** `dataset.venue_id`, `dataset.instrument_symbol`, nullable, backfilled `binance-spot`/pair for
   existing rows; `data-list` and section 8 carry them. RED: `data-list` carries no venue for a recorded dataset. Mutant (the venue taken from
   the request, not the row): a caller renames the source of its own evidence.
4. **A run's increment comes from the catalogue when the request omits it; a declared one still wins and only the declared model is hashed**,
   so no existing run id moves. RED: a request with no `--increment` runs at 1 on a pair the catalogue records at 0.00001. Mutant (the catalogue
   value folded into `Canonical`): `BacktestDeterminismTests` red — every run id changes.
5. **A refusal, never a guess:** an instrument the catalogue does not hold, or holds unverified, is refused in words when no increment was
   declared (the `FakeBroker.TickSize` null and `ContractSizeOrThrow` shape). RED: an unknown symbol silently runs at increment 1. Mutant (the
   refusal returning 1): the same. `CONTRACTS.md` and the guide describe the catalogue and what it does not claim (no fee, no min notional —
   COUNCIL is silent; fees stay declared per run, `:155`).

Not this unit: `PlaceAsync` or the gateway's risk pass (`U-freshness`); fees or min notional as fields; prop-firm rulebooks; live instrument
reads from ATAS; the box; a real venue.
Gate: `dotnet build TradeAgent.sln -c Release --no-incremental` → 0 warnings; the three test projects in Release to a file → 0 failed; touched
classes 3×; a `Timing` red re-run alone 3×, never loosened. Commit per item, one sentence, no trailers. Append `## Report` (≤20 lines): tip sha,
gate counts, one line per item with its RED and mutant, what you did NOT do.

## Report

**Gated tip `0deceab`** (this report commit is docs-only on top), **rebased onto `main` `c165b39`** — six commits, one per item plus one the gate itself found. I rebased THREE times: over `U-council-concurrent-1` with no conflict; over `U-council-concurrent-2`'s **schema 16**, which conflicted in `Database.cs` and `Versioning.cs` and I resolved inside the rebase — the ladder now reads 15 → 16 → 17 in file order, 16's paragraph sits under 15's and mine under 16's, and my rung's comment says 16 landed first rather than that it was in flight; and finally over two DOCS-ONLY commits, where `git diff 5bd8390 0deceab -- src tests` is **EMPTY**, so the build and the touched classes were re-run at this sha to say so and the three full suites were not. `git rev-list --count HEAD..main` prints 0; `git status --porcelain` prints nothing after every mutant was restored.
**Gate**, all Release, outputs written to `gate/` inside the worktree and deleted before this commit: `dotnet build TradeAgent.sln -c Release --no-incremental` → **`Build succeeded. / 0 Warning(s) / 0 Error(s)`**, 17 projects each printing its own dll. Touched classes 3× each, 0 failed on every run: unit (`VenueCatalogTests`, `VenueOpsTests`, `VenueIncrementTests`, `BacktestRequestTests`, `CampaignLedgerTests`, `RefereeVerdictTests`, `WorkerToolTests`, `BacktestDeterminismTests`, `DataOpsTests`) **`Failed: 0, Passed: 108`**; integration (`VenueOverPipeTests`, `DataOverPipeTests`, `BacktestOverPipeTests`, `HoldoutOverPipeTests`) **`Failed: 0, Passed: 33`**; fault (`UnconfirmedLatchTests`) **`Failed: 0, Passed: 4`**. `--no-build -c Release` once each: Unit **`Failed: 0, Passed: 1029`**, Fault **`Failed: 0, Passed: 277`**, Integration **`Failed: 0, Passed: 663, Skipped: 1`** (a skip that predates this unit). Nothing went red, so no `Timing` re-run; the ppid-1 scan over 40% found only Apple's `WindowServer` at 45.6%. Names vs `main`: **1629 → 1656, the removed set EMPTY**, the 27 added are this unit's own; `file` reads all 141 test sources as text.
**1 — `venue` and `venue_instrument` at schema 17, built-ins overridable by `venues.json`, `source`/`recorded_at`/`verified` on every row.** RED: **`Microsoft.Data.Sqlite.SqliteException : SQLite Error 1: 'no such table: venue_instrument'`**. Mutant (every row read back as verified): **`nothing in this build has confirmed Binance's own instrument definition`**. An unreadable `venues.json` empties the table and says why, `RuntimeManifest.Read`'s rule applied to the number a size is computed from.
**2 — `venue-list` is a READ, not in `Ops.Mutating`; `trade venue list`; on `GrantedWorkerTools.TradeOps` for every role.** RED: **`the schema does not name 'venue-list'`**. Mutant (it added to `Ops.Mutating`): **`ROLE_MAY_NOT_TRADE … 'venue-list' moves money, and the Research Director may not`** — the mutating list IS the role gate, so a read on it is refused to the one role whose job is to size positions.
**3 — `dataset.venue_id` and `dataset.instrument_symbol`, backfilled, carried by `data-list` and by section 8.** RED: **`data-list carries no venue for a recorded dataset`**. Mutant (the venue derived from the pair the caller collected instead of read from the recorded column): **`Assert.Equal() Failure: Expected: "sim-futures" / Actual: "binance-spot"`**. The backfill is proven by taking a real database back to 16 — dropping the columns and restamping `meta` — and reopening it.
**4 — the increment comes from the catalogue when the request omits it, a declared one wins, and the provenance is recorded but not hashed.** RED: **`Assert.Equal() Failure: Expected: 0.00001 / Actual: 1`**. Mutant (the provenance folded into `ExecutionModel.Canonical`): **`BacktestDeterminismTests … Expected: "9eff99d7…" / Actual: "d6eb52ba…"`** — every run id moves. A catalogue-sourced run and the same four numbers declared by hand have the SAME run id, and `strategy_run.increment_source` is what tells them apart.
**5 — a refusal, never a guess.** An instrument the catalogue does not hold, one whose row is not verified, and a dataset that records no instrument are each refused in words naming both routes out. RED: **`Assert.Throws() Failure: No exception was thrown / Expected: typeof(TradeAgent.Gateway.GatewayDeniedException)`** — an unknown symbol ran silently at 1. Mutant (the refusal returning 1): the same sentence on the unverified row. The increment is looked up by the DATASET's instrument and never by the program's, which is a line an agent types.
**6 — found by the gate and not by an item:** the catalogue sync in the gateway's constructor took the gateway down on a store that would not take a write (**`SQLite Error 5: 'database is locked'`**, `UnconfirmedLatchTests.A_startup_sweep_that_cannot_write_still_pauses_and_says_so`). It is guarded and logged now, the rule the startup sweep beside it already follows: a gateway that cannot write still has to exist, because the pause is what it IS.
**Where COUNCIL is silent, the choices — all in `CONTRACTS.md` and the guide.** **No fee and no minimum notional** anywhere in the catalogue (:155 keeps fees declared per backtest). No session table: `calendar_kind` is `continuous`, and `sessioned` means "it closes and TradeAgent cannot tell you when". **Binance spot's BTCUSDT ships `verified = false`** — nothing in this build has read Binance's own instrument definition and this unit reaches no network — so out of the box a backtest over collected Binance bars that omits `--increment` is REFUSED until the owner records the row; the simulator's four futures ship verified because the venue is this application's own, and a test holds them to `FakeBroker.Instruments`. That refusal is why **15 existing fixtures now declare `Increment: 1m`**, which is the number they already ran under: no canonical model and no run id moved.
**Schema 17**, `if (have < 17)` under 16, with the `Versioning.cs` paragraph. The pin in `VenueCatalogTests` is a FLOOR (`>= 17`) plus "the stamped number is this build's", and the empty-table assertion is deliberate: the rows are the catalogue's and a migration that seeded them would be a vendor fact nobody could correct with a one-line edit.
**NOT done.** Nothing near `PlaceAsync`, the gateway's risk pass or `TryAuthorizeExecution` — a catalogue increment reaches no order, and `U-runner-2` still states a live intent's quantity without one applied. No fee or min-notional field, no prop rulebook, no live instrument read from ATAS, no real venue, no `venues.json` committed to the repo. No screen: the catalogue is on the pipe and in the report, and there is no card in the app's own window. No op that writes a venue, and a test asks the shipped `Ops` vocabulary by name. `BUILD-STATUS.md` untouched, which is the manager's pass. No box, no ATAS, no money, no order placed, no authority granted, no network, no console.
