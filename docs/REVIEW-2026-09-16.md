# REVIEW 2026-09-16 — the money path on `main` at `c441120`, the third milestone review, before any release is cut

Fresh Opus reviewer, per `docs/briefs/REVIEW.md`. Worktree
`~/Projects/ai-trading-software-for-mihael-worktrees/review`, detached at `c441120`, probes on branch
`review-probes-c` (`review-probes` and `review-probes-b` are the first two reviews' and were left where
they were). No product file was changed, nothing was pushed, and no other worktree was entered.

**Reviewer's report.** Sha `c441120`. HIGH 5 · MED 3 · LOW 0 · UNVERIFIED 7; thirteen probes, all executed
and quoted below; probes branch `review-probes-c` @ `58aa4fa` (two test files, no product change). Gate on
that tip: `dotnet build TradeAgent.sln -c Release --no-incremental` → **0 warnings, 0 errors**; the full
suite in Release with the probes → **Unit 1132 + Fault 362 + Integration 669 = 2163 passed, 0 failed, 1
skipped** (= 2150 on `main` plus the 13 probes), and the two probe classes alone 12/12 and 1/1.

**Every row below is an EXECUTED refutation.** Each names a probe that was RUN, and its output is quoted
under the table. Reading-only observations are in the ranked UNVERIFIED list and are **not** findings.
Probes: `tests/TradeAgent.FaultTests/ReviewProbesC.cs` and
`tests/TradeAgent.IntegrationTests/ReviewProbesCPipe.cs`.

Four of the rows are the manager's addendum: Codex could not build in its sandbox and reported four
hypotheses with no execution. Each was run here and all four are VERIFIED, none REFUTED — C1 is finding 5,
C2 is finding 3, C3 is finding 4 (both arms), C4 is finding 6.

## Findings

| # | sev | file:line | finding | the probe that settles it | what would fix it |
|---|-----|-----------|---------|---------------------------|-------------------|
| 1 | HIGH | `src/TradeAgent.Gateway/LossBreach.cs:144-152`; `src/TradeAgent.Gateway/TradingGateway.cs:1958`, `:2026`, `:3110`, `:3433` | **A loss-budget closure on a symbol whose name carries a colon is written, is on disk, and is invisible to every reader — so the closed scope goes on trading.** `LossBreach.SymbolKey` writes `loss_breach:{account}:{symbol}:{day}`; `ScopeOf` reads it back with `key.Split(':')` and refuses anything that is not exactly three or four parts. A venue-qualified symbol (`ES:H6`, `BINANCE:BTCUSDT`) makes five, so `OpenClosures` (`:1958`) SKIPS the row and `DayClosed`, `SymbolClosed`, `SymbolsClosedToday`, `ReopenWhatHasEarnedIt` and the `alreadyClosed` set (all through `OpenClosures`), `LatestBreach` (`:2026`, which is what `APPROVAL_PREDATES_LOSS_BREACH` is judged on), `ClosureHistory` (`:3110`, section 4's episode list) and `PriorBreaches` (`:3433`, the strike count) all answer "nothing is closed". The same split makes a DAY closure invisible when the ACCOUNT id carries a colon. The record is never reopened either, because a receipt is only ever written for a closure something can see. The symbol is the PLATFORM's string, not one TradeAgent chooses. | `P1_a_symbol_with_a_colon_makes_its_own_loss_budget_closure_invisible`, `P1b_the_breach_key_does_not_round_trip_for_a_venue_qualified_symbol`, `P1c_the_scope_it_cannot_see_is_closed_and_flattened_over_and_over` — the same run on plain `ES` refuses, so the difference is the key and nothing else | Stop deriving the scope from the key's shape: put `Account`, `Symbol` and `Day` on the row (they are already there) and scan-then-parse, or delimit with a character no venue puts in an instrument name and refuse a symbol that contains it at the point the key is MINTED rather than at the point it is read. |
| 2 | HIGH | `src/TradeAgent.Gateway/TradingGateway.cs:783-794`, `:820`; `src/TradeAgent.Gateway/Pnl.cs:112`, `:124`; `src/TradeAgent.Core/Db/FillStore.cs:16-30` | **The fill ledger is not scoped to an account or a platform, and the loss budget is enforced off it.** `LedgerPnl` passes `_fills.Since()` — every row in the table — to `Pnl.Compute`, which orders by time and keys its average-cost book by SYMBOL alone (`:124`); nothing anywhere reads `Fill.AccountId`, and `Fill` carries no connector at all. One database that has seen two accounts (a prop firm's two, a simulator and a broker) therefore computes ONE figure out of all of them, and that figure is what `LossBudget.Read` closes a day on and what `trade pnl`, the Performance card and section 4 print. Both directions are live: a second account's loss closes an account that has never traded, and **a second account's PROFIT nets off a real loss so the budget never fires**. | `P3_one_accounts_losses_close_another_accounts_day`, `P3b_one_accounts_profit_keeps_another_trading_past_its_budget` — B's own 1250 USD loss against a 1000 USD budget does not close its day while a winner sits beside it, and closes it on a clean database | Give `Fill` a connector id, filter `LedgerPnl`'s read to the operating `(connector, account)` pair, and key `Pnl`'s book by `(account, symbol)` so one account's fills can never enter another's average cost. |
| 3 | HIGH | `src/TradeAgent.Gateway/TradingGateway.cs:6299`; `src/TradeAgent.Gateway/LossBreach.cs:179-261` | **A PAPER breach flattens the LIVE book after a platform switch: real positions closed and real orders cancelled, by code, on an installation that has set no loss budget at all.** `FlattenForBreachAsync`'s own summary says it refuses "unless the account this gateway is actually operating is the account the breach was recorded for, ON THE PLATFORM IT WAS RECORDED ON"; the code compares `account.Id == breach.Account` and nothing else, and `LossBreachRecord` carries neither a connector nor a mode. The flatten's OUTCOME key does carry the connector, so on the new platform the killed-flatten sweep finds no outcome for a breach that was fully flattened elsewhere and re-runs it. `docs/CONTRACTS.md` states the account-only breach KEY as an inherited limit; it does not state that the flatten follows the account onto another platform. | `C2_a_paper_breach_and_then_the_owner_switches_platform` — 1 cancel and 1 close reach the live platform, the owner's own 4-lot position and resting bid are gone, and a `loss_flatten:atas:SIM-001:…` record claims `Flat` | Put `Connector` and `Mode` on `LossBreachRecord` at `Compose`, and make `FlattenForBreachAsync` refuse when either differs from the gateway it is running on — with the same sentence the account check already uses. |
| 4 | HIGH | `src/TradeAgent.Gateway/TradingGateway.cs:4786-4790` (against `:3826-3851`) | **`ApproveAsync` re-runs two of the four gates `PlaceAsync` runs on the position reading, and the two it drops are the capital gate and the reconciliation refusal — two of the five `docs/COUNCIL.md`:14-15 says EVERY order passes.** It re-runs `OpenPositionCapOrThrow` and `LossBudgetOrThrow`; it never re-runs `RefuseAnUnresolvedReducerOrThrow` or `AllocationCeilingOrThrow`, and it never recomputes `record.AllocationId`. So (a) a parked order is approved onto a ceiling the owner has since withdrawn, and the sent order is attributed to an allocation that no longer stands — `docs/COUNCIL.md`:210-211's "which allocation caused an operation" recorded wrong on the row; and (b) a parked reduce is approved over an order this gateway cannot account for, which is the doubling `CLOSE_UNRESOLVED` exists to refuse. | `C3a_a_parked_order_approved_after_its_capital_was_withdrawn` (fresh: `ALLOCATION_EXCEEDED`; parked: SENT) and `C3b_a_parked_reduce_approved_over_an_unresolved_reducer` (fresh: `CLOSE_UNRESOLVED`; parked: SENT, and ES 4 becomes ES 1 on two reducers of 1 and 2) | Move the four gates into one method taking `(intent, positions, account, requestId)` and call it from both places, assigning `AllocationId` from the answer on the approval path too. |
| 5 | HIGH | `src/TradeAgent.Gateway/GatewayPipeServer.cs:658`, `:702`; `src/TradeAgent.Security/AgentGrants.cs:118-130` | **A launch grant is verified once, at `hello`, and the role it proved is then used for every later frame on that connection — so an EXPIRED, REVOKED or turn-ended grant keeps placing orders for as long as the caller holds the socket.** `GrantRefusal` runs inside the `hello` arm and the captured `grant` is handed to `Handle` at `:702`, where `ctx.MayPlaceOrders` is the only thing between a frame and the wire. `AgentGrants.Revoke` and `EndTurn` change the register and reach nothing that is already connected; the 60 s `Grace` is documented for a call already in flight, not for a socket that stays open. | `C1_an_expired_or_revoked_grant_on_an_already_authenticated_connection` — all three of expired-by-an-hour, revoked and `Dispose()`d still place; a NEW connection with the same grant is `IPC_UNAUTHENTICATED` | Re-verify the grant on every frame that reaches the role gate (`GrantRefusal` already does the work), or have `Revoke`/`EndTurn` close the live connections holding that token. |
| 6 | MED | `src/TradeAgent.Gateway/TradingGateway.cs:2508`, `:2605-2660`; `src/TradeAgent.Gateway/LossReopen.cs:135-148` | **One forward step of the machine clock ends a closure on the next tick, with no elapsed time at all — and the contract says the opposite.** The high-water mark refuses only `at < clock.HighWater`; a forward reading raises the mark, writes no `loss_clock_suspect` row, and `HeldBy`'s remaining test is `at < eligible`, which the jump has just satisfied. `docs/CONTRACTS.md` ("a step forward is ordinary and lengthens nothing") and `LossReopen.ClockKey` ("a clock that steps FORWARD is allowed and only ever leaves things closed longer") are both claims about the code that the code does not keep. Nothing an agent can reach moves the clock, which is why this is MED and not HIGH. | `C4_a_clock_that_jumps_forward_ends_the_closure_on_the_next_tick` — closed 12:00:40Z, eligible 2026-03-11 12:00:40Z, one `MoveTo(+2 days)`, one tick, receipt written, a new buy SENT | Hold the closure against a MONOTONE reading as well as a wall clock — carry `Environment.TickCount64` (or a `Stopwatch` seeded at the breach) on the clock mark and require both to have run the closure — or treat a forward step larger than a few watch intervals as suspect, as a backward one is. |
| 7 | MED | `src/TradeAgent.Gateway/TradingGateway.cs:3793` (against `:3815`, `:4514-4518`) | **The owner's per-order limits are decided before the awaited reads and are never asked again at the wire.** `RiskCheckOrThrow` — the instrument allowlist, `MaxOrderQuantity`, `MaxNotionalPerOrder`, the quote-age rule — runs at `:3793`, above `_dispatchGate.WaitAsync` at `:3815`; what is re-evaluated at the wire is the authorization chain and the record's mode (`ReauthorizeAtDispatchOrThrow`) plus the four position gates. `docs/CONTRACTS.md`'s heading "Every gate is evaluated at the moment of dispatch, after the awaited reads" over-states what is there. The window is one connector round trip — 50 s at shipped ATAS values — and it swallows an owner narrowing a limit on the Safety page. | `P2_an_instrument_taken_off_the_allowlist_inside_the_gate_still_reaches_the_wire` — ES removed from the allowlist and the size cap cut from 10 to 1 while the order sits in the gate's position read; `ES Buy 5` reaches the broker, the identical fresh order is `RISK_LIMIT_EXCEEDED` | Re-run the per-order limits inside the gate beside the other four, on the reference price already carried — or say in the contract that they are decided before the reads and that only authority is re-asked. |

| 8 | MED | `src/TradeAgent.Gateway/TradingGateway.cs:3301-3316`, `:2368-2369`, `:2377-2378` | **The two agreeing pulls a closure needs are keyed by the UTC DAY, so a pair that straddles midnight never agrees and the day that went through the budget writes nothing.** `Confirmed(key, …)` stores the first sighting under `LossBreach.DayKey(accountId, at)`; at 00:00:10Z the key is the NEW day's, there is no sighting under it, and the pull that agreed with the first one is filed as a first sighting of its own. The day that lost the money then has no `loss_breach` row: nothing is flattened, no `BoundaryKind.LossBudget` post-mortem opens, no strike is counted, and the only thing still refusing is the admission gate's live figure — which stops refusing the moment the figure recovers. At `LossWatchInterval` 15 s this is the last tick of every UTC day. | `P4_a_breach_first_seen_in_the_last_tick_of_a_utc_day_is_never_confirmed` — both pulls report `reached=True`, `closed=[]`, no breach row, `day closed: False`; the identical run an hour earlier closes on pull 2 | Key the sighting on the SCOPE (`account`, `symbol`) rather than on the day's key, and take the day from the CONFIRMING pull's instant when the record is composed — or carry the first sighting's own day into `Confirmed`'s lookup. |

**Counts: HIGH 5 · MED 3 · LOW 0 · UNVERIFIED 7.**

## The probe output, quoted

**P1** — the same losing day, twice, against the same simulator relabelled at the seam so the gateway sees
a platform whose instrument is called `ES:H6`. Everything is the gateway's own: the record is written by
`LossWatchAsync` on two agreeing pulls, and the buy afterwards goes through `PlaceAsync`.

```
[ES:H6]
  breach rows on disk   : loss_breach:SIM-001:ES:H6:2026-03-10
  the watch closed      : loss_breach:SIM-001:ES:H6:2026-03-10
  SymbolClosed says     : NOTHING IS CLOSED
  SymbolsClosedToday    : []
  a new buy on it       : SENT
  orders that reached the wire after the closure: 1

[ES]
  breach rows on disk   : loss_breach:SIM-001:ES:2026-03-10
  the watch closed      : loss_breach:SIM-001:ES:2026-03-10
  SymbolClosed says     : closed
  SymbolsClosedToday    : [ES]
  a new buy on it       : LOSS_BUDGET_REACHED
  orders that reached the wire after the closure: 0
```

**P1b** — the narrowest statement of the same thing, with no gateway in it: the product's own key builder
against the product's own reader.

```
loss_breach:SIM-1:ES:2026-03-10                -> day=2026-03-10 symbol=ES
loss_breach:SIM-1:ES:H6:2026-03-10             -> NOT A KEY THIS READER KNOWS
loss_breach:SIM-1:BINANCE:BTCUSDT:2026-03-10   -> NOT A KEY THIS READER KNOWS
loss_breach:SIM-1:2026-03-10                   -> day=2026-03-10 symbol=(account)
loss_breach:RITHMIC:SIM-1:2026-03-10           -> NOT A KEY THIS READER KNOWS
```

**P1c** — eight watch ticks with the price on the floor and the agent asking again on each one.

```
[ES:H6] breach rows=1 flatten rows=1 hold rows=0 closes at the wire=1 places at the wire=8
[ES]    breach rows=1 flatten rows=1 hold rows=0 closes at the wire=1 places at the wire=1
```

**P3** — two accounts on one platform in one database. `ACC-B` has never traded.

```
account A closed        : loss_breach:ACC-A:2026-03-10
account A realised      : -1000.00
account B has traded    : 0 fills at the platform
account B's own book    : []
what TradeAgent says B lost today: -1000.00
breach rows now         : loss_breach:ACC-A:2026-03-10 loss_breach:ACC-B:2026-03-10
B's day closed          : True
B's closure sentence    : TradeAgent closed today to new risk at 12:01 UTC: the day was down 1000 USD
                          against the 1000 USD you set as "Most it may lose in one day" …
```

**P3b** — the same defect in the direction that costs money. `ACC-B` holds a long that is 1250 USD down
against its own 1000 USD daily budget, once with a winning account's fills beside it in the database and
once on a clean one. (The `realised` figure in the second arm is `-1250` because the closure's own flatten
had already run by the time it was read.)

```
[a winning account beside it: True]
  what TradeAgent measures   : realised=1500.00
  B's day reached its budget : False
  B's day closed             : False

[a winning account beside it: False]
  what TradeAgent measures   : realised=-1250.00
  B's day reached its budget : True
  B's day closed             : True
```

**C2** — an ordinary paper day, closed and flattened on the simulator; then the owner switches to a
platform with a different connector id, a NON-simulated account carrying the same id, a position they put
on by hand and a resting bid. The second installation has both budgets at zero.

```
paper breach            : loss_breach:SIM-001:2026-03-10
paper flatten           : flat=True
paper account simulated : True
live book before        : ES 4  working=1
live cancels sent       : 1
live closes sent        : 1
live places sent        : 0
live book after         :   working=0
live flatten record     : flat=True
the breach row's keys   : loss_breach:SIM-001:2026-03-10 loss_clock_high_water:atas:SIM-001
                          loss_clock_high_water:fake:SIM-001 loss_flatten:atas:SIM-001:2026-03-10
                          loss_flatten:fake:SIM-001:2026-03-10
```

**C3a** — a promoted version really standing in this database, allocated five, an order for five parked in
`LIVE_CONFIRM`, then the owner withdraws the capital by recording a fresh allocation of zero.

```
parked                  : APPROVAL_REQUIRED
ceiling now standing    : 0
a FRESH order now       : ALLOCATION_EXCEEDED
the PARKED order, approved: SENT
orders that reached the wire: 1
the record's allocation_id: 9500f0aa8795c5c2228a38a79233c8a88fb4f1ae6a809d4a7e146140fcec0943
```

**C3b** — a long 4, an AI reduce of 2 parked, then the owner's own sell 1 whose acknowledgement is lost
after the broker accepted it, then the owner confirming on the card that they cannot say what happened to
it (which clears the flag and leaves the order resting at the broker).

```
parked                    : APPROVAL_REQUIRED
its record                : UNKNOWN
after the owner's card    : state=UNKNOWN flagged=False unconfirmed_work=False
a FRESH reduce now        : CLOSE_UNRESOLVED
the PARKED reduce approved: SENT
orders that reached the wire: 1
position at the broker    : ES 1
```

`ES 1` is the finding: a long 4 reduced by a lost 1 and an approved 2, both sized against the same
reading of 4.

**C1** — one connection, authenticated with the chair's own grant, a buy before and a buy after the turn
ends. Three ways of ending it.

```
[expired]
  buy before, on the live connection : SENT
  buy after,  on the live connection : SENT
  orders that reached the wire after : 1
  a NEW connection with the same grant: IPC_UNAUTHENTICATED

[revoked]
  buy before, on the live connection : SENT
  buy after,  on the live connection : SENT
  orders that reached the wire after : 1
  a NEW connection with the same grant: IPC_UNAUTHENTICATED

[turn ended (Dispose)]
  buy before, on the live connection : SENT
  buy after,  on the live connection : SENT
  orders that reached the wire after : 1
  a NEW connection with the same grant: IPC_UNAUTHENTICATED
```

**C4** — one forward step of the injected clock, no elapsed time, one tick.

```
closed at               : 2026-03-10 12:00:40Z
eligible at             : 2026-03-11 12:00:40Z
day closed now          : True
after ONE forward jump  : reopened=[loss_reopen:fake:SIM-001:2026-03-10]
day closed now          : False
clock suspect row       : none
a new buy after the jump: SENT
```

**P2** — the order held inside the dispatch gate's position read while the owner narrows the limits.

```
owner narrowed the limits while the order was inside the gate
the order in flight       : SENT
orders at the broker      : ES Buy 5
the same order, fresh     : RISK_LIMIT_EXCEEDED
```

**P4** — a loser REALISED before midnight, the first pull at 23:59:50Z and the confirming pull at
00:00:10Z; then the same run an hour earlier as the control.

```
[first sighting at 23:59:50Z]
  pull 1: reached=True closed=[]
  pull 2: reached=True closed=[]
  breach rows : []
  day closed  : False
  a new buy   : LOSS_BUDGET_REACHED

[first sighting at 22:59:50Z]
  pull 1: reached=True closed=[]
  pull 2: reached=True closed=[loss_breach:SIM-001:2026-03-10]
  breach rows : [loss_breach:SIM-001:2026-03-10]
  day closed  : True
  a new buy   : TRADING_PAUSED_UNRECONCILED
```

Both pulls agreed that the day was through its budget and no record was written. `LOSS_BUDGET_REACHED`
on the last line is the ADMISSION gate refusing off the live figure, which is what it has always done
and is not a closure: nothing was flattened, no boundary opened, no strike was counted, and that
refusal ends with the figure.

**P0** — every number the `2026-09-06`..`2026-09-16` `BUILD-STATUS.md` sections quote that depends on a
constant, re-derived from the shipped objects rather than read out of the prose. All of them hold.

```
LossWatchInterval                 : 15s   (records say 15 s)
LossBreachConfirmWithin           : 60s   (records say 60 s)
MaxQuoteAge                       : 30s   (records say 30 s)
LossMinClosure (fallback)         : 24h   (records say 24 h)
LossStrikeWindow (fallback)       : 7 dates   (records say 7)
ValuationLossExitAfter (fallback) : 15 min   (records say 15)
RiskPolicy.LossMinClosureHours    : 24
RiskPolicy.LossStrikeWindowDays   : 7
RiskPolicy.ValuationLossExitMin   : 15
ApprovalTtl                       : 15 min
AbsenceGrace                      : 15s
DispatchSettleSlack               : 20s
AtasConnector.WorstCaseOperationPath: 50s
=> the stranded bound             : 70s   (records say 70 s)
MaxClientOrderIdChars             : 64
MaxRequestIdChars                 : 61
ValuationLoss.BoundOf(15)         : 15 min
ValuationLoss.BoundOf(0) is OFF   : True
AgentGrants.MaxTurn / Grace       : 6h / 60s
EligibleAt(23:58Z, 24h)           : 2026-03-11 23:58:00Z
EligibleAt(00:02Z, 0h)            : 2026-03-11 00:00:00Z
press budget (5+5xlegs)x2.234 s   : 1 leg=23s  2 legs=34s  3 legs=45s
```

## Guards checked in both directions, and holding

Stated because the brief asks for both directions, and because a guard that was probed and held is worth
as much to the next reader as one that failed. None of these is a finding.

- **The loss-budget closure on an ordinary symbol refuses at the gate and sends nothing** (P1, the `ES`
  arm): `LOSS_BUDGET_REACHED`, 0 orders at the wire, on a day whose figure has completely recovered.
- **A NEW connection presenting an expired, revoked or ended grant is refused** (C1, every arm):
  `IPC_UNAUTHENTICATED`. Finding 5 is confined to a socket that is already open.
- **The allocation ceiling and the unresolved-reducer refusal both bite on the PLACE path** (C3a, C3b, the
  "fresh" lines): `ALLOCATION_EXCEEDED` and `CLOSE_UNRESOLVED`. Finding 4 is confined to the approval path.
- **The per-order limits do refuse a fresh order** (P2's last line), so finding 7 is a window and not an
  absent gate.
- **The loss budget closes a day correctly when the database holds one account's fills only** (P3b, the
  second arm), so finding 2 is the mixing and not the arithmetic.
- **No operator authority is reachable from the agent-facing pipe or from `trade`.** Executed as a grep
  over the shipped source at this sha: `ReleaseHold`, `Allocate`, `SetMode`, `ActivateLive`,
  `StopAiTrading`, `ApproveAsync`, `Decline`, `ForceResolve`, `Update(`, `FlattenForBreachAsync` and
  `SetHoldout` each occur **0** times in `GatewayPipeServer.cs` and **0** times in `TradeCli/Program.cs`.
- **Only `CouncilBoundaries.ApplyDue` writes a disposition** (`BoundaryStore.cs:441-458`), and it writes the
  row's own `DefaultDisposition` as `BoundaryAuthor.Policy`. There is no method that takes a disposition
  from a director, so a director's prose cannot become permission.
- **REVIEW 2026-09-05b's own six findings hold where they were crossed.** Finding 1's lease is on
  `ForceResolve` (`TradingGateway.cs:8127-8153`, `StillOnTheWire` refusing before any state is written);
  finding 4's `.part` file is bound to the URL and the expected length with an explicit `Integrity` type
  (`Downloader.cs:114-195`); finding 6's `ByShaPrefix` refuses non-hex, refuses under four characters and
  refuses an ambiguous prefix (`MaterialStore.cs`); finding 5's inbox origin is now claimed only for a
  window in which no agent process was alive (`MaterialScanner.cs:20-21`). Findings 2 and 3 were read, not
  run — see UNVERIFIED 5.

## UNVERIFIED — read, not run, ranked by how much I would want it settled

1. **An exit that throws between its close and its record gets a SECOND attempt.**
   `ExitLostValuationAsync` (`TradingGateway.cs:7053-7225`) writes `loss_valuation_exit:` only at the very
   end, and `AnswerLostValuationsAsync` (`:6966-6980`) catches everything and logs `valuation_answer_failed`.
   An exception thrown after the close went out and before the record lands leaves `episode.ExitKey` null,
   and the next tick's `episode.ExitKey is null` test sends another close over a book the first one may
   already have flattened. `docs/CONTRACTS.md` says "the record is written whatever the outcome"; the
   write is not in a `finally`. I could not arrange a throw from inside `CloseCapturedAsync` in the time.
2. **The same colon defect reached through the ACCOUNT id rather than the symbol.** `P1b` shows
   `loss_breach:RITHMIC:SIM-1:2026-03-10` does not round-trip, which makes a whole DAY closure invisible —
   strictly worse than finding 1's per-symbol case. Nothing in this build mints such an account id, and no
   connector here produces one, so it is read and not run.
3. **Whether any platform TradeAgent will actually attach to names an instrument or an account with a
   colon in it.** ATAS's own instrument names carry spaces and brackets (`ES 12-25 [CME Globex Futures]`,
   REVIEW 2026-09-05 finding 4) and no colon was observed; the venue-qualified form is the convention at
   crypto venues, which `docs/COUNCIL.md`:164-172 names as the second data source. Finding 1's DEFECT is
   executed; its reachability on the box is not.
4. **`record.State` and `record.Mode` are two separate reads of `Settings.Mode`** in one object
   initializer (`TradingGateway.cs:3798-3806`), both outside the dispatch gate. A mode moving
   `LIVE_AUTONOMOUS → LIVE_CONFIRM` between them builds a `CREATED` record stamped `LIVE_CONFIRM`, which
   `ReauthorizeAtDispatchOrThrow` then passes — an agent order dispatched in confirm mode with nobody
   approving it. The window is a few instructions and I could not make it deterministic.
5. **REVIEW 2026-09-05b findings 2 and 3 (the press's in-flight guard, and the two-press grant on the mode
   row).** Both are described as implemented in `docs/CONTRACTS.md` and both have named tests
   (`TwoPressGrantTests`, the press classes); neither was re-run here, and the brief asks the reviewer not
   to re-report them.
6. **Everything the bridge half does.** `AtasStrategyAdapter`, `CoidWitness` and `AdapterTeardown` were not
   opened at all in this review. `SupportsOrderHistory` (`AtasStrategyAdapter.cs:664`) is the second
   review's UNVERIFIED 1 and the first review's Codex F8, and it is unchanged at this sha.
7. **The valuation-loss exit against a real connection that drops mid-run.**
   `AnswerLostValuationsAsync` reads `_health.Get(Components.TradingConnection)`, which is refreshed by the
   health pass rather than by the connector at that instant, so a connection that dropped since the last
   pass is told READY. The failure direction is a timeout and an `UNKNOWN` record, which is rule 3's safe
   side — but it is the one place `CLAUDE.md`'s "with the connection down nothing is sent at all" rests on
   a cached row.

## What I did NOT do

- **No Windows box, no real ATAS, no real money.** Everything above is macOS against `FakeConnector`,
  `RecordingConnector` and two decorators written for these probes (a platform-relabelling connector and a
  connector-id-substituting one). Nothing here says what ATAS does.
- **No UI run and no screen.** Findings 4 and 7 name controls the owner presses; what is RUN is the
  gateway method behind them.
- **No mutation testing**, and no attempt to re-verify the mutants `BUILD-STATUS.md` quotes for the units
  landed since 2026-09-06.
- **Did not open the bridge half at all** — `AtasStrategyAdapter`, `CoidWitness`, `AdapterTeardown`,
  `BridgePipeAuth`, `AtasHealth`, the protocol-3 handshake.
- **Did not probe** the updater, `Doctor`, `ChatView`, `InboxView`, `OnboardingView`, `AgentSession`, the
  council relay's task delivery, the venue catalogue's override loader, the second candle source, the
  backtest or the referee. The council boundary's disposition writer was read, not run.
- **Did not re-report** the first two reviews' findings; the four I crossed are in "Guards checked".
- **Fixed nothing**, changed no product file, pushed nothing, merged nothing, and entered no other
  worktree (`U-allocator-2` and `review-codex` are other agents').

## Codex — `gpt-5.6-sol`, read-only, the same sha, in its own worktree (final answer only; it could not build in its sandbox — `MSB1025` — so it executed nothing)

| severity | file:line | the check that settles it | what would fix it |
|---|---|---|---|
| — | — | No finding met the executed-proof bar. HEAD was `c4411206fd6a4f35cc9bdc8702c3d8c54c0e24dd`, matching `c441120`. `dotnet build TradeAgent.sln -c Release --no-incremental` exited 82 before compilation: `MSB1025` / `UnauthorizedAccessException` creating MSBuild’s temporary directory; tests therefore did not run. | Re-run the audit in a sandbox permitting MSBuild temporary and `obj` output; no product fix is claimed here. |

UNVERIFIED, ranked:

1. **HIGH — expired/revoked Operations authority appears to survive indefinitely on an authenticated pipe.** `GatewayPipeServer.cs:658` verifies the grant only during hello; `:702-703` subsequently passes the cached `grant.Role` to every request. `AgentGrants.cs:119-163` removes or expires grants, but the live handler never re-verifies them. Settling test: authenticate, expire/revoke the grant, then issue `buy` on the same connection and assert zero connector calls. Fix: re-verify the presented grant before every mutating operation, or terminate connections when their grant ends.

2. **HIGH — a PAPER loss breach appears able to close and flatten LIVE money sharing the same account ID.** `LossBreach.cs:53-63,179-208` stores neither connector in the key nor record; `TradingGateway.cs:1947-1960` reads those rows under the current connector, while `:6298-6311` checks only the account and then creates current-connector flatten keys. `AppHost.cs:643-666` reuses the database across connector switches. Settling test: create a PAPER breach for account `X`, switch to a recording LIVE connector also exposing `X`, run the loss sweep, and inspect LIVE cancel/close calls. Fix: bind breach keys and records to connector/mode and reject legacy/unbound rows from another money domain.

3. **HIGH — approval appears to bypass two dispatch gates that initial placement runs.** `TradingGateway.cs:3828-3850` runs unresolved-reducer and allocation-ceiling checks; `ApproveAsync` at `:4785-4790` re-runs only open-position and loss-budget checks before dispatch at `:4802`. No approval coverage appears in `AllocationGateTests`. Settling tests: park a strategy order, withdraw/expire its allocation, then approve; separately introduce an unresolved same-side reducer while parked and approve. Both must produce zero wire calls. Fix: run all four position-dependent gates on the single approval-time position read and refresh the stored allocation attribution.

4. **HIGH — the persisted clock high-water mark appears unable to stop an immediate forward-clock reopen.** `TradingGateway.cs:2605-2662` treats only `at < mark.At` as suspect; a first jump beyond `EligibleAt` raises the mark and `HeldBy` at `:2504-2517` accepts the elapsed-time condition. The existing test at `LossReopenTests.cs:403-408` explicitly treats a forward step as ordinary. Settling test: close and flatten at T, jump the injected clock past eligibility without elapsed monotonic time, run one tick, and assert no receipt. Fix: establish elapsed time from a trustworthy monotonic/external source or hold for owner review after an unprovable forward discontinuity.

What I did NOT do: modify any file, run tests after the build bootstrap failure, exercise ATAS or real LIVE money, or re-report the fixed 2026-09-05b findings.
The manager handed these four hypotheses to the Opus reviewer after its own hunt; all four were executed and VERIFIED — C1 is finding 5, C2 finding 3, C3 finding 4 (both arms), C4 finding 6.
