> **SUPERSEDED 2026-09-04 — read `HANDOFF-2026-09-04.md` first.** This document is kept for its §1 (what the program is), §5 (the decisions as resolved) and §6–§7 (process facts and the docs backlog).

# Hardening program — handoff for the next top-level manager (written 2026-09-03 ~17:00)

**Read this first.** It replaces the session scratchpad, which was WIPED when the Claude Code process restarted at
15:02 on 2026-09-03 (every build/verify record, mutation table, machine log and screenshot from the session was in
`/private/tmp/claude-501/…/scratchpad`, which is not durable). What survives is in git (every unit branch and its
commits), on the Windows box, in the two GitHub releases, in the manager's memory files, and in this document —
reconstructed from the session transcript. Where a fact below was measured, the measurement is quoted; where the primary
log is gone, it says so. **Never again write a record only to the scratchpad: records live in `docs/hardening/records/`
inside the repo worktree and are committed as they are written.**

## 1. What this program is

Started 2026-09-02 on Nicolas's instruction: run TradeAgent to a "trustable first deployment and monitoring phase",
not rushed, with the battle-tested process of the sibling repos (innovision-os `docs/ORCHESTRATION-STANDARD.md`,
venture-agent's unattended doctrine). See `PROGRAM.md` (definition of done D1–D8, units, tiers) and
`sibling-process-survey.md` (the twelve ported patterns). The process in one line: every unit is briefed with hard file
ownership → built red-first on its own branch → verified by an independent adversarial Claude leg AND a Codex
cross-model read-only review, each in its OWN detached worktree → triaged on the record (findings are inputs, never
auto-applied) → bounced until no HIGH/MED → integrated onto `main` with the full suite + App build → pushed.

## 2. State of `main` and the box

- `main` = `origin/main` = **`3f1d8f2`**: 283d942 (2026-09-01 handoff) + version bump 0.1.1 + hermetic ATAS health test
  (`IAtasProbe` seam) + **U2b integrated** (approval re-authorization, 8 commits). Full suite 329 green on the Mac; CI
  green on ubuntu/macos/windows + package (run 33716906666).
- **v0.1.1 is published** (tag → 3931c10) and **the update path is proven on the box (D2)**: the installed 0.1.0
  offered 0.1.1, two-press, Setup replaced the running app, relaunch, bridge reconnected in ~6 s, database intact.
  Record: `records/U3-update-proof.md`.
- **Windows eyes done (D3)**: setup journey walked end to end, resume works, the reconciliation card reached through a
  genuinely ambiguous order and resolved, the bridge-refusal sentence rendered; 16 UX defects listed for U6.
  Record: `records/U4-windows-eyes.md`.
- **Box** (`windows-32gb-2000gb`, 100.125.31.46): left running the INSTALLED 0.1.1 (`%LOCALAPPDATA%\Programs\TradeAgent`)
  on the real home, mode LIVE_CONFIRM, live NOT activated, ATAS pid 24104 signed in, bridge `[Started]` on the BTCUSDT
  chart (simulated account CRYPTO5EB41; both ATAS accounts simulated; NO broker attached), `open_requests 1` (a legacy
  stranded cancel, expected), UI agent running. Start every box session with `tools/win-state.sh`. Nicolas powers the
  box on; it was rebooted 2026-09-02 17:40.

## 3. State of every unit (branches are the truth; records are reconstructed)

| Unit | Branch @ tip (base) | Rounds / last verdicts | What is open |
|---|---|---|---|
| U2a pipe hardening + operator hole + ids + emergency fast path | `u2a-pipe-hardening` @ **5c716aa** (283d942, 21 commits) | r1 built → verifier FAIL 1H/4M + Codex 3H/1M → r2 → verifier FAIL 0H/4M → r3 → verifier FAIL 0H/2M → **r4 built (360 green), verification NOT run** (leg killed while writing probes) | Verify r4 (targeted); then integrate FIRST (rebase onto main). Known gap → U2c-2: the agent's `close-all` legs are offsetting `Place`s, no fast path. NOT VERIFIED on the box: ATAS accepts the `op-…` id shape and ≤64-char client order ids (W10/W11). |
| U2b approval re-authorization + TTL | **INTEGRATED** into main (8 commits, tip 3f1d8f2) | r1 FAIL (2M) → r2 PASS WITH LOW → LOW doc batch | Done. The 5-s sweep of expired parked requests (AppHost) was declined for ownership → U2c-2. |
| U2c-1 dispatch recovery | `u2c1-dispatch-recovery` @ **1e10660** (283d942, 28 commits; based on the OLD U2b tip cb2ce2f, which no longer exists on main) | r1 → Codex 7H/1M → r2 → verifier FAIL 3H/3M/2L + Codex (r2) → r3 built (419 green, 21 mutants) → **Codex r3: 13 HIGH / 1 MED** (see `records/U2c1.md` §round 3); verifier r3 killed mid-batch | **Round 4 not yet briefed**: the manager's decision is written in `records/U2c1.md` — simplify the emergency controls (one-shot + pause + human; per-order cancels, no account-wide sweep; re-read the position before a close; outcome by the record's account) and derive the conservative rule correctly in reconciliation (connector identity, non-definite never clear, UNKNOWN never resolved, held-still is inconclusive, latch on the definite settle path, modify checks the returned order's identity, tick rule = round-down/round-up of the request and ≠ old price). Integration needs `git rebase --onto main cb2ce2f u2c1-dispatch-recovery`. |
| U2d updater fail-closed | `u2d-updater-fail-closed` @ **c519966** (3931c10, 12 commits) | r1 → verifier FAIL 0H/3M + Codex 2H/4M → r2 → verifier FAIL 0H/2M → r3 built (373 green) → **verifier r3 FAIL 0H/2M/4L; round 4 NOT started** (leg killed at start) | Round 4 list is in `records/U2d.md`; item 10 (the unconfirmed-work provider must count every wire-touched record via U2c-1's store query) waits for U2c-1's merge and a rebase. |
| U14 coid-witness durability | `u14-coid-witness-rewrite` @ **a8b3fb0** (3f1d8f2, 25 commits) | r1 → verifier FAIL 1H/4M + Codex 2H/3M → r2 → verifier PASS WITH LOW + Codex 4H → r3 → verifier **PASS** (3 real processes, 0 lost) + Codex 5H/2M/1L → **r4 items 1–5 committed** (one owner per witness, protocol v3, sidecar never drops a safety event, readers never write, probe RESOLVED); item 6 (LOW batch: superset by membership, `Identified` asymmetry test, lock tests) NOT done | Finish item 6, verify r4 (targeted), integrate. **Protocol is now 3**: the box's deployed bridge DLL must be rebuilt/redeployed with the next release or the app refuses it (by design). The adapter hunks compile ONLY on the box. |
| U13 Job Object around the CLI | `u13-job-object` @ 3931c10, **0 commits** | scaffold lost with the scratchpad | Re-brief from scratch (design: `JOB_OBJECT_LIMIT_KILL_ON_JOB_CLOSE`, no breakaway; verify on the box). |
| U12 agent containment design | no branch; design doc LOST (349 lines) — summary in `records/U12-containment-design.md` | designer → Codex design challenge: **UNSOUND on 5/6 sections, §6 SOUND WITH CHANGES** | Needs a revised design (Job Object universal first; spike AppContainer alongside Low IL; fix known pipe/file defects first; decisive test = a non-physical approval-state transition must be impossible) before it reaches Nicolas as decision 6. |
| U6 UX + small items | not started | — | The 16 defects in `records/U4-windows-eyes.md` + support-zip redaction by content + `ErrorCatalogueTests` weakness + NOT_APPLICABLE health state. Split U6a (product defects) / U6b (wording). |
| U2c-2 authority robustness | not started | — | Settings parse failure fails closed; G9 reads state; kill switch in-memory first; health loop never throws under DB lock; `SetMode` clears LiveActivated + widening two-press; final authorization at commit-to-wire for Place AND Approve; the 5-s expired-approval sweep; intent through `ITradingConnector` for agent close legs. |
| U2e / U2g / U10 | not started | — | Bridge health = command-loop responsiveness; connector-side peer image check (`OFT.Platform.exe` under Program Files); winagent hidden console. All need the box. |

## 4. Integration plan (do it in this order, one at a time, full suite + `dotnet build TradeAgent.sln` + push each)

1. U2a: verify r4 → `git rebase main` on the branch → ff-merge → gate → push.
2. U14: finish item 6 → verify r4 → rebase → merge → gate → push. (Conflicts expected with U2a? No — disjoint files.)
3. U2c-1: brief round 4 (`records/U2c1.md`) → verify → `git rebase --onto main cb2ce2f u2c1-dispatch-recovery` →
   merge → gate → push. Expect conflicts in `TradingGateway.cs`/`GatewayTypes.cs`/`AppHost.cs`/`Errors.cs` with U2d later.
4. U2d: round 4 + item 10 (after U2c-1) → verify → rebase → merge → gate → push.
5. **v0.1.2** cut BY HAND on the box (`packaging\build.ps1 -RequireInstaller -AtasInstallDir …`, detached via
   `Invoke-CimMethod Win32_Process Create` on a `.cmd` — `Start-Process` dies with the SSH session), publish with
   `gh release create v0.1.2 --target <full sha>` (short shas are rejected), watch it install over the running 0.1.1,
   redeploy the bridge DLL (protocol 3), then run on the box what only the box can prove: the Windows named-pipe
   backpressure tests, ATAS's acceptance of `op-…` ids and the 64-char limit, the adapter hunks from U14, the
   `CoidWitness` churn test.
6. Then the docs batch (§7) and the remaining units.

## 5. Decisions — answered 2026-09-03 evening ("no broker, no account, no SSH to Mihael's machine: do all that is possible with what we have")

1. **Broker/account:** none. The staged live trial stays outside this phase; the box's two SIMULATED ATAS accounts are
   the only trading venue. If a real exchange becomes a requirement later, Nicolas can connect his Binance account for
   testing (the intended use is futures, not crypto) — that is a future test option, not a target.
2. **Code signing:** no certificate → **explicitly deferred**; the SmartScreen consequence goes into the user guide and
   DEPLOYMENT.md (D7 is met by the documented deferral). U9's signing hook in `build.ps1` stays a stub until a cert exists.
3. **Target machine:** the test box (`windows-32gb-2000gb`) is the deployment target for this phase; Mihael's laptop gets
   the DEPLOYMENT.md walk-through, executed later by Nicolas at the keyboard.
4. **Monitoring channel:** A — Tailscale + SSH `trade status` / support zip — on the box, because it is what exists. The
   MONITORING-PHASE.md protocol is written for A; the outbound-alert variant (B) is documented as the fallback for a
   machine without Tailscale and is not built (no destination channel exists to build against).
5. **Five minutes at the keyboard** (Inbox picker COPY-path test): still needed; scheduled into the v0.1.2 box session —
   the manager pings when it is next.
6. **Containment direction:** not yet asked; U12's revised design must survive a Codex challenge first.

## 6. Process facts that cost real time this session (carry them)

- **Rate limits killed every running leg four times** (18:40, ~03:00, ~08:00, ~16:00 Brussels). Six legs, then four,
  then three. The usage window is ~5 h; heavy Opus legs (verifiers with mutant sweeps) burn 200–500K tokens each.
  Practical cap: **2 concurrent heavy legs**; expect one reset per ~4 h of continuous work; every leg checkpoints to
  the repo as it goes; a killed leg can be resumed by `SendMessage` ONLY within the same Claude Code process — after a
  process restart the transcripts are gone and it must be re-briefed from the records.
- **Explore agents cannot write files** — use general-purpose for anything that must checkpoint.
- **One worktree per leg, Codex included**; Codex runs directly in a background Bash (`codex exec -m gpt-5.6-sol
  -s read-only -C <worktree> --skip-git-repo-check --color never -o <last-message.txt> "$(cat prompt)" < /dev/null`),
  10–30 min per review, exit 0 even on failure — read the captured output, grep `^FINDING`, never tail the raw log.
- Builders destroyed their own uncommitted work with `git checkout --` during mutation runs three times; commit before
  mutating and restore from a copy. Restoring a file with an older mtime skips the rebuild (use `touch`/`os.utime`).
- Mutation testing found holes in the TESTS (vacuous tests) in four separate units; it is the single most valuable step.
- Bash on this Mac: no `timeout`; a literal `%` in a top-level command aborts the tool shell; each call ≤ 10 min.
- No `Co-Authored-By` trailers in commits (Nicolas's rule; the harness reminder is overridden).

## 7. Docs backlog (D8) — none of this is done

- `BUILD-STATUS.md`: a 2026-09-02/03 section exists now (reconstructed, marked as such) for U3, U4 and U2b; the
  U2a/U2c-1/U2d/U14 sections are written at their integration from `records/`.
- `docs/USER-GUIDE.md`: says trading through ATAS does not work (false since 2026-08-31); lists 12 setup screens (app
  says 16); "Installing the ATAS add-on" vs the app's "bridge"; update paragraph needs the failure branch and the
  hard-stop sentence (from U2d). `docs/RESUME-HERE.md`: "Do this first" now points here; the rest is the 2026-09-01
  text and must be refreshed after v0.1.2.
- `docs/DEPLOYMENT.md` and `docs/MONITORING-PHASE.md` (D6) do not exist yet.

## 8. Where things are

`docs/hardening/PROGRAM.md` · `docs/hardening/records/*` (reconstructed unit records, the four branch commit
histories, the 283d942 review consolidation, the safety map, the process survey) · memory files
`~/.claude/projects/-Users-nicolasbeeckman-Projects/memory/{tradeagent_mihael_stack,subagent_concurrency_cap,
delegated_leg_checkpointing,delegated_leg_serialization}.md`.
