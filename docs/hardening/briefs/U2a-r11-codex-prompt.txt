READ-ONLY AUDIT — do not modify, create, or delete any file; output findings only. This is a DELTA re-review of unit U2a (TradeAgent: agent-facing named-pipe hardening, connector send deadlines, replay contract, risk-reducing fast path) in a real-money trading orchestrator. Your prior delta review of sha c00fa08 (round 10) reported the items listed below. The builder applied fixes in the committed range c00fa08..HEAD (round 11; run: git log --oneline c00fa08..HEAD; git diff c00fa08..HEAD). Manager decisions in force, do not re-litigate but DO check they hold: (1) the transport state is recorded the moment a frame is fully written (PossiblyWritten), before any reply wait, and a NULL transport result never maps to not-sent — null is sent-not-confirmed (fail toward reconciliation); (2) cancellation BEFORE send-gate acquisition is NothingWritten → not-sent; (3) every classification arm consults the transport result first — DISPATCHING/RECONCILING with NothingWritten → not-sent, with PossiblyWritten → sent-not-confirmed, a definite reply → its word; (4) the drain table enumerates every handler the dispatcher knows, and the coverage test derives the handler set from the dispatcher; (5) DEFERRED-BY-DECISION to U2c-1 class C4 (do not re-report as not fixed — report as DEFERRED-BY-DECISION): your PRIOR 2 (disposal returns with DISPATCHING unsettled — TradingGateway.cs:696-700 must settle a cancelled handler; U2a's half, non-silent disposal, is done) and PRIOR V-F1 (the never-sent leg still ends UNKNOWN + flagged — TradingGateway.cs:660-665 maps every ConnectorTransportException to SettleUnknown; U2a exposes NothingWritten so the gateway can settle not-sent); also prior findings 5, 6, 8-gateway, F-A, and round-9 F2 (the close's offsetting Place). For each prior item emit one line of the exact form `PRIOR <id> — FIXED | NOT FIXED | PARTIAL | DEFERRED-BY-DECISION — <one-line reason with file:line>`, then hunt regressions in the changed files only; do not re-litigate the full surface. Also rule on: (a) is there any path on which a frame reaches the wire without PossiblyWritten being recorded first, or on which the transport result can be null at classification time; (b) can any arm still classify from record state alone; (c) does the dispatcher-derived handler set cover every operation the pipe accepts, including replays and reads; (d) if the builder changed the round-7 test An_emergency_a_busy_bridge_has_not_answered_yet_is_unknown_but_not_a_drop after the Windows flake, does the changed test still prove the busy-vs-stalled distinction or merely pass. New findings as blocks starting `FINDING <n> — <HIGH|MED|LOW> — <file:line> — <one-line risk>` with the exact check; finish with `SUMMARY — <n> HIGH / <n> MED / <n> LOW` counting only NEW or NOT-FIXED items. Default skeptical: unconfirmed = UNVERIFIED. Reading is your job; no dotnet runs over 10 minutes.

Prior items (records/codex-U2a-r10.txt, your delta on c00fa08):
PRIOR 2 — DEFERRED-BY-DECISION — disposal still logs and returns with DISPATCHING requests unsettled (GatewayPipeServer.cs:1360); the settlement is the gateway's (U2c-1 C4).
PRIOR R9-F4 — PARTIAL — unresolved states consulted transport, but definite-state arms bypassed it and DISPATCHING/RECONCILING remained eligible for sent-not-confirmed (GatewayPipeServer.cs:906).
PRIOR V-F1 — DEFERRED-BY-DECISION — NothingWritten produced not-sent, but the same leg remained UNKNOWN and reconciliation-flagged through the gateway (SweepRequestIdTests.cs:527); U2c-1 C4.
FINDING 1 — MED — AtasConnector.cs:739 — cancellation while waiting for the send gate bypassed the NothingWritten arm and the outer catch recorded PossiblyWritten.
FINDING 2 — MED — AtasConnector.cs:827 — a completed mutation write recorded no transport state until a reply, so caller cancellation during the reply wait left null and the mapper could classify a fully sent leg as not-sent.
FINDING 3 — LOW — GatewayPipeServer.cs:211 — the drain table omitted four handled operations.
