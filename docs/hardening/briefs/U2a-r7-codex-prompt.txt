READ-ONLY AUDIT — do not modify, create, or delete any file; output findings only. This is a DELTA re-review of unit U2a (TradeAgent: agent-facing named-pipe hardening, connector send deadlines, replay contract, risk-reducing fast path) in a real-money trading orchestrator. Your prior delta review of sha ffa1a3d (rounds 5+6) reported the items listed below; an independent adversarial verifier added F-E and F-F. The builder applied fixes in the committed range ffa1a3d..HEAD (round 7; run: git log --oneline ffa1a3d..HEAD; git diff ffa1a3d..HEAD). Manager decisions in force, do not re-litigate but DO check they hold: (1) EmergencyDeadline (2 s) bounds only the caller's wait, on ONE clock across gate + write + reply; (2) liveness uses the ordinary RPC deadline (10 s) as its grace — a peer that answers nothing within it is dropped, a late answer within it is kept and recorded on the pending RPC, and the stated cost is that a wedged heartbeating peer is detected at ≈10 s instead of ≈2 s; (3) the 1 KiB-chunk progress granularity is an accepted residual to be stated in docs/CONTRACTS.md; (4) prior findings 5, 6, 8-gateway and F-A are DEFERRED-BY-DECISION to U2c-1 (TradingGateway.cs, DashboardView.cs, Stores.cs, GatewayTypes.cs must be untouched). For each prior item emit one line of the exact form `PRIOR <id> — FIXED | NOT FIXED | PARTIAL — <one-line reason with file:line>`, then hunt regressions in the changed files only; do not re-litigate the full surface. Also rule on: (a) is there now exactly one clock for the emergency caller, or can any path (prerequisite read, gate, chunked write, reply wait) restart it; (b) with a 10 s liveness grace, can a dead bridge hold the connection so that a second emergency or an ordinary order queues behind it past its own deadline, and what is recorded for that second request; (c) where the late answer lands and whether any reader consumes it or it is silently dropped; (d) is the drain bound derived from live deadline values with no literal left. New findings as blocks starting `FINDING <n> — <HIGH|MED|LOW> — <file:line> — <one-line risk>` with the exact check; finish with `SUMMARY — <n> HIGH / <n> MED / <n> LOW` counting only NEW or NOT-FIXED items. Default skeptical: unconfirmed = UNVERIFIED. Reading is your job; no dotnet runs over 10 minutes.

Prior items (records/codex-U2a-r6.txt — your rounds 5+6 delta):
FINDING 1 — HIGH — AtasConnector.cs:894 — EmergencyDeadline can be spent once waiting for the gate and almost again writing (frameBudget captured before the gate wait, new clock at :739), so the two-second end-to-end ceiling was false.
FINDING 2 — MED — AtasConnector.cs:959 — answer-only liveness dropped a responsive but legitimately occupied bridge whenever one synchronous ATAS command held its serialized command loop longer than two seconds.
FINDING 3 — MED — GatewayPipeServer.cs:135 — the 55-second drain was hard-coded independently of the connector deadlines.
FINDING 4 — LOW — GatewayPipeServer.cs:424 — a valid frame with both ids explicitly null threw before the handler's error boundary and closed the connection without INVALID_REQUEST.
FINDING 5 — LOW — AtasConnector.cs:871 — millisecond timestamp equality could discard a genuine pending-RPC answer.
PRIOR 4 — PARTIAL — progress recognized only after a whole 1 KiB chunk completes (accepted residual; must be stated in CONTRACTS.md and the record).
PRIOR 8 (CLI half) — NOT FIXED — the CLI promised every order command that same-id replay returns the original outcome although only Place honours it today (Program.cs:234).
PRIOR 12 — NOT FIXED — the record still said the Windows pipe behaviours were unverified (records/U2a.md:63) although the round-6 verified-tree box run measured the pipe classes; mutant B4 and the ATAS limit remain open.
PRIOR 14 — NOT FIXED — the record claimed one close-all settles the 64-character limit although the generated id is shorter.
F-E — MED — (verifier) the liveness rule dropped a healthy bridge merely slower than 2 s on a quiet connection.
F-F — LOW — (verifier) the record said "restored verbatim" for a test whose single assertion had been replaced by five.
