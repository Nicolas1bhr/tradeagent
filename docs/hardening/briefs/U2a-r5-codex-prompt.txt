READ-ONLY AUDIT — do not modify, create, or delete any file; output findings only. This is a DELTA re-review of unit U2a (TradeAgent: agent-facing named pipe hardening, operator-context hole, connector send deadlines, cancel-all ids, replay contract, risk-reducing fast path) in a real-money trading orchestrator. A prior read-only review of sha d25dbb4 reported the 14 findings listed below; an independent adversarial verifier added V2 and V3 (below). The builder applied fixes in the committed range d25dbb4..HEAD (10 commits, one per finding; run: git log --oneline d25dbb4..HEAD; git diff d25dbb4..HEAD). BY DECISION, prior findings 5, 6 and the gateway half of 8 were moved to another unit (U2c-1, which owns TradingGateway.cs and the record store) — for those three, confirm only that the files were NOT touched and report them as `PRIOR <n> — DEFERRED-BY-DECISION`. For every other prior finding emit one line of the exact form `PRIOR <n> — FIXED | NOT FIXED | PARTIAL — <one-line reason with file:line>`, then hunt regressions in the changed files only; do not re-litigate the full surface. Also rule on: (a) the builder's judgment `FrameTimeout` = 30 s as a whole-frame ceiling, which makes the worst-case shutdown with an order in flight 55 s (`WorstCaseOrderPath` = 10+30+10) — is the bound real and composable now, and does disposal re-await so no request is left unsettled; (b) the rename `EmergencyGateWait` → `EmergencyDeadline` now bounding gate + write + reply at 2 s — does anything still bound only the queue wait; (c) the liveness rule that decides whether a stalled connection is dropped after the wire — what does it actually key on, and can a merely slow bridge be dropped or a dead one kept; (d) the CLI tri-state transport result NothingWritten / PossiblyWritten / ReplyReceived — is any PipeClient exit path outside it. New findings as blocks starting `FINDING <n> — <HIGH|MED|LOW> — <file:line> — <one-line risk>` with the exact check; finish with `SUMMARY — <n> HIGH / <n> MED / <n> LOW` counting only NEW or NOT-FIXED items. Default skeptical: unconfirmed = UNVERIFIED. Reading is your job; no dotnet runs over 10 minutes.

Prior findings (records/codex-U2a-r4.txt):
FINDING 1 — HIGH — src/TradeAgent.Gateway/GatewayPipeServer.cs:357 — The effective broker request id bypasses every new validation rule
FINDING 2 — HIGH — src/TradeAgent.Connectors.Atas/AtasConnector.cs:139 — The claimed 30-second worst-case path is not a bound, so the 35-second drain can abandon settlement
FINDING 3 — MED — src/TradeAgent.TradeCli/Program.cs:59 — CLI transmission state is asserted before the request write has completed
FINDING 4 — MED — src/TradeAgent.Connectors.Atas/AtasConnector.cs:605 — Chunk completion is mistaken for byte progress, so a healthy slow reader is dropped as stalled
FINDING 5 — HIGH — src/TradeAgent.Gateway/TradingGateway.cs:709 — Agent close operations are transformed into Place and never receive the emergency path
FINDING 6 — HIGH — src/TradeAgent.Gateway/GatewayPipeServer.cs:478 — Replaying cancel-all or close-all repeats effects against new account state
FINDING 7 — MED — src/TradeAgent.TradeCli/PipeClient.cs:38 — Common lost-reply failures bypass CliReplayContract entirely
FINDING 8 — MED — src/TradeAgent.Gateway/TradingGateway.cs:635 — The blanket replay contract is implemented only for Place
FINDING 9 — MED — src/TradeAgent.Gateway/GatewayPipeServer.cs:512 — Sweep uniqueness is limited to 32 bits and eventually collides with durable history
FINDING 10 — LOW — src/TradeAgent.Gateway/GatewayPipeServer.cs:209 — The reserved operator session is accepted on hello frames
FINDING 11 — HIGH — src/TradeAgent.Gateway/GatewayPipeServer.cs:480 — Real gateway cancellation paths perform ordinary RPCs before reaching the emergency operation
FINDING 12 — MED — docs/hardening/records/U2a.md:63 — Core pipe behavior is still UNVERIFIED on the only production platform
FINDING 13 — LOW — tests/TradeAgent.IntegrationTests/CliReplayContractTests.cs:97 — The “before sending” binary test does not observe ordering
FINDING 14 — LOW — docs/hardening/records/U2a.md:65 — The proposed ATAS check cannot verify the claimed 64-character budget
V2 — HIGH — an emergency cancel-all on a stalled bridge with a FREE send gate took 10005 ms, returned "ATAS did not answer 'cancel-all' within 10s" instead of the owner-readable "not confirmed — check ATAS" sentence, and left the dead connection up with no reconnect; EmergencyGateWait bounded only the queue wait.
V3 — MED — the ordinary half of SendOutcome was pinned by wall-clock only (swapping the two ordinary sentences at AtasConnector.cs:723/725 survived the suite).
