READ-ONLY AUDIT — do not modify, create, or delete any file; output findings only. This is a DELTA re-review of unit U2a (TradeAgent: agent-facing named-pipe hardening, connector send deadlines, replay contract, risk-reducing fast path) in a real-money trading orchestrator. Your prior delta review of sha a974142 (round 7) reported the items listed below; an independent adversarial verifier added F-G. The builder applied fixes in the committed range a974142..HEAD (round 8; run: git log --oneline a974142..HEAD; git diff a974142..HEAD). Manager decisions in force, do not re-litigate but DO check they hold: (1) one ABSOLUTE deadline per emergency operation, carried by the risk-reducing scope, shared by every RPC inside it (prerequisite reads, target resolutions, every cancel/close leg); legs that cannot be sent before the deadline are never skipped silently — the caller's answer names per-leg outcomes and the gateway records UNKNOWN for the unconfirmed; (2) the drain is derived from the handler's worst-case COMPOSITE and disposal never returns with a request unsettled; (3) the caller's sentence at the deadline leads with the order outcome and carries the connection state as detail; (4) liveness uses the ordinary RPC deadline (10 s) as grace — unchanged; (5) prior findings 5, 6, 8-gateway and F-A are DEFERRED-BY-DECISION to U2c-1 (TradingGateway.cs, DashboardView.cs, Stores.cs, GatewayTypes.cs untouched); (6) PRIOR 12/14/F-F are REFUTED — the record corrections live on main and the branch carries a stale snapshot. For each prior item emit one line of the exact form `PRIOR <id> — FIXED | NOT FIXED | PARTIAL | DEFERRED-BY-DECISION | REFUTED-BY-DECISION — <one-line reason with file:line>`, then hunt regressions in the changed files only; do not re-litigate the full surface. Also rule on: (a) can any RPC inside an emergency operation still obtain a fresh budget (a retry, a reconnect, a nested scope); (b) if legs are sent concurrently, can a cancel leg race its own target resolution, and does concurrency bypass the send gate's backpressure or the per-frame ceiling; (c) is the per-leg outcome report complete — is there a leg state that is neither confirmed, unconfirmed nor not-sent; (d) is the composite drain bound derived from live values with no literal, and can a customised timeout still exceed it. New findings as blocks starting `FINDING <n> — <HIGH|MED|LOW> — <file:line> — <one-line risk>` with the exact check; finish with `SUMMARY — <n> HIGH / <n> MED / <n> LOW` counting only NEW or NOT-FIXED items. Default skeptical: unconfirmed = UNVERIFIED. Reading is your job; no dotnet runs over 10 minutes.

Prior items (records/codex-U2a-r7.txt, your delta on a974142):
FINDING 1 — HIGH — AtasConnector.cs:974 — the clock was per Rpc, so prerequisite reads and every cancel-all leg restarted the two-second budget; the agent-facing emergency had no single end-to-end clock (three 1.9 s legs → ≈5.7 s).
FINDING 2 — MED — GatewayPipeServer.cs:161 — the drain covered one connector call plus a literal five seconds, not a composite handler, so multi-RPC sweeps could outlive it and strand the active request DISPATCHING.
FINDING 3 — LOW — AtasConnector.cs:909 — a reply racing between caller timeout and _abandoned registration could remove _pending first; no late-answer counter recorded it and _abandoned leaked the id.
PRIOR 12 / PRIOR 14 / PRIOR F-F — REFUTED-BY-DECISION (record corrections are on main; the branch copy of records/U2a.md is the 2026-09-03 snapshot).
F-G — LOW — (verifier, a974142) the caller's 2 s sentence led with connection state instead of the order outcome (AtasConnector.cs:183-188).
