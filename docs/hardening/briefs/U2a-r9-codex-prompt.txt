READ-ONLY AUDIT — do not modify, create, or delete any file; output findings only. This is a DELTA re-review of unit U2a (TradeAgent: agent-facing named-pipe hardening, connector send deadlines, replay contract, risk-reducing fast path) in a real-money trading orchestrator. Your prior delta review of sha 5624cd1 (round 8) reported the items listed below; the round-8 finisher added F5. The builder applied fixes in the committed range 5624cd1..HEAD (round 9; run: git log --oneline 5624cd1..HEAD; git diff 5624cd1..HEAD). Manager decisions in force, do not re-litigate but DO check they hold: (1) one ABSOLUTE deadline per emergency operation shared by every RPC in it; (2) the per-leg outcome vocabulary is exactly four values, each mapping to exactly one record state — confirmed (settled), rejected (definite broker refusal → REJECTED), not-sent (never reached the wire; the owner is told which orders are still working), sent-not-confirmed (→ UNKNOWN + reconciliation); (3) the composite drain bound is derived from the handler's REAL longest chain of serial RPCs, an override may only lengthen it, and disposal never returns with a request unsettled; (4) build gates are non-incremental and must show 0 warnings; (5) prior findings 5, 6, 8-gateway and F-A remain DEFERRED-BY-DECISION to U2c-1 (TradingGateway.cs, DashboardView.cs, Stores.cs, GatewayTypes.cs untouched beyond what compiles). For each prior item emit one line of the exact form `PRIOR <id> — FIXED | NOT FIXED | PARTIAL — <one-line reason with file:line>`, then hunt regressions in the changed files only; do not re-litigate the full surface. Also rule on: (a) is every leg outcome reachable in code mapped to exactly one of the four values, and is there any leg state (cancelled-by-disposal, connector Busy, PeerStalled, a late definite answer) that falls outside them; (b) which handler path is really the longest serial chain, and does the derived drain cover it at customised timeouts; (c) does any path still hand a fresh budget to an RPC inside an emergency operation; (d) does the CS8619 fix change nullability semantics anywhere a null result was previously meaningful. New findings as blocks starting `FINDING <n> — <HIGH|MED|LOW> — <file:line> — <one-line risk>` with the exact check; finish with `SUMMARY — <n> HIGH / <n> MED / <n> LOW` counting only NEW or NOT-FIXED items. Default skeptical: unconfirmed = UNVERIFIED. Reading is your job; no dotnet runs over 10 minutes.

Prior items (records/codex-U2a-r8.txt, your delta on 5624cd1):
PRIOR 2 — NOT FIXED — GatewayPipeServer.cs:161 assumed only three serial RPCs where a cold placement issues five, and an explicit drain override could still permit disposal with an unsettled request.
FINDING 1 — MED — GatewayPipeServer.cs:748 — the outcome mapper conflated definitive rejection and pre-send failure with an unknown sent leg (RefuseCancel=1 → "sent-not-confirmed" while the record was REJECTED; target resolution expiring before TryCreate → "sent-not-confirmed" with attempted=0 and no record).
FINDING 2 — LOW — AtasConnector.cs:946 — a disconnect or disposal during late-answer grace left the request permanently in _abandoned.
FINDING 3 — LOW — FakeConnector.cs:69 — deadline accounting used the maximum of two latency faults that lines 78–79 execute serially.
FINDING 4 — LOW — AtasConnector.cs:1158 — Left returned a fresh one-millisecond budget after an absolute deadline had expired.
F5 — LOW/gate — (round-8 finisher) a --no-incremental build reported CS8619 at GatewayPipeServer.cs(626,32): RunLegs declared Task<ExecutionRequest?> while CancelAsync returned the non-nullable form.
