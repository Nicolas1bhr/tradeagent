READ-ONLY AUDIT — do not modify, create, or delete any file; output findings only. This is a DELTA re-review of unit U14 (TradeAgent's CoidWitness: the durable write-ahead record that answers "did this product submit this client order identifier" before any order reaches ATAS; CLAUDE.md rule 1). Your prior delta review of sha 6a40fa7 reported the items listed below (three PARTIAL priors and six new findings); an independent adversarial verifier added R1–R5 on that sha and V1–V3 on the next (f8a724c). The builder applied fixes in the committed range 6a40fa7..HEAD (rounds 6, 7 and 7b; run: git log --oneline 6a40fa7..HEAD; git diff 6a40fa7..HEAD). Manager decisions in force, do not re-litigate but DO check they hold: (1) "absent" means FileNotFound on the committed path and nothing else — every other read failure is UNREADABLE with writes refused and bytes preserved, sharing one predicate with the parse-failure case; (2) refusal of a protocol-mismatched peer is decided once at the top of Dispatch for the whole connection, the peer is DROPPED (return false) and Drop preserves _incompatible when our own refusal closed the connection, and that marker survives any later disconnect until a COMPATIBLE hello; (3) the lease is released on every terminal path and a stopped adapter's handler cannot reacquire; (4) refused writers never append to the owner's sidecar — per-writer files, collected by every reader, with the noted/degraded state computed over the whole set; (5) the unresolved-safety state survives rotation and "historical" means RESOLVED after the last safety line. For each prior item emit one line of the exact form `PRIOR <id> — FIXED | NOT FIXED | PARTIAL — <one-line reason with file:line>`, then hunt regressions in the changed files only; do not re-litigate the full surface. Also rule on: (a) with maxNumberOfServerInstances = 1, is there any state in which a refused, dropped or half-closed peer still prevents the next bridge from connecting; (b) can anything other than a compatible hello clear _incompatible (connector restart, SwitchConnectorAsync, a health probe, a later unrelated disconnect); (c) the builder reports that two mutants on the noted/degraded boundary (widen the degraded guard; widen the deciding-line scan) each survive ALONE because each blocks the other, and only both together go RED — can a real defect hide in that gap; (d) the Redial fixture added for a flake (a bridge dialling while the single pipe instance recycles) — does it model a real bridge or hide a real failure. New findings as blocks starting `FINDING <n> — <HIGH|MED|LOW> — <file:line> — <one-line risk>` with the exact check; finish with `SUMMARY — <n> HIGH / <n> MED / <n> LOW` counting only NEW or NOT-FIXED items. Default skeptical: unconfirmed = UNVERIFIED. Reading is your job; no dotnet runs over 10 minutes.

Prior items (records/codex-U14-r5.txt, your delta on 6a40fa7):
PRIOR 5 — PARTIAL — the witness refusal preceded ITradingManager.ClosePosition but the unavailable-witness runtime assertion was unverified (AtasStrategyAdapter.cs:1775).
PRIOR 9 — PARTIAL — incompatible-peer events were gated, but mismatch still returned true and the same session could send a compatible hello to clear the refusal (AtasConnector.cs:316).
PRIOR 16 — PARTIAL — the adapter's unavailable-witness dispatch behaviour had no executable gate.
FINDING 17 — HIGH — CoidWitness.cs:892 — an unreadable committed file could be treated as absent and replaced.
FINDING 18 — MED — CoidWitness.cs:1264 — rotation could hide an unresolved safety event when the first entry in the new log is diagnostic.
FINDING 19 — MED — CoidWitnessReport.cs:43 — a diagnostic-only sidecar was mislabeled historical and made a zero falsely non-provisional.
FINDING 20 — MED — AtasConnector.cs:296 — successful authentication alone permitted trusted events before protocol compatibility was established.
FINDING 21 — MED — AtasStrategyAdapter.cs:468 — a stopped adapter could reacquire and retain the released witness lease through its still-subscribed order handler.
FINDING 22 — LOW — CoidWitness.cs:1129 — a viable candidate contributing no acknowledgement was reported recovered and left for repeated processing.
R1 — HIGH — (verifier, 6a40fa7) a refused v2 peer set _hello and ReconciliationProvable=True through one heartbeat claiming protocol 3.
R2 — MED — (verifier) the lease was released only via StopBridge <- OnStopping.
R3 — MED — (verifier) safety events were dropped under concurrent sidecar appends by refused writers.
R4 — LOW — (verifier) unlinking the lock file yields two live owners on macOS (costs no claim).
R5 — LOW — (verifier) record wording on when the CAS fires.
V1 — HIGH — (verifier, f8a724c) parking the refused peer with return true blocked the fixed bridge (one server instance; the next is created only after the read loop ends).
V2 — MED — (verifier, f8a724c) _noted was gated on the canonical sidecar existing, so per-writer refusals read as a confident zero.
V3 — LOW — (verifier, f8a724c) the F18 shape cannot be produced by this build.
