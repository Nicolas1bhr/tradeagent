READ-ONLY AUDIT — do not modify, create, or delete any file; output findings only. This is a DELTA re-review of unit U2a (TradeAgent: agent-facing named-pipe hardening, connector send deadlines, replay contract, risk-reducing fast path) in a real-money trading orchestrator. Your prior delta review of sha 088c059 (round 9) reported the items listed below; the independent adversarial verifier's rounds 8+9 findings are appended after them. The builder applied fixes in the committed range 088c059..HEAD (round 10; run: git log --oneline 088c059..HEAD; git diff 088c059..HEAD). Manager decisions in force, do not re-litigate but DO check they hold: (1) the drain is the maximum over a per-handler serial-depth table (place, modify, cancel, cancel-all, close, close-all, reads) computed from live values — the table is written in docs/CONTRACTS.md; disposal never returns with a request unsettled; (2) the per-leg outcome vocabulary is EXACTLY five words — confirmed / rejected / not-sent / sent-not-confirmed / sent-still-working — each mapping to exactly one record state; nothing-to-do exists ONLY as the whole-operation result of a sweep with zero targets; (3) the leg outcome is derived from the connector's transport tri-state (NothingWritten → not-sent; PossiblyWritten → sent-not-confirmed with UNKNOWN + NeedsReconciliation; ReplyReceived → by the reply), never from the record state alone; (4) DEFERRED-BY-DECISION to U2c-1: your round-9 F2 (a close's final offsetting Place outside the emergency scope) — the round-4 rule that a Place never takes the fast path stands until U2c-1 carries a Close intent through ITradingConnector; also prior findings 5, 6, 8-gateway, F-A; (5) your round-9 "PRIOR F5 NOT FIXED" is REFUTED — the zero-warning non-incremental build is quoted in the record on main (the branch carries a stale snapshot of records/U2a.md). For each prior item emit one line of the exact form `PRIOR <id> — FIXED | NOT FIXED | PARTIAL | DEFERRED-BY-DECISION | REFUTED-BY-DECISION — <one-line reason with file:line>`, then hunt regressions in the changed files only; do not re-litigate the full surface. Also rule on: (a) a handler or path missing from the drain table; (b) a reachable reply value outside the five words, or a leg word whose record state can differ from the mapping; (c) a leg whose transport result is unknown to the mapper (cancelled before the gate, cancelled mid-write, a reply that arrives after the leg was reported); (d) whether the table's numbers in CONTRACTS.md match the code's constants. New findings as blocks starting `FINDING <n> — <HIGH|MED|LOW> — <file:line> — <one-line risk>` with the exact check; finish with `SUMMARY — <n> HIGH / <n> MED / <n> LOW` counting only NEW or NOT-FIXED items. Default skeptical: unconfirmed = UNVERIFIED. Reading is your job; no dotnet runs over 10 minutes.

Prior items (records/codex-U2a-r9.txt, your delta on 088c059):
PRIOR 2 — PARTIAL — the risk-reducing formula undercounted close-all (E + W where one wave serialises four ordinary placements → E + 4W), and disposal could return with unfinished handlers (GatewayPipeServer.cs:157,220,1187).
FINDING 1 — HIGH — GatewayPipeServer.cs:220 — same as PRIOR 2: with E=30 s, W=4 s, S=5 s and four positions the drain must be ≥ 51 s; the formula returned 39 s.
FINDING 2 — HIGH — AtasConnector.cs:1018 — DEFERRED-BY-DECISION to U2c-1 (a close's final offsetting Place receives fresh budgets).
FINDING 3 — MED — GatewayPipeServer.cs:816 — the public per-leg vocabulary contained six values (sent-and-confirmed, sent-still-working, nothing-to-do, …) and SweepRequestIdTests.cs:395 approved all six.
FINDING 4 — MED — GatewayPipeServer.cs:786 — record-only classification: pre-gate Busy/PeerStalled legs became UNKNOWN though never sent; disposal cancellation could leave DISPATCHING/RECONCILING described as sent-not-confirmed.
PRIOR F5 — REFUTED-BY-DECISION (zero-warning build recorded on main).
