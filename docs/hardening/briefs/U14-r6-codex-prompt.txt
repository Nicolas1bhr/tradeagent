READ-ONLY AUDIT — do not modify, create, or delete any file; output findings only. This is a DELTA re-review of unit U14 (TradeAgent's CoidWitness: the durable write-ahead record that answers "did this product submit this client order identifier" before any order reaches ATAS; CLAUDE.md rule 1). Your prior delta review of sha 6a40fa7 reported the items listed below (three PARTIAL priors and six new findings); an independent adversarial verifier added R1–R5 (below). The builder applied fixes in the committed range 6a40fa7..HEAD (13 commits; run: git log --oneline 6a40fa7..HEAD; git diff 6a40fa7..HEAD). For each prior item emit one line of the exact form `PRIOR <id> — FIXED | NOT FIXED | PARTIAL — <one-line reason with file:line>`, then hunt regressions in the changed files only; do not re-litigate the full surface. Also rule on: (a) the ACCEPTED deviation for a protocol-mismatched peer — the read loop keeps `return true` (returning false drops the peer, which clears `_incompatible` by design and loses the "reinstall the add-on" sentence), so the refused peer is parked on the pipe and read by nobody: does the connector still accept a NEW connection from a fixed bridge, and does the parked peer hold a slot, buffer, thread, heartbeat timer or lock forever; (b) refusal decided once at the top of Dispatch — can any frame kind (event, heartbeat, hello, RPC reply) from a refused peer still change `_hello`, `ReconciliationProvable`, health rows or the witness; (c) the per-writer sidecar files chosen for R3 — does every reader (probe, support package, AtasHealth, `Trouble`) collect the whole set and compute the degraded state over it; (d) the lease release on every terminal path — can a stopped adapter's still-subscribed handler reacquire, and is any path left that holds the exclusive handle after the strategy stops; (e) F17 — is "absent" now exactly FileNotFound and every other read failure UNREADABLE with writes refused and bytes preserved, sharing one predicate with the parse-failure case. New findings as blocks starting `FINDING <n> — <HIGH|MED|LOW> — <file:line> — <one-line risk>` with the exact check; finish with `SUMMARY — <n> HIGH / <n> MED / <n> LOW` counting only NEW or NOT-FIXED items. Default skeptical: unconfirmed = UNVERIFIED. Reading is your job; no dotnet runs over 10 minutes.

Prior items (records/codex-U14-r5.txt):
PRIOR 5 — PARTIAL — the witness refusal now precedes `ITradingManager.ClosePosition`, but the exact unavailable-witness runtime assertion remains unverified at src/TradeAgent.AtasBridge/AtasStrategyAdapter.cs:1775
PRIOR 9 — PARTIAL — incompatible-peer events are gated, but mismatch still returns true and the same session can send a compatible hello to clear the refusal at src/TradeAgent.Connectors.Atas/AtasConnector.cs:316
PRIOR 16 — PARTIAL — the adapter now has a reported successful ATAS-enabled Windows compile, but its unavailable-witness dispatch behavior still has no executable gate at docs/hardening/records/U14.md:685
FINDING 17 — HIGH — src/TradeAgent.AtasBridge/CoidWitness.cs:892 — an unreadable committed file can be treated as absent and replaced, destroying history and potentially accepting the new claim
FINDING 18 — MED — src/TradeAgent.AtasBridge/CoidWitness.cs:1264 — rotation can hide an unresolved safety event when the first entry in the new log is diagnostic
FINDING 19 — MED — src/TradeAgent.AtasBridge/CoidWitnessReport.cs:43 — a diagnostic-only sidecar is mislabeled historical and makes a zero falsely non-provisional
FINDING 20 — MED — src/TradeAgent.Connectors.Atas/AtasConnector.cs:296 — successful authentication alone permits trusted events before protocol compatibility is established
FINDING 21 — MED — src/TradeAgent.AtasBridge/AtasStrategyAdapter.cs:468 — a stopped adapter can reacquire and retain the released witness lease through its still-subscribed order handler
FINDING 22 — LOW — src/TradeAgent.AtasBridge/CoidWitness.cs:1129 — a viable candidate that contributes no acknowledgement is reported as recovered and left for repeated processing
R1 — HIGH — a peer whose hello was refused as protocol 2 set `_hello` and `ReconciliationProvable=True` through one heartbeat claiming protocol 3, clearing the AUTONOMY_REQUIRES_PROVABLE_STATE refusal and the human-escalation while the row still said "reinstall the add-on".
R2 — MED — the lifetime lease was released only via StopBridge <- OnStopping, a path no test runs; if ATAS does not fire it, every later order is refused until ATAS restarts.
R3 — MED — safety events were dropped under concurrent sidecar appends by the writers the lease refused (unserialised by construction).
R4 — LOW — unlinking the lock file yields two live owners on macOS (costs no claim: CAS + read-back refuse); Windows not verified.
R5 — LOW — record wording: the CAS also fires after a legitimate ownership handover.
