READ-ONLY AUDIT — do not modify, create, or delete any file; output findings only. This is a DELTA re-review of unit U14 (TradeAgent's CoidWitness: the durable write-ahead record that answers "did this product submit this client order identifier" before any order reaches ATAS; CLAUDE.md rule 1). Your prior delta review of sha 01fcd60 (round 10, the structural round) reported the items listed below; the independent adversarial verifier's round-10 findings are appended after them. Round 11 completes the structure where the old shape had leaked back: (1) Unreadable propagates as a VALUE to every consumer — LastDecidingLine is a tri-state and Settled() refuses to write RESOLVED over an unreadable snapshot; (2) the snapshot carries captured LINES, and the probe, report and support package render from the snapshot they were handed — no path leaves it for re-reading; (3) candidates are read inside the before/after listing window and adoption decides from the snapshot; (4) rotation is resumable at every act — if .new exists and current does not, the last act is completed first, and a failed act degrades the standing with a reason rather than silencing later appends; (5) Stop() is idempotent and serialised under the state lock, Stopped is published exactly once after the one teardown, Started() is refused throughout; (6) a new connection stamps a "connecting — waiting to authenticate" status at accept time, counter-stamped newer than any marker; (7) the sidecar bound counts encoded bytes. The builder applied this in the committed range 01fcd60..HEAD (run: git log --oneline 01fcd60..HEAD; git diff 01fcd60..HEAD). For each prior item emit `PRIOR <id> — FIXED | NOT FIXED | PARTIAL — <one-line reason with file:line>`; then judge the structure again: (a) grep for any null-for-unreadable conversion, any consumer that reopens a sidecar path, any read of a candidate outside the snapshot window; (b) enumerate the four rotation acts and, for each, the state after a failure and whether a later append can land or the standing says why; (c) any path by which Stopped is published while a teardown step still runs, or Started() is accepted during Stopping; (d) any connection instant with no stamped status; (e) the interaction you consider most likely to be wrong between this unit's connector changes and unit U2a's (which rewrote the same AtasConnector.cs read loop for deadlines and liveness) — both are about to be merged. Then hunt regressions in the changed files only. New findings as blocks starting `FINDING <n> — <HIGH|MED|LOW> — <file:line> — <one-line risk>` with the exact check; finish with `SUMMARY — <n> HIGH / <n> MED / <n> LOW` counting only NEW or NOT-FIXED items. Default skeptical: unconfirmed = UNVERIFIED. Reading is your job; no dotnet runs over 10 minutes.

Prior items (records/codex-U14-r10.txt, your delta on 01fcd60):
PRIOR 33 — NOT FIXED — LastDecidingLine() still converted an unreadable snapshot to null, allowing Settled() to append RESOLVED without seeing the unresolved marker (CoidWitness.cs:2346).
PRIOR 35 — NOT FIXED — overlapping Stop() calls could let one finalizer publish Stopped while another still executed, making Started() legal during that teardown (AdapterTeardown.cs:233).
FINDING 39 — MED — CoidWitness.cs:999 — the snapshot exposed paths but not captured lines, forcing the probe and support collector to reopen files.
FINDING 40 — MED — CoidWitness.cs:1300 — candidate contents were read after the snapshot's before/after comparison.
FINDING 41 — MED — CoidWitness.cs:1652 — rotation could not resume after current→.1 succeeded but .new→current threw, suppressing later appends until restart.
FINDING 42 — MED — AtasConnector.cs:171 — a newly arrived peer had no stamped current status during AuthGrace, so a previous connection's refusal remained the displayed winner.
FINDING 43 — LOW — CoidWitness.cs:1915 — _sidecarBytes counted UTF-16 characters against a byte limit.
