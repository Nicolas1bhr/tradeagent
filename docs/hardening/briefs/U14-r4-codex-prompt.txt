READ-ONLY AUDIT — do not modify, create, or delete any file; output findings only. You are an independent second-model reviewer of a change that claims to satisfy unit U14 of a real-money trading orchestrator (TradeAgent: a Windows desktop app that lets an AI agent place orders through an ATAS bridge for a nontechnical owner). Review the committed range 3f1d8f2..HEAD in this checkout (run: git log --oneline 3f1d8f2..HEAD; git diff 3f1d8f2..HEAD). Rule 1 of CLAUDE.md is the whole point: the product must be able to answer "did this product submit this client order identifier" from a durable write-ahead record — the CoidWitness — before any order reaches ATAS; if it cannot, it must refuse the order (SupportsClientOrderId=false / AtasRejectedException "nothing was submitted") rather than trade without the record.

U14's acceptance: (a) rename/replace of the witness file retries IOException and UnauthorizedAccessException with a bounded budget; a reader prefers a newer parsing temp only under the LINEAGE rule (generation + predecessor fingerprint, FNV-1a 64), never a zero-record candidate, never without a committed anchor, never a candidate that shrinks the record set, and two rivals adopt neither; (b) Submitting returns bool, restores its pre-attempt snapshot on failure, and the adapter refuses the order when it is false; Identified deliberately does not restore; (c) sidecar coid-witness.errors.log + LastWriteFailure, surfaced through BridgeHello.witness_failure to AtasHealth DEGRADED, with a RESOLVED marker that a superseding commit writes; the sidecar never rations a safety event (rotates instead; the quota applies to warnings/markers); (d) round 4: ONE OWNER PER WITNESS — the lock is mandatory, cannot-acquire → Submitting false with Trouble "another writer owns this witness", a CAS miss is a refusal, the multi-writer Rebase path is REMOVED; bridge protocol bumped to 3 so an older bridge (which sent orders after a failed rename and omits witness_failure) is refused by the app; readers never write (adoption/quarantine only by the owner at startup under the lock; the writer re-reads the sidecar before RESOLVED); the probe treats a sidecar ending in RESOLVED as historical; (e) item 6: superset rule by MEMBERSHIP not count; the Identified asymmetry and the mandatory-lock refusal pinned by tests; an anchorless candidate reads as a flagged zero.

Constraints in force: the assembly may not gain a dependency; the two hunks in AtasStrategyAdapter.cs compile only on the Windows box (read them, do not try to compile them against ATAS); the four safety rules in CLAUDE.md; operator authority is in-process only.

Hunt specifically for: (1) acceptance not fully met — any path by which an order can reach the wire without a committed witness record (exceptions swallowed, a false return ignored, a lock timeout that falls through, a startup path that trades before adoption completes), or by which a committed record can be lost or a phantom record adopted; (2) adjacent regressions — enumerate the consumers of every changed symbol (the adapter, the probe, AtasHealth, the support package, BridgeHello/protocol negotiation, the tests) and flag what breaks, including the protocol bump's effect on the app's refusal path and on docs/RESUME-HERE.md and docs/CONTRACTS.md; (3) gates that would be RED — tests that cannot fail, tests asserting at test-only budgets, platform assumptions (APFS-only lock proof; Windows sharing violations injected at the seam only); (4) both directions — a second writer is refused AND the single legitimate owner still commits under load and across restarts; a v2 bridge is refused AND a v3 accepted; a safety event survives quota AND warnings are still rationed; (5) unbacked claims in docs/hardening/records/U14.md (read it at /Users/nicolasbeeckman/Projects/ai-trading-software-for-mihael/docs/hardening/records/U14.md) asserted without a test or measurement in the diff; (6) class over instances — if two or more findings share a root cause, name the CLASS and the structural fix.

Default skeptical: if you cannot confirm a claim from the code, report it as UNVERIFIED, not as fine. Emit each finding the moment it is settled, as its own block starting with a line of the exact form `FINDING <n> — <HIGH|MED|LOW> — <file:line> — <one-line risk>` followed by the concrete scenario and the exact check (command or test) that would confirm or refute it. Finish with a single line `SUMMARY — <n> HIGH / <n> MED / <n> LOW`. Do not run dotnet build or dotnet test for longer than 10 minutes; reading is your job.
