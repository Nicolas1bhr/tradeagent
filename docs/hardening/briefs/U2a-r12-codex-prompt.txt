READ-ONLY AUDIT — do not modify, create, or delete any file; output findings only. This is the FINAL delta re-review of unit U2a (TradeAgent: agent-facing named-pipe hardening, connector send deadlines, replay contract, risk-reducing fast path) in a real-money trading orchestrator, before the unit integrates onto main. Your prior delta review of sha 120c739 (round 11) reported the items listed below; the independent adversarial verifier's rounds 10+11 findings are appended after them. The builder applied fixes in the committed range 120c739..HEAD (round 12; run: git log --oneline 120c739..HEAD; git diff 120c739..HEAD). Manager decisions in force, do not re-litigate but DO check they hold: (1) your round-11 "PRIOR F2 NOT FIXED" is REFUTED-BY-MEASUREMENT — the verifier showed the three empty-transport legs start zero mutating connector calls and that null-after-attempt is unreachable in both shipped connectors, and that a blind null → sent-not-confirmed rule turns five true tests red; report it as REFUTED-BY-DECISION; (2) the third-party gap is closed on the pipe-server side: a mutating step that was dispatched and returns a null transport result is classified sent-not-confirmed by the pipe server's own knowledge, a leg that never dispatched a mutating step stays not-sent, the obligation is stated on ITradingConnector and in docs/CONTRACTS.md, and transport is emitted as explicit null; the gateway-side attempt marking is DEFERRED-BY-DECISION to U2c-1; (3) the DISPATCHING query and the handlers_did_not_finish sentinel run unconditionally at the end of disposal; (4) prior deferrals to U2c-1 stand (5, 6, 8-gateway, F-A, round-9 F2, round-10 PRIOR 2 and V-F1). For each prior item emit one line of the exact form `PRIOR <id> — FIXED | NOT FIXED | PARTIAL | DEFERRED-BY-DECISION | REFUTED-BY-DECISION — <one-line reason with file:line>`, then hunt regressions in the changed files only; do not re-litigate the full surface. Also rule, as the last cross-model reader before integration: (a) is there any mutating step the pipe server does not know is mutating, so a null transport could still read not-sent; (b) any disposal path that returns with a DISPATCHING row and no error log; (c) does anything in the unit's public surface (AGENTS.md, CONTRACTS.md, the CLI's wording) still promise more than the code does; (d) name the three claims in the unit you consider least proven. New findings as blocks starting `FINDING <n> — <HIGH|MED|LOW> — <file:line> — <one-line risk>` with the exact check; finish with `SUMMARY — <n> HIGH / <n> MED / <n> LOW` counting only NEW or NOT-FIXED items. Default skeptical: unconfirmed = UNVERIFIED. Reading is your job; no dotnet runs over 10 minutes.

Prior items (records/codex-U2a-r11.txt, your delta on 120c739):
PRIOR FINDING 2 (round 10) — REFUTED-BY-DECISION — the attempt-derived fallback and null → not-sent for never-started legs were measured sound by the verifier (see above).
FINDING 1 — LOW — ConnectorSendDeadlineTests.cs:848 — the Windows-flake rewrite captured its verdict before the delayed liveness judge ran, so a PeerAnsweredSince regression would not be observed by it.
F-1 — MED — (verifier, 120c739) the handlers_did_not_finish sentinel sat inside `if (handlers.Length > 0)`, which is empty once the agent disconnected; disposal returned in 3 ms with a DISPATCHING row and no log.
F-2 — MED — (verifier, 120c739) not-sent was an assurance a connector had to opt into via the attempt marker; ITradingConnector never stated it; a third-party connector that never called TransportLedger reported not-sent / attempted:0 and the transport field was omitted from the JSON.
L-1 — LOW — (verifier) AtasConnector._pending leaked on caller cancellation (0 → 1 → 1, AwaitingLateAnswer = 0).
L-2 — LOW — (verifier) a table row bounded the connector chain, not the handler; S added once and settable to zero (cancel-all 917 ms vs a 900 ms row).
L-3 — LOW — (verifier) the coverage test's candidate set came from Core.Ops literals, not the dispatch switch.
L-4 — LOW — (verifier) the simulator's deadline sentence was not op-aware.
