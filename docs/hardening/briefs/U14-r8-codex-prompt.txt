READ-ONLY AUDIT — do not modify, create, or delete any file; output findings only. This is a DELTA re-review of unit U14 (TradeAgent's CoidWitness: the durable write-ahead record that answers "did this product submit this client order identifier" before any order reaches ATAS; CLAUDE.md rule 1). Your prior delta review of sha 4de7c25 reported the items listed below; an independent adversarial verifier added V4 and V5. The builder applied fixes in the committed range 4de7c25..HEAD (round 8; run: git log --oneline 4de7c25..HEAD; git diff 4de7c25..HEAD). Manager decisions in force, do not re-litigate but DO check they hold: (1) "absent" means FileNotFoundException on the committed PATH only — a missing directory is UNREADABLE and refuses; (2) the lease is released on every terminal path including exceptional ones, and a stopped adapter cannot reacquire through any interleaving; (3) rotation carries the unresolved safety state into the new log and flushes it BEFORE deleting the old generation; (4) an unreadable sidecar is unreadable, never "no notes"; (5) after HeartbeatTimeout a silent, partial-frame or stale-open peer is DROPPED (markers preserved) so the single pipe instance recycles; (6) the status row describes the peer that is there NOW — the most recent refusal or live state takes precedence over older markers; (7) REFUTED by decision, do not re-report: PRIOR 5 (the on-box gate evidence is in the record on main), R4 (documented macOS residual — Windows holds the handle open), R5 (record wording), F24 (_incompatible is instance-scoped by design; a restart re-derives it), F25 ("degraded" spans the owner's canonical sidecar; "noted" spans the whole per-writer set — a refused writer's ERROR cost no order). For each prior item emit one line of the exact form `PRIOR <id> — FIXED | NOT FIXED | PARTIAL | REFUTED-BY-DECISION — <one-line reason with file:line>`, then hunt regressions in the changed files only; do not re-litigate the full surface. Also rule on: (a) does the heartbeat-timeout drop ever hit a legitimately quiet but healthy bridge, and what keeps such a bridge alive; (b) is the rotation carry-forward crash-safe at every interleaving (marker written, flushed, old generation deleted) and does a reader during the window see a consistent state; (c) can any read-failure variant on the committed path or the sidecar still read as absent or empty; (d) precedence on the status row — is there an order of refusals or live events that still leaves a stale sentence. New findings as blocks starting `FINDING <n> — <HIGH|MED|LOW> — <file:line> — <one-line risk>` with the exact check; finish with `SUMMARY — <n> HIGH / <n> MED / <n> LOW` counting only NEW or NOT-FIXED items. Default skeptical: unconfirmed = UNVERIFIED. Reading is your job; no dotnet runs over 10 minutes.

Prior items (records/codex-U14-r7.txt, your delta on 4de7c25):
PRIOR 17 — NOT FIXED — DirectoryNotFoundException was still classified as absence (CoidWitness.cs:1539).
PRIOR 21 — PARTIAL — the stopped guard's unsynchronised check could race with lease disposal and reacquire via Identified (AtasStrategyAdapter.cs:2038).
PRIOR R2 — PARTIAL — an exception from UntrackSecurities bypassed the later witness disposal (AtasStrategyAdapter.cs:501).
PRIOR V3 — NOT FIXED — the "cannot be produced" claim excluded a process death between diagnostic rotation and the next deciding line.
FINDING 23 — MED — AtasConnector.cs:175 — a silent, partial-frame or stale-but-open peer could monopolize the only pipe instance because heartbeat degradation never terminated the read loop.
FINDING 25 — MED — CoidWitness.cs:957 — REFUTED-BY-DECISION (degraded = owner's canonical sidecar; noted = whole set).
FINDING 26 — MED — AtasStrategyAdapter.cs:501 — teardown lacked a finally, so a failure removing security subscriptions skipped witness disposal.
FINDING 27 — MED — CoidWitness.cs:1982 — rotation deleted the previous generation before the unresolved safety state was carried forward.
FINDING 28 — MED — CoidWitness.cs:949 — an existing but unreadable sidecar was treated as containing no notes.
FINDING 29 — LOW — CoidWitnessReport.cs:68 — Noted described every state as a rejected candidate beside the witness.
FINDING 24 — LOW — REFUTED-BY-DECISION (instance-scoped marker by design).
PRIOR 5 / R4 / R5 — REFUTED-BY-DECISION (see above).
V4 — MED — (verifier, 4de7c25) StatusDetail preferred the OLDER refusal: after a v2 refusal a reinstalled bridge failing authentication was still reported as "speaks protocol 2 — reinstall the add-on".
V5 — MED — (verifier, 4de7c25) mutant MV2c (widen the deciding-line scan) went RED against a state the builder's tests never built: canonical sidecar diagnostic-only plus a refused writer's own unresolved safety line.
