# Manager log — session resumed 2026-09-03 (evening), top-level manager on Fable, legs on Opus + Codex

Pick-up point was `HANDOFF-2026-09-03.md`. `main` = `origin/main` = `9fd5eb7` (the handoff commit on top of `3f1d8f2`).
No stray worktrees; every unit branch tip matched the handoff table. Tailscale on the Mac reports "stopped"; the box is
unreachable and not needed until the v0.1.2 cut (integration step 5). Toolchain checked: dotnet 10.0.400, codex-cli
0.144.1, gh 2.92.0.

## Structure for this session

- Manager (this session, Fable) directs and integrates; it writes no product code. Every build/verify leg is an Opus
  general-purpose agent; Codex runs as a background `codex exec` (not a Claude leg). Hard cap: **2 heavy Claude legs at
  once** (four rate-limit kills last session).
- One worktree per leg under `~/Projects/ai-trading-software-for-mihael-worktrees/` (durable; never the scratchpad):
  `u14-build` (branch), `u2a-rebase-probe` (probe branch), `u2a-verify-r4` and `u2a-codex-r4` (detached at the sha).
- Briefs are committed to `docs/hardening/briefs/` so a killed leg can be re-briefed from disk; legs write their records
  into `docs/hardening/records/` in the main worktree and the manager commits them.

## Decision: rebase U2a BEFORE verifying round 4 (not after, as the handoff's step 1 had it)

Claims attach to a sha. Verifying `5c716aa` and then rebasing onto a `main` that changed `GatewayTypes.cs` (U2b) would
leave the merged combination unverified. Probe rebase in `u2a-rebase-probe`: **clean, no conflicts**, new tip `e91293e`,
21 commits ahead of `9fd5eb7`. Full suite on `e91293e`: see the next entry. The branch `u2a-pipe-hardening` is moved to
`e91293e` only after that suite is green; the pre-rebase tip `5c716aa` stays reachable by sha.

## Timeline

- 17:40 U14 builder (Opus) dispatched on `u14-build` @ `a8b3fb0` with `briefs/U14-r4-item6-build.md`.
- 17:40 U2a rebased build + full suite running in `u2a-rebase-probe` (background).
- 17:52 Rebased U2a (`e91293e`): `dotnet build` exit 0; full suite **390 green / 1 red** (FaultTests 75, UnitTests 108,
  IntegrationTests 207 + 1 failed): `ConnectorSendDeadlineTests.An_emergency_behind_a_busy_but_healthy_bridge_says_busy_and_does_not_drop_it`
  — "Assert.ThrowsAny() Failure: No exception was thrown" (ConnectorSendDeadlineTests.cs:413). Re-run in isolation
  three times: red, red, red (≈0.8 s each) — deterministic, not a flake. The total 391 = 360 (U2a tip) + 31 (U2b on
  main), so the count is right and one behaviour changed.
- **Decision:** insert round 4b — an Opus BUILDER diagnoses (bisect against `5c716aa` in `u2a-orig`) and fixes on the
  probe branch before ANY verifier or Codex touches the unit; verifying a red sha wastes both legs. The branch ref
  `u2a-pipe-hardening` stays at `5c716aa` until the probe branch is green. Codex waits for the green sha (a review of
  a sha that will change is a review to redo). Brief: `briefs/U2a-r4b-build.md`.
- 17:55 Concurrency: U14 builder (Opus) + U2a round-4b builder (Opus) = 2 heavy legs. The U2a verifier waits.
- 18:05 Rebase dry-runs (probe worktrees, removed afterwards): **U2c-1** `--onto main cb2ce2f` → clean, 21 of 28
  commits survive (the 7 old U2b commits drop out); **U2c-1 onto the U2a tip `e91293e` → CONFLICT in
  `src/TradeAgent.Gateway/GatewayTypes.cs`** (so the U2c-1 builder resolves that after U2a lands); **U2d** onto main →
  clean (12 commits); U2d onto the U2a tip → clean.
- **Pipeline order (slots, ≤2 heavy):** U2a r4b builder → U2a verifier + Codex → integrate U2a · U14 builder → U14
  verifier + Codex → integrate U14 · then U2c-1 round 4 (long pole; rebases over U2a with the known conflict) and U2d
  round 4 items 1–3 (item 10 after U2c-1 merges) as slots free · then v0.1.2 on the box (needs Nicolas: power + Tailscale).
- Briefs on disk for every remaining leg: `briefs/U2c1-r4-build.md`, `U2d-r4-build.md`, `U14-r4-verify.md`,
  `U14-r4-codex-prompt.txt` (plus the U2a pair). A killed leg is re-briefed from these, not from memory.
- 18:25 **U14 builder reported** (Opus, 219K tokens, 18 min): tip `e22eec6`, suite 382 → **387 green** (75/111/201),
  item 6 done. Two REAL gaps it found while pinning: the membership rule's missing-prefix allowance was unconditional
  (a swap-one candidate under the cap was adopted — RED quoted, fixed in `6e9027c`), and `Identified`'s lock check had
  no test (mutant M3b survived every pre-existing test). Docs: RESUME-HERE now expects `proto=3`; CONTRACTS.md carries the
  protocol-3 sentence. Flagged for the verifier: the allowance now reads THIS instance's `_cap`, so a temp from a
  larger-capped build is refused where `a8b3fb0` adopted it (safe direction, unmeasured).
- 18:27 Pinned `u14-verify-r4` and `u14-codex-r4` (detached, `e22eec6`); Codex `gpt-5.6-sol` launched detached on the
  codex worktree, output → `records/codex-U14-r4.txt`; U14 verifier (Opus) dispatched with `briefs/U14-r4-verify.md`.
  Heavy legs: U2a r4b builder + U14 verifier = 2.
- 19:55 **U2a round-4b builder reported** (Opus, 234K tokens, 26 min): the red test was U2a's OWN (red at `5c716aa`
  too) — the fixture assumed 400 × 512 KiB RPCs would still be draining at 2 s; this Mac drained them in ≈1 s and the
  cancel-all took a free gate at 0.71 s. Fix in the TEST only (`ConnectorSendDeadlineTests.cs`, 143+/35−): a
  `BridgePeer.ReadingSlowly` peer (≤ 8 KiB per 200 ms — a wall-clock bound, 12.95 s for the 512 KiB order) and the test
  asserts its own premise before the verdict. Three mutants bite, incl. the OLD fixture under the new assertions ("it was
  never queued behind anything"). Tip **`d25dbb4`**, full suite **391 green** (75/108/208). Lesson for the record: the
  round-4 "360 green" claim never covered this test as written — a claim without the run is not a claim.
- 19:58 `u2a-pipe-hardening` moved `5c716aa` → **`d25dbb4`** (24 commits ahead of main; old tip pinned in `u2a-orig`).
  `u2a-verify-r4` + `u2a-codex-r4` detached at `d25dbb4`; Codex `gpt-5.6-sol` launched on the codex worktree
  (→ `records/codex-U2a-r4.txt`); U2a verifier (Opus) dispatched with the updated `briefs/U2a-r4-verify.md` (target 6 =
  the rewritten fixture is a tooth). Heavy legs: U14 verifier + U2a verifier = 2.
- 20:10 **Nicolas answered the six decisions** in one line: no broker, no account, no SSH to Mihael's machine — do all
  that is possible with what we have; Binance could be connected for testing later (intended use is futures). Recorded
  as resolutions in `HANDOFF-2026-09-03.md` §5. Consequences: the live trial stays out of scope; signing deferred with
  the SmartScreen sentence (D7 by deferral); the test box is the target (D6 walk-through on it); monitoring = A on the
  box (D5); the five keyboard minutes fold into the v0.1.2 box session; containment waits for the U12 revision.
- 20:10 Tailscale up; box reachable (`win-state.sh`: uptime 1d 04h, console session active, ATAS running, TradeAgent
  0.1.1 installed, home present, **UI agent NOT running** — restart with `tools/win-agent.sh start` at the box session).
- 20:35 **Codex on U14 `e22eec6` (gpt-5.6-sol, ~60 min): 6 HIGH / 6 MED / 4 LOW** (`records/codex-U14-r4.txt`). The
  HIGHs cluster into two classes the design already names: ownership is a per-call lock, not a lifetime lease (F1);
  recovery adopts temps it cannot prove legitimate (F3 full replacement at cap, F4 semantically invalid envelope →
  unflagged zero, F8 a refused submission's temp adopted after restart — pinned by a test at line 1800, F13 corrupt
  committed bytes still an anchor); plus F5: operator close-all reaches ATAS without `Submitting` (money path, adapter).
  **Triage:** all six HIGH plausibly real by the record's own design; none refuted from the manager's seat. Round 5
  bounce written with the manager's direction on each class (`briefs/U14-r5-bounce.md`): lifetime lease = an exclusive
  handle released by the OS on death; a temp is never adopted as a NEW claim; a legal transition preserves every committed
  record but at most one trimmed; an unparseable file is not an anchor; close-all witnessed before the wire. The box is
  up, so the adapter hunks (F5/F16) get their first compiler on the box via the win tools. The bounce goes to the SAME
  builder (§9.3) once the U14 verifier vacates its slot; the verifier's findings are appended to the same round.
- 21:05 **U14 verifier reported** (Opus, 274K tokens, 31 min): **FAIL 0H/5M/4L**; behaviours held (3 processes × 240
  claims → 80 durable / 0 lost / 0 phantom, one owner; nine builder mutants reproduced; 387 green twice). Its V1 = Codex
  F7 (RESOLVED rationed), V3 = Codex F2 (readers write) with the class fix "degraded = unresolved SAFETY lines"; V5 says
  the builder's cap-direction argument was backwards (safe direction, wrong record). Findings appended to
  `briefs/U14-r5-bounce.md`; the SAME builder resumed by message (§9.3). Heavy legs: U2a verifier + U14 builder r5 = 2.
- 21:20 **Codex on U2a `d25dbb4` (gpt-5.6-sol, ~75 min): 5 HIGH / 6 MED / 3 LOW** (`records/codex-U2a-r4.txt`).
  Triage: F1 (effective id `RequestId ?? Id` unvalidated) and F11 (prerequisite Orders RPCs before the emergency frame,
  and the "agent leg" test bypasses the gateway) are U2a's own files → round 5. F2 is a CLASS defect: per-chunk progress
  budgets made the 35 s drain bound fictional and disposal never re-awaits the cancelled handler → decision: a whole-frame
  ceiling the drain derives from, and disposal re-awaits so UNKNOWN is recorded before the store closes. F5 (agent
  close → `Place`, no fast path — the record's known gap, single close too) and F6/F8 (sweep replay re-sweeps the
  current book; idempotency only for Place) live in `TradingGateway.cs`/the store, which U2c-1 round 4 is reworking →
  moved into `briefs/U2c1-r4-build.md` as class C (C1 intent through `ITradingConnector`, C2 composite persisted before
  effects, replay returns the stored outcome) and recorded as HIGH-open-with-owner at U2a's integration. F10 (reserved
  session accepted on hello frames) is mandatory despite LOW. The box being up turns F12 ("NOT VERIFIED on Windows")
  into an on-box test run in round 5. Bounce: `briefs/U2a-r5-bounce.md`; the SAME builder is resumed when the U2a
  verifier vacates its slot.
- 21:35 **U2a verifier reported** (Opus, 256K tokens, 33 min): **FAIL 2H/1M/1L**. V1 = Codex F1 PROVEN (a 203-char
  `ClientOrderId` reached the fake broker; a forged `op-…` id became a live idempotency key). V2 NEW: emergency
  cancel-all on a stalled bridge with a FREE gate → 10005 ms, non-owner wording, dead connection left up — decision:
  one end-to-end emergency deadline for the caller's wait, UNKNOWN + the owner sentence on expiry, the connection's fate
  by liveness. V3: ordinary `SendOutcome` sentences pinned by wall-clock only (M14 survived). Everything targeted held
  with numbers (2002 ms emergency vs 9602 ms ordinary; saturation kept; seven spellings refused; 391 green, 0 flakes).
  Findings appended to `briefs/U2a-r5-bounce.md`; the SAME builder resumed. Heavy legs: U14 builder r5 + U2a builder
  r5 = 2. Both Codex worktrees (`u14-codex-r4`, `u2a-codex-r4`) and the verify worktrees stay until integration (the
  probe branches hold reusable tests).
- 22:40 **U14 builder round 5 reported** (Opus, 491K tokens cumulative, 62 min): tip **`6a40fa7`** (14 commits), suite
  387 → **417 green** (75/111/231) on the Mac AND on the Windows box (first on-box run for this unit); the four adapter
  hunks compiled on the box against real ATAS (5 warnings, 0 errors); every finding real, each with a RED and a biting
  mutant; two survivors declared unreachable/no-effect (MF4b, MV9); F9's disconnect half refuted with a code reason and
  an adjacent "unproved hello" peer flagged; the F8 residual named. V5 subsumed (F8 removed the `_cap` dependency).
- 22:45 Round-5 verification: `u14-verify-r5` + `u14-codex-r5` pinned at `6a40fa7`; Codex DELTA re-review (fallback form
  §4.2: fresh read-only audit naming the 16 prior findings, `PRIOR n — FIXED|NOT FIXED|PARTIAL` + new findings only)
  launched → `records/codex-U14-r5.txt`; the round-4 verifier resumed with `briefs/U14-r5-verify.md` (targets: the two
  classes as implemented, the survivors, the class fix both ways, F9 partial, the harness again). Heavy legs: U2a builder
  r5 + U14 verifier r5 = 2.
- 23:20 **Codex delta re-review of U14 `6a40fa7`** (gpt-5.6-sol, ~30 min, `records/codex-U14-r5.txt`): 13 of 16 priors
  FIXED; PARTIAL: 5/16 (the adapter's close-all refusal has no executable gate — needs the ATAS stub on the box) and 9
  (v2 peer events gated, but mismatch returns true and a later hello clears it). New: **F17 HIGH** — an I/O-unreadable
  committed file is treated as ABSENT and replaced (the I/O sibling of the parse-failure fixes); F18/F19 rotation hides
  an unresolved safety event / diagnostic-only sidecar mislabelled historical; F20 events trusted before the hello;
  F21 a stopped adapter reacquires the lease via its still-subscribed handler; F22 LOW. Round 6 bounce written
  (`briefs/U14-r6-bounce.md`) with one rule per class: absent = FileNotFound only; degraded state survives rotation;
  protocol compatibility is connection-level and a mismatch poisons the connection; StopBridge releases and unsubscribes;
  the adapter gate gets written against the ATAS stub on the box. Waits for the round-5 verifier, then the same builder.
- 23:45 **U2a builder round 5 reported** (Opus, 506K tokens cumulative, 71 min): tip **`0909ada`** (10 commits, one per
  finding), suite 391 → **421 green** (75/108/238) on the Mac and IDENTICAL on the Windows box (the named-pipe classes
  measured on Windows for the first time — F12 closed). Every finding real with a mutant; F4 measured (a peer that
  accepted 2048 B at 1 KiB/800 ms was dropped); red-first INVERTED on V2/F11/F2 (disclosed). **Rulings:** `FrameTimeout`
  30 s whole-frame ceiling ACCEPTED (worst-case shutdown with an order in flight 35 → 55 s; abandoning an unsettled
  request is the worse failure); rename `EmergencyGateWait` → `EmergencyDeadline` ACCEPTED (2 s unchanged, now bounds
  gate + write + reply). `u2a-pipe-hardening` moved `d25dbb4` → `0909ada` (34 ahead of main).
- 23:50 Round-5 verification: `u2a-verify-r5` + `u2a-codex-r5` at `0909ada`; Codex delta review launched
  (`records/codex-U2a-r5.txt`, priors 5/6/8-gateway marked DEFERRED-BY-DECISION); the round-4 verifier resumed with
  `briefs/U2a-r5-verify.md` (own red-first probe against `d25dbb4` for one inverted item). Heavy legs: U14 verifier r5 +
  U2a verifier r5 = 2; U14 builder r6 queued behind the U14 verifier.
- 23:55 **Codex usage limit hit** mid-way through the U2a round-5 delta review (99K tokens in, no output file; the CLI
  printed "You've hit your usage limit … try again at Sep 4th, 2026 2:34 AM"). The U14 round-5 delta had completed
  before the limit. Decision: **no same-model substitute** for the U2a delta (the U2a verifier already IS an independent
  Opus leg on the same diff; a second Opus read adds no cross-model property) — the U2a integration WAITS for the Codex
  reset; a wake-up is armed for 02:36 and the delta re-review is re-launched then (same prompt file
  `briefs/U2a-r5-codex-prompt.txt`). Skip-or-wait logged per §4.5: WAIT. The U14 round-6 delta review will also need
  Codex after the reset.
- 23:42 **U14 verifier round 5 reported** (Opus, 425K tokens cumulative, 32 min): **FAIL 1H/2M/2L**. R1 HIGH = the heartbeat branch of the refused-peer class (one v3-claiming heartbeat from a refused v2 peer sets `_hello` and `ReconciliationProvable`, clearing the autonomy refusal) → rule: refusal decided once at the top of `Dispatch` for the whole connection. R2 lease released only via `OnStopping`; R3 safety events lost under concurrent appends by refused writers; R4/R5 LOW. Held: lease on real processes incl. SIGKILL, 80/0/0, MF4b + MV9 confirmed, the unproved-hello item closed. Round-6 bounce completed; SAME builder resumed. Heavy legs: U2a verifier r5 + U14 builder r6 = 2.
- 00:10 **U2a verifier round 5 reported** (Opus, 377K tokens cumulative, 37 min): **FAIL 1H/2M/1L**. F-A HIGH: the operator's Close All stays on the ordinary deadline (9759 ms vs the agent's 2018 ms) because the scope opened only in the pipe server → moved to U2c-1 class C3 (gateway-level scope in the press rewrite); recorded HIGH-open-with-owner at integration (`main` has no fast path for anyone today). F-B liveness keys on frames-in only (zero bytes accepted + heartbeats → kept 6/12); F-C mutant W3 survives (read failure → NothingWritten → a second real order); F-D read wording. Held: both round-4 exploits refused; own red-first for V2 (RED at d25dbb4, GREEN at 0909ada); 421 green; 13/14 mutants bit. U2a round 6 = F-B/F-C/F-D (`briefs/U2a-r6-bounce.md`), SAME builder resumed. Heavy legs: U14 builder r6 + U2a builder r6 = 2. Codex delta for U2a will cover r5+r6 in one run after the 02:34 reset.
- 00:59 **U14 builder round 6 reported** (Opus, 711K tokens cumulative, 76 min): tip **`f8a724c`** (13 commits), Mac **432 green** (75/111/246); every finding real with a biting mutant; PRIOR 5/16 CLOSED — the money-path gate ran on the box against the ATAS stub both directions; R3 needed three attempts (.NET FileStream append is not the kernel's append; per-writer files lose nothing in five runs). Deviation ACCEPTED by the manager: the mismatched peer keeps `return true` (false → Drop → clears `_incompatible` by design); the peer is parked unread — the verifier checks it holds no resource and does not block a fixed bridge.
- 00:59 **BOX INCIDENT (process failure, manager's).** Both builders pushed to the same `C:\ta\repo`; `win-push.sh` deletes src/tests/packaging/tools before unpacking; the tree was replaced under each leg repeatedly and once WIPED by the U14 builder when the mixed state would not resolve. **Every on-box suite figure from both units (U14 r5 417, U14 r6, U2a r5 421 + three classes) is of unknown standing**; the U14 builder withdrew its own; the U2a builder was told to mark its round-5 box figures so. **Rule from now on: box access is serialised by an explicit manager grant, one leg at a time; the leg pushes immediately before each run, verifies the box tree is its own (content hash of changed files or a tip-only marker) and quotes the check; no grant → no win-push/win-run.** Grant now: U2a builder (round 6). Two builders' own tests were silently deleted by a later edit and caught by a mutant reporting no failure — rule: a SURVIVED mutant is evidence only if the test count moved.
- 00:59 U14 round-6 verification: `u14-verify-r6` pinned at `f8a724c`; the verifier resumed with `briefs/U14-r6-verify.md` (F17, the parked-peer deviation, rotation, per-writer sidecars, lease release, test-count integrity). Codex delta for r6 after the 02:34 reset. Heavy legs: U2a builder r6 + U14 verifier r6 = 2.
- 01:11 **U2a builder round 6 reported** (Opus, 622K tokens cumulative, 60 min): tip **`ffa1a3d`** (4 commits), Mac **436 green** (75/108/253), box 436 identical with the tree identity VERIFIED (SHA-256 of four changed files + `.cs` count, single ssh session, re-hashed after) — the first on-box figure that counts; round-5 box figures marked STANDING WITHDRAWN. F-B implemented as **liveness = an answer to a pending RPC** (the bounce's write-progress rule was tried and failed both directions: the kernel accepts a ~100-byte frame into the socket buffer regardless) — RATIFIED; consequence: a bridge that reads but never answers is dropped. F-C W3 bites; F-D read wording. The box caught a Windows-only fixture race (cancel + dispose vs an overlapped write) that crashed the test host — the shape carried as NOT VERIFIED since round 1. Two tests silently deleted by a text slice, restored (caught by a name diff, not a run). `u2a-pipe-hardening` → `ffa1a3d` (38 ahead). Box grant released (nobody holds it).
- 01:11 U2a round-6 verification: `u2a-verify-r6` at `ffa1a3d`; verifier resumed with `briefs/U2a-r6-verify.md` (liveness-as-answer both directions incl. the new consequence, W3, wording, the restored tests, the fixture fix). Codex delta (r5+r6, range d25dbb4..ffa1a3d) after the 02:34 reset. Heavy legs: U14 verifier r6 + U2a verifier r6 = 2.
- 01:29 **U14 verifier round 6 reported** (Opus, 511K tokens cumulative, 29 min): **FAIL 1H/1M/1L**. V1 HIGH: the manager's round-6 ruling (park the refused peer, keep `return true`) is UNSAFE — one server instance, the next is created only after the read loop ends, so a silent refused peer blocks a fixed bridge's connect (timeout 3/3) while the row says reinstall; fix = drop, with `Drop` preserving `_incompatible` for our own refusal (the code already argues it for `_refused`). Ruling WITHDRAWN. V2 MED: `_noted` gated on the canonical sidecar existing → the probe reads a confident zero with five refusals on disk (R3 reopened the flagged-zero class). V3 LOW: the F18 shape is unreachable by this build. Closed: R1, R2 (testable half), R3 (160 events / 0 dropped over four per-writer files), F17 four variants + predicate unsplittable, 432 twice, both restored tests real. Round 7 bounce: `briefs/U14-r7-bounce.md`; SAME builder resumed; no box this round. Heavy legs: U2a verifier r6 + U14 builder r7 = 2.
- 02:52 **Session rate limit killed both legs** (~02:2x, HTTP 429 "session limit, resets 2:50") — the U14 builder at the START of round 7 (nothing lost: no commits, no record section) and the U2a verifier inside round-6 target 1 (record + probes banked). Fifth kill of the program, at TWO heavy legs after ~8.5 h of continuous work — the window is total tokens, and two builders at 500–700K each fill it. Both resumed by message at 02:51 after the reset; the Codex quota also reset (02:36) — U2a delta review (rounds 5+6, range d25dbb4..ffa1a3d) launched → `records/codex-U2a-r6.txt`; the U14 delta waits for the round-7 tip so one run covers rounds 6+7. Heavy legs: U14 builder r7 + U2a verifier r6 = 2.
- 02:56 **U2a verifier round 6 reported** (Opus, 445K cumulative): **FAIL 0H/1M/1L** — first round with no HIGH. F-E MED: liveness-as-answer drops a healthy bridge merely slower than 2 s on a quiet connection (the bridge answers sequentially, so it cannot answer anything while working on our emergency; `BridgeProtocol.cs` names the legitimate cause). **Decision:** two bounds, two meanings — `EmergencyDeadline` 2 s bounds only the caller's wait; liveness uses the EXISTING ordinary RPC deadline (10 s) as grace, late answers are kept and recorded; cost = a wedged heartbeating peer detected at ≈10 s instead of 2 s, stated in the record. F-F LOW: record said "restored verbatim" for a strengthened test. Held: 0/12 kept for the wedged peer; W3 RED with the whole chain; 11/11 mutants bit; 436 green. Round 7 bounce `briefs/U2a-r7-bounce.md`; SAME builder resumed with the box grant for one verified run. Heavy legs: U14 builder r7 + U2a builder r7 = 2.
- 03:02 **Codex delta on U2a rounds 5+6** (gpt-5.6-sol, `records/codex-U2a-r6.txt`): 13 priors FIXED, 4 DEFERRED-BY-DECISION (5/6/8-gateway/F-A), PARTIAL 4 (chunk-granular progress — accepted as the stated residual), NOT FIXED 8-CLI (the CLI overpromises same-id replay for every op), 12/14 (record lines stale). New: **C1 HIGH** — the emergency budget is captured before the gate wait and restarted for the write (≈4 s possible) → one clock; C2 = F-E; C3 the 55 s drain is a literal, not derived from live deadlines; C4 both-ids-null frame closes the connection without INVALID_REQUEST; C5 ms-equality liveness. All appended to `briefs/U2a-r7-bounce.md` and sent to the builder mid-round.
- 03:29 **U14 builder round 7 reported** (Opus, 777K cumulative, 37 min): tip **`bb5f53c`** (4 commits), Mac **438 green** (75/111/252). V1 both halves: mismatch returns false AND `Drop` keeps `_incompatible` when our own refusal closed the connection (MD1 RED 8/23; the repaired bridge connects). V2 gated on the whole per-writer set (MV2/MV2d RED; MV2b/MV2c each survive alone because each blocks the other — the pair pins the boundary). V3 pinned as an invariant. A real flake fixed (a bridge dialling while the single pipe instance recycles → `Redial`). **Decision on the named edge:** a later unrelated disconnect must NOT clear `_incompatible` before a compatible hello arrives — keep it as `_unauthenticated` is kept (round 7b, one item, same builder). Then verify r7 + Codex delta over rounds 6+7+7b. Heavy legs: U2a builder r7 + U14 builder r7b = 2.
- 03:40 **U14 builder round 7b reported**: tip **`4de7c25`**, Mac **439 green** (75/111/253, integration run twice). `Drop` no longer clears `_incompatible` at all — only an accepted hello does (two unrelated disconnects driven in the test; MD1 RED 9/23, MD1b RED only on the new test, MD1c pins the clearing half). Round-7 verification: `u14-verify-r7` + `u14-codex-r7` at `4de7c25`; Codex delta over rounds 6+7+7b launched (`records/codex-U14-r7.txt`); verifier resumed with `briefs/U14-r7-verify.md`. Heavy legs: U2a builder r7 + U14 verifier r7 = 2.
- 03:41 **U2a builder round 7 reported** (Opus, 729K cumulative, 44 min): tip **`a974142`** (6 commits), Mac **451 green** (75/108/268), box 451 identical with the tree proven (SHA-256 on five files + count, before and after, one ssh session). C1 one clock: 3.40 s → 2.0 s (needed an oversized emergency frame and a peer that drains then stops — a ~100-byte frame only ever measures the gate); F-E grace = the ordinary deadline, late answers exposed as `LateAnswers`/`LateAnswerReceived` (unconsumed — U2c-1's); C3 drain derived (a 100 s worst path no longer drained at 55 s); C4; PRIOR 8 CLI pinned per op; records corrected. **Rulings:** integration suite 3 m 06 s → 5 m 19 s ACCEPTED as the decision's price (future lever: a slow-test category, never shorter deadlines); the caller's 2 s sentence must LEAD with the outcome ("not confirmed — check ATAS") and carry the connection state as detail — the verifier checks. `u2a-pipe-hardening` → `a974142` (44 ahead). Round-7 verification: `u2a-verify-r7` + `u2a-codex-r7`; Codex delta launched (`records/codex-U2a-r7.txt`); verifier resumed with `briefs/U2a-r7-verify.md`. Heavy legs: U14 verifier r7 + U2a verifier r7 = 2.
- 03:51 **Codex delta on U2a round 7** (`records/codex-U2a-r7.txt`): 8/11 priors FIXED; PRIOR 12/14/F-F "NOT FIXED" REFUTED — the corrections are on `main` (`records/U2a.md` lines 71–73, the STANDING WITHDRAWN note, the verbatim correction) and the branch's copy of the record is the 2026-09-03 snapshot Codex read. New: **F1 HIGH** — the one-clock fix was applied per `Rpc`, so prerequisite reads and every cancel-all leg restart the 2 s budget (three 1.9 s legs → 5.7 s); rule: one absolute deadline per emergency OPERATION carried by the scope, per-leg outcomes reported at the deadline, legs never skipped silently, concurrent legs where possible. F2 MED — the drain covers one connector call + a literal 5 s, not a composite handler → derive from the composite worst case, disposal never returns unsettled. F3 LOW late-answer race. Round 8 bounce written (`briefs/U2a-r8-bounce.md`); waits for the round-7 verifier's findings and slot.
- 03:55 **Codex delta on U14 rounds 6–7b** (`records/codex-U14-r7.txt`): 10/17 priors FIXED. Real → round 8: PRIOR 17 (DirectoryNotFound still = absence; the rule allows FileNotFound only), PRIOR 21 (stopped-guard race with lease disposal), F26=R2 (teardown without `finally` keeps the lease), F27+V3 (rotation deletes the old generation before carrying the safety state forward — the "cannot be produced" claim is false for a crash in that window), F28 (an unreadable sidecar reads as clean), F23 (a silent compatible peer monopolises the single pipe instance — heartbeat timeout must drop it), F29 wording. Refuted with reasons: PRIOR 5 (gate evidence is on main), R4 (documented macOS residual), R5 (record on main), F24 (instance scope by design), F25 (degraded = owner's sidecar, noted = whole set — the round-6 verifier's accepted reasoning; my decision text was imprecise). Bounce: `briefs/U14-r8-bounce.md`, waits for the round-7 verifier.
- 03:59 **U14 verifier round 7 reported** (Opus, 562K cumulative, 18 min): **FAIL 0H/2M/0L**; all three round-6 findings closed (MD1 RED 12; MV3 both rows). V4 MED: `StatusDetail` prefers the OLDER refusal — a v2 refusal outranks a newer authentication failure (class: nothing asks whether a kept refusal still describes the present peer) → rule: the row describes the peer that is there now. V5 MED: MV2c bites a single probe the builder never built (a refused second bridge would drop `SupportsClientOrderId`). Round 8 bounce completed; SAME builder resumed. Heavy legs: U2a verifier r7 + U14 builder r8 = 2.
- 04:05 **U2a verifier round 7 reported** (Opus, 522K cumulative, 23 min): **PASS WITH LOW 0H/0M/1L** — first round with no HIGH/MED. F-G LOW: the caller's 2 s sentence leads with connection state (record IS UNKNOWN at 2040 ms) → outcome-first wording in round 8. Held independently: C1 2005 ms with the write reached; F-E drop at 10.05 s 12/12 while callers answered at 2000–2004 ms; nothing unsettled during the grace; six mutants bit. Round 8 (Codex F1 one deadline per OPERATION, F2 composite drain, F3 race, F-G wording) → SAME builder resumed. Heavy legs: U14 builder r8 + U2a builder r8 = 2.
- 07:52 **Sixth rate-limit kill (~06:xx, resets 07:50)** took both round-8 builders; on resume the agent message channel was gone (process restart), so both are RE-BRIEFED from disk as fresh Opus legs — the case the on-disk briefs and per-finding commits exist for. Banked: **U2a r8** = 5 commits on `a974142` (tip `5624cd1`, tree clean; record table complete: F1 one deadline per operation, F2 composite drain, F3, F-G, refutations; last message "Mac 455 green, no test lost") — only the verified box run and the closing report remain. **U14 r8** = 5 commits on `4de7c25` (tip `877f5e0`: PRIOR 17, F28, F27+V3 (MF27b SURVIVED, recorded), V5, F29) + uncommitted F23 work in `AtasConnector.cs`/`BridgeRoundTripTests.cs` ("RED … now the drop"); PRIOR 21, F26/R2, V4, the five refutations still to confirm. Heavy legs: U2a finisher + U14 continuation = 2.
- 08:03 **Codex delta on U2a round 8 (`5624cd1`)**: 10/11 priors FIXED, **0 HIGH / 2 MED / 3 LOW**. PRIOR 2 still open (drain assumes 3 serial RPCs; a cold placement issues 5; the override can shorten below the composite). F1 MED: the per-leg outcome vocabulary is not 1:1 with the record (a definite rejection and a never-sent leg both read "sent-not-confirmed"). F2–F4 LOW. Round 9 written for a FRESH builder (`briefs/U2a-r9-bounce.md`): four outcomes each mapping to one record state; the chain length from the real maximum; the override may only lengthen. The fresh verifier covers rounds 8+9 on the round-9 tip. Dispatch after the round-8 finisher reports (its box run on `5624cd1` stands for the round-8 code).
- 08:11 **U2a round-8 finisher reported** (fresh Opus, 138K tokens, 18 min): the killed builder's claim CONFIRMED — Mac 455 green (75/108/272, 5 m 39 s); box 455 identical, pipe classes 52/52, SHA-256 of all seven changed files box-before ≡ box-after ≡ Mac, 88 source .cs; test-name diff 0 removed (+4 = the round's tests). Correction: "0 warnings" was an incremental build — `--no-incremental` shows CS8619 (nullable variance in `RunLegs`, new in round 8) → round 9 F5; **rule: build gates are `--no-incremental`**. `u2a-pipe-hardening` → `5624cd1` (49 ahead). Fresh round-9 builder dispatched (`briefs/U2a-r9-bounce.md`). Heavy legs: U14 builder r8 + U2a builder r9 = 2.
- 09:01 **U14 round 8 complete** (fresh Opus, 298K tokens, 68 min): tip **`10fa21f`**, **454 green** (75/112/267), 0 warnings on `--no-incremental`. F23 heartbeat-timeout drop (MF23b survived until the test's beat was slower than the poll — rewritten, RED); V4 markers stamped with a counter, the row takes the newer and consults the machine first; PRIOR 21 + F26 as one structural fix (lock + `finally`) moved into a testable `AdapterTeardown` driven against a real witness — **the adapter file itself was only syntax-parsed: box compile + `tools/atas-gate` required before those two close**; MF27b now bites (a `_writeSidecar` seam observes the ordering); five refutations written. Round-8 verification: `u14-verify-r8` + `u14-codex-r8` at `10fa21f`; Codex delta launched (`records/codex-U14-r8.txt`); fresh verifier dispatched with a ONE-run box grant (bridge compile + atas-gate, hash-verified). Heavy legs: U2a builder r9 + U14 verifier r8 = 2.
- 09:12 **Codex delta on U14 round 8 (`10fa21f`)**: 13/16 priors FIXED, **0 HIGH / 3 MED / 0 LOW** — each MED is a leftover edge of a class fixed once: F30+PRIOR 27 the rotation staging file hides the unresolved marker in a crash window and the carry-forward has no durability flush; F31+PRIOR 28 existence/enumeration probe failures still collapse to "no notes"; F32 an older refusal marker outranks the derived state of a NEW silent peer (the V4 class); PRIOR 29 wording. Round 9 written for a fresh builder (`briefs/U14-r9-bounce.md`) with one demand added: a class-closure argument per item (enumerate the operations of the class, show each is covered) — three rounds of edge-chasing is the signal to close the class, not the instance (§9.10). Waits for the round-8 verifier's findings.
- 09:17 **U2a round 9 complete** (fresh Opus, 371K tokens, 65 min): tip **`088c059`** (7 commits), **466 green** (75/108/283), 0 warnings `--no-incremental`; six items red-first + mutants (F1: five mutants, one per mapping arm); the longest ordinary chain COUNTED over the real pipe = a cold buy/sell, five serial calls; drain = max(5 × worst path, emergency budget + worst path) + settle. **Rulings:** (1) shutdown ceiling 265 s at shipped values ACCEPTED — paid only with an order genuinely in flight; the alternative is an abandoned DISPATCHING order; must be stated in CONTRACTS.md and surfaced by the UI ("waiting for an order to settle") → U6 item; (2) the fifth outcome word `sent-still-working` ACCEPTED (a resting close leg is sent, answered, neither confirmed nor unknown) — five words, 1:1 with the record; (3) `TreatWarningsAsErrors` DEFERRED to after v0.1.2 — the box's release build showed 5 warnings in the adapter compile and would go red. `u2a-pipe-hardening` → `088c059` (56 ahead). Verification of rounds 8+9: `u2a-verify-r9` + `u2a-codex-r9` at `088c059`; Codex delta launched; fresh verifier dispatched (`briefs/U2a-r8-verify.md` with its rounds-8+9 extension). Heavy legs: U14 verifier r8 + U2a verifier r8+9 = 2.
- 09:28 **Codex delta on U2a round 9 (`088c059`)**: **2 HIGH / 2 MED / 1 LOW**. F1+PRIOR 2 HIGH — the drain (third time): a close-all wave serialises four ordinary placements through the dispatch gate → real path E+4W vs the formula's max(5W, E+W) → rule: a per-handler serial-depth TABLE in CONTRACTS.md, drain = max over it from live values. F2 HIGH — the close's offsetting Place gets fresh budgets → DEFERRED-BY-DECISION to U2c-1 C1 (the round-4 rule: a Place never takes the fast path; U2c-1 makes close legs Close intents). F3 MED — vocabulary drifted to six values → EXACTLY five per leg, `nothing-to-do` only as a whole-operation result, table in CONTRACTS.md. F4 MED — classification by record state → by the connector's transport tri-state (the CLI's), the class fix. PRIOR F5 "no zero-warning build recorded" refuted (the record on main quotes it). Round 10 bounce written (`briefs/U2a-r10-bounce.md`); waits for the rounds 8+9 verifier.
- 09:51 **U14 verifier round 8 reported** (fresh Opus, 324K tokens, 50 min): **FAIL 3H/1M/0L** — class: each round-8 guard proved only in the state its author built. R8-1 = Codex F30 in the ORDINARY state (ERROR in the current log lost across a real SIGKILL in the rotation window); R8-2 the teardown guard covers 1 of 4 witness write sites (frame-loop writes re-lease after Stop); R8-3 the heartbeat drop only when the idle poll wins (a 200 ms-ping peer is never dropped); R8-4 a half-moved guard survives the unit tests. Box run (identity-checked): bridge compiled (5 warnings, 0 errors), atas-gate PASSED both directions — the adapter call sites bind. Round 9 bounce completed; fresh builder dispatched with a ONE-run box grant at the end of the round. Heavy legs: U2a verifier r8+9 + U14 builder r9 = 2.
- 09:54 **U2a verifier rounds 8+9 reported** (fresh Opus, 349K tokens, 36 min): **FAIL 0H/2M/3L**. F-1 = Codex F4 measured through the real pipe (a pre-send refusal reads sent-not-confirmed + UNKNOWN and pauses execution; the route runs through `TradingGateway.cs:660-665`); F-2 disposal can return with a DISPATCHING row unsettled and unlogged when the connector honours cancellation — settlement is the gateway's (U2c-1's startup sweep + cancellation path) → deferred with owner, U2a's half = never return silently; F-3 catch-all in Classify; F-4 = Codex F1; F-5 the sweep test never exercises a mixed answer. Everything on the deadlines held (2001 ms; idle shutdown 1 ms; 13/13 mutants). Round 10 bounce completed; fresh builder dispatched with a ONE-run box grant at the end (round 9 never ran on the box). Heavy legs: U14 builder r9 + U2a builder r10 = 2.
- 11:19 **U2a round 10 complete** (fresh Opus, 410K tokens, 84 min): tip **`c00fa08`** (7 commits), Mac **485 green** (75/108/302), 0 warnings; drain = max over the handler table (in CONTRACTS.md and `HandlerPaths`; close-all = E + L·W; shipped value unchanged at 255 s); five words exactly; classification by the connector's transport result (a pre-wire refusal → not-sent, mutant RED both ways); disposal never returns silently. Box run at `12e2c65` (identity proven): 1 pipe-class failure = a real fixture coupling (a flagged request from an earlier sweep refuses every later leg) fixed in `c00fa08` — NOT yet run on the box; plus `An_emergency_a_busy_bridge_has_not_answered_yet…` (round 7) failed in the full Windows run and passed alone — uncharacterised. Routed to U2c-1 with measurements: F2, F-1's record half (one never-sent leg paused the other four), F-2's settlement half. `u2a-pipe-hardening` → `c00fa08` (63 ahead). Round-10 verification: `u2a-verify-r10` + `u2a-codex-r10`; Codex launched; fresh verifier dispatched WITH the box grant (full suite twice + pipe classes at c00fa08, the flake characterised). Heavy legs: U14 builder r9 + U2a verifier r10 = 2.
- 11:20 **Box-grant overlap risk (manager's, accepted):** the U14 round-9 builder holds an end-of-round grant and the U2a round-10 verifier holds a one-session grant; with no message channel to either, their box sessions may overlap. Both verify tree identity by hash BEFORE and AFTER their runs, so a collision is DETECTABLE (a mismatch invalidates that run and the leg reports it), not prevented. If either reports a mismatch, its box figures are withdrawn and the run is repeated under a fresh single grant.
- 12:53 **Seventh rate-limit kill (~12:2x, reset 12:50)**: U14 r9 builder died at the last item's tests — but round 9 is fully committed (8 commits, tip `e113c4c`, record complete incl. the identity-checked box compile+gate and R3 sweep; `_witness in the adapter: 0`); the U2a r10 verifier died before writing anything. **Codex delta on U2a r10 (`c00fa08`)**: 12/15 FIXED; PRIOR 2 + V-F1 "not fixed" = the gateway halves deferred to U2c-1 C4 (Codex counts them); PRIOR R9-F4 partial (definite arms bypass transport); **F2 MED — a fully sent leg can be classified not-sent when the caller cancels during the reply wait (no transport state recorded until a reply) — the dangerous direction**; F1 MED (cancel before gate → PossiblyWritten); F3 LOW (table omits four ops). Round 11 written (`briefs/U2a-r11-bounce.md`): null transport → sent-not-confirmed, never not-sent; the r11 builder gets the ONLY box grant (full suite twice + the flake test thrice). Manager runs the U14 r9 gate itself (build + suite, mechanical) while a fresh U14 verifier + Codex delta start on `e113c4c`. Heavy legs: U2a builder r11 + U14 verifier r9 = 2.
- 12:55 **Manager's gate on U14 round-9 tip `e113c4c`** (run by the manager in `u14-verify-r9` before the verifier's own): `dotnet build --no-incremental` 0 warnings 0 errors; full suite **477 green** (75/112/290), exit 0. The builder's final-gate section (killed mid-report) is thereby confirmed by a run, not a claim.
- 13:07 **Codex delta on U14 round 9 (`e113c4c`)**: **4 HIGH / 5 MED / 0 LOW**, PRIOR 27/28/31 still NOT FIXED — the three classes (unreadable ≠ empty; the rotation window; the teardown door) survived the class-closure round because each still has many sites. Decision (§9.10): **round 10 is STRUCTURAL** — one function touches the sidecar filesystem with one failure mode and every consumer reads a snapshot; concurrent change detected by listing before/after; rotation = atomic renames over a snapshot with the carry written and flushed FIRST, no staging file; teardown = a locked three-state machine (`Started()` legal only from Stopped); the row always has a current status. Bounce: `briefs/U14-r10-bounce.md`. Waits for the round-9 verifier, then a fresh builder with a box run at the end.
- 13:50 **U14 verifier round 9 reported** (fresh Opus, 343K tokens, 56 min): **FAIL 1H/2M/2L** — R9-1 = Codex F34 measured (Rotate deletes a generation it could not read; chmod 000 and SIGKILL); R9-2 `_noted` correct only by evaluation order; R9-3 an untested precedence cell; R9-4/R9-5 LOW, same classes. Class: unreadability must be a VALUE the caller handles — exactly directive 1 of the structural round. Closed: R8-2/3/4; 477 twice. Round 10 (structural) bounce completed; fresh builder dispatched; box run at its end (the U2a r11 builder holds the grant now — overlap detectable by hash). Heavy legs: U2a builder r11 + U14 builder r10 = 2.
- 14:09 **U2a round 11 complete** (fresh Opus, 358K tokens, 76 min): tip **`120c739`** (5 commits), Mac **491 green** (75/108/308), 0 warnings; box: 491 twice, pipe classes 116/116, identity 8 hashes + count OK before/after. F2 closed by an ATTEMPT marker (stamping at `Sent` would beat `ReplyReceived` — mutant shown), F1, R9-F4 (one mutant per definite arm), F3 (rows found by asking the dispatcher). **The round-7 Windows flake = a test premise defect** (it read `IsConnectedAsync` after its own teardown; cancelling a chatter mid-write drops the connection by design) — fixed; five green runs on the box. Deviation: a bare null transport stays `not-sent` for three legs that never started a mutation (failed target resolution, parked-for-approval, nothing-to-close) — to the verifier as a target. Flagged LOW: `_pending` leaks an entry when a caller cancels an emergency. `u2a-pipe-hardening` → `120c739` (68 ahead). Verification rounds 10+11: `u2a-verify-r11` + `u2a-codex-r11`; Codex launched; fresh verifier dispatched (no box needed). Heavy legs: U14 builder r10 + U2a verifier r11 = 2.
- 14:10 **U2a integration dry-run**: `u2a-pipe-hardening` (`120c739`, 68 commits) rebases onto today's `main` CLEAN (probe tip `b5ce0ef`, removed); the only non-code files the branch touches are `docs/CONTRACTS.md` and the one-line pointer at the top of `records/U2a.md` (merges with main's version). Integration is mechanical once the rounds 10+11 verdict and the Codex r11 delta are clean: rebase → `--no-incremental` build + full suite → `branch -f` → ff-merge → push → CI → records/BUILD-STATUS/RESUME-HERE (a light scribe leg writes the docs from the record).
- 14:17 **Codex delta on U2a round 11 (`120c739`)**: 10/11 priors FIXED, **0 HIGH / 1 MED / 1 LOW**. The MED = PRIOR F2 counted NOT FIXED because of the builder's argued deviation (attempt marker instead of stamping `PossiblyWritten` at `Sent`; a bare null still maps to `not-sent` for three legs that never started a mutation) — the rounds 10+11 verifier is judging exactly that with probes; refuted with the mechanism's reason if it holds, round 12 if a null-after-attempt path exists. LOW: the flake-rewrite test captures its verdict before the liveness judge runs (a `PeerAnsweredSince` regression would not be observed by it) — batch. Integration of U2a waits only for the verifier.
- 14:47 **U2a verifier rounds 10+11 reported** (fresh Opus, 363K tokens, 37 min): **FAIL 0H/2M/4L**. The deviation JUDGED IN THE BUILDER'S FAVOUR with both halves measured (the three empty-transport legs start zero mutating calls; null-after-attempt unreachable in both shipped connectors; blind null → sent-not-confirmed turns five true tests RED) → Codex r11's MED refuted. F-1 MED: the `handlers_did_not_finish` sentinel is inside a branch that is empty once the agent disconnected (3 ms disposal, DISPATCHING row, no log). F-2 MED: `not-sent` is an assurance a third-party connector must opt into — rule: a dispatched MUTATING step with a null transport is sent-not-confirmed by the pipe server's own knowledge; the obligation on the interface + CONTRACTS; explicit null in JSON; gateway-side marking → U2c-1. L-1..L-4 + Codex's LOW → the same round. Held: 21 rows ↔ 21 ops, 13/13 mutants, 491, 0 removed. **Round 12 = the last before integration** (`briefs/U2a-r12-bounce.md`); fresh builder with the box grant. Heavy legs: U14 builder r10 + U2a builder r12 = 2.
- 15:18 **U14 structural round 10 complete** (fresh Opus, 440K tokens, 87 min): tip **`01fcd60`** (6 commits), **517 green** (75/112/330, from 477), 0 warnings. One `ReadSidecarSet()` with one try/catch and before/after listing (5 mutants RED); rotation = four acts over a snapshot, carry first, no staging file, no `File.Delete` (crash points = a one-sentence subset argument; real SIGKILL 40/40); teardown = a locked three-state machine (MP21-half now deterministic RED); the row always derives a status (the verifier's MV9-a now RED in the shipped suite). Box (hash-verified): bridge compile 5 warnings (the same five as round 9) 0 errors; atas-gate PASSED 6/6. Stated survivors: flush-to-platter unverifiable on APFS; the lock on Running→Stopping not load-bearing (MR10-4d) — for the verifier. Three test methods renamed with reasons. Round-10 verification: `u14-verify-r10` + `u14-codex-r10`; Codex launched; fresh verifier dispatched (no box). Heavy legs: U2a builder r12 + U14 verifier r10 = 2.
- 15:19 **Integration order measured**: U14 (`01fcd60`) rebases onto main CLEAN (85 commits) but onto the U2a tip CONFLICTS in `AtasConnector.cs` (at U14's round-8 row-precedence commit); both touch `docs/CONTRACTS.md` (clean). The handoff's "disjoint files" stopped being true in round 5 of each unit — both rewrote the connector's read loop and status logic. **Rule:** the first unit to pass its final verify + Codex integrates; the second is rebased over the new main by a builder leg (both units' guards preserved, contradictions escalated) and a COMBINATION verify (both probe sets, the liveness-grace-vs-heartbeat interaction) + Codex delta pass before it merges. Brief: `briefs/second-unit-rebase.md`.
- 15:29 **Codex delta on U14 round 10 (`01fcd60`)**: 12/14 priors FIXED; **2 HIGH / 4 MED / 1 LOW**, every one an edge where the old shape leaked back into the new structure: PRIOR 33 `LastDecidingLine` turns an unreadable snapshot into null so `Settled()` can append RESOLVED over an unseen marker; PRIOR 35 overlapping `Stop()` calls publish Stopped while another executes; F39 the snapshot exposes paths not lines (the probe reopens files); F40 candidates read after the change check; F41 rotation unresumable on Windows after a failed last rename; F42 no stamped status during `AuthGrace`; F43 UTF-16 vs bytes. Round 11 = completing the structure (`briefs/U14-r11-bounce.md`: Unreadable propagates as a value, the snapshot carries lines, candidates inside the snapshot, resumable rotation, idempotent serialised Stop, a stamped connecting status at accept). Waits for the round-10 verifier.
- 15:55 **U14 verifier round 10 reported** (fresh Opus, 299K tokens, 36 min): **FAIL 1H/2M/3L**. R10-1 HIGH: a FIFTH crash point inside act 1 — `FileMode.Create` empties an existing `log.new`; one transient write error destroys the marker and the retry rotates over the hole. R10-2 = Codex F39 at the support package and probe; R10-3 both declared survivors are load-bearing; R10-4..6 LOW (record corrections, dead code, flush policy). Held: 517 twice, 40/40 SIGKILL, 55,927 out-of-process readings with zero clean reads. Round 11 bounce completed; fresh builder dispatched (box run at the end if the adapter/teardown changes). Heavy legs: U2a builder r12 + U14 builder r11 = 2.
