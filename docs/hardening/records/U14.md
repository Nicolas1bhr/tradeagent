# U14 — CoidWitness rewrite durability (the rule-1 write-ahead record)

Branch `u14-coid-witness-rewrite` @ **a8b3fb0** (base 3f1d8f2, 25 commits; messages in `commits-u14-coid-witness-rewrite.md`).
Tier 1. Reconstructed 2026-09-03; the build record (380+ lines), verify record (824 lines), mutation files and Codex raw
outputs were lost. Suite at c167bbf (round 3): **377 green** (75/110/192); round-4 items 1–5 are committed on top;
their suite figure was not captured before the kill — run it.

## Why it exists

GitHub CI (windows-latest) on 3931c10 failed one test, `CoidWitnessTests.The_file_is_never_absent_while_it_is_being_rewritten`
("the temporary file was left behind"): a rename of the temp onto `coid-witness.json` had failed under a concurrent
reader on a contended runner. The same commit passed on the ATAS box and on macOS, and the next CI run passed — a
load-dependent race. The builder established from source what that meant: `Save()` retried only `IOException` for 60 ms,
`UnauthorizedAccessException` escaped on the first attempt, `Submitting` swallowed everything (`MarkWriteFailed`), `Place`
wrapped it in `Guard(...)` and continued to ATAS, and **`EnsureLoaded` never read the temp** — so a claim whose rename
failed lived only in a file no reader opened, and the durable answer to "did this product submit this identifier" was NO
for an order handed to ATAS microseconds later. That is the rule-1 defect; the leftover `.tmp` was the symptom.

## What the branch does

**Round 1 (a159bf4).** Rename seam made injectable; retry `IOException` and `UnauthorizedAccessException` (5 attempts,
20/40/60/80 ms — a judgment, ≈200 ms measured per fully refused rewrite against an 8.42 s worst path and the 10 s RPC
deadline); reader prefers a newer parsing temp; `Submitting` returns `bool`; sidecar `coid-witness.errors.log` +
`LastWriteFailure`; the churn test asserts durability (301 records readable) before the `.tmp` check.
**Round 2 (1107712).** The recovery rule is LINEAGE, not time: the envelope carries `generation` and `predecessor`
(FNV-1a 64 fingerprint of the committed bytes — kept over SHA-256 because it authenticates lineage, not authorship, and the
assembly may not gain a dependency, trap 34); a temp with zero records is never adopted (round 1 had let a newer `{}`
shadow a good file and the next save commit the void); per-writer temp names; post-rename read-back compares fingerprints;
`_readFailed` semantics fixed (a truncated temp with no committed file no longer reads `io:ok`); `io:degraded` in the token;
the probe prints the sidecar; the support package collects `bridge/*.errors.log`; **the adapter edit**: `Submitting` moved
above `lock (_gate)` and `Place` throws `AtasRejectedException` ("nothing was submitted") on false;
`BridgeHello.witness_failure` → `AtasHealth.BridgeRow` DEGRADED naming the file; an import route the builder's own sweep
found (mutant A4) closed.
**Round 3 (c167bbf).** No adoption without a committed anchor; `Submitting` restores its pre-attempt snapshot on failure
(`Identified` deliberately does not); compare-and-swap + rebase + lock file for two writers; absent/unreadable read-back →
false; a candidate may never shrink the record set, two rivals → adopt neither; a superseding commit clears
`LastWriteFailure`; rejected candidates reported once and renamed out of the glob, adopted rewrite deleted once committed,
`_degraded` = unresolved failure with a RESOLVED marker; `Trouble` on the wire, `SupportsClientOrderId = false` while set;
`Fingerprint` public and tested for discrimination (the verifier's mutant collapsing FNV to an 8-bit fold had stayed green).
**Round 4 (496e2f5..a8b3fb0, items 1–5 of 6):** **one owner per witness** — the lock is mandatory (cannot acquire →
`Submitting` false, `Trouble` "another writer owns this witness"), a CAS miss is a refusal, `Rebase` removed (§9.10: two
bridges on one pipe is a misconfiguration, trap 35, and multi-writer support was generating its own race surface);
**bridge protocol bumped to 3** (the write-ahead promise changed; an older v2 bridge still sent orders after a failed
rename and omits `witness_failure` — the app now refuses it, by the established mechanism); the sidecar never rations a
safety event (rotates instead; quota applies to warnings/markers); readers never write (adoption/quarantine only by the
owner at startup under the lock; the writer re-reads the sidecar before RESOLVED); the probe treats a sidecar ending in
RESOLVED as historical.

## Verification history

| Round | Opus verifier | Codex |
|---|---|---|
| 1 | FAIL 1H/4M/6L — zero-record temp shadows a good file; second-writer poisoning; one temp name; adapter edit correct but a directory at the tmp path refuses every order invisibly; sidecar surfaced nowhere | 2H/3M/2L — adapter discards the signal; mtime is not lineage; single lock/temp name; unreadable temp reads ok; sidecar unsurfaced |
| 2 | PASS WITH LOW 0H/4M — `LastWriteFailure` never clears; crash mid-rewrite → permanently degraded; empty-dir import narrowed not closed; lineage checks parent not content | 4H/1M — no-anchor import; refused claim completable by a stray `Identified`; stale second writer erases a claim; unreadable read-back → true; prior-session gap not on the wire |
| 3 | **PASS** 0H/0M/6L — three real processes × 80 claims → 238 durable / 0 lost / 0 phantom; every round-2 MED closed; 19 mutants RED | 5H/2M/1L — rebase interleavings (stale snapshot + rebased lineage; lock timeout falls through UNLOCKED; read-back takes lineage not records; rebase order vs trim), protocol not bumped, sidecar quota suppresses a safety event, concurrent reader marks a good rewrite unresolved, probe treats RESOLVED as active |
| 4 | not run | not run |

## Open at handoff

- Item 6 (LOW batch): superset rule by MEMBERSHIP not count; pin the `Identified` asymmetry (V24) and the mandatory-lock
  behaviour (V20/V26 → "refused without the lock"); keep "an anchorless candidate reads as a flagged zero".
- Verify round 4 (targeted) and integrate; the verifier's three-process harness should now demonstrate REFUSAL of a second
  writer (nothing lost, nothing phantom) rather than merging.
- **Protocol 3**: the box's deployed bridge DLL (`%APPDATA%\ATAS\Strategies`, protocol 2) must be rebuilt and redeployed
  with the next release, and `docs/RESUME-HERE.md` "Verifying what you inherited" must say the expected reading is
  `proto=3`. Until then a new app refuses the old bridge — by design.
- **Compiles only on the box:** two hunks in `AtasStrategyAdapter.cs` (`Place` refusal above `lock (_gate)`; `Describe()`
  `WitnessFailure = _witness.Trouble`, `SupportsClientOrderId = proof.ProvesRoundTrip() && _witness.Trouble is null`) — read by
  two verifiers, compiled against the real witness with ATAS stubbed, never against ATAS. The next release build on the
  box is their first compiler. The cross-process lock was proven on APFS only; Windows sharing violations remain injected
  at the seam; the churn test's `.tmp` assertion stays load-dependent on Windows (if it fails while the durability count
  passes, that says a replace was refused for the whole budget and nothing was lost — relaxing it is the manager's call).
- Consequence ratified by the manager: a machine with no bridge directory (or an unacquirable lock) refuses every order
  rather than trade without a write-ahead record — fail-closed, and the row says why.

## Round 4 — item 6 (build record, 2026-09-03)

Builder leg [1], worktree `…-worktrees/u14-build`, branch `u14-coid-witness-rewrite`, starting sha
`a8b3fb0`. Toolchain `PATH="$HOME/.dotnet:$PATH" DOTNET_ROOT="$HOME/.dotnet"`, .NET 10, macOS.
The Windows box is offline for this leg: the two `AtasStrategyAdapter.cs` hunks are untouched and
uncompiled here, as they were.

### Item 0 — the missing figure at `a8b3fb0`, before anything was changed

Verified by running `dotnet build TradeAgent.sln` → `Build succeeded. 0 Warning(s) 0 Error(s)`
(Time Elapsed 00:00:03.59).

Verified by running `dotnet test TradeAgent.sln` → three projects, all green:

```
Passed!  - Failed:     0, Passed:    75, Skipped:     0, Total:    75 - TradeAgent.FaultTests.dll (net10.0)
Passed!  - Failed:     0, Passed:   111, Skipped:     0, Total:   111 - TradeAgent.UnitTests.dll (net10.0)
Passed!  - Failed:     0, Passed:   196, Skipped:     0, Total:   196 - TradeAgent.IntegrationTests.dll (net10.0)
```

**382 green at `a8b3fb0`** (75 fault / 111 unit / 196 integration), 0 failed, 0 skipped. The round-3
figure in this record was 377 (75/110/192); round-4 items 1–5 added 5 tests (+1 unit, +4 integration).

### Item 1 — the superset rule is MEMBERSHIP, not count (commit `6e9027c`)

**What was actually wrong at `a8b3fb0`.** Round-4 item 5 (`fc91771`) had already replaced the bare
count comparison with `KeepsCommittedRecords`, so the sentence "it compares counts" was no longer
literally true — but the hole it names was still open, one step along. `KeepsCommittedRecords` skipped
a LEADING RUN of committed identifiers unconditionally, on the argument that `Trim` drops the oldest
off the front. `Trim` only removes at the cap. So a candidate under the cap that dropped the first
committed claim and added one of its own passed the check, and adoption dropped the claim, because
the adopted set is what the next save commits.

**RED first.** Verified by running
`dotnet test …IntegrationTests… --filter "…A_candidate_that_swapped_a_committed_claim_for_another_is_ignored"`
at `a8b3fb0` + the new test:

```
Assert.Equal() Failure: Collections differ
Expected: ["TA-1", "TA-2", "TA-3"]
Actual:   ["TA-2", "TA-3", "TA-SWAPPED"]
```

Three records against three, perfect lineage, and `TA-1` was silently dropped.

**The fix.** `KeepsCommittedRecords` → `DropsACommittedRecord(candidate, committed)`, an instance
method (it needs `_cap`) returning the identifier that would be lost, or null. Membership over the
committed identifiers; the missing-prefix allowance is now conditional on the candidate being FULL
(`i > 0 && candidate.Records.Count < _cap` → refuse), because `Trim` removes nothing below the cap.
The "may never shrink the record set" rule is kept as the first check, unchanged in effect. The
rejection line names the identifier instead of printing "3 records against 3", which is the sentence
that made this invisible.

**GREEN.** Verified by running `dotnet test TradeAgent.sln` → 75 / 111 / **198** = 384, 0 failed.

**Mutants (source restored from a `cp` copy each time, then `touch`ed; sha of the copy
`0ad2cd11f89841ae24c5bd5b16895a49e3a99778`):**

| # | Mutant | Result |
|---|---|---|
| M1 | membership body replaced by `return null` — the count check alone | RED: `A_candidate_that_swapped_a_committed_claim_for_another_is_ignored [FAIL]` (1 failed / 76 passed) |
| M1b | the cap condition removed (`if (false)`) — the `a8b3fb0` prefix rule | RED: same test (1 failed / 76 passed) |
| M1c | the trim allowance removed (`if (i > 0)`) — too strict | RED: `A_rewrite_that_trimmed_the_oldest_at_the_cap_is_still_adopted [FAIL]` (1 failed / 76 passed) |

M1c is the both-directions half: the rule refuses a swap AND still adopts the legitimate rewrite that
trimmed at the cap. A tightening that broke recovery on every full witness would fail loudly.

**§9.9 — could a test class or gate catch "compared a count where a set was meant" next time?**
A gate, no: `.Count` in a comparison is everywhere and a lint for it would be noise, and the defect
here was not even a bare count — it was a membership check with an unbounded escape hatch, which no
textual rule distinguishes from the correct one. A TEST CLASS, yes: adoption wants a table-driven
suite over candidate/committed shapes — drop-one, add-one, **swap-one**, hole-in-the-middle,
duplicate, at-cap, under-cap — asserting the committed identifier SET survives in each. The swap row
is precisely what a count cannot express, so its absence from the table is visible in a way its
absence from a bool is not. The cheap general form of the same rule, and the one worth carrying into
review: **when the rule is about a set, the assertion names the members, never the size.** Not built
(item 6 is a LOW batch); recorded for the manager.

### Item 2 — the `Identified` asymmetry is pinned (commit `23f14d3`)

A pin, not a fix: no production code changed. `Submitting` restores its pre-attempt snapshot when the
rewrite does not land; `Identified` deliberately does not, because there the order is live and the
broker id is real.

**Why nothing caught it before.** The existing test
(`An_acknowledgement_whose_rename_never_landed_is_still_readable_after_a_restart`) reads the id back
through a RESTART, and the stranded temp already holds it — so it is satisfied whether or not the
running session kept the id in memory. The new test asks the RUNNING session and then watches the next
successful commit carry the id onto disk, which is the only place the asymmetry is visible. It pins
both halves in one test: the acknowledgement survives the refusal, the abandoned claim does not.

**The bite.** Mutant M2 — `Identified` given the same snapshot/restore `Submitting` has:

```csharp
var before = _records.ToArray();
_records[i] = record with { BrokerOrderId = brokerOrderId, IdentifiedAt = DateTimeOffset.UtcNow };
if (!Save(clientOrderId)) { _records.Clear(); _records.AddRange(before); }
```

Verified by running `dotnet test …--filter "FullyQualifiedName~CoidWitnessTests"` under M2 →

```
A_refused_acknowledgement_is_kept_where_a_refused_claim_is_taken_back [FAIL]
Assert.Equal() Failure: Strings differ   Expected: "BRK-LIVE"   Actual: null
Failed!  - Failed: 1, Passed: 77, Skipped: 0, Total: 78
```

**77 of 78 passed under M2, and the one failure is the new test** — so M2 survived every pre-existing
test in the class, which is what the pin was for. Source restored from the `cp` copy and `touch`ed;
`git status --porcelain` empty afterwards.

### Item 3 — "refused without the lock", both paths and both writers (commit `7f5932a`)

A pin, not a fix: no production code changed. Round 4 item 1 made the lock mandatory; what was
asserted was the CLAIM path only. `Identified` takes the same lock and runs through the same `Save`,
and nothing covered either of its refusals — the harder half to argue, because there the record is
this session's own, the broker id is real, and the write is a small in-place edit.

Two tests, each naming both writing paths:

- `A_claim_and_an_acknowledgement_are_both_refused_without_the_lock` — a second owner holds
  `coid-witness.json.lock`; `Submitting` returns false and `Trouble` reads "another writer owns this
  witness"; `Identified` writes nothing to memory or disk; the committed file is exactly as its owner
  left it (nothing lost, nothing phantom).
- `A_claim_and_an_acknowledgement_are_both_refused_when_the_file_changed_underneath` — a CAS miss is
  a refusal on both paths, `LastWriteFailure` says "changed underneath this writer", the foreign
  bytes are left byte-for-byte, and `Token()` reads `io:failed`.

**Mutants** (each restored from the `cp` copy, then `touch`ed; `git status --porcelain` empty after):

| # | Mutant | Result |
|---|---|---|
| M3a | `Submitting`'s `if (owned is null) {…return false;}` deleted — the lock failure falls through to a write | RED, 2 failed / 78 passed: `A_claim_and_an_acknowledgement_are_both_refused_without_the_lock`, `A_second_writer_is_refused_rather_than_merged_with` |
| M3b | the same deletion in `Identified` | RED, **1 failed / 79 passed** — only the new test. The acknowledgement-lock path had no coverage at all before this item |
| M3c | the compare-and-swap block in `Save` disabled (`if (false)`) | RED, 2 failed / 78 passed: `A_witness_file_changed_by_something_else_is_refused_not_merged`, `A_claim_and_an_acknowledgement_are_both_refused_when_the_file_changed_underneath` |

M3b is the finding this item was worth: a build in which a writer that does not own the witness could
still stamp a broker id into it would have shipped green.

### Item 4 — "an anchorless candidate reads as a flagged zero" still holds (commit `eee32ca`)

**Still true after items 1–3.** Verified by running the class after each item: 77 → 78 → 80 green,
`With_nothing_committed_only_a_first_rewrite_is_adopted` passing throughout. Item 1 cannot reach it —
`DropsACommittedRecord` is only consulted when a committed file exists, and `DescendsFrom` refuses an
anchorless candidate before that.

**Made literal.** The existing test asserted the flag (`io:degraded`) on one of its two candidate
shapes and the count on neither. Both halves now assert `records:0` AND `io:degraded` together, plus
`Unreadable == false`: the zero is a fact about the disk, not a failure to read it. That is what
"flagged zero" means — a confident zero from this file means "this product never submitted that
identifier", and something WAS refused, so the reader is told.

**Mutants** (restored from the `cp` copy, `touch`ed; `git status --porcelain` empty after):

| # | Mutant | Result |
|---|---|---|
| M4a | `DescendsFrom`: `if (committedText is null) return temp.Generation == 1 && temp.Predecessor is null;` — the round-2 shape test put back | RED, 2 failed / 78 passed |
| M4b | `DescendsFrom`: `if (committedText is null) return true;` — no anchor needed at all | RED, 2 failed / 78 passed |

Both fail `With_nothing_committed_only_a_first_rewrite_is_adopted` and
`A_sidecar_left_by_an_earlier_run_makes_the_token_say_so`. The M4b message, verified by running
`dotnet test … --filter "…With_nothing_committed_only_a_first_rewrite_is_adopted"`:

```
Assert.Empty() Failure: Collection was not empty
Collection: [CoidWitnessRecord { ClientOrderId = TA-IMPORTED, SessionId = a-dead-session, …,
             BrokerOrderId = BRK-TA-IMPORTED, … }]
```

That is the import route in one line: another witness's acknowledged identifier becoming this
machine's record of what it submitted.

### Item 5 — the protocol-3 lines in the docs (commit `e22eec6`)

Both halves existed on the branch and both were stale. Verified by running
`grep -rn "proto=" docs/` at `a8b3fb0` → one hit,
`docs/RESUME-HERE.md:1000: and answering with \`proto=2\` and \`auth=ok\``, and
`grep -rni "protocol 3|protocol-3" docs/` → no hit in `docs/CONTRACTS.md`. So round 4 had NOT already
done either; nothing to quote.

- **`docs/RESUME-HERE.md`, "Verifying what you inherited"** — the section exists on the branch (line
  997). The expected reading is now `proto=3` and `auth=ok`, and the paragraph says what that makes the
  deployed DLL: rebuilt from this tree on 2026-08-30, answering `proto=2`, therefore **out of date and
  refused by design** until it is rebuilt with the next release. The 2026-08-30 fact is kept rather
  than deleted, because it is what dates the DLL.
- **`docs/CONTRACTS.md`, "Bridge protocol"** — one sentence added: the version is 3
  (`Versions.BridgeProtocolVersion`), U14 raised it from 2 because a version-2 bridge places the order
  whether or not the `coid-witness.json` rewrite reached the disk and omits `witness_failure` from its
  hello, so its silence cannot be read as "no trouble"; the mismatch routes it to `IncompatibleBridge`.

Both claims checked against the code, not remembered: `src/TradeAgent.Core/Versioning.cs:35`
`public const int BridgeProtocolVersion = 3;` and
`src/TradeAgent.Connectors.Atas/BridgeProtocol.cs:148` `public sealed record IncompatibleBridge(...)`.

### Item 6 — the final gate at `e22eec6`

Verified by running `dotnet build TradeAgent.sln` → `Build succeeded. 0 Warning(s) 0 Error(s)`
(Time Elapsed 00:00:01.81), working tree clean (`git status --porcelain` empty).

Verified by running `dotnet test TradeAgent.sln`:

```
Passed!  - Failed:     0, Passed:    75, Skipped:     0, Total:    75 - TradeAgent.FaultTests.dll (net10.0)
Passed!  - Failed:     0, Passed:   111, Skipped:     0, Total:   111 - TradeAgent.UnitTests.dll (net10.0)
Passed!  - Failed:     0, Passed:   201, Skipped:     0, Total:   201 - TradeAgent.IntegrationTests.dll (net10.0)
```

**382 → 387 green** (75 / 111 / 201), 0 failed, 0 skipped. Five tests added, all in
`CoidWitnessTests`; `CoidWitnessTests` itself went 75 → 80.

### Adjacent-regression sweep (R3)

| Symbol changed | Consumers | Result |
|---|---|---|
| `KeepsCommittedRecords` → `DropsACommittedRecord` (private, static → instance, `bool` → `string?`) | one call site, `ScanCandidates` | `grep -rn KeepsCommittedRecords src tests tools docs` → no hits; the three `DropsACommittedRecord` hits are the call site, its doc reference and the definition |
| `_cap` (now read by the adoption rule as well) | constructor, `Trim`, `DropsACommittedRecord` | unchanged in the first two; `dotnet build` clean |
| the candidate-rejection sidecar wording | `tools/probe` prints the sidecar tail; the support package collects `bridge/*.errors.log`; tests | `grep -rn "records against\|would drop committed claims"` → no test or tool asserts the old wording; the only hits are the new production strings and one comment in the new test |
| `CoidWitness` public surface | `AtasStrategyAdapter` (`Trouble`, `PriorSessionIds`, `Token`, `Submitting`, `Identified`, `PriorSession`, `Path`, `LastWriteFailure`, `DefaultCap`), `ClientOrderIdProof` (doc reference only), `tools/probe/Program.cs` (`Path`, `ErrorLogPath`, `Trouble`, `All`, `Unreadable`, `FileName`) | **no public member changed** — no signature, no name, no return type. Every external caller is untouched and the solution builds clean, `tools/probe` included |
| `docs/RESUME-HERE.md`, `docs/CONTRACTS.md` | prose | no code reads them |

Files touched since `a8b3fb0` (`git diff --stat a8b3fb0..HEAD`): `docs/CONTRACTS.md` (+6/-…),
`docs/RESUME-HERE.md`, `src/TradeAgent.AtasBridge/CoidWitness.cs`,
`tests/TradeAgent.IntegrationTests/CoidWitnessTests.cs` — 4 files, 264 insertions, 24 deletions.
Verified by running `git diff --name-only a8b3fb0..HEAD | grep -E "TradeAgent.Gateway/|AppHost.cs|AtasStrategyAdapter.cs|Updater|TradeAgent.App/"`
→ no match: the Gateway, `AppHost.cs`, the updater, the App UI and `AtasStrategyAdapter.cs` are
untouched. Verified by running `git log a8b3fb0..HEAD --format=%B | grep -i co-authored` → no match.

Commits, in order: `6e9027c` (item 1), `23f14d3` (item 2), `7f5932a` (item 3), `eee32ca` (item 4),
`e22eec6` (item 5). Tip **`e22eec6`**. Not pushed, not merged, not rebased.

### What I did NOT do, and what is NOT verified

- **`AtasStrategyAdapter.cs` was not compiled and not touched.** It is `<Compile Remove>`d off Windows
  and the box is offline. This item changed no public member of `CoidWitness`, so the two hunks the
  adapter carries are exactly as the earlier rounds left them — but "the adapter still compiles" is
  NOT verified here, by anything, and the next release build on the box is still their first compiler.
- **The new adoption rule reads THIS instance's `_cap` to decide whether a missing prefix is
  explicable.** The candidate was written by a different instance, whose cap this build cannot see. In
  production both are `DefaultCap` (512) and in the tests both are set equal, so the assumption holds
  where it is exercised — but a temp written by a build with a LARGER cap would now be refused where
  `a8b3fb0` adopted it. That is the safe direction (keep the committed file, say why) and it is a
  deliberate choice, NOT a measured one: no such cross-cap case exists to measure.
- **No Windows run.** Every replace failure in these tests is injected at the `_replace` seam; the
  cross-process lock is exercised on APFS only. The `proto=3` claim in `RESUME-HERE.md` is a claim
  about what this tree expects, NOT a reading taken from the box — the box's DLL still answers
  `proto=2` as far as anyone here knows.
- **The three-process durability harness was not re-run.** That is the verify leg's instrument; this
  leg ran the suite and targeted mutants only.
- **No new gate or test class was built for §9.9** — the brief asks for the answer, and the answer is
  in item 1 above.
- Items 1–5 were each committed before any mutant ran; every mutated file was restored from a `cp`
  copy and `touch`ed, never with `git checkout --`, and `git status --porcelain` was confirmed empty
  after each restore.

## Round 5 (build record, 2026-09-03)

Builder leg [1] continuing (§9.3), worktree `…-worktrees/u14-build`, branch `u14-coid-witness-rewrite`,
from `e22eec6`. Inputs: Codex `records/codex-U14-r4.txt` (6H/6M/4L) and the Opus verifier
`records/U14-verify-r4.md` (FAIL 0H/5M/4L). Toolchain `PATH="$HOME/.dotnet:$PATH" DOTNET_ROOT="$HOME/.dotnet"`.
**The Windows box is reachable this round** — `tools/win-state.sh` → `tailscale up (pong … 4ms)`,
`DESKTOP-K8VRIT9`, session `Active (id 1, console)`, desktop live, ATAS installed and running,
repo at `C:\ta\repo`. Verdict "console work only" (no UI agent), which is all an on-box build needs.

Findings are worked one commit each, in the order below. Every mutant ran after its commit, on a `cp`
copy restored and `touch`ed, never `git checkout --`.

### F14 (LOW) — the whitespace gate · REAL · commit `729d5fd`

Verified by running Codex's own check, `git diff --check 3f1d8f2..HEAD` →
`tests/…/CoidWitnessTests.cs:788: trailing whitespace.` (exit 2). The line also carried a ternary on a
string literal's own length, which is always the same branch; both are gone. Re-run → no output, exit 0.
No mutant: this is a gate, and the gate is the test.

### F7 (MED) = V1, and the verifier's CLASS fix (V3) — the sidecar conflated a tidy-up with a lost claim · REAL · commit `30d6091`

Adopted the class fix rather than the two instances (§9.10). `Note()` set `_degraded` for EVERY line,
and `_degraded` is the sole input to `Trouble`'s third branch, which is what puts DEGRADED on the ATAS
bridge row and drops `SupportsClientOrderId` to false. So a quarantined foreign leftover and a claim
that never reached the disk were the same state downstream. On top of that the RESOLVED marker — the
line that ENDS a gap — was written `safety: false`, so the 32-line warning quota could ration it away.

**RED first**, two tests, both from the reviewers' exact checks:

- `A_clean_commit_says_the_gap_is_closed_even_after_the_warning_quota_is_spent` (Codex F7's check;
  the verifier's measured 8/8 flip) → `Assert.Contains() Failure: Sub-string not found … "RESOLVED"`.
- `A_quarantined_leftover_is_noted_without_claiming_a_durability_gap` (V3's consequence) →
  `Assert.Contains() Failure: Sub-string not found … "io:noted"`.

**The fix.** A safety line is one carrying the `ERROR ` prefix it already carried; diagnostics already
lacked it, so the file format does not change — what changes is that the distinction is load-bearing
and named (`SafetyPrefix`). `LastDecidingLine()` scans back for the last safety line or marker;
`EnsureLoaded` decides `_degraded` from that, `Note(safety: false)` no longer degrades, and the marker
is written `safety: true` with its duplicate guard reading the deciding line. A new, weaker flag
`_noted` reports "there is something in the sidecar" through `Token()` only, as **`io:noted`** — so a
zero is still a FLAGGED zero without claiming a durability gap. `Trouble` is now reachable only by an
unresolved safety line.

**Five existing assertions changed, deliberately, and this is the semantic change to review:**
`With_nothing_committed_only_a_first_rewrite_is_adopted` (×2),
`A_candidate_holding_fewer_records_than_the_committed_file_is_ignored`,
`A_candidate_that_swapped_a_committed_claim_for_another_is_ignored`,
`Two_rival_candidates_at_the_same_generation_are_both_declined` — all four were candidate REJECTIONS
asserting `io:degraded`; they now assert `io:noted` **and `Trouble is null`**, which is the stronger
statement. `A_gap_that_was_committed_over_stops_reading_as_degraded` asserted `io:ok`; it now asserts
not-degraded plus `io:noted`, because the history stays on disk. Item 4's "flagged zero" is kept —
the flag is now precise about what it is flagging.

**GREEN.** `dotnet test TradeAgent.sln` → 75 / 111 / **203**, 0 failed.

**Mutants:**

| # | Mutant | Result |
|---|---|---|
| MC1 | `Note()`: `if (safety) _degraded = true` → `_degraded = true` (the old conflation) | RED 4/78 |
| MC2 | the marker written `safety: false` again | RED **1/81 — only the new F7 test**, so the rationing defect had no coverage at all |
| MC3 | `EnsureLoaded` decides from `LastSidecarLine()` (any line) instead of `LastDecidingLine()` | RED 2/80 |

### F1 (HIGH, class: ownership lifecycle) = V2, and F2 (MED) = V3's other half · REAL · commit `e63e13c`

One class, one commit: `Own()` was taken and released **inside each call**, so "one owner" was
operation-scoped. Two live bridges that merely did not overlap took turns writing the file, and the
read paths took the same lock opportunistically and treated getting it as *being* the owner.

**RED first**, both from the reviewers' exact checks:

- `A_second_live_writer_is_refused_even_when_it_never_overlaps_a_call` (Codex F1's check: A alive
  after `TA-A`, B submits `TA-B` with no overlap) → `Assert.False() Failure` — B returned **true**.
- `A_reader_changes_nothing_on_disk_even_when_no_owner_holds_the_witness` (Codex F2's check:
  snapshot the directory, `new CoidWitness(path).All()`, compare) → `Assert.Equal() Failure:
  Collections differ` — the reader had renamed the temp and written the sidecar.

**The fix.** `Lease()` replaces `Own()`: an exclusive `FileShare.None` handle taken at the instance's
FIRST WRITE and held until `Dispose()` or process death. `CoidWitness` is now `IDisposable`; `Dispose`
releases and the instance stays usable, because that is the ATAS stop/start cycle inside one process
(`StopBridge` now calls it). `ApplyRecovery` is split: `AdoptInMemory()` (pure, any reader, any
process — it changes no file) and `ReportAndQuarantine()` (owner only, under the lease). Read paths
call only the first and never take the lease. A reader that REFUSED a candidate still sets `_noted`
from the scan itself, so the flagged zero of item 4 survives without a reader writing anything.

**GREEN.** `dotnet test TradeAgent.sln` → 75 / 111 / **206** = 392, 0 failed.

**The semantic change to review — 13 existing tests were updated, not deleted.** Almost all modelled
successive RUNS of the bridge with two live instances; a run now owns the witness for as long as it
lives, so ending one is `Dispose()` and each test says so on the line where the earlier run ends. Two
tests changed shape rather than gaining a line: `Two_writers_do_not_share_a_temp_name` (a second LIVE
writer never reaches the temp stage now — it asserts that, then proves the per-writer name across
runs), and `A_claim_and_an_acknowledgement_are_both_refused_without_the_lock` (the rival is now a real
second witness holding a real lease, not a hand-held file handle — the shape the defect actually has).
Three tests that asserted the sidecar's REASON from a reader now let the next owner write it, and say
why in place.

**Mutants:**

| # | Mutant | Result |
|---|---|---|
| MV2 (the verifier's) | `Lease()` `FileShare.None` → `FileShare.ReadWrite` | RED 4/81 — including `The_lease_is_what_stops_a_claim_reported_durable_from_being_dropped`. **It left all 80 green at `e22eec6`**; that is the finding closed |
| ML1 | the lease released at the end of every call (a `PerCallLease` scope) — round-4 behaviour exactly | RED 3/82 |
| ML2 | `EnsureRecovered` takes the lease and acts on disk again | RED 5/80 |

A first ML1 attempt (dispose-then-reacquire *inside* `Lease()`) **survived 85/85** — it released and
immediately retook the handle, so it never reproduced the defect. Recorded because a mutant that
survives for the wrong reason is worse than none; the faithful one is the table row above.

**Both directions, on real processes rather than in-process handles.** A standalone console with a
`ProjectReference` to this worktree's `TradeAgent.AtasBridge`, driving the real `CoidWitness`:

```
holder pid=17540 submitted=True
rival  pid=17653 submitted=False trouble=another writer owns this witness (…/coid-witness.json.lock): IOException
--- kill -9 the owner (no finally, no Dispose ever runs) ---
rival  pid=17654 submitted=True trouble=
committed ids: ['TA-OWNER', 'TA-RIVAL-AFTER-DEATH']
```

A live owner refuses a second process with the exact sentence; a SIGKILLed one strands nothing and the
next process takes the witness. Nothing lost, nothing phantom.

**Handle semantics, macOS vs Windows — stated, not assumed.** On macOS/APFS .NET enforces
`FileShare.None` with an advisory `flock`, and the kernel drops it when the last descriptor closes,
which is what the SIGKILL run above measures. On Windows the same `FileShare.None` is a mandatory
share-mode on the kernel file object, released when the handle closes — a stronger guarantee, and it
is also the platform where a second opener fails with the sharing violation this code already expects.
**NOT verified on Windows:** no lease/refusal run was performed on the box this round (the on-box work
was the compile of F5), so the Windows behaviour is reasoned from the API contract, not measured.

### F3 (HIGH, class: recovery validated a shape, not a transition) + V5 (the cap direction) · REAL · commit `39bafb0`

The membership rule skipped a LEADING RUN of committed identifiers the candidate lacked, because a
trim at the cap takes one. When **all** of them are absent that search walks off the end of the list
and there is nothing left to check — so committed A/B/C at cap 3 against a perfectly lined-up X/Y/Z
was adopted, and X/Y/Z are acknowledged records that walk into `PriorSessionIds`, the cross-session
reading and `SupportsClientOrderId`.

**RED first** — Codex F3's exact check plus the two clauses its CLASS names:

| test | RED at `e63e13c` |
|---|---|
| `A_candidate_that_replaces_the_whole_record_set_is_not_a_legal_rewrite` (Codex's A/B/C vs X/Y/Z at cap 3) | adopted; `PriorSession("TA-X")` non-null |
| `A_candidate_carrying_a_duplicate_identifier_is_not_a_legal_rewrite` | adopted |
| `A_candidate_that_adds_more_than_one_rewrite_can_is_ignored` | adopted |
| `An_at_cap_rewrite_from_a_differently_capped_build` (V5, both rows) | GREEN already — it pins the direction that existed |

**The fix.** `DropsACommittedRecord` → `IllegalTransition(candidate, committed)`, which asks what one
rewrite can do rather than what a file looks like: no duplicate identifiers; never fewer records than
the committed file; at most one more; and every committed identifier still present except possibly the
FIRST, and that one only when the candidate is full. A shape test cannot express the last clause,
which is the reason this is a transition rule and not a tightened membership rule.

**GREEN.** `dotnet test TradeAgent.sln` → 75 / 111 / **211** = 397, 0 failed.

**Mutants** — one per clause, and the last two in opposite directions:

| # | Mutant | Result |
|---|---|---|
| MF3a | `dropped > 1` disabled — the `e22eec6` hole | RED 1/89, `…replaces_the_whole_record_set…` |
| MF3b | the duplicate check disabled | RED 1/89 |
| MF3c | "adds at most one" disabled | RED 1/89 |
| MF3d | the cap guard on the dropped record disabled | RED 2/88 — the swap-under-cap test **and** the cross-cap row `(3, 5, adopted: False)` |
| MF3e | the trim allowance removed entirely (too strict) | RED 2/88 — `A_rewrite_that_trimmed_the_oldest_at_the_cap_is_still_adopted` **and** the cross-cap row `(5, 3, adopted: True)` |

**V5 — the round-4 record's cap-direction argument was BACKWARDS, and this corrects it.** Round 4's
"What I did NOT do" said *"a temp written by a build with a LARGER cap would now be refused"*. The
verifier measured the opposite and it is the opposite: the guard compares the candidate's record count
against the READER's cap, so it is a temp from a **smaller**-capped writer that is refused, and the
upgrade that costs is a cap **RAISE** (512 → 1024). `An_at_cap_rewrite_from_a_differently_capped_build`
now pins both rows — `(5, 3)` adopted, `(3, 5)` refused — and asserts what the refusal costs: the
committed file is kept whole and `PriorSession` answers null for the one un-recovered acknowledgement.
A proof refused, never a proof invented. The F3 rule does **not** subsume this — it carries the same
`_cap` comparison in clause 4 — so the pin is what keeps the direction honest.

### F13 (HIGH) — an unreadable committed file is not an anchor · REAL · commit `9909bd6`

`DescendsFrom` fell back to the predecessor fingerprint ALONE whenever the committed bytes existed but
did not parse, so a temp claiming any generation was adopted and its acknowledged identifiers reached
`PriorSession` while the witness was simultaneously reporting that it could not be read.

**RED first** — Codex F13's exact check (non-JSON committed bytes, temp at `generation=999` with
`predecessor=FNV1a64(corrupt bytes)`): `An_unreadable_committed_file_is_not_an_anchor` →
`Assert.Null() Failure: Value is not null`. The second half of the check (change only the generation —
must stay rejected) is in the same test.

**The fix.** One line: `if (committedText is null || committed is null) return false;` — both halves of
the lineage, off a PARSED anchor, or no adoption.

**Mutant MF13** (accept on fingerprint alone again) → RED **1/90, only the new test**: F13 had no
coverage at all.

### F4 (HIGH) — an envelope that deserialises is not one that means anything · REAL · commits `1a07bef`, `34d07f0`

**RED first** — Codex F4's exact check (`records:[null, <valid acknowledged A>]`, then `All()` as the
startup path, then submit B): `A_semantically_invalid_envelope_is_unreadable_and_no_write_replaces_it`
→ `Assert.True() Failure` on `Unreadable`. The reader reported an unflagged empty result.

**The fix.** `Parse` validates semantically — version ≥ 1, generation ≥ 0, a record list that exists,
no null elements, no empty identifiers, no identifier twice — and catches every exception rather than
`JsonException` alone (`Deserialize` also throws `NotSupportedException`). `_loaded` is set only after
`Take` has put the records in memory. `_committedUnreadable` refuses every write while something at the
path is not an envelope, and `Trouble` says so, which puts it on the bridge row and drops
`SupportsClientOrderId`.

**A behaviour reversed on purpose, and it is the one to review.**
`A_corrupt_file_reads_as_unreadable_rather_than_as_empty` ended *"and it recovers: this session can
still write"*, on the argument that the claims lost belonged to runs already over. That argument
assumes we know what the bytes held; unreadable means we do not, and they may hold the acknowledged
identifier for an order still resting in ATAS — the evidence the restart experiment exists to find.
The test is now `A_corrupt_file_reads_as_unreadable_and_is_not_written_over` and pins the refusal, plus
recovery by a person removing the file. **Consequence, named:** a corrupt `coid-witness.json` now
refuses every order until someone repairs or removes it. That is fail-closed and the row names the
file, and it is the same shape as the already-ratified "a machine with no bridge directory refuses
every order".

The duplicate-identifier clause was removed from `IllegalTransition` in the same commit: `Parse` now
refuses a duplicate on both sides of that comparison, so the clause was unreachable. Its test still
passes, through the new route (mutant MF4d below proves it).

**GREEN.** `dotnet test TradeAgent.sln` → 75 / 111 / **213** = 399, 0 failed, `0 Warning(s) 0 Error(s)`.

**Mutants:**

| # | Mutant | Result |
|---|---|---|
| MF4a | `Parse` validates syntax only | RED 2/90 |
| MF4c | an unreadable committed file no longer refuses writes | RED 2/90 |
| MF4d | duplicate identifiers allowed through `Parse` | RED 1/91 — the F3 duplicate test, now via its new route |
| MF4b | `_loaded` set before `Take` again | **SURVIVED 92/92** |

**MF4b survived, and the honest reason is that it is now unreachable rather than untested.** The route
Codex named — `Take` throwing on a null element, swallowed by the public reader — is closed at source
by `Parse`'s validation, so nothing in the class can reach the ordering the mutant changes. The
ordering is kept as the second guard and the widened `catch` is the third. **NOT verified:** that the
ordering bites, because no seam exists to make `EnsureLoaded` throw part-way; building one for a guard
whose only reachable cause is already closed was judged over-building, and it is recorded here rather
than left as a mutant that quietly passed.

### F8 (HIGH, class: a failed-submission temp is indistinguishable from a failed-acknowledgement temp) · REAL · commits `5129ad2`, `092bf1e`

**RED first** — Codex F8's exact check (commit A, force B's `Submitting` false leaving its temp,
restart, submit C; the final committed ids must exclude B): `A_temp_is_never_adopted_as_a_new_claim`
→ `Assert.Equal() Failure: Collections differ` — the committed file held `TA-B`. A second RED came
with it: `A_temp_cannot_acknowledge_a_claim_that_belongs_to_another_session` → `Assert.Null() Failure`.

**The fix, stated rather than inferred.** Recovery cannot tell the two temps apart by inspection —
both are "the rewrite that did not land" — so the rule is asserted from the contract instead: since
round 2 `Place` refuses the order when `Submitting` returns false, so a claim in a temp and not in the
committed file is a submission **that did not happen**. Adoption is now a MERGE: a temp may only fill
in the broker id on a claim the committed file already carries, only when the record has none, and
only when the sessions agree (`Identified` refuses to complete another session's record; recovery must
not be the way around that). `IllegalTransition` collapses to "exactly the committed identifiers".

**What that removed.** The arithmetic about trims and growth went with the case it existed for, and
with it the rule's dependence on `_cap` — so **V5's cross-cap asymmetry no longer exists to pin**: no
rewrite that trimmed is adoptable by any build's cap. The two-row theory added under F3 is replaced by
`An_at_cap_rewrite_is_refused_whatever_cap_the_reading_build_has`, which asserts the sameness.

**Five tests reversed, one of them the line Codex named.** `CoidWitnessTests.cs:1800` at `e22eec6` is
`Assert.Equal(["TA-SEED", "TA-4"], …)` inside `A_writer_leaves_at_most_one_uncommitted_rewrite` — a
refused claim reappearing after a restart. It now asserts the opposite and says so in place. Also
rewritten: `A_rewrite_that_trimmed_the_oldest_at_the_cap_is_still_adopted` →
`An_at_cap_rewrite_carrying_a_new_claim_is_refused_and_costs_nothing`;
`A_failed_rewrite_is_adopted_when_the_clock_went_backwards` (recast as the acknowledgement recovery it
now has to be); the cross-cap theory; and `Two_rival_candidates_at_the_same_generation_are_both_declined`,
whose rivals are now two LEGAL candidates disagreeing about the broker id — a better test, because the
old pair would be refused before rivalry mattered.

**GREEN.** `dotnet test TradeAgent.sln` → 75 / 111 / **216** = 402, 0 failed.

**Mutants:**

| # | Mutant | Result |
|---|---|---|
| MF8a | adoption replaces the record set again (round-4 behaviour) | RED 1/94 |
| MF8b | a temp may complete another session's claim | RED 1/94 |
| MF8d | the transition rule stops requiring the committed identifiers | RED 1/94 |
| MF8c | a temp may revise a broker id already recorded | **SURVIVED 95/95** → closed by `A_temp_cannot_revise_a_broker_id_that_is_already_recorded` (commit `092bf1e`); re-run → RED 1/95 |

MF8a bites only the session test because `IllegalTransition` refuses B's temp before adoption is
reached — the two layers are pinned by different tests, which is what defence in depth should look
like under a mutant.

**The residual, named with its direction.** A rename that threw AFTER the replace landed leaves the
claim COMMITTED while `Submitting` returns false and `Place` refuses the order. That is a claim with no
order — the file says this product was about to submit an identifier it never sent. It is the opposite
direction from the one rule 1 exists to prevent (an order with no claim), and its cost is bounded: the
identifier is never acknowledged, so `PriorSession` answers null for it for ever and it can never
become cross-session evidence. It occupies one of the `DefaultCap` slots until trimmed. **NOT closed**,
and not closable from this side of the rename — the durable write and the caller's answer cannot be
made one atomic act with a file rename.

**§9.9 — could a property test over (committed, temp) shapes catch the F8 and F13 classes?** For F13,
yes and cheaply: the anchor's validity is a function of the committed bytes alone, so a table over
{absent, valid, non-JSON, valid-JSON-invalid-envelope} × {temp generation ±1, predecessor right/wrong}
covers it exhaustively and the missing row is visible. For F8, **no — and that is the point of the
finding**: the two temps it distinguishes are byte-identical in shape, and which one is legitimate
depends on a fact that is not in either file (whether `Submitting` returned true, i.e. whether the
order was sent). No property over (committed, temp) can separate them; only the contract can, which is
why the fix is a stated rule and not a sharper test. What a property test WOULD catch is the weaker
invariant now enforced: *for every (committed, temp) pair, the adopted identifier set equals the
committed identifier set.* That one is worth having and is what the four new tests sample.

### F5 (HIGH, money path) and F16 (MED, the uncompiled hunks) · REAL · commit `b73329d` · **F16 CLOSED**

`ClosePosition` — the operator's close-all — ended in `ITradingManager.ClosePosition` with no
`_witness.Submitting` before it. ATAS creates the order and the identifier is written onto it
afterwards by hand, because ATAS decides the side, so a witness that could not write could not stop
this order the way it stops every other one: `Place` asks and refuses; this asked nothing. The order
that moves a real position was the one order in the product with no durable record that it was
submitted.

**The fix.** The claim goes in after the position is found (with no position there is nothing to
submit and nothing to record) and before anything is asked of ATAS, in `Place`'s exact shape: `Guard`
around the witness call so an exception from a diagnostic can never become an order's outcome, then
`AtasRejectedException` — a definite refusal, "nothing was submitted" — on false. The side is still
never inferred: the record says `Close` and `Math.Abs(position.Volume)`, which is what is true of the
submission, with ATAS choosing the direction.

**F16 — the on-box build, which is the first compiler these hunks have ever seen.** The box is
reachable this round. `tools/win-push.sh` → `packed 660K`, `unpacked: 420 files`, then:

```
$ tools/win-run.sh 'cd C:\ta\repo && dotnet build src\TradeAgent.AtasBridge\TradeAgent.AtasBridge.csproj \
      -p:AtasBridgeBuild=true -p:AtasInstallDir="C:\Program Files (x86)\ATAS Platform"'
  TradeAgent.AtasBridge -> C:\ta\repo\src\TradeAgent.AtasBridge\bin\Debug\net10.0-windows\TradeAgent.AtasBridge.dll
Build succeeded.
    5 Warning(s)
    0 Error(s)
Time Elapsed 00:00:06.97
```

The five warnings are all pre-existing in kind: one `MSB3277` WindowsBase 4.0.0.0/10.0.0.0 unification
between the .NET ref pack and the ATAS assemblies, and four `CS0618` for the synchronous
`ITradingManager` methods this build uses on purpose (`OpenOrder`, `ModifyOrder`, `CancelOrder`,
`ClosePosition` — the sync→async flip is out of scope and gated on a real broker's latency, per
PROGRAM.md). **Zero errors.** `AtasStrategyAdapter.cs(1792,24)` in the warning list is the
`ClosePosition` call itself, at the line my hunk pushed it to — so the compiled file is the one with
the change in it, confirmed independently by
`Select-String … 'the write-ahead record for {clientOrderId}'` → line 1779 and `_witness.Dispose` →
line 468 on the box's copy.

**So all four adapter hunks now have a compiler behind them:** round 2's `Place` refusal above
`lock (_gate)`, round 3's `Describe()` (`WitnessFailure`, `SupportsClientOrderId`), round 5's
`StopBridge` lease release, and round 5's `ClosePosition` witness. F16 is closed for COMPILATION.

**Nothing was deployed and nothing was touched.** `%APPDATA%\ATAS\Strategies` holds **14 files**
before and after, newest `TradeAgent.AtasBridge.runtimeconfig.json @ 09/01/2026 01:02:40` — the
deployed protocol-2 DLL is exactly as it was. The build wrote only to
`…\bin\Debug\net10.0-windows\TradeAgent.AtasBridge.dll` (141312 bytes, 9/3/2026 10:43 PM). The csproj
has no post-build deploy target (checked). `tools/win-state.sh` after the build: ATAS installed and
**running**, session Active (console), Strategies 14 files — unchanged.

**NOT verified, and it is the half F5's acceptance actually asks for.** Codex's check is *behavioural*
— force the witness unavailable, invoke operator close-all, assert `ITradingManager.ClosePosition` is
never called. That needs ATAS driving a real position, which is a trading test, not a build. The
adapter is `<Compile Remove>`d off Windows so no test in the 402-test suite can reach the branch
either. What is verified is that it **compiles against the real ATAS assemblies** and that the refusal
is written in the same shape as `Place`'s, which two verifiers have read. The behavioural half stays
open and belongs with the next on-box trading session.

### F9 (MED) + V8 (LOW) — a refused peer is refused as a connection · REAL · commit `03e0358`

`Dispatch`'s event branch asked only `_authenticated`. A version-2 bridge authenticates perfectly well
— it is this product's own older DLL — so it went on raising order, execution, position, account,
quote and connection events into the application while the same connector reported FAILED and refused
it every RPC. "Refused outright" was true of what this process ASKS and false of what it BELIEVES.

**RED first.** `A_bridge_speaking_the_previous_protocol_raises_no_events_into_the_application` — a
REAL authenticated `StubBridge` at protocol 2 over a real named pipe (`StubBridge` gained a
`RaiseEvent`; nothing in the suite had ever sent a bridge event) → `Assert.Equal() Failure` on the
callback count. **Fix:** `if (_authenticated && _incompatible is null)`. **Mutant MF9** (back to
`_authenticated` alone) → RED 1/22.

**Refuted, half of Codex's check:** it also asks that "the session closes". I did not add a
disconnect. The reason is in the code at the branch above it: discarding leaves the peer with no
effect on this process at all, the status row already names it, and a disconnect adds a reconnect loop
against a bridge that will keep failing the same way. The finding's substance — *no trusted events
from a refused peer* — is closed; the disconnect is a design choice left as it was, named here rather
than silently skipped. **Adjacent question NOT expanded (R2):** a peer whose hello is refused as
*unproved* (rather than incompatible) is also authenticated; its events still flow. That is a
different finding from F9 and is flagged for the manager rather than fixed under this one.

**V8** in the same commit: `AtasHealthTests.cs:125` built its hello at `BridgeProtocolVersion = 2` for
a row a v2 peer can never reach (`_hello` stays null for it) — now `Versions.BridgeProtocolVersion`.

### F6, F10, F11, V7, V9 — the batch of things that looked right and could not fail · REAL · commit `9e5ddff`

| finding | what was wrong | fix | mutant |
|---|---|---|---|
| **F6** (MED) | the post-quota safety test spent its allowance on 31 FAILURES, and failures never counted against the quota — so it was never exhausted and the assertion proved nothing | spends it on 40 quarantine warnings, asserts exactly 32 survive, then asserts a failure after that still reaches the file | MF6 (warnings unrationed) RED 2/96; MF6b (ration applied to safety too) RED 3/95 |
| **F10** (LOW) | the support package globbed `*.errors.log`, missing `coid-witness.errors.log.1` — and rotation happens on exactly the machine whose package matters, with the older generation holding the FIRST failures | `*.errors.log*`, and the test creates both | MF10 → RED, `Collection: [engineering.log, bridge-coid-witness.errors.log, activity.txt, environment.json]` — the `.1` absent |
| **F11** (LOW) | the retry tests succeeded on the fifth attempt with only a LOWER time bound, so 5 → 500 stayed green while a contended order would sit in `Place` past the gateway's 10 s deadline; the access-error test injected one refusal, not the budget | `A_refused_rename_is_attempted_exactly_five_times_and_then_gives_up`, a Theory over both exception classes, counting attempts with an upper bound as well as a lower; the access-error test now drives all four refusals | MF11 (5 → 12) RED on both rows. **A first attempt at 5 → 500 HUNG the run rather than failing it** — 20 ms × 500 rising is ~2.5 s… ×500 of sleeps — which is itself the finding, and is why the mutant used a bounded wrong number |
| **V7** (LOW) | `Committed()`'s doc claimed a lineage re-sync round 4 removed with the rebase | replaced with what round 4 does: refuse, do not negotiate, and stay refused for the run — with the 80/80-per-rival measurement that shows it | doc |
| **V9** (LOW) | `Trouble` skipped `EnsureRecovered()` while its four siblings ran it | it runs it | **MV9 SURVIVED 98/98** |

**MV9 survived, and the honest reason is that the class fix closed the finding underneath it.** V9's
symptom was `Trouble: <null>` beside `Token: …io:degraded` from one instance — possible when recovery
could set the degraded state. After the class fix `_degraded` is decided at LOAD from the sidecar's
last safety line, and `AdoptInMemory` touches only `_records`, so nothing recovery does can change any
value `Trouble` reads. The call is kept for uniformity — all five public readers now do the same two
steps — and it is **NOT verified by any test, because after the class fix it has no observable
effect.** Recorded rather than left as a quietly passing mutant.

### F12 (MED) + V4 (MED) — the sentence an operator reads · REAL · commit `02acca3`

Codex's F12 has two halves. **The second is closed by F2**: the probe no longer changes any file,
because readers no longer write — asserted by
`An_anchorless_temp_makes_the_probes_zero_provisional_without_touching_anything`, which compares the
directory before and after. **The first is real and was made worse by the class fix**: with `Trouble`
narrowed to unresolved safety, a directory holding only a refused anchorless temp now reports no
trouble at all, so the probe would print "none recorded" and then interpret `records:0` as "no
experiment has been set up" — a confident zero, which for this file means "this product never
submitted that identifier".

**V4 says why that could not be caught:** the probe's witness block sits behind a live bridge-pipe
connection, cannot execute off Windows, is referenced by no test project, and the verifier's mutant
MV7 (every sidecar reads UNRESOLVED) left all 81 tests green.

**The fix for both.** The decision and the wording move into `CoidWitnessReport` — a pure function in
`TradeAgent.AtasBridge`, which is compiled and tested on every machine — and the probe becomes a
renderer. Four standings (`Clean`, `Noted`, `Historical`, `Unresolved`), `Headline`, `Explanation`,
and `ZeroIsProvisional`. `CoidWitness.Noted` is exposed so a reader that declined a candidate can flag
its own zero without writing anything. The probe now returns a `provisional-zero` verdict instead of
"NO EXPERIMENT HAS BEEN SET UP" when something was refused.

**Mutants:** MV7 (the verifier's, which survived at `e22eec6`) → RED 4/104. MV7b (`ZeroIsProvisional`
always false) → RED 3/105.

### F15 (LOW) — the Windows gate that could not fail · REAL · commit `6a40fa7` · **verified ON the box**

`A_candidate_that_cannot_be_opened_is_a_failed_read_not_an_empty_one` returned immediately on Windows,
so the branch that matters most — a candidate that is present and unreadable, which must count as a
failed read or a witness with nothing committed beside it reports a confident zero — was covered on
macOS and Linux and skipped on the platform the product trades on.

**Fix:** an opener seam (`_open`), the same remedy `_replace` already uses for the rename, with the
same argument written at it. The Unix chmod version is KEPT as a second test driven through a real
unopenable file with no injection, so a seam that stopped resembling the real refusal would show up.

**Mutant MF15** (`_candidateUnreadable` not set) → RED 2/107 — both the seam test and the real one.

**And run on Windows**, which is what the finding actually asked for:

```
$ tools/win-run.sh 'cd C:\ta\repo && dotnet test …IntegrationTests… --filter "…cannot_be_opened…|…real_unopenable…"'
Passed!  - Failed: 0, Passed: 2, Skipped: 0, Total: 2, Duration: 171 ms
```

Of those two, the seam test genuinely executed on Windows; the chmod one returns early there by
design, as its own doc says. So the branch is now asserted on the trading platform.

### V6 (LOW) — what actually refuses a rival, stated

The round-4 record named "another writer owns this witness" as the refusal. The verifier measured the
opposite in a real three-process race: **158 of 160 refusals were the compare-and-swap miss** ("the
witness file changed underneath this writer"), because `Own()` was taken and released per call, so a
rival usually got the lock and was turned away later.

**The lifetime lease inverts that, and it is measured, not argued.** In the real two-process run under
F1 above, the rival never reaches the compare-and-swap at all:

```
rival  pid=17653 submitted=False trouble=another writer owns this witness (…/coid-witness.json.lock): IOException
```

So as of this round the LOCK is the mechanism and the CAS is the second line — it now fires only for a
writer that is not this build (an older bridge, a hand edit, a restored backup), which is exactly what
its message says. Both are refusals, both return `Submitting` false, both are safety events in the
sidecar.

### Round 5 final gate — tip `6a40fa7`

Verified by running `dotnet build TradeAgent.sln` → `Build succeeded. 0 Warning(s) 0 Error(s)`
(Time Elapsed 00:00:01.23), working tree clean.

Verified by running `dotnet test TradeAgent.sln` on macOS:

```
Passed!  - Failed: 0, Passed:  75, Skipped: 0, Total:  75 - TradeAgent.FaultTests.dll (net10.0)
Passed!  - Failed: 0, Passed: 111, Skipped: 0, Total: 111 - TradeAgent.UnitTests.dll (net10.0)
Passed!  - Failed: 0, Passed: 231, Skipped: 0, Total: 231 - TradeAgent.IntegrationTests.dll (net10.0)
```

**402 → 417 green** (75 / 111 / 231) from `e22eec6`'s 387. `CoidWitnessTests` 82 → 109.

**And the same suite ON THE WINDOWS BOX, which has never been done for this unit:**

```
$ tools/win-run.sh 'cd C:\ta\repo && dotnet test TradeAgent.sln'
Passed!  - Failed: 0, Passed: 231, Skipped: 0, Total: 231 - TradeAgent.IntegrationTests.dll (net10.0)
Passed!  - Failed: 0, Passed: 111, Skipped: 0, Total: 111 - TradeAgent.UnitTests.dll (net10.0)
Passed!  - Failed: 0, Passed:  75, Skipped: 0, Total:  75 - TradeAgent.FaultTests.dll (net10.0)
```

**417 green on Windows too**, same counts. That includes the churn test whose `.tmp` assertion the
record has carried as "load-dependent on Windows" since round 1 — it passed here, on a machine running
ATAS, though one green run is not a distribution and the assertion stays load-dependent.

### Adjacent-regression sweep (R3)

`git diff --stat e22eec6..HEAD` → 10 files, +1465 / −223: `CoidWitness.cs`, the new
`CoidWitnessReport.cs`, `AtasStrategyAdapter.cs`, `AtasConnector.cs`, `Doctor.cs`,
`tools/probe/Program.cs`, and four test files.
`git diff --name-only e22eec6..HEAD | grep -E "TradeAgent.Gateway/|AppHost.cs|Updater|TradeAgent.App/"`
→ **no match**: the Gateway, `AppHost.cs`, the updater and the App UI are untouched.

| changed surface | consumers, enumerated | proven |
|---|---|---|
| `CoidWitness` gained `IDisposable`, `Noted`; `Trouble` gained an unreadable branch; the ctor gained an `open` parameter (optional, defaulted) | `AtasStrategyAdapter` (`Submitting` ×2, `Identified` ×2, `PriorSession`, `PriorSessionIds`, `Token`, `Trouble` ×2, `Path` ×2, `LastWriteFailure` ×2, `Dispose`), `tools/probe/Program.cs` (`Path`, `ErrorLogPath`, `Trouble`, `Noted`, `All`, `Unreadable`, `FileName`) | solution builds clean on macOS AND on the box with `AtasBridgeBuild=true`; 417 green on both. No existing signature changed — `open` is a new optional parameter, so every existing call site compiles unchanged |
| `_witness.Dispose()` in `StopBridge` | the only lifecycle hook; `_witness` is a readonly field, and the instance stays usable so a restarted strategy re-leases | compiled on the box; **behaviour NOT verified** — needs ATAS to stop and start a strategy |
| `AtasConnector.Dispatch` event branch | `TradingGateway` subscribes `OrderChanged`, `ExecutionReceived` (and the adapter's other events) | 111 unit + 231 integration green; the new wire test drives a real pipe |
| `Doctor.CreateSupportPackage` glob | `src/TradeAgent.App/DashboardView.cs:830` — one caller, unchanged signature and unchanged behaviour except that it now collects one more file | full suite green |
| `tools/probe` witness block | no code consumes the probe; a person reads it | the decision it renders is now a tested pure function |

`tools/probe` constructs a `CoidWitness` and does not dispose it. That is correct rather than an
oversight: it never writes, so it never takes a lease, and there is nothing to release.

### What I did NOT do, round 5

- **F5's behavioural half.** Codex's check is "force the witness unavailable, invoke operator
  close-all, assert `ITradingManager.ClosePosition` is never called". That needs ATAS driving a real
  position. What is verified is that the hunk COMPILES against the real ATAS assemblies and is written
  in `Place`'s shape. **NOT verified: that it refuses at runtime.**
- **F9's disconnect.** Refuted with the reason, above; the events half is fixed. The adjacent
  "unproved hello" peer is flagged, not fixed (R2).
- **MV9 and MF4b survived**, both because the finding under them was closed by a different fix
  (the class fix; `Parse`'s validation). Neither is pinned by a test and both say so above.
- **The `_witness.Dispose()` stop/start cycle** was not exercised — no ATAS strategy was started or
  stopped. The box's ATAS was left running and untouched; nothing was deployed to
  `%APPDATA%\ATAS\Strategies` (14 files before and after).
- **No three-process durability harness this round.** That is the verify leg's instrument; the
  two-process lease proof above is mine and is narrower on purpose.
- **The residual named under F8** — a rename that throws after the replace landed leaves a committed
  claim for an order `Place` refused — is NOT closed and is not closable from this side of the rename.
- **`Quarantine`'s 64-slot exhaustion** is no longer a permanent-degradation path (quarantine warnings
  do not degrade any more), but the slots are still finite and still never reclaimed. Not addressed.
- Every mutated file was restored from a `cp` copy and `touch`ed, never `git checkout --`, and
  `git status --porcelain` was confirmed empty after each. One mutant (MF11 at 500 attempts) hung the
  run and was killed; the tree was restored and verified clean before continuing.
- Not pushed, not merged, not rebased.

## Round 6 (build record, 2026-09-03)

Builder leg [1] continuing (§9.3), worktree `…-worktrees/u14-build`, from `6a40fa7`. Inputs: Codex's
delta review `records/codex-U14-r5.txt` (13/16 priors FIXED, 5/9/16 PARTIAL, F17 HIGH + F18–F22) and
the Opus verifier `records/U14-verify-r5.md` (FAIL 1H/2M/2L). Box reachable.

| # | real/refuted | RED | GREEN | mutant | commit |
|---|---|---|---|---|---|
| F17 (HIGH) | real | `Submitting` returned **true**, A destroyed | 419 | MF17a/MF17b RED, one row each | `6fd1420`, `fd8e071` |
| R1 (HIGH) + F20 + PRIOR 9 | real | 3 tests RED (heartbeat, later hello, pre-hello events) | 424 | MR1a RED 1/26, MF20 RED 1/26, MR1b RED 1/27 | `251d5cd`, `a0c9012` |

### F17 (HIGH, class: unreadable ≠ absent) · commits `6fd1420`, `fd8e071`

**RED first**, Codex's injected-opener check exactly: commit acknowledged A, deny the load's four
reads and the compare-and-swap's four, permit the post-replace read, submit B →
`Assert.False() Failure` — `Submitting` returned **true** and A was gone.

**The fix is one predicate.** Absent is exactly `FileNotFound` on the committed path; every other way
of not having the bytes — a refused read, or bytes that are not an envelope — is UNREADABLE, and
unreadable refuses every write. `_committedUnreadable = unreadable` (was: parse failure only). The
compare-and-swap read carries the same rule instead of discarding its failure flag: it used to produce
null, null matched a null `_committedHash`, and the swap went ahead against a file this instance had
never seen.

**The mutants first survived, and the second attempt is the interesting part.** MF17a (split the load
predicate again) and MF17b (CAS discards the flag again) each left 110/110 green, because the other
guard refused the write anyway — with the WRONG SENTENCE. "Could not be read" sends a person to a lock
or a damaged file; "changed underneath this writer" sends them hunting a second bridge that is not
there. The test is now a Theory over three denial windows — the load's four reads `(0,4)`, the CAS's
four after a successful load `(1,5)`, and both `(0,8)` — asserting the diagnosis and not only the
refusal. MF17a → RED on row 1; MF17b → RED on row 2. Each guard is separately load-bearing.

### R1 (HIGH) + F20 + PRIOR 9 (class: a refused peer is still allowed to speak) · commits `251d5cd`, `a0c9012`

**RED first**, three tests over a real pipe with a real authenticated `StubBridge`:

- `A_refused_bridge_cannot_set_capabilities_through_a_heartbeat` — v2 hello, then one heartbeat
  claiming v3. The verifier's HIGH: `_hello` was set, and with it `ReconciliationProvable = true`,
  which `TradingGateway.cs:213` reads to refuse LIVE_AUTONOMOUS (`AUTONOMY_REQUIRES_PROVABLE_STATE`)
  and `TradingGateway.cs:818` reads to send an UNKNOWN order to a person. Both removed by one frame.
- `A_refused_bridge_cannot_clear_its_refusal_with_a_later_hello` — PRIOR 9's other half.
- `An_authenticated_peer_raises_no_events_before_a_compatible_hello` — Codex F20: authentication alone
  let trusted events through before anything was established about the protocol.

`A_fresh_connection_from_a_compatible_bridge_is_still_accepted` was written with them and passed
throughout — the gate has to stay a gate.

**The fix is one decision.** `_refused` is a fact about the CONNECTION, tested at the top of `Dispatch`
above every frame type there is and any added later; nothing clears it but a new connection (`Drop`).
`_compatible` — "a compatible hello has been accepted on THIS connection" — gates the event branch and
the heartbeat branch, because authentication says what a peer HAS and nothing about what it speaks.

**Deviation from the brief's letter, with the evidence — this needs the manager's eye.** The brief says
"mismatch returns a refusal not true". I implemented the substance and **kept `return true`**, because
returning false breaks the read loop, which runs `Drop`, which **clears `_incompatible` by design** —
`Drop`'s own comment: *"A version mismatch is a fact about the peer, so it leaves with the peer"*, and
`When_an_incompatible_bridge_disconnects_the_status_row_is_told` pins it. Verified by running it: with
`return false` the suite went **5 RED**, all `System.TimeoutException: condition was not met in time` —
`An_incompatible_bridge_names_its_version_and_gains_nothing_by_it`,
`When_an_incompatible_bridge_disconnects_the_status_row_is_told`,
`A_bridge_speaking_the_previous_protocol_raises_no_events_into_the_application` and both new refusal
tests — because the version number and the repair vanish from the row a moment after being written,
while the bridge redials every two seconds. The same applies to the top-of-`Dispatch` guard, which
returns true and DISCARDS the frame. The refused peer is left on the pipe and read by nobody, which is
the whole of what "refused" has to mean here; every behavioural clause of the rule holds. If the
manager wants the disconnect regardless, it is a one-line change plus a reversal of that UX decision,
and the five tests above are what it costs.

**Mutants:** MR1a (the connection-level refusal removed) → RED 1/26. MF20 (event gate back to
authentication alone) → RED 1/26. **MR1b (heartbeat gate back to authentication alone) SURVIVED
27/27** — the refusal blocks a refused peer's heartbeat before the gate is reached, so the gate only
matters for an authenticated peer that has sent NO hello and then heartbeats, handing itself every
capability. That is a real second route the connection-level refusal cannot reach — nothing is refused
because nothing has been claimed — and it is now pinned by
`An_authenticated_peer_sets_no_capabilities_by_heartbeat_before_any_hello` (commit `a0c9012`);
MR1b re-run → RED 1/27.

### F18 + F19 (class: the degraded state must survive rotation and be read off the whole set) · commits `638fdcb`, `62ae5e1`

**RED first.** F18, Codex's construction exactly — an oversized sidecar ending in `ERROR`, then an
aged-candidate quarantine driven through `Identified` for an identifier the file does not carry, which
writes one diagnostic and commits nothing: `An_unresolved_gap_is_not_lost_when_the_sidecar_rotates` →
`Assert.NotNull() Failure: Value is null`. F19, Codex's literal check run against the pre-fix function
(`Standing(sidecarExists: true, troubled: false, noted: true)`) → `Assert.Equal() Failure: Values
differ` — `Historical` where `Noted` was required.

**Why the verifier's own rotation probe missed it, and it is worth keeping:** the round-5 verifier
built the same hypothesis and refuted it, because the session that rotates in their construction is a
WRITING session and puts a deciding line in the new file either way. The case that breaks it is a
session that writes a WARNING and commits nothing — which is exactly what `Identified` on a missing
identifier does, and which Codex found by naming that call.

**The fix.** The sidecar is read as the set it is: `SidecarGenerations()` yields the current log then
`.1`, newest first, and the last deciding line wins wherever it lives — so a gap open before the
rotation stays open and one closed before it stays closed (`A_gap_closed_before_the_rotation_stays_closed_after_it`
is the other direction). `Standing`'s first argument is now `gapClosed` — "a RESOLVED marker stands
after the last safety line" — never "the file exists", and the probe asks the witness through a new
`Standing(CoidWitness)` overload so the test and the probe cannot drift on what they feed it, which is
the mistake that produced F19.

**Mutants:** MF18 (deciding line from the current log only) → RED 1/114. MF19 (standing decided by
`Noted` again, i.e. by the file existing) → RED 1/114. **MF19 first SURVIVED** because the test fed
the three booleans by hand and never called the overload the probe calls; commit `62ae5e1` routes it
through the overload and MF19 then bites.

### R3 (MED) — a safety event dropped under concurrent appends · commit `6ccfc57`

**RED first, and reproduced with DISTINCT lines**, which is the gap the verifier named in its own
record: four concurrent writers × 40 claims each over one witness →
`4 of 160 safety events were dropped`, then `10 of 160`, then `26 of 160` on three consecutive runs.

**Two things the measurement turned up before the fix could even be judged.** A refused writer's line
did not name the claim it refused — it said only that somebody else owned the witness, which is true
and useless to anyone asking which order went unrecorded — so the first run reported 120 of 160
"dropped" that were never named. And the claim was the LAST field of a failure line whose two absolute
paths can spend 350 characters before it, so `OneLine`'s clip at 400 removed the one field the file
exists to record. Both now come first, and both are fixes in their own right.

**Three attempts, measured, and the third is the finding.**

| append | lost of 160 |
|---|---|
| `File.AppendAllText` as it stood (`FileShare.Read`, failure swallowed) | 4, 10, 26 |
| exclusive open + 10 retries over 275 ms | 7, 17, 11 (and 14, 36) |
| `FileMode.Append` + `FileShare.ReadWrite`, one `Write` call per line | 6, 11, 25, 36, 10 |

The third is the telling one: .NET's `FileStream` writes at a position **it** tracks rather than
through the kernel's append, so two writers place two lines at the same offset and one of them is
simply not there afterwards. **No share mode and no retry budget makes a shared file safe here** —
which is why the brief's first option is the right one.

**The fix: one writer per file.** The OWNER keeps the canonical sidecar — it is the file whose last
deciding line says whether this machine has an open gap, and the only instance that can close one,
because closing one means committing. An instance the lease refused writes beside it as
`coid-witness.errors.log-<pid>-<session8>`: the name carries `.errors.log`, so the probe's glob and the
support package's `*.errors.log*` collect it untold, and `Noted` counts it. It deliberately does NOT
decide the degraded state — a second bridge turned away is a misconfiguration that cost no order,
since the refusal is what stops the order being sent, and letting it mark the machine degraded for
ever is the row crying wolf again.

**GREEN, five consecutive runs of the 160-claim test, nothing lost.** The support-package test now
also asserts a per-writer file is collected.

**Mutants:** MR3 (every writer shares the canonical sidecar again) → RED 1/115. MR3b (the refusal
stops naming its claim) → RED 1/115.

### F21 (MED) + R2 (MED) — the lease and a strategy that has been taken down · commits `c79b33b`, `e7f75f7`

**RED first.** `An_acknowledgement_for_an_identifier_this_witness_does_not_have_takes_no_lease` →
`Assert.True() Failure`: after a stopped strategy's witness was asked about somebody else's
identifier, the live bridge could not write at all.

**The fix, in two places.** `Identified` leased and THEN looked for the record — and the order-event
fan calls it for every order in ATAS's book carrying a comment, so a witness belonging to a stopped
strategy reacquired the file on the next event about a foreign identifier and held it for the life of
the ATAS process, refusing every order the live bridge then tried to record until ATAS restarted.
Reading the record needs no lease, so it looks first now. The adapter says the same thing at the place
that decides whether this instance is still anybody's bridge: `_stopped` is set in `StopBridge` and the
fan does nothing while it stands, because that lambda cannot be unsubscribed.

**R2 — released on both terminal paths.** `StopBridge` was reached only from `OnStopping`. `OnDispose`
now calls the same idempotent teardown. The hook's existence is not a guess: probed by compiling it on
the box, `Build succeeded. 0 Error(s)`, against `ChartStrategy : … IDisposable` at dump line 269.
**NOT verified: which callback ATAS actually fires**, and it cannot be until this build is deployed —
the box still runs the protocol-2 DLL. Two hooks and a compiler, not a measurement.

**Mutant MF21** (`Identified` leases at the top again) → RED 1/116.

**A mutant that survived for the wrong reason, and the correction.** MF21 first reported
`Passed! 116/116`. The test did not exist: the round-6 rewrite of the rotation tests sliced up to the
same section marker the F21 test had been inserted before and deleted it, and `--filter` reports
`No test matches` rather than failing. Restored in `e7f75f7`; MF21 then bites. **The general lesson is
worth carrying: a mutant reported as SURVIVED is only evidence if the test it was meant to bite was
actually run** — check the count moved, not just that nothing failed.

### F22 (LOW) — a candidate that gave nothing · commit `8f65fc8`

**RED first.** `A_candidate_that_recovers_nothing_is_moved_aside_rather_than_re_examined_for_ever` →
`Assert.Empty() Failure: Collection was not empty` — the temp was still in the candidate glob. A legal
transition that restates a broker id already on file contributes nothing, so adoption correctly takes
nothing from it; it was then reported as "recovered an uncommitted rewrite" and left, and every later
session found it, declared it recovered again and wrote another line — the permanent-noise loop commit
`5e5b011` closed, re-entered through the one candidate that is neither adopted nor rejected. It is
quarantined now, and the "recovered" line counts what was actually recovered.

Codex's route to it (`Identified` on a missing id) is closed by F21, so the test drives owner recovery
through `Submitting` instead. **Mutant MF22** → RED 1/117.

### PRIOR 5 + PRIOR 16 (PARTIAL → CLOSED) — the money path, executed · commit `ae3d457`

`AtasStrategyAdapter` is `<Compile Remove>`d unless built against a real ATAS install, so nothing in
the cross-platform suite can reach the branch that refuses an order whose write-ahead record did not
land. `tools/atas-gate` is that branch, run: a console outside the solution, built against the real
assemblies, with stubs for `ITradingManager` and `IIndicatorDataProvider`, exiting non-zero on failure.

```
$ tools/win-run.sh 'cd C:\ta\repo\tools\atas-gate && dotnet run -c Release -p:AtasBridgeBuild=true …'
TRADEAGENT_HOME = C:\Users\Nicolas\AppData\Local\Temp\ta-atas-gate-551d32e3
  [PASS] ITradingManager.ClosePosition was never called — calls = 0
  [PASS] the refusal says nothing was submitted — the write-ahead record for TA-CLOSE-REFUSED could not
         be written to …\coid-witness.json; nothing was submitted. ERROR claim=TA-CLOSE-REFUSED another
         writer owns this witness (…\coid-witness.json.lock): IOException
  [PASS] the refusal names the witness file
  [PASS] ITradingManager.ClosePosition WAS called once the witness could be written — calls = 1
  [PASS] the refused close left no write-ahead record — records = [TA-CLOSE-ALLOWED]
  [PASS] the permitted close left one — records = [TA-CLOSE-ALLOWED]
GATE PASSED
```

Both directions, on the machine that trades. **PRIOR 5 and PRIOR 16 are closed for the close-all
branch.** Two traps are written into `tools/README.md` because both cost a round trip: `AtasBridgeBuild=true`
must be passed on the command line (a property in the gate's csproj does not reach the referenced
project, and the adapter is then compiled OUT of the assembly the gate exists to exercise), and the
ATAS assemblies are `Private=false` by the bridge's own deployment rule, so a console outside ATAS
resolves them from the install directory itself.

**A push that silently did not happen, and what it invalidated.** `tools/win-push.sh` refuses when a
process is running from the repo — a `testhost.exe` left over from round 5's on-box suite run had been
holding it. I had piped two pushes to `/dev/null`, so the refusals were invisible and an `OnDispose`
compile "succeeded" against a tree that did not contain `OnDispose`. Caught by checking the box's copy
directly (`Select-String … OnDispose` → 0, mtime 09/03 21:11). The stray process was killed, the push
re-run, the presence of both markers confirmed on the box, and **every on-box result in this record was
re-run against the current tree**. Never pipe a push to `/dev/null`.

### R4 (LOW) — the lease is a claim on a handle, not on a name

Recorded, no code. The verifier measured that unlinking `coid-witness.json.lock` while the owner lives
lets the next writer create a fresh inode and take its own lease — two live owners, the state the
lifetime lease exists to prevent. What it costs was measured too and is nothing: the compare-and-swap
and the read-back still refuse, so no claim reported durable is dropped, and it degrades the lease to
exactly the CAS-only protection round 4 shipped with (0 lost / 0 phantom over 240 concurrent claims).
It needs an external process to delete a file inside the bridge directory, and on Windows a file open
without `FILE_SHARE_DELETE` cannot be unlinked at all — **NOT verified on the box**, reasoned from the
API contract, and it is why this is LOW. No one-line guard exists: holding a name rather than a handle
means a different mechanism, not a smaller one.

### R5 (LOW) — what the compare-and-swap actually catches

The round-5 record says the CAS "now fires only for a writer that is not this build (an older bridge,
a hand edit, a restored backup)". The verifier measured a third case: an ownership HANDOVER. An owner
that exits mid-run releases its lease, the next process legitimately becomes the owner and carries the
committed records forward, and a third process holding stale lineage takes CAS refusals — 156 lock / 0
CAS under fully live contention, the reverse when processes are allowed to exit. So a reader who sees
CAS lines should not conclude a foreign writer: **the CAS catches a writer whose lineage is stale,
which is a foreign build OR a rival that was overtaken during a handover.** Corrected here rather than
by editing the round-5 section, so the sequence of what was believed when stays legible.

### ⚠ THE BOX IS SHARED, AND ANOTHER SESSION IS BUILDING ON IT — stop before reading the on-box figures

`C:\ta\repo` is being pushed to and built by a **second live session working on U2a**, concurrently
with this one. Confirmed rather than suspected: the box's
`tests/TradeAgent.IntegrationTests` holds `ConnectorSendDeadlineTests.cs`,
`GatewayPipeBackpressureTests.cs`, `OperatorContextTests.cs`, `SweepRequestIdTests.cs` and
`CliReplayContractTests.cs` — none of which exist in this worktree, all of which
`git ls-tree u2a-rebase-probe` lists — with mtimes of **09/04 00:21 and 00:54**, i.e. during this
round.

**How it surfaced.** The on-box integration suite reported 83, then 146, then 253-with-1-failure for
the same source; `tools/win-push.sh` refused twice because a `dotnet.exe`/`testhost.exe` was running
from the repo; and one push warned that it could not clear the tree. Every one of those reads as a
local flake and none of them is.

**What I did to their tree, said plainly.** `win-push.sh` deletes `src`, `tests`, `packaging` and
`tools` before unpacking, so each of my pushes replaced their working tree; and when the mixed state
would not resolve I **wiped `C:\ta\repo` entirely** and pushed clean. The U2a session's box state was
destroyed by that. They have pushed again since (the mtimes above), so the box now holds theirs, and I
have stopped using it. **This needs the manager to serialise box access** — §9.4's "one DB-toucher at
a time" applies to the single Windows machine exactly as it did to the shared database.

**Which on-box results survive this, and which I withdraw.**

| on-box result | stands? |
|---|---|
| the bridge compile against real ATAS, `Build succeeded. 5 Warning(s) 0 Error(s)` | **Stands.** `src/TradeAgent.AtasBridge` is not a project U2a touches, and the two round-6 markers (`OnDispose`, `_stopped`) were confirmed present in the box's copy of `AtasStrategyAdapter.cs` immediately before the compile |
| `tools/atas-gate` → GATE PASSED, both directions | **Stands.** The refusal it printed is this round's own wording — `ERROR claim=TA-CLOSE-REFUSED another writer owns this witness` puts `claim=` FIRST, which only the round-6 `NotOursDetail` does — so the gate ran this branch's bridge, not somebody's older copy |
| **the on-box FULL SUITE** | **WITHDRAWN.** The three counts disagreed because the tree was a mixture of two branches; the single failure it reported was in that mixture, not necessarily in either branch. **There is no trustworthy Windows suite figure for `f8a724c`** |
| round 5's on-box 417-green | Not re-run, and now of unknown standing for the same reason |

### Round 6 final gate — tip `f8a724c`

**Mac, authoritative.** `dotnet build TradeAgent.sln` → `Build succeeded. 0 Warning(s) 0 Error(s)`,
working tree clean. `dotnet test TradeAgent.sln`:

```
Passed!  - Failed: 0, Passed:  75, Skipped: 0, Total:  75 - TradeAgent.FaultTests.dll (net10.0)
Passed!  - Failed: 0, Passed: 111, Skipped: 0, Total: 111 - TradeAgent.UnitTests.dll (net10.0)
Passed!  - Failed: 0, Passed: 246, Skipped: 0, Total: 246 - TradeAgent.IntegrationTests.dll (net10.0)
```

**417 → 432 green** (75 / 111 / 246). `CoidWitnessTests` 109 → 119.

**Windows: the compile and the money-path gate only** (above). No suite figure.

### Adjacent-regression sweep (R3)

`git diff --stat 6a40fa7..HEAD` → 13 files, +1138 / −60. `git diff --name-only … | grep -E
"TradeAgent.Gateway/|AppHost.cs|Updater|TradeAgent.App/"` → **no match**.

| changed surface | consumers | proven |
|---|---|---|
| `CoidWitness`: new `GapClosed`; `Noted` now reads the whole sidecar set; `_committedUnreadable` widened; `Identified` reordered; `SidecarPath` split per writer | `AtasStrategyAdapter` (11 call sites), `tools/probe` (7), the test class | Mac suite green; bridge compiles against real ATAS on the box |
| `CoidWitnessReport.Standing` first argument re-meant, `Standing(CoidWitness)` added | `tools/probe/Program.cs:1066` and the tests — enumerated, both updated | MF19 bites through the overload |
| `AtasConnector`: `_refused`, `_compatible` (both private) | the observable surface is `Capabilities` / `Incompatible` / `Bridge`, read by `TradingGateway.cs:139/213/279` and `GatewayPipeServer.cs:149` — none edited | 111 unit + 246 integration green, including the wire tests that drive a real pipe |
| `Doctor` support-package glob (round 5) now also collects per-writer sidecars | `DashboardView.cs:830`, unchanged signature | its test asserts all three shapes |
| `tools/atas-gate` (new) | nothing consumes it; it is outside `TradeAgent.sln` on purpose | builds and passes on the box |

### What I did NOT do, round 6

- **No trustworthy Windows suite figure**, and round 5's is now doubtful — see the shared-box section.
  This is the largest open item and it is an environment problem, not a code one.
- **The brief's "mismatch returns a refusal not true"** is implemented in substance and NOT literally;
  the five tests that go RED under the literal form are quoted under R1. The manager's call.
- **`OnDispose` vs `OnStopping`:** which one ATAS fires on a strategy teardown is NOT verified and
  cannot be until this build is deployed. Two hooks and a compiler.
- **R4's Windows half** (a file open without `FILE_SHARE_DELETE` cannot be unlinked) is reasoned from
  the API contract, not measured — unchanged from the verifier's own statement.
- **The F8 residual** (a rename that throws after the replace landed leaves a claim with no order)
  stays open; it is not closable from this side of the rename.
- **`Quarantine`'s 64 slots** are still finite and never reclaimed.
- **Two tests I wrote were silently deleted by my own later edit** — the F21 one and the R3 one, both
  cut by a slice that ran from a doc comment to a section marker that later edits had inserted before.
  The first was caught by a mutant reporting no failure, the second by a test-count mismatch. Both are
  restored and their mutants re-run. **The general rule this earns: a mutant reported SURVIVED is
  evidence only if the total test count moved**, and slicing a test file by section marker is unsafe
  once anything has been appended before that marker.
- Every mutated file was restored from a `cp` copy and `touch`ed, never `git checkout --`, and
  `git status --porcelain` was confirmed empty after each.
- Not pushed, not merged, not rebased.

## Round 7 (build record, 2026-09-04)

Builder leg [1] continuing (§9.3), worktree `…-worktrees/u14-build`, from `f8a724c`. Input: the Opus
verifier's round-6 record (FAIL 1H/1M/1L). **No box access this round** — the grant is the U2a
builder's and nothing here needs Windows; every Windows claim in this unit stays where round 6 left it,
withdrawn or unverified.

| # | real/refuted | RED | GREEN | mutant | commit |
|---|---|---|---|---|---|
| V1 (HIGH) | real | the fixed bridge's `ConnectAsync` timed out against a parked refused peer | 249 int. | MD1 RED 8/23, MV1b RED 1/30 | `6fda449`, `f30a8af` |

### V1 (HIGH) — a refused peer parked on the pipe held the trading path shut · commits `6fda449`, `f30a8af`

The round-6 ruling is withdrawn and this reverses it. Parking a mismatched peer kept `Drop` from
erasing the version and the repair — but the pipe is created with `maxNumberOfServerInstances = 1`
(`AtasConnector.cs:220`, `:223`) and `AcceptLoop` creates the next instance only after the inner read
loop ENDS, so a refused peer that holds the connection open and says nothing occupies the only slot
there is. The row tells the operator to reinstall the add-on; doing so cannot work.

**RED first**, three tests, the first lifted from the verifier's `ParkedPeerVerifyR6Probes`:

| test | RED at `f8a724c` |
|---|---|
| `A_parked_refused_peer_does_not_keep_a_fixed_bridge_off_the_pipe` | the repaired bridge never arrives — `Wait` times out after 10 s |
| `A_refused_bridge_that_reconnects_is_refused_again` | the second v2 dial cannot get on the pipe at all |
| `A_refused_peer_leaves_the_version_and_the_repair_on_the_row` | GREEN already — it is the half that must SURVIVE the change |

**The fix is both halves at once, because either alone is wrong** (the verifier's MD1 showed
`return false` on its own trades a blocked pipe for a blank row). The mismatch branch returns **false**,
so the peer is dropped inside the read-loop turn and the accept loop can offer the instance again. And
`Drop` reads `_refused` before resetting the per-connection flags and keeps `_incompatible` when **our
own refusal** caused the disconnection — the same distinction the file already draws two paragraphs
below for a credential refusal, for the same stated reason. A peer that hangs up on its own still
clears it; a compatible hello ends it either way.

**GREEN.** `dotnet test TradeAgent.sln` → 75 / 111 / **249**.

**One test reversed, four adjusted, one harness gap closed.**
`When_an_incompatible_bridge_disconnects_the_status_row_is_told` asserted the reason is cleared when
the peer goes; it now asserts the announcement still fires, the version and repair survive **our own**
disconnection, and a compatible bridge is what ends it. Four refused-peer tests sent frames after the
refusal; those sends now die in the socket rather than in the connector — the same outcome, and the
tests say so through an `Unheard(...)` helper rather than pretending the send succeeded. `StubBridge`'s
own `DisposeAsync` threw while tidying a connection the far end had closed, which failed tests for the
behaviour they were asserting; it is tolerant now.

**A flake I fixed rather than retried.** The first full-suite run after the change failed
`A_refused_bridge_that_reconnects_is_refused_again` with `Broken pipe` — a bridge dialling while the
connector recycles its single instance can land on a connection already going away. A real bridge
answers that by redialling; the test dialled once. `Redial(...)` models it, and three consecutive
integration runs are clean (249 / 249 / 249).

**Mutants:**

| # | Mutant | Result |
|---|---|---|
| MD1 (the verifier's) | `Drop` wipes `_incompatible` unconditionally again | RED **8/23** — every refused-peer test plus `An_incompatible_bridge_names_its_version_and_gains_nothing_by_it` |
| MV1b | the mismatch branch parks the peer again (`return true`, the round-6 ruling) | RED 1/30 — `A_parked_refused_peer_does_not_keep_a_fixed_bridge_off_the_pipe` |

**NOT verified: V1 on Windows.** Measured on macOS, where .NET emulates named pipes over Unix domain
sockets. The mechanism — one server instance, occupied — is the same in kind on Windows (a busy
instance returns `ERROR_PIPE_BUSY`), but no box access this round, so that is reasoning and not a
measurement. Unchanged from the verifier's own statement.

### V2 (MED) — a zero not flagged when only a refused writer wrote anything · commit `bb16033`

Round 6 split the sidecar per writer so a refused bridge cannot lose its lines to a race: the owner
keeps the canonical file, everyone else writes beside it. `_noted` was computed over the whole set —
and the whole block was gated on the CANONICAL file existing. On the machine that split was built for
the owner never fails, so there is no canonical file, so the gate is false and `_noted` stays false
with five refusals sitting on disk. R3's own fix reopened the flagged-zero class.

**RED first.** `A_zero_is_flagged_when_only_a_refused_writer_wrote_anything` — an owner that writes
cleanly, a second instance refused five times, no canonical sidecar → `Assert.True() Failure` on
`reader.Noted`; `Standing` was `Clean` and `ZeroIsProvisional` false.

**The fix.** The "something was written down" question moves outside the canonical guard and is asked
of `SidecarSet()`. The deciding-line work stays inside it, deliberately and now with the reason in the
code: a second bridge turned away cost no order — the refusal is what stops the order being sent — so
its lines must not drop `SupportsClientOrderId` over somebody else's misconfiguration. They must only
stop a zero being read as a fact about submission. The test asserts that bound too (`Trouble` null,
`GapClosed` false).

**GREEN.** 75 / 111 / **250**.

**Mutants, and two of them survived for a reason worth recording:**

| # | Mutant | Result |
|---|---|---|
| MV2 | the noted question gated on the canonical file again | RED 1/119 |
| MV2b | the degraded guard widened to the whole set | **SURVIVED 120/120** |
| MV2c | `LastLineWhere` scans every per-writer file | **SURVIVED 120/120** |
| MV2d | both of the above at once — a refused writer's lines decide the machine's degraded state | RED 1/119 |

MV2b and MV2c survive because **each blocks the other**: widening the guard changes nothing while the
deciding line still only scans the canonical generations, and widening that scan changes nothing while
the guard still requires a canonical file. The boundary is real and is pinned by the pair, not by
either half — which is what MV2d shows. Recorded rather than left as two quiet passes.

### V3 (LOW) — the F18 shape is unreachable from inside this build · commit `bb5f53c`

The verifier could not drive a real rotation into the shape F18 defends against, and neither can this
build: `ReportAndQuarantine` sits after `Lease()` on both write paths, and both then run `Save`, which
writes a deciding line either way — RESOLVED when the commit lands, an `ERROR ` when it does not.

Taken as **both** halves of the brief's choice rather than either. The hand-built test's doc now says
the shape is reachable by a foreign writer, an older build or an edited directory and by nothing this
build does, so a later reader does not treat it as a live path. And the invariant that makes that true
is now pinned rather than argued: `A_rotation_by_this_build_always_leaves_a_deciding_line`, a Theory
over a rotation whose save lands and one whose save does not, asserting the current log holds the
quarantine diagnostic AND a deciding line, and that the reader agrees with what that line says.

**Why pin an invariant about something that cannot happen:** if a later change breaks it — a
quarantine that can happen without a save, a read path that writes again — the defensive guard becomes
load-bearing silently and nothing would say so. **Mutant MV3** (quarantine, then never save) → RED on
**both rows**.

### Round 7 final gate — tip `bb5f53c`

`dotnet build TradeAgent.sln` → `Build succeeded. 0 Warning(s) 0 Error(s)`, working tree clean.
`dotnet test TradeAgent.sln`:

```
Passed!  - Failed: 0, Passed:  75, Skipped: 0, Total:  75 - TradeAgent.FaultTests.dll (net10.0)
Passed!  - Failed: 0, Passed: 111, Skipped: 0, Total: 111 - TradeAgent.UnitTests.dll (net10.0)
Passed!  - Failed: 0, Passed: 252, Skipped: 0, Total: 252 - TradeAgent.IntegrationTests.dll (net10.0)
```

**432 → 438 green** (75 / 111 / 252). `CoidWitnessTests` 119 → 122; `BridgeRoundTripTests` 27 → 31.
**Mac only — no box access this round**, by instruction.

### Adjacent-regression sweep (R3)

`git diff --stat f8a724c..HEAD` → 5 files, +375 / −42: `CoidWitness.cs`, `AtasConnector.cs`,
`BridgeRoundTripTests.cs`, `CoidWitnessTests.cs`, `Harness.cs`.
`git diff --name-only … | grep -E "TradeAgent.Gateway/|AppHost.cs|Updater|TradeAgent.App/"` → **no match**.

| changed surface | consumers | proven |
|---|---|---|
| `AtasConnector`: the mismatch branch returns false; `Drop` conditionally preserves `_incompatible` (both private) | the observable surface is `Incompatible` / `Bridge` / `Capabilities`, read by `tools/probe/Program.cs:697` and `TradingGateway.cs:139/213/279` — none edited | 111 unit + 252 integration green, including 31 wire tests over real named pipes; three consecutive clean integration runs |
| `CoidWitness`: `_noted` moved outside the canonical guard | `CoidWitnessReport.Standing(witness)` → `tools/probe` | MV2/MV2d bite; the degraded boundary asserted in the same test |
| `StubBridge.DisposeAsync` / `Loop` made tolerant of a far end that closed first | test harness only — `grep -rln StubBridge src tools` → no hits | the whole integration suite |

### What I did NOT do, round 7

- **No box access**, by instruction. Every Windows claim stays where round 6 left it: the on-box suite
  figures from rounds 5 and 6 remain **withdrawn**, and the bridge compile and `tools/atas-gate` run
  remain unrepeated by anyone since. **V1 itself is measured on macOS only** — the one-instance
  mechanism is the same in kind on Windows (`ERROR_PIPE_BUSY`), but that is reasoning.
- **`Drop`'s edge case is not pinned:** a mismatch we refused survives the drop, and a LATER
  connection that drops for some other reason (an unproved peer, a silent hang-up) clears it before
  any good bridge arrives. That is the brief's literal rule and the minimal change; the alternative —
  keeping it until a compatible hello, as `_unauthenticated` is kept — is a bigger behavioural claim
  and I did not make it unasked. Named here so it is a decision and not an accident.
- **MV2b and MV2c survive** and are recorded as such, with the reason.
- **Which ATAS callback fires on a strategy teardown**, the F8 residual, `Quarantine`'s 64 slots and
  R4's Windows half are all unchanged and still open.
- Every mutated file was restored from a `cp` copy and `touch`ed, never `git checkout --`, and
  `git status --porcelain` was confirmed empty after each.
- Not pushed, not merged, not rebased.

### Round 7b — the edge named under V1 is a defect, and it is closed · commit `4de7c25`

The manager ruled the edge round 7 named ("a mismatch survives OUR drop, but a later disconnect for
another reason still clears it") a defect rather than an option: the mismatch must survive **any**
later disconnect until a **compatible hello** arrives, exactly as `_unauthenticated` is kept.

**RED first.** `A_refusal_survives_an_unrelated_disconnect_until_a_good_bridge_arrives` — a v2 peer is
refused, then something else connects to the pipe and leaves without saying a word, which ends a read
loop and runs `Drop` for a reason that has nothing to do with any refusal →
`Assert.NotNull() Failure: Value is null`. The row went blank with nothing repaired and nothing having
replaced the bridge: the operator is told to reinstall the add-on and the instruction disappears while
they are doing it.

**The fix removes the condition rather than widening it.** `Drop` no longer clears `_incompatible` at
all; the accepted-hello branch is the only thing that does. That is the same argument the file has
always made two paragraphs below for a credential refusal, now stated for both: neither is a fact that
leaves with the peer, and neither is resolved by hanging up — a peer proving itself ends the credential
refusal, a peer speaking the right protocol ends this one.

**GREEN**, and the both-directions half is in the same test: a compatible v3 hello clears the mismatch,
the status line goes null, and `ReconciliationProvable` comes back true. Two unrelated disconnects are
driven, not one, so this is a rule and not a one-drop grace.

**Mutants:**

| # | Mutant | Result |
|---|---|---|
| **MD1** (the verifier's) | `Drop` wipes `_incompatible` unconditionally | **RED 9/23** — stays RED, as the brief requires |
| MD1b | the round-7 half-measure: kept only when `_refused` | RED **1/31 — only the new test**, so the new test is exactly what closes the edge |
| MD1c | the accepted-hello branch stops clearing it — nothing ends the mismatch at all | RED 4/28 — the both-directions half is pinned, not assumed |

**Final gate at `4de7c25`:** `dotnet build TradeAgent.sln` → `Build succeeded. 0 Warning(s) 0 Error(s)`,
tree clean. `dotnet test TradeAgent.sln` → **75 / 111 / 253 = 439 green**, 0 failed. The integration
project run a second time: 253 again, so the new pipe test is not flaky.

**What I did NOT do, round 7b:** no box (unchanged — every Windows claim stays withdrawn or
unverified); the change is one file plus one test, so the sweep is the same as round 7's — `Drop` and
the mismatch branch are private, and the observable surface (`Incompatible`, `StatusDetail`, `Bridge`,
`Capabilities`) is read by `tools/probe/Program.cs:697` and `TradingGateway.cs:139/213/279`, none of
which were edited. `git diff --name-only bb5f53c..HEAD` → `AtasConnector.cs`, `BridgeRoundTripTests.cs`.
Not pushed, not merged, not rebased.

## Round 8 (build record, 2026-09-04)

Builder leg [1] continuing (§9.3), worktree `…-worktrees/u14-build`, from `4de7c25`. Inputs: Codex's
delta `records/codex-U14-r7.txt` (10/17 priors FIXED, F23–F29) and the Opus verifier
`records/U14-verify-r7.md` (FAIL 0H/2M/0L, all its round-6 findings closed). **No box** — every
Windows claim stays where round 6 left it, withdrawn or unrepeated.

| # | real/refuted | RED | mutant | commit |
|---|---|---|---|---|
| PRIOR 17 | real | `Trouble` said "changed underneath this writer" for a folder that is gone | MP17 RED 1/122 | `e0b8cf3` |
| F28 | real | `Assert.True() Failure` on `Noted` — a locked sidecar counted as empty | MF28a RED 1/123, MF28b RED 1/123 | `9123587` |
| F27 + V3 | real | `Assert.NotNull() Failure: Value is null` — the gap gone after rotation | MF27a RED 1/124; **MF27b SURVIVED then RED 1/267** (below) | `97c92ec` |
| V5 | real (test-only) | none — the state was simply never built; MV2c had no biting test | MV2c RED 1/260 | `fd33f63` |
| F29 | real | `Assert.DoesNotContain() Failure` on "candidate"; `Assert.Single()` on the listed sidecars | MF29a RED 2/260, MF29b RED 1/260 | `877f5e0` |
| F23 | real | second bridge's `ConnectAsync` timed out — `OperationCanceledException` after 2 m 3 s | MF23a RED 1/260, MF23b RED 1/260 (after the test was fixed) | `c66257c` |
| V4 | real | `StatusDetail` was "bridge 0.1.1 speaks protocol 2" for a peer failing AUTHENTICATION; `BridgeRow` said it with ATAS closed | MV4a RED 1/261, MV4b RED 1/261, MV4c RED 1/112 | `6884cdf` |
| PRIOR 21 + F26=R2 | real (one class) | "a stopped strategy took the lease back after its own teardown released it"; "the lease survived a terminal path" | MP21 RED 1/265, MF26 RED 1/265, **MP21b SURVIVED then RED 1/266** | `6da2d0e` |
| MF27b | the round-8 residual — resolved, NOT equivalent | the mutant went from SURVIVED to RED | MF27b RED 1/267, MF27a re-run RED 2/267 | `10fa21f` |

### PRIOR 17 (Codex HIGH) — a missing bridge directory is unreadable, not absent · commit `e0b8cf3`

Absence is one exception and no other: `FileNotFoundException` on the committed path, meaning nothing
has ever been written here, which is the only outcome a write may proceed against.
`DirectoryNotFoundException` was classified with it and is the opposite kind of fact — the folder this
machine's whole history lives in is gone. The ratified rule for that case is already on the record: a
machine with no bridge directory refuses every order rather than trade without a write-ahead record.

**RED first**, with the folder removed under a LIVE witness that already holds its lease, so the
refusal has to come from the read and not from the lock:
`A_missing_bridge_directory_is_unreadable_rather_than_absent` →
`Assert.Contains() Failure: Sub-string not found` for "could not be read". The writer was refused
either way — and told the witness "changed underneath this writer", which sends an operator hunting a
second bridge instead of the folder that is missing. Same shape as round 6's F17 finding, where the
diagnosis was the deliverable.

**Mutant MP17** (classify it as absence again) → RED 1/122.

### F28 (MED) — an unreadable sidecar is not an empty one · commit `9123587`

Every read of the sidecar was wrapped in a catch that answered "no lines", so a file held open
exclusively — a scanner, a viewer, another process's writer — counted as EMPTY: nothing noted, no
deciding line, standing `Clean`, and a count of zero records below it **not provisional**. That is
"this product never submitted that identifier" asserted on the strength of a file this build could not
open.

**RED first.** `A_sidecar_that_cannot_be_read_is_not_a_sidecar_with_nothing_in_it` — an `ERROR` line in
the canonical sidecar, the file then held `FileShare.None` → `Assert.True() Failure` on `Noted`.

**The fix, in both directions.** `HasNotes(path, out unreadable)` replaces the swallowing read.
Unreadable counts as written down (so the zero is flagged) **and** as a gap this run cannot rule out
(so it is degraded — the fail-closed direction the committed file already takes), with its own sentence
saying which of the two problems it is rather than borrowing the "an earlier run could not write"
wording. Scoped to the canonical generations, because a refused writer's own file being locked is not
this machine's durability problem — the F25 boundary, kept.

**Mutants:** MF28a (an unreadable sidecar counts as empty again) → RED 1/123. MF28b (unreadable is
noted but never degraded) → RED 1/123.

### F27 + V3 (MED) — the rotation crash window, and the round-7 claim it disproves · commit `97c92ec`

Rotation deleted the older generation and THEN moved the current log onto its name, so between those
two acts the only copy of the last safety line was gone. The session that rotates does write a deciding
line of its own — round 7's invariant — but it writes it AFTERWARDS.

**RED first, observed where the window actually is:** inside the rename seam, after the quarantine line
has been appended and the rotation has happened, and before the save has committed anything. A reader
constructed at that instant is exactly what the next process would see.
`An_unresolved_gap_survives_a_crash_inside_the_rotation_window` → `Assert.NotNull() Failure: Value is
null`. The gap was plainly visible before the rotation and gone after it.

**The fix restates before it destroys.** `Rotate(log)`: read the last deciding line while both
generations are intact; move the current log aside under a staging name (the older generation is
untouched, so the gap is still readable from it); create the new log with the restatement as its FIRST
line, flushed; and only then delete the older generation and move the staging file onto its name. A
crash at any point leaves the unresolved line readable from one of the two files a reader scans. With
nothing unresolved to carry, the plain two-step rotation is used — there is nothing to protect.

**V3's claim was wrong and is corrected in both test docs.** Round 7 said no session of this build can
write a diagnostic without also writing a deciding line. That is true of a session that RUNS TO
COMPLETION, and the window is between the two acts: a machine that dies there completes neither. Codex
found it; round 7 excluded it by assumption. `A_rotation_by_this_build_always_leaves_a_deciding_line`
now says it covers the completed session and names the test that covers the crash;
`An_unresolved_gap_is_not_lost_when_the_sidecar_rotates` says the guard is still defensive and now for
a stated reason — what remains reachable is a foreign writer, an older build, or a hand-edited
directory.

**GREEN.** 75 / 111 / **256**.

**Mutants:**

| # | Mutant | Result |
|---|---|---|
| MF27a | no carry — the old delete-then-move rotation | RED 1/124 |
| MF27b | the carry computed but the old generation deleted BEFORE it is written | **SURVIVED 125/125** |

**MF27b survived, and the reason is that its window has no observation point in this process.** The
fixed order and the mutant's differ only for a crash landing between two consecutive syscalls
(`File.Delete(rolled)` and `File.WriteAllText(log, …)`), with the current log already moved aside. My
test observes the window at the rename seam, which is after the whole rotation; nothing in-process can
stop the machine between those two lines. **NOT verified: that the ordering inside `Rotate` is
load-bearing** — it is reasoned from the file states, not measured, and it is a genuine if very narrow
weakening if a later edit reverses it. Recorded rather than left as a quiet pass; a seam existing only
to observe it would be production surface added for one mutant.

### V5 (verifier, MED) — the noted/degraded boundary, pinned by a test instead of by two mutants · commit `fd33f63`

Round 7 recorded MV2b and MV2c as surviving because "each blocks the other". The verifier showed that
account is half right. MV2b — widening the degraded guard to the whole per-writer set — is genuinely
inert, because `_noted` is computed OUTSIDE that guard; that half stands. MV2c — the deciding-line
scan reading every per-writer file — changes real behaviour, and the state that shows it was never
built by any test: **the canonical sidecar EXISTS but holds only a diagnostic, while a refused
writer's own file holds an unresolved safety line.**

`A_refused_writers_safety_line_flags_the_zero_without_degrading_the_machine` builds exactly that: an
owner writes cleanly and quarantines a leftover (canonical sidecar exists, `ignored`, no `ERROR`),
then a second session is refused the lease and writes its own `ERROR` line beside it. The outcome it
pins is both halves of the round-6 decision at once — **flagged** (`Noted`, `ZeroIsProvisional` true)
and **not degraded** (`Trouble` null, no `io:degraded`, `GapClosed` false), because the refusal is what
stopped that order being sent, and dropping `SupportsClientOrderId` over another program's
misconfiguration is the row crying wolf.

No production change: the committed behaviour was already correct, and the defect was the missing
test. **Mutant MV2c** (`LastLineWhere` iterates `SidecarSet()` instead of `SidecarGenerations()`) →
**RED 1/260**, the new test alone. MV2b is left as recorded — inert, not unpinned.

### F29 (LOW) — a Noted row that names one of its two causes, and a report that omits the file · commit `877f5e0`

`Noted` was written when the only way to reach it was a temp beside the witness being declined, and it
still said "a candidate beside the witness was refused" after the sidecar was split per writer. So a
machine whose real problem was two bridges contending for the witness was described as a rejected
candidate — and `tools/probe` printed the tail of the canonical file, which on that machine does not
exist. The one thing an operator would act on was the one thing not on the page.

The headline now names neither cause as if it were the only one, the explanation names both and says
nothing was lost either way, and `CoidWitness.SidecarPaths` lists every sidecar that exists so the
probe prints each with its tail.

**Mutants:** MF29a (the old single-cause wording restored) → **RED 2/260**
(`A_refused_second_bridge_is_named_and_its_own_sidecar_is_listed` and
`The_three_witness_headlines_are_distinguishable`). MF29b (`SidecarPaths` narrowed to the canonical
generations, which is what the probe printed before) → **RED 1/260**.

### F23 (MED, the V1 class from the other side) — a peer nobody can hear is dropped · commit `c66257c`

The read loop sat inside `ReadLineAsync` for ever. `HeartbeatTimeout` was consulted only by
`GetHealthAsync`, for what the row DISPLAYS — nothing acted on it. The pipe is created with
`maxNumberOfServerInstances = 1` and the accept loop recreates that instance only after the read loop
ends, so a bridge whose ATAS froze, or a peer that opened the pipe and stalled before its first
newline, kept every later bridge off the machine while the row reported nothing worse than a stale
connection. Round 7 dropped a peer whose PROTOCOL was refused; this is the same class reached from the
other side.

**RED first**, with the production side reverted to `4de7c25` and the test kept:
`A_silent_peer_is_dropped_so_the_pipe_can_be_taken_again` →
`System.OperationCanceledException : The operation was canceled` inside `StubBridge.Authenticate`,
after **2 m 3 s** — the second bridge exhausted twelve redials against a pipe the silent peer still
owned. **GREEN in 2 s** with the fix.

**The fix polls beside the read rather than replacing it.** `IdlePoll` (a third of the heartbeat
timeout, floored at 100 ms) races `Task.Delay` against the pending `ReadLineAsync`; when the delay
wins, `PeerHasGoneQuiet()` decides. **The pending read is never restarted and never cancelled** —
abandoning it to start another would put two reads on one `StreamReader` and split a frame between
them, and cancelling one mid-flight can consume bytes that are then lost. The same task is carried
across polls; staleness breaks the loop and the `finally` disposes the stream, which is what actually
aborts the read. Staleness is measured from the LATER of the peer's arrival and its last heartbeat, so
a peer that never says anything at all is dropped once its window is spent rather than held for ever,
and a peer still completing its handshake gets the same window as one that has been talking. The
markers a refusal left behind are untouched: the drop goes through the existing `Drop`, which since
round 7/7b keeps `_incompatible` and `_unauthenticated`.

**Both directions, and the second one only exists because a mutant refused to die.** MF23a (never
drop) → RED 1/260 immediately. MF23b (drop at every idle poll, alive or not) **SURVIVED 260/260** on
the first attempt: my liveness test beat every 200 ms against a 333 ms poll, so the poll never once
woke with no frame in hand and the mutated predicate was never consulted. The test was wrong, not the
mutant. `A_peer_that_keeps_beating_is_not_dropped_when_the_window_passes` now beats every 800 ms
against a 500 ms poll inside a 1.5 s window — an empty poll in every gap, five gaps — and MF23b is
**RED 1/260**.

### V4 (verifier, MED) — the row describes the peer that is there NOW · commit `6884cdf`

**The class (§9.10), in the verifier's words:** a refusal is kept until its own repair, and nothing
asks whether it still describes the peer that is there now. Round 7b made a protocol refusal permanent
until a compatible hello; a credential refusal already lasted until a peer proved itself. Neither
supersedes the other, and both readers preferred the older fact — `StatusDetail` returns
`_incompatible` first, and `AtasHealth.BridgeRow` returns any non-empty refusal ahead of everything
derived from the machine. Two instances, one root cause, so the fix is stated once and applied to
both: **the newer observation leads; the older marker stays recorded and stops outranking it.**

**RED first, on the sequence an operator actually walks.** `A_live_refusal_is_not_masked_by_a_stale_one`
— a version-2 bridge authenticates and is refused on protocol; it leaves; the operator reinstalls; the
new DLL reaches the pipe and says hello without ever presenting the secret (a stale `bridge.auth` or
the wrong `TRADEAGENT_HOME`, both documented) →
`Assert.Contains() Failure: Sub-string not found. String: "bridge 0.1.1 speaks protocol 2, this
buil"··· Not found: "did not authenticate"`. The operator is told to repeat what they just did, while
the live sentence — which says another program may have taken the pipe name and TradeAgent will not
trade through it — is held behind it.

**RED for the second instance**, `A_recorded_refusal_does_not_outrank_a_platform_that_is_not_there` →
`Not found: "waiting for ATAS to start"` with ATAS closed. A refusal describes a PEER, and no peer can
be on the pipe from a platform that is not running.

**The fix is structural on the connector side.** `_incompatible` and `_unauthenticated` are no longer
assignable directly: `NoteIncompatible` / `NoteUnauthenticated` are the only writers, and each stamps
its marker with `Interlocked.Increment(ref _observed)` — a counter, not a clock, because two
observations in the same tick must still order and nothing here needs to know how long ago anything
was. `StatusDetail` compares the two stamps when both are live. The derived `Silent` reading needs no
stamp: the `Unauthenticated` getter only produces it while `_incompatible` is null, so it can never be
the loser of that comparison. On the health side `BridgeRow` consults the machine facts — the DLL
installed, ATAS running, both re-read on every pass — before it renders a refusal, and keeps the
refusal after them rather than dropping it, because it is the repair that will be needed once the
platform is back.

**Nothing about round 7b is undone.** `Incompatible` still reports the protocol refusal after the
newer one lands (the test asserts `ReportedProtocolVersion == 2` at the end), and no round-7/7b test
moved. Both suites re-run: unit 112/112, integration 261/261.

**Mutants:**

| # | Mutant | Result |
|---|---|---|
| MV4a | `StatusDetail` returns the incompatible marker whenever it is set — the old precedence | RED 1/261 |
| MV4b | a newer refusal ERASES the older one instead of outranking it (the shape MS1 diagnosed) | RED 1/261 — the older marker must stay readable |
| MV4c | `BridgeRow` returns the refusal before consulting the machine again | RED 1/112 |

### PRIOR 21 (MED) + F26 = R2 (MED) — one class, and it had to be moved before it could be tested · commit `6da2d0e`

Two findings, one root cause: **the adapter's teardown was a sequence of unsynchronised statements,
and nothing made "this strategy is down" and "this strategy no longer owns the witness" the same
fact.**

- **PRIOR 21.** The order-event fan asked `if (!_stopped)` and then reached for the witness; the
  teardown set that flag and then disposed the lease. Four statements, two pairs, no lock between
  them. So the fan reads the flag while the strategy is still running, ATAS stops the strategy, the
  lease is released, and the fan — already past its check — calls `Identified`, which leases the file
  again for a strategy that no longer exists and holds it for the life of the ATAS process. Round 6's
  "look before leasing" narrows this to a record of the RUNNING session with no broker id yet, which
  is exactly the order the fan is about to identify.
- **F26 = R2.** `UntrackSecurities()` and `_witness.Dispose()` were two plain statements.
  `UntrackSecurities` calls into ATAS while ATAS is taking the strategy down — the one moment the
  platform is most likely to answer with an exception — and if it throws, the disposal never runs and
  the lease survives a TERMINAL path.

**Why the fix is a new file.** `AtasStrategyAdapter.cs` is `<Compile Remove>`d unless the build runs
against a real ATAS install, so neither defect could be given a failing test where it stood — and
**there is no box this round**. Both rules need no ATAS type at all: one is a `finally`, the other is
a lock. They are now `AdapterTeardown` (`src/TradeAgent.AtasBridge/AdapterTeardown.cs`), which
compiles everywhere, and `AdapterTeardownTests` drives both interleavings against a **real
`CoidWitness` and a real lease on the real filesystem** — a double for the lease would prove nothing
about the lease. Same move, same reason, as `AdapterTouchedOrders`, `ClientOrderIdProof` and
`AtasCall`, each of which was extracted from this file for exactly this.

**RED first, against the extraction carrying the committed adapter's logic verbatim** — flag checked
outside any lock, release as a plain statement after the steps:

```
An_exception_in_teardown_does_not_keep_the_witness [FAIL]
  the lease survived a terminal path — a stopped strategy is still refusing the witness to the live one
A_write_that_began_before_the_stop_cannot_take_the_lease_back_after_it [FAIL]
  a stopped strategy took the lease back after its own teardown released it
```

**The fix.** `Record(Action write)` takes the check and the write under the same lock the release
takes, so one of the two orders is always chosen — a second read of the flag would not close the
window, only choosing does. `Stop(steps, releaseWitness)` raises the flag BEFORE the steps and
releases in a `finally`; the exception still reaches `Guard`, it simply no longer decides whether the
release happens. `ProveClientOrderId` stays OUTSIDE the lock and runs only when the write was allowed:
it scans ATAS's order book, and holding a lock teardown waits on across that scan would put an
unbounded wait on ATAS's own thread — the shape `AtasCall` exists to keep out of this file.

**Mutants:**

| # | Mutant | Result |
|---|---|---|
| MP21 | the check leaves the lock again | RED 1/265 |
| MF26 | the release goes back to a plain statement after the steps | RED 1/265 |
| MP21b | `_stopped = true` moved down beside the release | **SURVIVED 5/5**, then RED 1/266 |

**MP21b survived, and it was not equivalent — it was a gap.** With the flag raised only where the
release is, the lease still ends up free, so nothing about PRIOR 21's own harm shows; what it changes
is round 6's rule — a stopped strategy records nothing — for the whole width of the teardown, which
disposes the bridge under a deadline and unsubscribes from ATAS. A fan callback landing in there would
write into the witness of a strategy ATAS has already taken down.
`A_write_that_arrives_while_the_teardown_is_running_does_not_run` calls `Record` from inside the
teardown steps and asserts it is refused; MP21b is then RED 1/266.

**Adjacent-regression sweep.** `AdapterTeardown` is new and has exactly one consumer,
`AtasStrategyAdapter` (`Started()` in `StartBridge`, `Stop(...)` in `StopBridge`, `Record(...)` in
`OnOrderPayload`). The `volatile bool _stopped` field is gone from the adapter — **anything grepping
the box's copy for the round-6 marker `_stopped` must now grep for `_teardown`.** Unit 112/112,
integration 266/266 on the Mac.

**NOT verified, and it is the load-bearing gap of this item:** `AtasStrategyAdapter.cs` was **not
compiled** — it cannot be, off a Windows box with ATAS installed, and this round has no box. What was
verified about that file is that it still PARSES: a Roslyn syntax-only parse
(`CSharpSyntaxTree.ParseText`, `LanguageVersion.Preview`, errors only) → `PARSE OK`, exit 0. That
catches a broken edit; it does not catch a type error, an overload that does not bind, or a wrong
member name. **The adapter's three call sites into `AdapterTeardown` are unproven until the bridge is
compiled on the box with `-p:AtasBridgeBuild=true` and `tools/atas-gate` is re-run.**

### MF27b — the residual, closed: the mutant was not equivalent, the test was looking in the wrong place · commit `10fa21f`

Earlier this round MF27b (the carry computed, but the older generation deleted BEFORE the restatement
is written) **SURVIVED 125/125**, recorded with the reasoning that its window has no observation point
in this process — two consecutive syscalls with nothing between them — and an explicit **NOT verified:
that the ordering inside `Rotate` is load-bearing.** A surviving mutant on a crash-window fix is a
defect in the test, so it was re-opened rather than left standing.

**The reasoning was wrong about where the window is.** It treated the window as reachable only by a
machine dying between two instructions. The second of those instructions is a **file write**, and a
file write fails: a full disk, a directory a backup tool made read-only, a scanner holding the name.
`Rotate` runs inside `AppendToErrorLog`'s catch, so none of those stop the process — they leave the
sidecar in exactly the state the ordering produced, and that state is what the next start reads. So
the mutant is **not equivalent**: it is a real weakening reachable without any crash at all.

**And a write is a step this class already has seams for.** `_replace` exists because "a rename
refused because the destination is open … cannot be provoked on the machine the code is written on";
`_open` exists for the same reason one step along. `_writeSidecar` is the third of that family, and it
buys the thing the round-8 note said could not be bought: **standing inside the restatement write is
standing in the window.** What is on disk at that instant is what a machine that never finished the
write would leave behind.

`The_restatement_lands_before_the_generation_holding_the_gap_is_destroyed` builds the busy-machine
state (the unresolved failure one generation back, the current log oversized and diagnostic-only),
takes a reading from inside the write, and then lets the write proceed — **nothing is made to fail**,
so the only thing the assertion can be measuring is which of the two acts happened first:

- restate first → the older generation is still intact at that instant, the gap is read off it,
  `Trouble` is not null;
- delete first → the generation is gone and the restatement has not arrived, so the machine reads
  healthy over an open durability gap.

It also asserts that the rotating session went on to resolve the gap (round 7's invariant), which is
the reason the reading had to be taken where it was rather than at the end.

| # | Mutant | Result |
|---|---|---|
| MF27b | `File.Delete(rolled)` moved above the restatement | **RED 1/267** (was SURVIVED 125/125) |
| MF27a | no carry at all — the old delete-then-move rotation | re-run with the seam in place: **RED 2/267** |

**The round-8 "NOT verified" on the rotation ordering is withdrawn**: the ordering is now measured, not
reasoned. `_writeSidecar` is null in every production use, exactly as `_replace` and `_open` are.

### Refuted this round — the manager's five, written down with the reason (no code)

Recorded rather than silently dropped (§4.4): a finding that was considered and found wrong leaves a
record saying so.

- **PRIOR 5 PARTIAL — refuted.** The on-box gate execution evidence exists, in `records/U14.md` on
  main, round 6 (the `tools/atas-gate` transcript: `GATE PASSED`, both directions). What Codex read
  was the BRANCH copy of the record, which is the 2026-09-03 snapshot and predates it. The finding is
  about which file was read, not about the evidence.
- **PRIOR R4 — refuted as an accepted, documented LOW residual.** The lease is a claim on a PATHNAME,
  not on a name that cannot be unlinked. On macOS an unlink while the owner lives yields a second
  owner — measured in round 6, and measured to cost no claim, because the compare-and-swap and the
  read-back still refuse. On Windows the handle is held with `FileShare.None`, which blocks the
  deletion outright. macOS is not the production platform. Holding a name rather than a handle is a
  different mechanism, not a smaller one, so there is no one-line guard to add.
- **PRIOR R5 — refuted.** Record wording; the record lives on main and says what the compare-and-swap
  actually catches. Same branch-copy staleness as PRIOR 5.
- **F24 (LOW) — refuted.** `_incompatible` being instance-scoped is BY DESIGN. A connector restart
  re-derives the refusal at the next hello, within seconds; persisting it across process lifetimes
  buys nothing and would need a store the connector does not own. (Round 7b made it permanent for the
  life of the INSTANCE, which is the scope that was actually wrong, and V4 above is what stops that
  permanence outranking a newer observation.)
- **F25 (MED) — refuted, and the imprecise sentence corrected here.** "Degraded" spans the OWNER's
  canonical sidecar; "noted" spans the whole per-writer set. A refused writer's `ERROR` line records a
  refusal that **cost no order** — the adapter refused it — so it makes the zero provisional (Noted)
  but does not degrade the owner's witness. The round-6 verifier examined and accepted that reasoning.
  The decision text in `briefs/U14-r6-bounce.md` was imprecise; this sentence is the correct one, and
  V5 above pins the boundary with a test in the one state that distinguishes the two.

### Adjacent-regression sweep for round 8 (R3)

| Changed surface | Consumers enumerated | Proof |
|---|---|---|
| `AtasConnector.StatusDetail` — property expression became a body; `_incompatible` / `_unauthenticated` now only writable through `NoteIncompatible` / `NoteUnauthenticated` | `IConnectorStatusDetail` (`ConnectorSdk/Contracts.cs:75`), `TradingGateway.cs:72` (reads it when the state is FAILED), `AtasHealth.cs:169` (passes it in as `refusal`). No signature changed — still `string?`, still the interface member | solution builds clean; unit 112/112, integration 267/267 |
| `AtasHealth.BridgeRow` — the refusal branch now consults `d.BridgeInstalled` / `d.Running` first | `AtasHealthReporter` (`AtasHealth.cs:169`) → `HealthRegistry` → the dashboard rows. Signature unchanged; both new strings are display text. The three pre-existing refusal tests use `Machine()` (installed + running), which is the branch that still returns the refusal alone | `AtasHealthTests` 14/14 |
| `AtasConnector` read loop — `ReadLineAsync` now raced against an idle poll | nothing outside the class: the loop is private and the pending read is never restarted or cancelled | integration 267/267, including every `BridgeRoundTripTests` / `BridgePipeAuthTests` handshake |
| **NEW** `AdapterTeardown` (public class, `TradeAgent.AtasBridge`) | one consumer: `AtasStrategyAdapter` — `Started()` in `StartBridge`, `Stop(...)` in `StopBridge`, `Record(...)` in `OnOrderPayload`. **`volatile bool _stopped` is gone from the adapter; the round-6 grep marker on the box is now `_teardown`** | `AdapterTeardownTests` 6/6. **The adapter's three call sites are NOT compiled** — see the NOT-verified note under PRIOR 21 |
| `CoidWitness` ctor gained `writeSidecar` (optional, defaulted, null in every production use); `Rotate` routes its restatement through it | `AtasStrategyAdapter` uses `new CoidWitness()`; `tools/probe` and the tests use the positional forms. No existing signature changed, so every call site compiles unchanged | solution builds clean including `tools/probe`; integration 267/267 |

### Final gate (this Mac, 2026-09-04)

```
$ dotnet build TradeAgent.sln --no-incremental
  … 17 projects …
Build succeeded.
    0 Warning(s)
    0 Error(s)

$ dotnet test TradeAgent.sln
Passed!  - Failed: 0, Passed:  75, Skipped: 0, Total:  75 - TradeAgent.FaultTests.dll
Passed!  - Failed: 0, Passed: 112, Skipped: 0, Total: 112 - TradeAgent.UnitTests.dll
Passed!  - Failed: 0, Passed: 267, Skipped: 0, Total: 267 - TradeAgent.IntegrationTests.dll
```

**75 / 112 / 267 = 454**, from 439 at `4de7c25`. Tip `10fa21f`. No test method present at `4de7c25` was
removed — checked with `git diff 4de7c25 -- tests/ | grep -E '^-.*public (async )?Task |^-.*public void '`
after every structural edit; empty every time.

### What round 8 did NOT do

- **`AtasStrategyAdapter.cs` was not compiled and not executed.** It is `<Compile Remove>`d off a
  Windows box with ATAS installed, and this round had no box. Its three new call sites into
  `AdapterTeardown` are unproven. What WAS verified is that the file still parses — a Roslyn
  syntax-only parse (`CSharpSyntaxTree.ParseText`, errors only) → `PARSE OK`, exit 0 — which catches a
  broken edit and catches nothing about types or overload resolution. **The bridge compile
  (`-p:AtasBridgeBuild=true`) and `tools/atas-gate` must both be re-run on the box before PRIOR 21 and
  F26 can be called closed.**
- **No on-box run of any kind.** Every Windows claim stands exactly where round 6 left it, withdrawn
  or unrepeated. Nothing in this round's text is an on-box measurement.
- **PRIOR 21 and F26 share one commit** (`6da2d0e`) rather than one each, because they share one root
  cause and one structural fix — the extracted seam (§9.10, class over instances). The record
  attributes them separately.
- **The V4 fix does not renegotiate round 7b.** A refusal is still permanent until its own repair;
  only its PRECEDENCE against a newer observation changed. Whether the dashboard renders the new
  `BridgeRow` strings well is **NOT verified** — the row was measured as a pure function, not on a
  screen; that is the same gap the round-7 verifier declared for its own V4 probe.
- **MF23b and MP21b were each SURVIVED before they were RED.** Both are recorded with what the test
  was missing, not quietly re-run until green.
- **No push, no merge, no rebase.** Nothing was run in the main worktree.

## Round 9 (build record, 2026-09-04)

Fresh builder leg [1] (round 8's session is gone), worktree `…-worktrees/u14-build`, branch
`u14-coid-witness-rewrite`, from `10fa21f` (454 green). Inputs: the Opus verifier
`records/U14-verify-r8.md` (**FAIL 3H/1M/0L**) and Codex's delta `records/codex-U14-r8.txt`
(0H/3M/0L, F30–F32 + PRIOR 27/28/29).

**The class this round exists to end, in the verifier's words (§9.10):** *each round-8 guard was
proved in exactly the state its own author built, and each has a neighbouring state one step away
that the fix does not reach.* So every item below carries a **class-closure argument**: the
operations or states of the class are enumerated, and each one is shown to be covered by a test.
Fixing the instance is not the deliverable this round.

| # | RED | mutant | commit |
|---|---|---|---|
| R8-1 = F30 + PRIOR 27 | 3 tests RED at `10fa21f`; and a real SIGKILL: CASE B read `Trouble <null>` / `io:noted` | MR9-1a RED 3/131, MR9-1b RED 1/131, MR9-1c RED 1/131, MF27b RED 1/131 | `62779f0`, `9b74dd0` |
| R8-2 | 3 tests RED against the committed unguarded write shape | MR9-2 RED 3/9 | `ae36984` |
| R8-4 | MP21-half survived the lifted 40-round race 10/10 — so the lock is staged instead | MP21-half RED 1/10, MP21 RED 2/10, MF26 RED 2/10, MP21b RED 7/10 | `04be628` |
| R8-3 | `OperationCanceledException` after 2 m 3 s — the replacement bridge shut out by a dribbling peer | MR9-3 RED 1/36, MF23a RED 2/36, MF23b RED 30/36; **MR9-3b SURVIVED** (recorded) | `3d8e793` |
| F31 + PRIOR 28 | a directory at the sidecar's name read as Clean; and the theory found a THIRD probe | MR9-4a RED 4/141, MR9-4b RED 3/141, MR9-4c RED 4/141 | `0d01976` |
| F32 | `TimeoutException` — `Unauthenticated` stayed null for a newly arrived silent peer | MV4a RED 2/37, MR9-5a RED 1/37, MR9-5b RED 1/37 | `cd481c2` |
| PRIOR 29 | 4 tests RED against round 8's single `Noted` sentence | MR9-6a RED 4/144, MR9-6b RED 1/144, MR9-6c RED 1/144 | `e113c4c` |

### R8-1 = Codex F30 + PRIOR 27 (HIGH, class: the rotation crash window) · commits `62779f0`, `9b74dd0`

Round 8 moved the current log to `<log>.rotating` before restating — and **no reader scanned that
name**. `SidecarGenerations()` yielded the log and `.1` and nothing else. Both round-8 rotation
tests seed the unresolved `ERROR` into `.1`, one generation BACK, where it is untouched until the
last two statements of `Rotate`; the ORDINARY state is the other one — safety events are unrationed,
the cap is 64 KiB, and nothing moves an `ERROR` out of the current log except a rotation — and in
that state the gap is invisible from the `File.Move` until the restatement lands, and gone for good
if it never lands.

**RED first, with the final test text, against `10fa21f`'s production file** (swapped in from
`git show`, restored from a `cp` copy afterwards):

```
A_gap_in_the_current_log_is_readable_at_every_instant_of_the_rotation           [FAIL]
  Assert.DoesNotContain() Failure: Sub-string found
  String: ···"re the restatement: <null>\n3 after the re"···   Found: "<null>"
A_restatement_that_never_lands_leaves_the_gap_where_a_reader_still_finds_it     [FAIL]
  Assert.NotNull() Failure: Value is null
A_rotation_does_not_delete_a_staging_file_that_still_holds_the_gap              [FAIL]
  Assert.NotNull() Failure: Value is null
Failed!  - Failed: 3, Passed: 0, Skipped: 0, Total: 3
```

**And the real SIGKILL the brief asked for, out of process** (`scratchpad/rotkill`, a console with a
`ProjectReference` to the real `TradeAgent.AtasBridge`, killing itself with
`Process.GetCurrentProcess().Kill()` from inside the restatement write; a second invocation reads
what a restart sees). CASE A is round 8's arrangement, CASE B is the ordinary one:

```
############ PRE-FIX (10fa21f) CASE B — the ERROR in the CURRENT log
  killed, exit=137
  files      : coid-witness.errors.log.rotating
  Trouble    : <null>
  Token      : session:3f5b71e0,records:1,prior:0,io:noted
  Noted      : True   GapClosed : False   Standing : Noted        ← the verifier's figure, reproduced

############ FIXED, CASE B
  killed, exit=137
  files      : coid-witness.errors.log.rotating
  Trouble    : an earlier run could not write the write-ahead record; the account of it is in …
  Token      : session:36c4c774,records:1,prior:0,io:degraded
  Noted      : True   GapClosed : False   Standing : Unresolved
```

CASE A reads `io:degraded` / `Unresolved` before and after — it was never the broken one, which is
exactly the point of the finding.

**The fix, three parts.** (1) `SidecarGenerations()` yields `log + ".rotating"` **between** the log
and `.1`, which is where it sits in age. (2) `Rotate` computes the deciding line over the whole
scanned set BEFORE anything is removed, and a leftover staging file — the only copy of its own gap,
by construction, since it exists only because a previous rotation died before its restatement landed
— is **restated into the current log before it is deleted**. It used to be deleted first, under a
comment saying its content is already restated in the current log, which is true of every rotation
that COMPLETED and false of exactly the one that did not: two rotations then did between them what
neither does alone. (3) The restatement is written through `WriteDurably` — a `FileStream` plus
`Flush(flushToDisk: true)` — so "written before destroyed" is a fact about the disk and not about
this process's view of it.

**Class-closure argument (the interleavings of `Rotate`, carry path, enumerated).** A reader scans
`{log, log.rotating, log.1}`; the claim is that at every instant at least one of them holds the
unresolved line.

| # | instant | where the line is | covered by |
|---|---|---|---|
| 1 | entry, before anything runs | `log` (ordinary) / `.1` (round 8's) / `.rotating` (a previous crash) | reading 1 of `…every_instant…`; `…staging_file_that_still_holds_the_gap`'s pre-assertion; CASE A + CASE B pre-reads |
| 2 | a leftover is carried into `log`, before it is deleted | `log` **and** `.rotating` | `…staging_file_that_still_holds_the_gap` (the seam reading) — mutant MR9-1c |
| 3 | after `File.Move(log, staging)`, restatement not yet written | `.rotating` | reading 2 of `…every_instant…`; the real SIGKILL, CASE B — mutant MR9-1a |
| 4 | the restatement write never lands at all | `.rotating`, and it is the ONLY copy (asserted) | `A_restatement_that_never_lands_leaves_the_gap_where_a_reader_still_finds_it` |
| 5 | after the restatement lands, before `File.Delete(rolled)` | `log` **and** `.rotating` | reading 3 of `…every_instant…` |
| 6 | after `File.Delete(rolled)`, before `File.Move(staging, rolled)` | `log` **and** `.rotating` | reading 4 of `…every_instant…` (the delete is performed inside the seam, so the state is built rather than assumed) |
| 7 | after the rotation completes | `log` (restatement) **and** `.1` | `An_unresolved_gap_is_not_lost_when_the_sidecar_rotates`, `The_restatement_lands_before_the_generation_holding_the_gap_is_destroyed` |
| 8 | the plain path (`carry is null`) | nothing unresolved exists to lose — the deciding line was read off all three files first and is absent or `RESOLVED` | `A_gap_closed_before_the_rotation_stays_closed_after_it` |

There is no ninth: `Rotate`'s carry path is exactly `AppendDurably? · Delete(staging)? · Move ·
write · Delete(rolled) · Move`, six acts, and rows 2–7 are the six gaps between and after them.

**Mutants:**

| # | Mutant | Result |
|---|---|---|
| MR9-1a | `SidecarGenerations()` stops yielding the staging name (round 8's reader) | **RED 3/131** |
| MR9-1b | the leftover staging file deleted at the TOP of `Rotate` again, before the deciding line is read | **RED 1/131** |
| MR9-1c | the leftover deleted WITHOUT the carry being restated first | **RED 1/131** |
| MF27b | round 8's own — `File.Delete(rolled)` moved above the restatement | **RED 1/131**, still bites at this tip |

**NOT verified: that `Flush(flushToDisk: true)` reaches the platter.** No in-process observation on
this machine distinguishes a flushed write from an unflushed one, and a `SIGKILL` does not either —
the page cache survives it, so only a power cut or a crashed kernel would tell them apart. No mutant
that removes the flush goes RED, and I am recording that rather than claiming a test for it. What IS
measured is the ORDER — the restatement is complete and readable by another handle before any delete
runs (rows 5 and 6 above) — and the flush is what makes that order hold across a power cut as well.

### R8-2 (HIGH, class: the guard was a convention at four call sites) · commit `ae36984`

`AdapterTeardown.Record` guarded ONE of FOUR witness write sites. The other three run on the
BridgeServer frame loop, which outlives the teardown by construction — `DisposeAsync` waits 5 s and
gives up (`BridgeServer.cs:450`), `StopBridge` wraps that in `StopTimeout` and catches its own
timeout (`AtasStrategyAdapter.cs:499-502`), and its own doc says the abandoned loop "still holds its
pipe client until whatever wedged it returns". So a `Place` in flight reached the witness AFTER the
lease was released and leased it back for a strategy ATAS had already stopped.

**The four sites, enumerated (this is the class-closure enumeration, at `10fa21f`):**

| line at `10fa21f` | call | through the guard then? | now |
|---|---|---|---|
| `:2055` | `_teardown.Record(() => _witness.Identified(o.Comment, o.Id))` — the order-event fan | yes | `_teardown.Identified(…)` |
| `:1409` | `_witness.Submitting(…)` — `Place`'s write-ahead record | **no** | `_teardown.Submitting(…)` |
| `:1562` | `_witness.Identified(cmd.ClientOrderId, order.Id)` — `Place`'s identification | **no** | `_teardown.Identified(…)` |
| `:1824` | `_witness.Submitting(…)` — `ClosePosition`'s write-ahead record | **no** | `_teardown.Submitting(…)` |

**The fix is not four call-site edits — it is that the adapter no longer has a witness to call.**
`AdapterTeardown` OWNS the `CoidWitness` (`AdapterTeardown(CoidWitness)`, and the parameterless
production ctor makes its own). `Submitting` and `Identified` are methods on it and both go through
`Record`; the six read members the adapter needs (`Trouble`, `Path`, `LastWriteFailure`, `Token()`,
`PriorSession`, `PriorSessionIds`) are forwarded UNGUARDED, because reading takes no lease —
`EnsureRecovered` is explicitly the path that never does — and refusing a diagnostic to a strategy
that is going down would blank the sentences an operator needs exactly then. `Stop(steps)` releases
its own witness in its `finally`; the `releaseWitness` parameter is gone.

```
$ grep -n "_witness" src/TradeAgent.AtasBridge/AtasStrategyAdapter.cs
$ echo $?
1
```

**Class-closure argument.** The class is "a witness WRITE that does not pass the guard". A write is
`CoidWitness.Submitting` or `CoidWitness.Identified` — the only two members that lease (`Lease()` is
called from those two paths and from nothing else; every other public member is a read, and
`EnsureRecovered`'s doc is the standing statement that readers never take the lock). After this
commit the adapter holds no `CoidWitness` reference at all, so the set of call sites that can reach
either write from that file is EMPTY unless someone adds a new `CoidWitness` to it — which is a new
field, not a new call. A fifth site cannot be added by forgetting a wrapper, which is exactly how the
three unguarded ones came to exist.

**RED first**, with the two doors present but carrying the committed adapter's logic verbatim (a
plain `_witness.Submitting` / `_witness.Identified`, no `Record`) — which is the state of the code
at `10fa21f`, moved to where a test can reach it:

```
A_write_that_arrives_while_the_teardown_is_running_does_not_run          [FAIL]
   the strategy was already being taken down and the fan still wrote to its witness
An_identification_that_arrives_after_the_teardown_is_refused_not_raced   [FAIL]
   a strategy ATAS has already stopped recorded a broker id
A_write_ahead_record_that_arrives_after_the_teardown_is_refused_not_raced [FAIL]
   a strategy ATAS has already stopped wrote a write-ahead record for a new order
Failed!  - Failed: 3, Passed: 6, Skipped: 0, Total: 9
```

**GREEN** with both doors routed through `Record`: `AdapterTeardownTests` 9/9, then 10/10 with R8-4's
test below. Both directions: `A_running_strategy_records_through_both_doors` asserts a live strategy
still writes through each and that the record reaches the disk, so the refusals above are not
satisfied by a guard that refuses everything.

`Place` and `ClosePosition` treat a `false` return as the refusal a failed `Submitting` already was —
`AtasRejectedException`, "nothing was submitted" — and the message names which of the two it was:
`_teardown.LastWriteFailure ?? (_teardown.Stopped ? "this strategy is being taken down." : "")`.

### R8-4 (MED) — a half-moved guard, killed deterministically instead of probabilistically · commit `04be628`

The verifier's finding: PRIOR 21's rule is "the check and the write are ONE act, under the lock the
release takes", and no test distinguished that from "the write is under the lock". **MP21-half**
(only the CHECK leaves the lock) survived every `AdapterTeardownTests` case; the verifier's 40-round
two-thread probe caught it on the verify box.

**I lifted that probe into the suite (`A_stop_that_lands_mid_write_never_leaves_the_lease_held`, the
verifier's harness verbatim modulo the new API) and MP21-half SURVIVED it on this machine — twice,
10/10 both times.** The losing interleaving needs the whole `Stop` (a thrown exception, the lock, a
`Dispose`) to complete between the writer's check and its lock acquisition, and those two
instructions are adjacent; on this hardware it never landed. A guard on a T1 surface whose only
witness is a race that may or may not land is not pinned, so the probe is not the answer on its own.

**So the interleaving is staged, and only the LOCK is staged.**
`The_stopped_flag_that_decides_is_the_one_read_under_the_lock`: a first caller holds the guard's lock
and waits; a write-ahead record arrives and parks on that lock; ATAS then stops the strategy — and
`Stop` raises the flag as its FIRST statement, before its steps, so the flag goes up while the lock
is still held and the write is still parked. When the lock is let go, the parked write is the only
contender and gets it with no race at all. What it must find there is the flag that went up *while it
was waiting*, not the one it read before it started waiting.

**Both are kept.** The staged test pins the rule; the 40-round race proves the terminal-path release
under real contention and is what MF26 fails.

**Mutants (`AdapterTeardownTests`, 10 tests):**

| # | Mutant | Result |
|---|---|---|
| MR9-2 | both doors bypass `Record` — the committed adapter's shape | **RED 3/9** (the RED-first above) |
| MP21-half | only the CHECK leaves the lock | **SURVIVED 10/10** against the lifted race probe; **RED 1/10** against the staged test |
| MP21 | check AND write both leave the lock (the pre-round-8 code) | **RED 2/10** |
| MF26 | the release goes back to a plain statement after the steps | **RED 2/10** — including the lifted 40-round probe |
| MP21b | `_stopped = true` moved down beside the release | **RED 7/10** |

### R8-3 (HIGH, class: the guard was gated on which side of a race won) · commit `3d8e793`

`PeerHasGoneQuiet()` reads `_lastHeartbeat`, which is written by an accepted hello (`:557`) and by a
heartbeat frame (`:574`) and by nothing else. Round 8 CONSULTED it only when the idle poll won the
race against the pending read. Those are two different questions, and the gap is a reachability hole
the width of the whole guard: a peer that completes ANY line more often than `IdlePoll` never lets
the poll win, so it is never asked, and it holds the only server instance there is while this
connector's own health reports DEGRADED about it.

**RED first**, `A_peer_that_dribbles_frames_without_beating_is_dropped_like_a_silent_one` — a peer
that authenticates, says a compatible hello, then writes one `{"op":"ping"}` every 200 ms against a
333 ms poll and a 1 s window, and never beats:

```
A_peer_that_dribbles_frames_without_beating_is_dropped_like_a_silent_one [FAIL]
  System.OperationCanceledException : The operation was canceled.
Failed!  - Failed: 1, Passed: 0, Skipped: 0, Total: 1, Duration: 2 m 3 s
```

Two minutes three seconds — the replacement bridge exhausting twelve redials against a pipe the
dribbler still owns, the same figure round 8 measured for the silent peer before F23. **GREEN in
16 s** for the whole class (36/36) with the fix.

**The fix is one question at one place.** The turn is now `WhenAny` → if the read won, dispatch →
then `PeerHasGoneQuiet()`, on every turn, whichever side won. AFTER the dispatch and not before it,
because the frame in hand may BE the heartbeat and `Dispatch` is what records it.

**Class-closure argument (the outcomes of one loop turn, enumerated).** `Task.WhenAny` over two
tasks has two outcomes, and the frame branch has exactly the three exits `Dispatch`'s contract
allows:

| turn outcome | is the predicate asked? | covered by |
|---|---|---|
| the delay won, peer still beating | yes → not quiet → next turn | `A_peer_that_keeps_beating_is_not_dropped_when_the_window_passes` (beats at 800 ms against a 500 ms poll: an empty poll in every gap) |
| the delay won, peer quiet | yes → drop | `A_silent_peer_is_dropped_so_the_pipe_can_be_taken_again`; the verifier's partial-frame peer |
| the read won, the frame is a hello | yes, after `Dispatch` stamps `_lastHeartbeat` | every handshake test in the class (36/36) |
| the read won, the frame is a heartbeat | yes, after `Dispatch` stamps it → not quiet | `A_peer_that_keeps_beating…` |
| the read won, any other frame, peer beating | yes → not quiet | the order/event round-trip tests |
| **the read won, any other frame, peer NOT beating** | **yes → drop** | **`A_peer_that_dribbles_frames_without_beating_is_dropped_like_a_silent_one`** |
| the read won, the line is null (EOF) | the loop ends anyway | the disconnect tests |
| the read won, `Dispatch` refuses the peer | the loop ends anyway | `A_parked_refused_peer_does_not_keep_a_fixed_bridge_off_the_pipe` |

The sixth row is the state round 8 never built. There is no ninth row: those are the two outcomes of
the race and the three exits of the frame branch.

**Mutants (`BridgeRoundTripTests`, 36 tests):**

| # | Mutant | Result |
|---|---|---|
| MR9-3 | the predicate asked only when the poll wins — round 8's shape | **RED 1/36** |
| MF23a | never drop (`PeerHasGoneQuiet` → false) | **RED 2/36** — the silent peer and the dribbler |
| MF23b | drop on every turn, alive or not (`PeerHasGoneQuiet` → true) | **RED 30/36** |
| MR9-3b | the predicate asked BEFORE the frame in hand is dispatched | **SURVIVED 36/36** — see below |

**MR9-3b survived, and I could not build the state that separates it.** The two placements differ on
exactly one turn: the one that straddles the heartbeat deadline with the beat itself in hand. Every
construction of it I tried is bounded by scheduling jitter of the same order as the window it needs —
the turn grid is dense (a frame every 200 ms, or a poll every third of the timeout), so the turn
immediately before the deadline lands within a few milliseconds of it, and the peer is already
"quiet" on that turn under both placements. I wrote a test for it
(`A_beat_that_is_read_late_is_still_a_beat`), watched it fail against the FIXED code for that reason,
and **removed it rather than tune it until it passed** — a flaky test in the suite is worse than a
recorded near-equivalence. **NOT verified: that the placement of the question after the dispatch is
load-bearing.** It is chosen for the direction it fails in — a frame in hand that is a beat is
evidence of liveness, and the drop exists for peers nobody can hear — and the shipped configuration
(5 s beats against a 15 s timeout, a 3x margin) is nowhere near the band where the two differ. Same
shape as the verifier's own unreproducible interleaving at `AtasConnector.cs:276-278`, recorded the
same way.

### F31 + PRIOR 28 (MED, class: unreadable ≠ empty, the probe branch) · commit `0d01976`

Round 8 classified a failing `ReadAllLines` as unreadable — and then asked `File.Exists` first.
**`File.Exists` never throws.** It answers false for a denial as readily as for an absence, so an ACL
that refuses the attributes, or a directory this account may not stat, short-circuited the fixed read
and produced "a sidecar with nothing in it" again. Same for `Directory.GetFiles`, whose catch
returned an empty array.

**RED first, on the real filesystem and with no seam at all.** A DIRECTORY at the sidecar's name:
`File.Exists` answers false about it (a directory is not a file) while it is manifestly not nothing.
Same shape as round 6's F17 variant for the committed file.
`A_directory_at_the_sidecars_name_is_unreadable_rather_than_absent` → RED at the round-8 probe
shape (measured as mutant MR9-4a, below, which restores exactly that shape).

**The seams.** `readSidecar` and `listSidecars` join `_replace` / `_open` / `_writeSidecar`, for the
identical stated reason: an ACL that denies attributes or refuses an enumeration cannot be provoked
on this machine without also breaking the committed read in the same directory, which is a different
state from the one under test. Null in every production use.

**Class-closure argument — every filesystem call on the sidecar path, enumerated.**

| # | call site | probe | failure ⇒ | covered by |
|---|---|---|---|---|
| 1 | `HasNotes` | `_readSidecar` (was `File.Exists` + `ReadAllLines`) | `FileNotFoundException` / `DirectoryNotFoundException` ⇒ absent; **every other exception ⇒ UNREADABLE** | `A_sidecar_read_that_fails_is_unreadable_unless_it_says_the_file_is_not_there` (5 rows: the two absences and three failures); the directory-at-the-name test; round 8's `FileShare.None` test |
| 2 | `LastLineWhere` | `_readSidecar` (was `File.Exists` + `ReadAllLines`) | a failure moves to the next generation, and the unreadability is reported by `_sidecarUnreadable`, which probes the same names | the degraded rows of the same theory (`io:degraded`, `Trouble` "could not be read") |
| 3 | `SidecarSet` | `_listSidecars` (was `Directory.GetFiles`) | `DirectoryNotFoundException` ⇒ empty; **every other exception ⇒ NOTED**, not degraded (the F25 boundary: what it hides is the per-writer set) | `A_sidecar_enumeration_that_fails_flags_the_zero_without_degrading_the_machine` (4 rows) |
| 4 | `EnsureLoaded`'s `SidecarGenerations().Any(File.Exists)` | **REMOVED — it was the third probe, and my own theory found it.** It guarded nothing (`LastDecidingLine()` answers null when there is no sidecar, giving the same two values) while gating the whole degraded computation on a question that answers "no" for a denial. Before removing it the state read `Trouble` = "could not be read" beside a token saying `io:ok` | mutant MR9-4c restores it → RED 4/141 |
| 5 | `SidecarPaths`'s `.Where(File.Exists)` | **REMOVED** — redundant after the enumeration, which returns files that are there, and it put a probe that cannot report a denial back on the path | — |
| 6 | `ErrorLogPath`'s `Path.GetDirectoryName` | a string operation, not a filesystem call; its catch yields null, and with no sidecar path there is nothing to probe. `Trouble` is dominated by the committed read in that state (`_committedUnreadable` is consulted first) | recorded, not tested |

**And the four `File.Exists` calls that remain in the file are all on the WRITE path** — `Rotate`'s
staging handling (twice), `Quarantine`'s search for a free `.rejected-N` name, and
`AppendToErrorLog`'s size check. None of them decides a state this class REPORTS: a wrong answer
costs at worst a rotation or a quarantine attempt, and every one of them is inside
`AppendToErrorLog`'s catch. `grep -n "File.Exists" CoidWitness.cs` returns those four and nothing
else.

**One behaviour change beyond the classification**, and it is the round-8 rule applied where round 8
got it for free: an unreadable canonical generation now counts as `Noted` **by name** rather than by
whether the enumeration happened to list it. Round 8's locked file was one the glob had already
found; a generation whose very existence this run cannot establish was not, so `Noted` read false and
the token read `io:ok` beside a `Trouble` that said "could not be read".

**GREEN:** `CoidWitnessTests` 141/141.

**Mutants:**

| # | Mutant | Result |
|---|---|---|
| MR9-4a | `File.Exists` back in front of the sidecar read — round 8's shape | **RED 4/141** |
| MR9-4b | a failed enumeration is an empty directory again | **RED 3/141** |
| MR9-4c | `File.Exists` gating the degraded computation again | **RED 4/141** |

### F32 (MED, class: the row describes the peer that is there NOW) · commit `cd481c2`

Round 8 stamped the two EXPLICIT refusals and ordered them against each other. The reading that is
DERIVED — a peer that took the pipe and has said nothing at all past `AuthGrace` — carried no stamp,
and the getter that produces it was deliberately blind while `_incompatible` was set. That is right
about one connection and wrong across two: `Drop` keeps a protocol refusal, a DIFFERENT program takes
the pipe and says nothing, and the row goes on naming the version of a bridge that has left until the
new peer's own heartbeat window runs out. The operator is sent to reinstall an add-on while something
else is sitting on the pipe.

**RED first**, `A_newly_arrived_silent_peer_is_not_masked_by_the_previous_peers_refusal` — v2 refused,
that peer leaves, a raw pipe client connects and sends nothing, `AuthGrace` 300 ms:

```
A_newly_arrived_silent_peer_is_not_masked_by_the_previous_peers_refusal [FAIL]
  System.TimeoutException : condition was not met in time            ← Unauthenticated stayed null
Failed!  - Failed: 1, Passed: 0, Skipped: 0, Total: 1, Duration: 10 s
```

**The fix: a derived state is stamped with the moment its connection began.** `_peerArrivedAt =
Interlocked.Increment(ref _observed)` beside `_peerArrived`, cleared by `Drop`. That stamp is by
construction newer than everything recorded before this peer arrived and older than everything
recorded during it — which is the ordering wanted at BOTH ends: a new peer's silence outranks the last
peer's refusal, and this peer's own refusal outranks its earlier silence. `UnauthenticatedNow()`
returns the reading and its stamp; the getter is no longer blind to `_incompatible`, because ordering
is what stops two live readings disagreeing, and `StatusDetail` compares the winner against
`_incompatibleAt`.

**Class-closure argument.** `StatusDetail` can report exactly three non-null things, and there is no
fourth: `Incompatible` is only ever explicit (`NoteIncompatible` is its one writer), `Unauthenticated`
is explicit (`NoteUnauthenticated`) or the single derived `Silent`, and nothing else is rendered. Each
now carries a stamp, so every pair is ordered:

| pair | across two connections | within one connection |
|---|---|---|
| explicit protocol vs explicit credential | `A_live_refusal_is_not_masked_by_a_stale_one`; the verifier's reverse-order probe | a peer that proves itself REPAIRS the credential refusal (`NoteUnauthenticated(null)`), so they cannot both be live from one peer |
| explicit protocol vs derived silence | **`A_newly_arrived_silent_peer_is_not_masked_by_the_previous_peers_refusal`** (new) | `_incompatibleAt > _peerArrivedAt` by construction — the refusal is written after the peer arrives — so the refusal leads, which is what `A_refused_peer_leaves_the_version_and_the_repair_on_the_row` asserts |
| explicit credential vs derived silence | ordered inside `UnauthenticatedNow()` by the same two stamps | `_unauthenticatedAt > _peerArrivedAt` by construction, which is what keeps `A_live_refusal_is_not_masked_by_a_stale_one`'s reinstalled peer reporting "did not authenticate" and not a silence |
| all three cleared | a compatible hello clears `_incompatible`, repairs `_unauthenticated` and sets `_hello`, which is what makes the derived reading null | the last three assertions of the new test |

**Mutants (`BridgeRoundTripTests`, 37 tests):**

| # | Mutant | Result |
|---|---|---|
| MV4a | the incompatible marker wins whenever it is set — round 8's precedence | **RED 2/37** |
| MR9-5a | the derived reading blind again while a protocol refusal is live — round 8's shape | **RED 1/37** |
| MR9-5b | a derived state carries no stamp (`_peerArrivedAt` left at 0) | **RED 1/37** |

### PRIOR 29 (LOW) — three states, three sentences · commit `e113c4c`

`Noted` has THREE causes, not two, and one of them is a SUCCESS: a rewrite that never reached the
disk, read back and its acknowledgements taken into the record. Round 8's headline said "something
beside the witness was refused" for all of them, which tells an operator something went wrong at the
exact moment the mechanism did its job.

**RED first, in three states built on the real filesystem** (measured as mutant MR9-6a, which
restores round 8's single sentence — the new tests are the RED against it):

```
The_three_noted_causes_read_differently_from_each_other                    [FAIL]
A_declined_candidate_is_reported_as_declined_rather_than_refused           [FAIL]
A_recovered_rewrite_is_reported_as_recovered_rather_than_refused           [FAIL]
A_refused_second_bridge_is_named_and_its_own_sidecar_is_listed             [FAIL]
Failed!  - Failed: 4, Passed: 140, Skipped: 0, Total: 144
```

**The fix.** `WitnessNotes` (flags) on `CoidWitnessReport`, and `CoidWitness.Notes` accumulating each
cause where it is discovered: `RefusedWriter` from the sidecar names (the canonical file and its two
generations are the only other names in that glob, so anything else is somebody the lease turned
away), `RejectedCandidate` from the candidate scan, `RecoveredRewrite` from the adoption.
`Headline`/`Explanation` take it as an optional parameter, so every existing call compiles unchanged
and keeps the unattributed wording. Flags rather than an enum because a machine can be in more than
one at once — and when it is, the report names NONE of them and lists the files, rather than guessing.

**Class-closure argument.** The causes of `Noted` are the places `_noted` is set true, and there are
exactly four: `Note()` (every sidecar line this session writes), the candidate scan
(`_rejected.Count > 0 || _viable.Count > 1`), the unreadable/enumeration probes (F31, above), and the
adoption. Each maps to one flag or to none:

| where `_noted` becomes true | flag | test |
|---|---|---|
| a second writer's own sidecar exists in the set | `RefusedWriter` | `A_refused_second_bridge_is_named_and_its_own_sidecar_is_listed` |
| a candidate was declined, or two rivals were adopted from | `RejectedCandidate` | `A_declined_candidate_is_reported_as_declined_rather_than_refused` |
| a stranded rewrite was adopted | `RecoveredRewrite` | `A_recovered_rewrite_is_reported_as_recovered_rather_than_refused` |
| a probe on the sidecar path could not be performed | none — the wording that names all three, and the files listed under it | the F31 theories |
| more than one of the above | none, deliberately | `The_three_noted_causes_read_differently_from_each_other` |

**One behaviour change came out of writing the test**, and it is a reader/writer disagreement rather
than wording: a READER that recovers a stranded rewrite wrote no sidecar line, so `Noted` stayed
false and the standing read `Clean` — while a WRITER in the same state reported `Noted` through the
line `ReportAndQuarantine` writes. Two readings of one machine disagreeing is the shape this unit
keeps finding, so the adoption now sets `_noted` itself. The count that follows rests on a file that
had to be repaired to produce it, which is what `Noted` is for.

**Mutants:**

| # | Mutant | Result |
|---|---|---|
| MR9-6a | round 8's single `Noted` sentence for all three causes | **RED 4/144** |
| MR9-6b | a recovery attributed as a refusal | **RED 1/144** |
| MR9-6c | the canonical file counted as a refused writer's own | **RED 1/144** |

### Adjacent-regression sweep for round 9 (R3)

| Changed surface | Consumers enumerated | Proof |
|---|---|---|
| `CoidWitness.SidecarGenerations()` gained the staging name; `Rotate` restructured; `DefaultWriteSidecar` now flushes | `LastLineWhere` / `LastDecidingLine` (deciding state), `_sidecarUnreadable`, `_degraded` / `_gapClosed`, `AppendToErrorLog`. All private; no signature changed | solution builds clean; `CoidWitnessTests` 144/144; the full suite 477 |
| `CoidWitness` ctor gained `readSidecar` and `listSidecars` (optional, defaulted, **null in every production use**, exactly as `replace` / `open` / `writeSidecar` are) | `AtasStrategyAdapter` uses `new CoidWitness()`; `tools/probe` and the tests use the named forms. No existing signature changed, so every call site compiles unchanged | solution builds clean including `tools/probe` |
| **`CoidWitness.Notes` (new public property) and `WitnessNotes` (new public enum)** | one consumer, `tools/probe`, which now passes it to `Headline`/`Explanation` | solution builds clean; `The_three_noted_causes_read_differently_from_each_other` |
| `CoidWitnessReport.Headline` / `Explanation` gained an OPTIONAL third/second parameter | `tools/probe` (updated to pass `witness.Notes`) and 10 existing assertions in `CoidWitnessTests` that call the two-argument forms — all still compile and still pass, because `WitnessNotes.None` keeps the unattributed wording | `CoidWitnessTests` 144/144 |
| **`AdapterTeardown` now OWNS the witness**: ctor takes a `CoidWitness`, `Stop(Action steps)` lost its `releaseWitness` parameter, and `Submitting` / `Identified` / `Trouble` / `Path` / `LastWriteFailure` / `Token()` / `PriorSession` / `PriorSessionIds` are new members | one consumer, `AtasStrategyAdapter`: thirteen sites, every one of them changed and enumerated in the R8-2 table above. `AdapterTeardownTests` updated to the new shape — **no test method was removed** (`git diff 10fa21f -- tests/ \| grep '^-.*public …'` empty after every structural edit) | `AdapterTeardownTests` 10/10; **and the adapter COMPILED against the real ATAS assemblies on the box** — see below |
| `AtasConnector` read loop restructured; `Unauthenticated` no longer blind to `_incompatible`; `UnauthenticatedNow()` added | `IConnectorStatusDetail` (`ConnectorSdk/Contracts.cs:75`), `TradingGateway.cs:72`, `AtasHealth.cs:169`. `Unauthenticated` and `StatusDetail` keep their types and their interface membership | `BridgeRoundTripTests` 37/37, `AtasHealthTests` inside unit 112/112, full suite 477 |

### THE BOX — the one granted run, identity-checked before and after

`tools/win-state.sh` first: host `DESKTOP-K8VRIT9`, session Active (id 1, console), desktop live,
ATAS installed True / running True, 14 strategy files. `tools/win-push.sh` → `packed 720K`,
`unpacked: 153 files`. Then, in ONE ssh session (`tools/win-ps.sh`), the identity check BEFORE
anything ran:

```
LOCAL (worktree)                                                  BOX (C:\ta\repo)
5543935473b0d24af723b63d43d460d98f6b061e59f6ef92a0fb76a12384a7d1  5543935473…a7d1  AtasStrategyAdapter.cs
95bd516e0015204cf5974c71846816a3c8ca42d2d220f8683c4aa66be7de816a  95bd516e00…816a  AdapterTeardown.cs
9787a3a2a97cc7e22e48f82038e634610b283fd7e548bdca9177e645b8146a93  9787a3a2a9…6a93  CoidWitness.cs
c7891ae9aa95d307cbf5ded15d0c81c874f4855c1bf81247e46d61b6d53edc6e  c7891ae9aa…dc6e  CoidWitnessReport.cs
c1a113471e8f4be5393638ab4118174e17a29722d9b54f3453b53eea383e28f0  c1a1134718…e28f  AtasConnector.cs
c60ee31c76e9ee50d8a791d32919b1cfb7040feb248c9a3ea3c437fa6b37a0c1  c60ee31c76…a0c1  tools/atas-gate/Program.cs
cs-count(src+tests): 82                                           cs-count(src+tests): 82
_teardown markers in the adapter: 19                              _teardown markers in the adapter: 19
_witness in the adapter: 0                                        _witness in the adapter: 0
```

**All six hashes and all three counts match. The tree on the box is mine**, and the worktree is clean
at `e113c4c` (`git status --porcelain` empty), so this is `e113c4c`'s bridge that was compiled and
gated. **`_witness in the adapter: 0` is R8-2's class-closure claim, measured on the box's own copy.**

**1. The bridge compiled against the real ATAS assemblies — R8-2's thirteen call sites BIND.**

```
$ dotnet build src\TradeAgent.AtasBridge\TradeAgent.AtasBridge.csproj -c Release --no-incremental \
      -p:AtasBridgeBuild=true -p:AtasInstallDir="C:\Program Files (x86)\ATAS Platform"
  TradeAgent.AtasBridge -> C:\ta\repo\src\TradeAgent.AtasBridge\bin\Release\net10.0-windows\TradeAgent.AtasBridge.dll
Build succeeded.
    5 Warning(s)
    0 Error(s)
BRIDGE BUILD EXIT: 0
```

The 5 warnings are the same five the round-8 verifier measured at `10fa21f`: `MSB3277` (a
`WindowsBase` 4.0/10.0 conflict introduced by ATAS's own `ATAS.Indicators.dll` /
`ATAS.Indicators.Technical.dll`) and four `CS0618` obsolete-API warnings at
`AtasStrategyAdapter.cs:1548/1632/1693/1858` (`OpenOrder`, `ModifyOrder`, `CancelOrder`,
`ClosePosition`) — the same four calls the verifier saw at `:1532/1616/1677/1841`, moved down by this
round's added documentation. **None names `AdapterTeardown`, `_teardown` or a round-9 line, and the
count did not change from the verifier's run at `10fa21f`.** So the ownership refactor added no
warning: `_teardown.Submitting`, `_teardown.Identified` and the six forwarded reads all bind against
the real ATAS types.

**2. `tools/atas-gate` — the money path, both directions, re-run at this sha.**

```
$ cd C:\ta\repo\tools\atas-gate && dotnet run -c Release -p:AtasBridgeBuild=true \
      -p:AtasInstallDir="C:\Program Files (x86)\ATAS Platform"
TRADEAGENT_HOME = C:\Users\Nicolas\AppData\Local\Temp\ta-atas-gate-997a67c9
  [PASS] ITradingManager.ClosePosition was never called — calls = 0
  [PASS] the refusal says nothing was submitted — the write-ahead record for TA-CLOSE-REFUSED could not
         be written to …\coid-witness.json; nothing was submitted. ERROR claim=TA-CLOSE-REFUSED another
         writer owns this witness (…\coid-witness.json.lock): IOException
  [PASS] the refusal names the witness file
  [PASS] ITradingManager.ClosePosition WAS called once the witness could be written — calls = 1
  [PASS] the refused close left no write-ahead record — records = [TA-CLOSE-ALLOWED]
  [PASS] the permitted close left one — records = [TA-CLOSE-ALLOWED]
GATE PASSED
ATAS-GATE EXIT: 0
```

That gate now executes `ClosePosition`'s write-ahead record **through `AdapterTeardown.Submitting`**
— the guarded door — so R8-2's third site is not merely compiled but run, in both directions.

**Re-hash after the run — all six identical, all three counts unchanged:**

```
5543935473…a7d1 AtasStrategyAdapter.cs · 95bd516e00…816a AdapterTeardown.cs · 9787a3a2a9…6a93 CoidWitness.cs
c7891ae9aa…dc6e CoidWitnessReport.cs · c1a1134718…e28f AtasConnector.cs · c60ee31c76…a0c1 Program.cs
cs-count(src+tests): 82 · _teardown: 19 · _witness: 0
```

`tools/win-state.sh` afterwards: ATAS `installed True`, `running True`, 14 strategy files —
unchanged. The installed app, ATAS and the real home were not touched; nothing but `C:\ta\repo` and
the `%TEMP%\ta-atas-gate-*` directory the gate makes for itself was written.

### Final gate (this Mac, 2026-09-04)

```
$ dotnet build TradeAgent.sln --no-incremental
Build succeeded.
    0 Warning(s)
    0 Error(s)

$ dotnet test TradeAgent.sln
Passed!  - Failed: 0, Passed:  75, Skipped: 0, Total:  75, Duration: 987 ms - TradeAgent.FaultTests.dll
Passed!  - Failed: 0, Passed: 112, Skipped: 0, Total: 112, Duration: 3 s    - TradeAgent.UnitTests.dll
Passed!  - Failed: 0, Passed: 290, Skipped: 0, Total: 290, Duration: 2 m 8 s - TradeAgent.IntegrationTests.dll
EXIT=0
```

**75 / 112 / 290 = 477**, from 454 at `10fa21f`. Tip `e113c4c`. No test method present at `10fa21f`
was removed — `git diff 10fa21f -- tests/ | grep -E '^-.*public (async )?Task |^-.*public void '`
run after every structural edit, empty every time.

### What round 9 did NOT do

- **NOT verified: that `Flush(flushToDisk: true)` reaches the platter.** No in-process observation on
  this machine distinguishes a flushed write from an unflushed one, and a `SIGKILL` does not either —
  the page cache survives it. No mutant that removes the flush goes RED. What IS measured is the
  ORDER: the restatement is complete and readable by another handle before any delete runs.
- **NOT verified: that asking the heartbeat predicate AFTER the dispatch rather than before it is
  load-bearing.** Mutant MR9-3b survived 36/36; the state that separates the two placements is
  bounded by scheduling jitter of the same order as the window it needs. I wrote a test for it,
  watched it fail against the FIXED code for that reason, and removed it rather than tune it until it
  passed. The choice is recorded with its direction and the 3x shipped margin.
- **No strategy was loaded on a chart in a running ATAS.** The adapter compiles and `tools/atas-gate`
  executes it, but which callback ATAS fires on a teardown (`OnStopping` vs `OnDispose`) is still two
  hooks and a compiler. Unchanged since round 6.
- **No on-box test suite, no UI, no `probe atas`, no second on-box build.** One run, as granted:
  the bridge compile and `tools/atas-gate`. Whether the 5 on-box warnings predate this unit is still
  unverified — it would need a second build at an older sha.
- **R8-1's rotation behaviour is measured on macOS/APFS only.** The mechanism is
  `File.Move` / `FileStream.Flush(true)`; that Windows behaves the same in kind is reasoning, not
  measurement. Same standing limitation the round-8 verifier recorded for its own rotation work.
- **The F8 residual and `Quarantine`'s 64 slots** were not touched. Neither was in this round's brief.
- **No push, no merge, no rebase.** Nothing was run in the main worktree; the record above was
  appended to the main copy by hand, as briefed.
