# REVIEW 2026-09-05b — the money path on `main` at `d92a61b`, before v0.1.2 is cut

Fresh Opus reviewer, per `docs/briefs/REVIEW.md`. Worktree
`~/Projects/ai-trading-software-for-mihael-worktrees/review`, detached at
`d92a61b534408d623129edcd95d39c0364abbd0d`. **Probes are on branch `review-probes-b`, not
`review-probes`:** that name already exists at `b952851` and holds the FIRST review's three probe
files, unmerged, so it was left exactly where it was.

**Reviewer's report (≤20 lines, as the brief asks).** Sha `d92a61b`. HIGH 3 · MED 2 · LOW 1 ·
UNVERIFIED 7; eleven probes, all executed and quoted below; probes branch `review-probes-b` @ `80f19f0`
(three test files, no product change; Release build 0 warnings, 0 errors; full suite in Release at
`0768ec2` **Unit 224 + Fault 244 + Integration 582 = 1050, 0 failed** = 1040 on `main` plus 10 probes,
and P6b, added after that run, 2/2 green on its class at `80f19f0`). Three least-proven claims: (a) that a real ATAS dispatch can
outlive the 70 s stranded bound at all — findings 1 and 2 are proven on the fake, and the bound is
re-derived from the shipped `AtasConnector`, but nothing here observed a real bridge; (b)
`SupportsOrderHistory` (`AtasStrategyAdapter.cs:664`) with the coverage watermark skipped whenever
`ClearCachePeriod <= 0` (`:936`) — bridge code, box only; (c) that the one-press mode row in finding 3
is reachable as a mis-press on a real screen — the gateway half is run, the widget was read.
Did NOT do: no box, no real ATAS, no money, no UI run, no mutation testing; nothing on
`CoidWitness`/`AdapterTeardown`/`AtasStrategyAdapter` beyond reading; no probe of `BridgePipeAuth`,
`AtasHealth`, `ChatView`, `InboxView`, `OnboardingView` or `AgentSession`; entered no other worktree;
changed no product file, pushed nothing.

**Every row is an executed refutation.** Each names a probe that was RUN on `review-probes-b`, and
its output is quoted below the table. Reading-only observations are in the ranked UNVERIFIED list and
are **not** findings.

Probes: `tests/TradeAgent.FaultTests/ReviewProbes2.cs`, `tests/TradeAgent.UnitTests/ReviewProbes2Unpack.cs`,
`tests/TradeAgent.UnitTests/ReviewProbes2Ledger.cs`. All are named `P<n>_…`.

## Findings

| # | sev | file:line | finding | the probe that settles it | what would fix it |
|---|-----|-----------|---------|---------------------------|-------------------|
| 1 | HIGH | `src/TradeAgent.Gateway/TradingGateway.cs:3052`, `:535`, `:990`; `src/TradeAgent.App/DashboardView.cs:228`, `:445` | **The human override has no dispatch lease, so the owner writes off an order that is still on the wire — and the broker's real answer is then discarded.** `U-stranded` gave the RECONCILER a lease (it refuses the row and says "still on the wire for 120s of a possible 50s") and made a late definite answer win. The Dashboard's unconfirmed card is fed by `Unreconciled()` (`DashboardView.cs:228`), which does **not** consult the lease, so the same live row is rendered with its two override buttons. Pressing **"No order exists"** calls `ForceResolve`, which writes a TERMINAL state straight onto a `DISPATCHING` row, clears the flag and the latch; the Dashboard then refreshes health and trading resumes. When the dispatcher's `FILLED` arrives, `Settle`'s rescue arm is `actual.State is UNKNOWN or RECONCILING` (`:990`) — a `ForceResolve`d row is neither — so `LateDefiniteSettle` never runs and the answer is filed `already_settled`. End state: broker holds a filled position, the durable record says CANCELLED "resolved by user: no such order exists", unflagged, and the caller was told CANCELLED. This is REVIEW 2026-09-05 finding 1 reached through the button instead of the reconciler. Reachability rests on the same premise `U-stranded` already accepted when it built the lease — "a DISPATCHING row on disk cannot say whether anyone is still flying it" — and the ordinary instance is wall-clock, not a slow broker: a laptop that sleeps for a minute mid-dispatch puts `dispatched_at` more than 70 s in the past while the handler is still awaiting. | `P3_the_operator_override_writes_off_an_order_whose_dispatcher_is_still_on_the_wire` | Give `ForceResolve` the same lease the reconciler has — refuse (or require a second, differently worded confirmation) while `_dispatches[id].Live`; and widen `LateDefiniteSettle`'s `from` set so a broker's definite answer can still land on a row a human moved, since the human had no evidence and the broker does. |
| 2 | HIGH | `src/TradeAgent.Gateway/TradingGateway.cs:2454`, `:2497`, `:2269` | **The emergency press's atomicity covers PRESSES, not a closing order already in flight, so Close All reverses the position instead of flattening it.** `U-press-atomic` made "an open press of this control" a uniqueness constraint on press rows (`TryCreateFlagged`), and the press re-reads the position immediately before each close so a landed fill refuses that leg. Neither sees an agent's own `close`, which is an ordinary `execution_request` inside the connector call: the re-read still shows the old position, so the press captures 2, re-reads 2 and sends a market sell 2 while the agent's sell 2 is on the wire. Long 2 becomes **short 2** after the owner pressed the control whose whole purpose is to flatten. `U-gates`' dispatch-time re-check closes the reverse ordering (press row first → the agent's leg is refused `TRADING_PAUSED_UNRECONCILED`), which is why this needs the agent to be inside the call. | `P6_an_agent_close_in_flight_and_the_operators_close_all_press_both_send`, bounded by `P6b_the_guard_that_does_exist_a_press_row_on_disk_refuses_the_agents_close` | Make the drift re-read consider open work as well as position: refuse a leg whose instrument has a `DISPATCHING`/`UNKNOWN` `execution_request` against it, or take the press's claim over the *instrument* rather than over the control. The check and the wire must be one statement over the same set the gateway already keeps. |
| 3 | HIGH | `src/TradeAgent.App/DashboardView.cs:669`, `:685`; `src/TradeAgent.App/MainWindow.cs:659` | **One press on the Safety page's mode row moves a live-activated installation from LIVE_CONFIRM to LIVE_AUTONOMOUS**, and the AI's very next order reaches the real-money account with nobody approving it. The row is `Ui.Secondary(...)` — `Make(...)`, a plain button whose `Click` runs the action once (`Ui.cs:184` → `Make`, `Ui.cs:158`) — while the live switch immediately below it is `Ui.Confirm` (two presses, `:672`) and the real-money account picker on Settings is `Ui.Confirm` too (`SettingsView.cs:406`). `CLAUDE.md`: "Anything that moves money or removes permission is two-press." Removing the human from the loop between two adjacent buttons is exactly that, and the page's own note for the mode it reaches says "The AI places real orders by itself." **Same class, same fix:** the STOP button is a one-press TOGGLE (`Ui.Big`, `DashboardView.cs:685` and `MainWindow.cs:659`), so RESUME AI TRADING is also one press — and `docs/USER-GUIDE.md:328` justifies the single press with "a mis-press costs nothing", which is true of STOP and false of RESUME in LIVE_AUTONOMOUS with real money switched on. | `P1_one_press_on_the_mode_row_turns_a_human_approved_installation_autonomous`, bounded by `P2_the_guard_that_does_exist_leaving_a_live_mode_rearms_the_live_switch` | Make every control that GRANTS authority two-press and leave every control that removes it at one: the two live modes get `Ui.Confirm` with the sentence spelled out ("Confirm: let the AI place real orders without asking"), and the emergency toggle arms only in the RESUME direction. OBSERVE and PAPER stay one press — they only ever reduce. |
| 4 | MED | `src/TradeAgent.Provisioning/Downloader.cs:62`, `:70`, `:105`; `src/TradeAgent.Provisioning/Prerequisites.cs:112-118`, `:136`; `src/TradeAgent.Provisioning/NodeRuntime.cs:83-88` | **Integrity is optional on every download path except the updater, and the one file that is executed ELEVATED is on an optional one — where a stale `.part` is concatenated into the finished file.** `DownloadAsync` resumes from any `.part` at the destination (`:62`), appends when the server answers 206 (`:70`), and runs the checksum block only `if (sha256 is { Length: > 0 })` (`:105`). The ATAS platform installer's destination is the fixed `%LOCALAPPDATA%\TradeAgent\tools\atas\ATASPlatform.exe`, nothing ever cleans it, `DownloadAsync` is called with no hash, and the result is handed to `_runElevated`. `NodeRuntime` is the same class by choice: `if (sha is null)` it reports a progress line and installs anyway. So "TLS is the whole integrity story" is not true even as stated — what is executed is TLS bytes appended to whatever an earlier, interrupted attempt left behind. | `P4c_a_stale_part_file_is_resumed_into_the_finished_installer_when_there_is_no_checksum` | Bind the `.part` to what it is a part of — name it after the URL and the expected length, and discard it when either differs; and make the checksum-less callers use a `DownloadVerifiedAsync`-shaped refusal or an explicit, recorded "unverified" decision rather than a silent skip. One structural fix covers ATAS, Node and the runtime archives. |
| 5 | MED | `src/TradeAgent.Core/MaterialScanner.cs:25`, `:67`; `src/TradeAgent.Core/Paths.cs:19`; `src/TradeAgent.AgentRuntime/CliAgentRuntime.cs:309` | **The agent can write a `material` row that says the OWNER handed it the file.** `MaterialOrigin.Inbox` is documented as "Where the account owner drops things. Everything under it is theirs, not the agent's", the scanner assigns origin from the directory alone, and `Paths.Inbox` is `workspace/inbox` — one relative path inside the working directory every runtime is started in. `AGENTS.md` asks the agent not to modify `inbox/`; nothing enforces it. `material` is the measurement table precisely so nobody has to take the agent's word, and this is a measurement row the observed party can author. (Distinct from the unsandboxed-runtime question: no escape of any kind is needed.) | `P5a_a_file_the_agent_writes_into_the_inbox_is_recorded_as_the_owners` | Take the inbox out of the agent's tree (scan a directory the agent's working directory does not contain), or drop the write bit on `inbox/` for the runtime, or record the origin as claimed-inbox unless the scanner saw the file appear while no agent process was running. Any of the three keeps `material` a measurement. |
| 6 | LOW | `src/TradeAgent.Core/Db/MaterialStore.cs:45`, `:92` | **A row stamped `removed_at` comes back carrying the hash of content that is gone.** `Observe` clears `removed_at` for a sighting matching `(path, size, mtime)`, and `NeedingHash` only picks up rows whose `sha256 IS NULL`, so a different file at the same path with the same length and mtime resurrects the old row and the ledger states a hash that is provably not this file's. The DDL records the in-place blind spot ("content swapped with size AND mtime both preserved reads as the same version"); this is the other half — the ledger itself watched the file go and brought the old measurement back for new bytes. | `P5b_a_removed_row_comes_back_carrying_the_hash_of_content_that_is_gone` | Never un-remove: a sighting after `removed_at` is a new version, so insert a fresh row (the unique index is on `(rel_path,size_bytes,modified_at)`, so it needs `removed_at` in the key or a version column). Or clear `sha256` when un-removing, so the next pass re-hashes. |

**Counts: HIGH 3 · MED 2 · LOW 1 · UNVERIFIED 7.**

## The probe output, quoted

**P1** — a gateway in LIVE_CONFIRM, real-money trading already switched on, a non-simulated account, a
connector whose `ReconciliationProvable` is true. One call to the mode row's handler in between:

```
mode                    : LIVE_CONFIRM, live activated: True
account is simulated    : False
agent buy before        : APPROVAL_REQUIRED — request p1-before is waiting for your approval
orders at the broker    : 0
after ONE press         : LIVE_AUTONOMOUS
agent buy after         : FILLED
orders at the broker    : 1 FB-1 FILLED 1 ES
awaiting approval rows  : 1
```

**P2** — the guard that does exist, so finding 3 is bounded rather than overstated. A mode press that
LEAVES a live mode re-arms the two-press switch (`TradingGateway.cs:434`), so the one press bites only
between the two LIVE modes:

```
OBSERVE  -> live activated: False
back to LIVE_AUTONOMOUS -> live activated: False
agent buy               : LIVE_NOT_ACTIVATED — real-money trading is not switched on
```

**P3** — one row, the reconciler and the button disagreeing about it, at the same instant. The
placement is inside `PlaceOrderAsync` throughout; nothing about the record is faked (the gateway wrote
`DISPATCHING` itself), and the clock is the movable one `U-stranded`'s own tests use:

```
card shows              : p3-owned/DISPATCHING
the reconciler          : resolved=0 inconclusive=1 p3-owned: still on the wire for 120s of a possible 50s   (leases the row — U-stranded)
after the override      : CANCELLED, needs_reconciliation=False
trading resumed         : True
dispatch answered       : CANCELLED
orders at the broker    : 1 FB-1 FILLED 1 ES
position at the broker  : ES 1
record now              : CANCELLED, needs_reconciliation=False, last_error=resolved by user: I checked in ATAS and no such order exists
engineering             : already_settled
```

The last four lines are the finding: the broker holds a filled position, the durable record says no
such order exists, nothing is flagged, trading has resumed, and the dispatcher was told CANCELLED.
`already_settled` is where the broker's FILLED went.

**P6** — an agent's `close ES` held inside the connector call, then the owner presses Close all
positions:

```
position before         : ES 2
agent close             : on the wire, record DISPATCHING
press                   : 1 of 1 record(s) from this press are still waiting for you.
position after the press:
agent close answered    : FILLED
orders at the broker    : 3 FB-1 Buy 2 FILLED FB-3 Sell 2 FILLED FB-5 Sell 2 FILLED
position at the end     : ES -2
```

**P6b** — the other direction, so finding 2 is bounded rather than overstated. With the press's
flagged row already on disk, `U-gates`' re-check at the dispatch gate refuses the agent's leg and
nothing extra reaches the wire — which is why the race above needs the agent to be inside the call:

```
press                   : 1 of 1 record(s) from this press are still waiting for you.
orders after the press  : 2
agent close             : TRADING_PAUSED_UNRECONCILED — 1 earlier request(s) are unconfirmed
orders at the end       : 2
```

**P4a / P4b** — the two escapes that do NOT work, run first so the finding below is not overstated.
`ZipFile.ExtractToDirectory` and `TarFile.ExtractToDirectoryAsync` both refuse, including a tar symlink
whose target is outside the destination:

```
zip entry               : ../../bridge.auth
unpack                  : IOException: Extracting Zip entry would have resulted in a file outside the specified destination directory.
file outside dest now   : the real credential

tar.gz (climb  )        : IOException: Extracting the Tar entry '../../bridge.auth' would have resulted in a file outside the specified destination directory
tar.gz (symlink)        : IOException: … would have resulted in a link target outside the specified destination directory
file outside dest now   : the real credential
```

**P4c** — the resume path, against a real socket, with the argument `Prerequisites.cs:118` passes:

```
stale .part on disk     : 38 bytes
range header sent       : bytes=38-
sha256 argument         : null (Prerequisites.cs:118 passes none)
finished file           : 58 bytes — "STALE-PREFIX-FROM-AN-EARLIER-DOWNLOAD;REAL-INSTALLER-BYTES"
```

That file is what `_runElevated` is handed.

**P5a** — one scan of a workspace holding one file the owner dropped and one the agent wrote, one
relative path from its own working directory:

```
scan                    : seen=2 added=2 hashed=2
  Inbox  inbox/signed-authority.pdf       sha=41a6e6a56d1a
  Inbox  inbox/broker-statement.pdf       sha=6d6a04120c1a
origin of the agent's own file : Inbox  (documented as "what the account owner handed the agent")
```

**P5b**:

```
seen      : sha=acbef4a66f6b5ec95440edb3f5dbd986e20a3e6473d1706ddc5a468f8b56f85c size=30 removed=False
deleted   : removed_at=True
replacement             : 30 bytes (was 30)
replaced  : rows at this path=1, removed_at=False
ledger says sha          : acbef4a66f6b5ec95440edb3f5dbd986e20a3e6473d1706ddc5a468f8b56f85c
the file on disk hashes  : d947557fc6a45e79b6eb803e07f80abfc7e68b4f8d87d11703f298d0ed9d3f48
content now              : print('the OTHER strategy')###
```

**P7** — every number the `## 2026-09-05` sections quote that depends on a constant, re-derived from
the shipped objects rather than read. All of them hold:

```
AtasConnector.WriteTimeout            : 10s
AtasConnector.FrameTimeout            : 30s
AtasConnector.WorstCaseOperationPath  : 50s   (U-stranded says 50)
GatewayOptions.DispatchSettleSlack    : 20s   (U-stranded says 20)
=> TradingGateway.DispatchStrandedAfter : 70s   (U-stranded says 70)
   GatewayOptions.AbsenceGrace          : 15s
   ApprovalTtl                          : 15 min
MaxClientOrderIdChars                 : 64
MaxRequestIdChars                     : 61   (U-press-atomic says 61)
a press leg request id                : op-close-0123456789abcdef-0 (27 chars); sendable=True
its client order id                   : TA-op-close-0123456789abcdef-0 (30 chars, U-press-atomic says 30)
```

## Guards checked in both directions, and holding

Stated because the brief asks for both directions, and because a guard that was probed and held is
worth as much to the next reader as one that failed. None of these is a finding.

- **`U-stranded`'s dispatch lease** refuses the row and names the two numbers the owner needs (P3,
  line 2). It is finding 1's other half that is missing, not this one.
- **`U-gates`' re-check at dispatch** refuses an agent leg once a press row exists — which is why
  finding 2 needs the agent to be inside the connector call already (P6b).
- **Leaving a live mode re-arms the two-press live switch** (P2), so finding 3 is confined to the
  LIVE_CONFIRM ↔ LIVE_AUTONOMOUS pair.
- **The archive escapes are refused by the framework** (P4a, P4b).
- **Every constant the 2026-09-05 sections quote re-derives** (P7).
- **`AgentContext` cannot be forged** (read, `GatewayTypes.cs:99-126`): the only operator context is a
  static built by a private constructor, `ForAgent` cannot return one, and the reserved session name is
  additionally refused at the pipe (`GatewayPipeServer.cs:794`).
- **`ReconcileAsync` refuses evidence from the wrong platform** (read, `TradingGateway.cs:2679`):
  `req.ConnectorId != Connector.Id` is the first thing asked and the record stays flagged.
- **`IpcToken.Ensure`** regenerates rather than accepting a short or blank token
  (`SecretStore.cs:65`), and `Matches` is constant-time.

## UNVERIFIED — read, not run, ranked by how much I would want it settled

1. **`SupportsOrderHistory` is true whenever a cache object exists, and the coverage watermark is
   skipped when the platform does not state a retention period.** `AtasStrategyAdapter.cs:664` is
   `cache.Cache is not null`; the watermark at `:936` refuses a `since` older than the cache only
   `if (cache.ClearCachePeriod > TimeSpan.Zero)`. A platform reporting zero or nothing therefore
   answers every `since` as covered while `ReconciliationProvable` stays true — which is rule 2's
   "a partial history is worse than none". Bridge code, compiles only on the box; Codex raised the
   same line in the first review and it is unchanged at this sha.
2. **Findings 1 and 2 against a real ATAS bridge.** Both are proven on the fake, and finding 1's
   premise (a dispatch outliving `DispatchStrandedAfter`) is exactly the case `U-stranded`'s lease was
   built for — but a real bridge's deadlines cap one call at the 50 s P7 re-derives, so how a real
   dispatch gets past 70 s is not shown here. Finding 2 needs no such premise: it only needs the two
   calls to overlap.
3. **The four obsolete synchronous ATAS order calls** (`AtasStrategyAdapter.cs:1487`) wedging the
   bridge frame loop and keeping a legitimate emergency off the wire. Read only; Codex's item from the
   first review, unchanged.
4. **`MainWindow.OpenPath` ShellExecutes whatever the sign-in regex matched.** `MainWindow.cs:739` is
   `UseShellExecute = true` with no scheme check; the value comes from a runtime's stdout matched
   against `RuntimeManifest.AuthUrlPattern`. Both built-in patterns are anchored on `https?://`, so the
   shipped paths are safe — but the field exists to be overridden from `runtimes.json`
   (`RuntimeManifest.cs:136`, override file `:181`, loader `:411`), and an override is not anchored on anything.
5. **The ATAS installer's integrity story is still a decision nobody has taken** (first review's
   UNVERIFIED 1, `Prerequisites.cs:118`, recorded in `docs/RESUME-HERE.md`). Finding 4 makes the
   "TLS is enough" option strictly weaker than it was stated to be, but the decision itself is
   Nicolas's.
6. **A malformed `runtimes.json` or `atas.json` is swallowed in silence.**
   `RuntimeCatalog.Load` (`RuntimeManifest.cs:411`) catches every exception and returns the built-ins;
   nothing logs, no health row moves. It fails safe, which is why it is here and not in the table — but
   an owner who edited the file to fix a vendor command has no way to learn it was ignored.
7. **`AtasConnector`'s refusal and park behaviour on a real Windows pipe.** The single-server-instance
   argument, the refused-peer drop, the kept-vs-cleared status fields and `PeerHasGoneQuiet` were read
   closely and are internally consistent; none of it was exercised, here or anywhere, against a real
   named pipe with a real bridge.

## What I did NOT do

- **No Windows box, no real ATAS, no real money.** Everything above is macOS/APFS against
  `FakeConnector`, `HangingConnector` and the real `MaterialScanner`, `MaterialStore` and `Downloader`.
  Nothing here says what ATAS does.
- **No UI run.** Finding 3's widget is read (`DashboardView.cs:669`, `Ui.cs:184`, `Ui.cs:158`); what is RUN is the
  consequence of one invocation of its handler. `BridgeReinstallTests`' own comment sets that
  precedent: "The button itself is judged on the running app."
- **No mutation testing**, and no attempt to re-verify the mutants `BUILD-STATUS.md` quotes for the
  nine fix units.
- **Barely touched the bridge half:** `CoidWitness` (2,927 lines), `AdapterTeardown`,
  `AtasStrategyAdapter` (3,339) were read around the witness ordering in `Place` and `Close` — which is
  correct, the record goes down first and a failure to write refuses the order — and around
  `SupportsOrderHistory`. Nothing there was run.
- **Did not probe** `BridgePipeAuth`, `AtasHealth`, the protocol-3 handshake, `ChatView`, `InboxView`,
  `OnboardingView`, `AgentSession`, `Doctor` or the updater's release-reading half.
- **Did not re-report** the first review's ten findings; the two I crossed (`U-stranded`'s lease,
  `U-gates`' dispatch re-check) hold where they were probed.
- **Fixed nothing**, changed no product file, pushed nothing, merged nothing, and did not enter
  `~/Projects/ai-trading-software-for-mihael-worktrees/codex-review` or any other worktree.

## Codex (gpt-5.6-sol, read-only, the same sha, in its own worktree)

Read-only claims, not executed refutations; each one's fixer turns it RED first or refutes it with a probe and a written
reason. The configured `gpt-6-astra` was refused by the installed Codex CLI 0.144.1, so this ran on the model the first
review used. **Triage (manager, 2026-09-05 evening):** F12 = finding 3 → `U-two-press-grant`, which also takes its
risk-cap widening; F3 → `U-press-inflight` (the class of finding 2: a close decided on a stale position); F15 = finding
4, and F17 and F19 beside findings 5–6 → `U-batch-2`; F16 (= UNVERIFIED 6), F20 and UNVERIFIED 4 → `U-batch-2b`; F1, F2,
F5, F18 → `U-codex-2a` (gateway risk and authority); F6, F7, F13 → `U-codex-2b` (pipe and connector); F4, F8 (= UNVERIFIED
1), F9, F10 (= UNVERIFIED 3), F11, F14 and Codex's two UNVERIFIED → `U-bridge-2`, with the box, after `U-box-precut`.

```
FINDING 1 — HIGH — src/TradeAgent.Gateway/TradingGateway.cs:741 — MaxOpenPositions counts only filled positions, so concurrent or resting opening orders can all pass and later exceed the cap. — With a cap of one and an empty account, barrier two different-symbol buys before fills and assert at most one connector place call.
FINDING 2 — HIGH — src/TradeAgent.Gateway/TradingGateway.cs:757 — The notional gate swallows instrument-read failure and substitutes ContractSize 1, while the ATAS mapping supplies unproved LotSize, so futures exposure can be understated. — Return missing metadata and real ES metadata, set a cap between raw and multiplied notionals, and assert both paths refuse without reaching the connector.
FINDING 3 — HIGH — src/TradeAgent.Gateway/TradingGateway.cs:1855 — Agent close sizes and sides a market order from a position read before PlaceAsync’s awaited account and risk reads, so an intervening fill can turn a close into a new or reversing position. — Block a later connector read, change the position after the initial snapshot, and assert the stale close is recomputed or refused.
FINDING 4 — HIGH — src/TradeAgent.AtasBridge/AtasStrategyAdapter.cs:2669 — The ATAS adapter exports raw Position.Volume even though its sign convention is explicitly unproved, and agent close uses that sign to choose Buy or Sell despite the claim it is display-only. — On Windows ATAS, invoke agent close against a known short position and assert the emitted order flattens rather than doubles it.
FINDING 5 — HIGH — src/TradeAgent.Gateway/TradingGateway.cs:1234 — Dispatch reauthorization is not atomic with SetMode, ActivateLive, StopAiTrading, or Update because those writers do not take _dispatchGate, leaving a last-check-to-wire window where revoked authority still sends. — Pause a fake connector immediately before its simulated wire write, revoke authority, release it, and assert no write occurs.
FINDING 6 — HIGH — src/TradeAgent.Core/Protocol.cs:49 — Malformed present optional order fields are collapsed to absence, making limit "bad" a Market order and empty tif a Day order instead of failing closed. — Send authenticated raw buy frames containing malformed limit, stop, and empty tif values and assert INVALID_REQUEST with zero connector calls.
FINDING 7 — HIGH — src/TradeAgent.Connectors.Atas/AtasConnector.cs:920 — A heartbeat whose Describe payload is absent or malformed refreshes liveness while retaining prior account and capability proof, so autonomous eligibility can outlive the bridge’s ability to attest it. — Establish provable capabilities, make every later Describe fail while pulses continue, and assert capabilities clear and autonomous dispatch is refused.
FINDING 8 — HIGH — src/TradeAgent.AtasBridge/AtasStrategyAdapter.cs:664 — SupportsOrderHistory is true from cache presence alone and GetOrders checks configured retention rather than a coverage watermark, so cold or partial cache absence can be treated as definite. — Present a cold cache missing a known in-window order and assert Describe remains false or GetOrders refuses until coverage reaches the requested timestamp.
FINDING 9 — HIGH — src/TradeAgent.AtasBridge/CoidWitness.cs:2431 — Witness Save declares success after File.WriteAllText, rename, and page-cache readback without Flush(true), so an order can be submitted without a power-loss-surviving write-ahead claim. — Power-cut a VM after Submitting returns true but before or just after the wire write and assert the claim is recovered on restart.
FINDING 10 — HIGH — src/TradeAgent.AtasBridge/AtasStrategyAdapter.cs:1548 — The four obsolete synchronous ATAS money calls have no deadline and run on BridgeServer’s serial frame loop while heartbeat remains READY, so one wedge can block later emergency effects indefinitely. — Stall each synchronous SDK call in a Windows shim, issue an emergency operation behind it, and assert bounded failure, degraded health, and emergency progress.
FINDING 11 — HIGH — src/TradeAgent.AtasBridge/AtasStrategyAdapter.cs:1863 — ClosePosition identifies the first newly seen same-symbol order without account, side, quantity, or causal correlation and then writes the press COID onto it, so an unrelated concurrent order can be returned as the close. — Inject an unrelated same-symbol order during the before-and-after window and assert only the actual closing order is returned and labelled.
FINDING 12 — HIGH — src/TradeAgent.App/DashboardView.cs:669 — The UI applies permission-expanding mode changes, kill-switch re-enable, and risk-limit widening in one click, allowing an activated live account to move from LIVE_CONFIRM to autonomous dispatch without two-press confirmation. — Drive each control once from restrictive live settings and assert no authority changes until a distinct confirmation press.
FINDING 13 — MED — src/TradeAgent.Core/Protocol.cs:36 — IpcRequest defaults an omitted v to the current version, so a versionless peer passes the protocol-3 hello instead of failing closed. — Send a raw hello without v followed by buy and assert the connection is refused before authentication with no effect.
FINDING 14 — MED — src/TradeAgent.AtasBridge/AtasStrategyAdapter.cs:457 — StartBridge ignores AdapterTeardown.Started() returning false and still starts a BridgeServer during Stopping, leaving a seemingly live restarted bridge whose witness writes are refused. — Block teardown steps, invoke StartBridge during Stopping, finish teardown, and assert no server starts unless the teardown reaches Running.
FINDING 15 — MED — src/TradeAgent.Provisioning/NodeRuntime.cs:80 — Executable installers for Node, ATAS, and built-in agent runtimes permit a missing SHA-256 and still unpack or run downloaded bytes. — Make checksum resolution unavailable while serving substituted archives and assert every executable installation refuses before unpacking or elevation.
FINDING 16 — MED — src/TradeAgent.AgentRuntime/RuntimeManifest.cs:411 — Malformed runtimes.json is silently replaced by built-ins, so corruption of a restrictive runtime definition launches a different default command and sandbox policy instead of refusing. — Select a restrictive override, corrupt its JSON, and assert agent startup fails visibly without launching a built-in runtime.
FINDING 17 — MED — src/TradeAgent.Core/Db/MaterialStore.cs:106 — ByShaPrefix feeds an agent-controlled unescaped and unbounded prefix to LIKE with LIMIT 1, so wildcard or ambiguous short prefixes attach a note to an arbitrary material row. — Create colliding material hashes, submit percent, underscore, and one-character prefixes over the pipe, and assert every ambiguous or non-hex reference is refused.
FINDING 18 — MED — src/TradeAgent.Gateway/TradingGateway.cs:1954 — A second concurrent composite call treats a still-running owner as crash recovery, can observe DISPATCHING legs, and can persist that transient answer before the first call completes. — Block the first sweep’s connector call, issue the same request concurrently, and assert the duplicate waits for and returns the owner’s final stored result.
FINDING 19 — LOW — src/TradeAgent.Core/Db/MaterialStore.cs:36 — Material identity is only path, size, and mtime and hashing never revalidates those attributes, so a same-size restored-mtime replacement reuses the old material and hash row. — Scan a file, replace its bytes at equal length while restoring mtime, rescan, and assert a new correctly hashed version is recorded.
FINDING 20 — LOW — src/TradeAgent.Connectors.Atas/AtasHealth.cs:219 — AtasHealthReporter caches bridge installation and version facts for a minute and invalidates only after in-app reinstall, so external removal or replacement leaves the status row stale. — Change a fake probe’s bridge-install result inside the TTL and assert the next report reflects it immediately.
UNVERIFIED — src/TradeAgent.AtasBridge/AtasStrategyAdapter.cs:119 — The ATAS-facing file is excluded from non-Windows builds and its lifecycle overrides and event signatures cannot be confirmed against the real SDK by source inspection.
UNVERIFIED — src/TradeAgent.AtasBridge/AtasStrategyAdapter.cs:1454 — Whether ITradingManager honors a supplied off-chart portfolio and security when Connector is null is explicitly unmeasured, so exact account and instrument routing is unverified.
SUMMARY — 12 HIGH / 6 MED / 2 LOW / 2 UNVERIFIED.
```
